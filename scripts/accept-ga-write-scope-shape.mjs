import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const fp=path=>({path,sha256:createHash('sha256').update(fs.readFileSync(path)).digest('hex')}),read=p=>JSON.parse(fs.readFileSync(p));
const source='docs/planning/GA_REGISTRATION_WRITE_SCOPE_CORRECTION_REV1.json',review='docs/planning/GA_REGISTRATION_WRITE_SCOPE_REVIEW_REV1.json',r=read(review);assert.equal(r.status,'WRITE-SCOPE SUCCESSOR SHAPE REVIEW PASS');for(const f of r.sources)assert.deepEqual(fp(f.path),f);
const acceptance='docs/formal/GENERAL_ATTENTION_WRITE_SCOPE_SHAPE_ACCEPTANCE.md',manifest='docs/formal/GENERAL_ATTENTION_WRITE_SCOPE_SHAPE_MANIFEST.json',proposal='docs/planning/GA_WRITE_SCOPE_ALLOCATION_DRAFT.json';for(const p of[acceptance,manifest,proposal])assert(!fs.existsSync(p));
fs.writeFileSync(acceptance,`# General attention write-scope successor shape acceptance\n\n2026-09-13. PRIMARY-AGENT SHAPE ACCEPTANCE. Accept the two-record general-attention-registration-write-scope/0.1-candidate representational successor. GeneralStageRegistrationV02 preserves the prior wrapper fields except the explicit write-capability union. GeneralPathWriteCapability uses the existing authority and path semantics. Eight adversarial controls pass. This corrects a wrapper expressibility gap locally under the escalation policy; no ontology, route or authority boundary changes.\n\nRead-only uses322 ReadOnly; character-learning writes retain322 StateWrites. Exactly the two perception stages, two goal lifecycle owners and physical replenishment use explicit path authorization. No transition receives protocol-root authority. Exact patterns and model-bound holder checks remain mandatory.\n\n704/1 and all prior allocation bytes remain frozen and unchanged. A separate append-only allocation may assign these two record types and fields; no namespace or member is required. This shape receipt does not itself allocate numbers or qualify model/public execution.\n`);
fs.writeFileSync(manifest,JSON.stringify({version:'general-attention-registration-write-scope/0.1-candidate',status:'SHAPE ACCEPTED; ALLOCATION SEPARATE',files:[source,review,acceptance].map(fp)},null,2)+'\n');
const shape=read(source),records=shape.records.map((r,i)=>({typeId:705+i,schemaVersion:1,name:r.name,fields:r.fields.map((f,j)=>({id:j+1,name:f.name,required:f.type.kind!=='optional',type:f.type.kind==='optional'?f.type.value:f.type}))}));
const byName=new Map(records.map(r=>[r.name,r]));
const rolePositions=r.roles.map(role=>{const[owner,name]=role.path.split('.'),record=byName.get(owner);return {...role,recordTypeId:record.typeId,fieldId:record.fields.find(f=>f.name===name).id,requiredNamespace:role.namespace};});
fs.writeFileSync(proposal,JSON.stringify({version:'general-attention-write-scope-allocation/0.1-draft',status:'NUMERIC PROPOSAL; NOT PERMANENT',records,newNamespaces:[],members:[],rolePositions,recordBoundaries:r.boundaries,
 fieldEnums:[{recordTypeId:706,fieldId:1,members:records[1].fields[0].type.values.map((name,i)=>({name,value:i+1}))}],
 recordUnions:[{recordTypeId:706,fieldId:9,members:[{name:'TransitionWriteAuthorization',typeId:322,schemaVersion:1},{name:'GeneralPathWriteCapability',typeId:705,schemaVersion:1}]}],
 priorAllocations:fs.readdirSync('docs/formal').filter(f=>f.endsWith('ALLOCATION_TABLE.json')).map(f=>fp('docs/formal/'+f)),sources:[manifest,source,review,'scripts/accept-ga-write-scope-shape.mjs'].map(fp)},null,2)+'\n');
console.log('Write-scope shape accepted; separate records705/706 proposal written.');
