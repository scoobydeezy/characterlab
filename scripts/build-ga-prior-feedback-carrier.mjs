import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const output='docs/planning/GA_PRIOR_FEEDBACK_CARRIER_REV1.json';assert(!fs.existsSync(output));
const ref=name=>({kind:'ref',name}),en=(...values)=>({kind:'enum',values}),union=(...names)=>({kind:'union',alternatives:names.map(ref)});
const records=[],record=(name,fields)=>records.push({name,fields:Object.entries(fields).map(([name,type])=>({name,type:typeof type==='string'?ref(type):type}))});
record('ActualConcernCarry',{Concern:'ConcernOccurrenceId',Subject:'CharacterId',SourceAt:'Instant',Response:'TaskConcernResponse'});
record('NoConcernAvailable',{Subject:'CharacterId',SourceAt:'Instant',Reason:en('NoSelectedTask')});
record('FeedbackDelivery',{TargetOriginal:'OriginalAddress',TargetAt:'Instant',Value:union('ActualConcernCarry','NoConcernAvailable')});
record('PriorConcernModulation',{SourceStatus:en('KnownIntensity','Unavailable','NoSelectedTask'),Branch:en('EnabledKnown','DisabledFeedback','BaselineWithoutAvailableFeedback'),ResidualMultiplier:'UnitRational',AssociativeWeight:'OneToTwoRational'});
record('PriorConcernEncodingInput',{ObserverId:'ObserverId',Selected:'EncodingJoinInput',Feedback:'FeedbackDelivery'});
record('PriorConcernEncodingEvaluation',{Evaluation:'EncodingEvaluation',Feedback:'FeedbackDelivery',Modulation:'PriorConcernModulation'});
record('PriorConcernRecallInput',{ObserverId:'ObserverId',Cue:'CueEvidence',Feedback:'FeedbackDelivery'});
record('PriorConcernRecallEvaluation',{Result:'EventRecallResult',Feedback:'FeedbackDelivery',Modulation:'PriorConcernModulation'});
const external={ConcernOccurrenceId:'existing1130; TaskConcern sole producer',CharacterId:'existing1002 with character qualification',ObserverId:'existing1000',Instant:'nonnegative SimInstant',OriginalAddress:'existing unsigned original-input list index',TaskConcernResponse:'existing386',UnitRational:'exact0..1',OneToTwoRational:'exact1..2',EncodingJoinInput:'GA_VISUAL_PIPELINE_CARRIER_REV1 baseline',EncodingEvaluation:'GA_VISUAL_PIPELINE_CARRIER_REV1 baseline',CueEvidence:'GA_VISUAL_PIPELINE_CARRIER_REV1',EventRecallResult:'GA_RECOLLECTION_CARRIER_REV3'};
const fp=path=>({path,sha256:createHash('sha256').update(fs.readFileSync(path)).digest('hex')});
fs.writeFileSync(output,JSON.stringify({version:'ga-prior-feedback-carrier/0.1-draft',status:'SYMBOLIC PRIOR FEEDBACK CARRIER; PUBLIC GATES OPEN',records,external,storage:[],newIdentityFamilies:[],invariants:{delivery:'Actual completed same-run parent and exact target/bytes/subject; never original ingress.',chronology:'Producer instant strictly earlier than actual sensing/cue instant; no receiver relabelling.',separation:'Earlier concern is neither later goal qualification nor persistent affect.',selected:'No full source/preparation reaches encoder.',provenance:'Feedback is explicit trace-side input/result; durable children retain historical encoding only.'},sources:['docs/planning/GA_PRIOR_FEEDBACK_CARRIER_REV1.md','docs/planning/GA_VISUAL_PIPELINE_SCOPE_CORRECTION_REV1.md','docs/planning/GA_VISUAL_PIPELINE_CARRIER_REV1.json','docs/planning/GA_RECOLLECTION_CARRIER_REV3.json','docs/planning/ATTENTION_FEEDBACK_JOIN_BINDING_REV2.md'].map(fp)},null,2)+'\n');
