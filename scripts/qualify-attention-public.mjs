// Receipt assembly only: qualification depends on executed tests and fault controls.
import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const output='docs/planning/ATTENTION_PUBLIC_QUALIFICATION_REV1.json';assert(!fs.existsSync(output));
const read=p=>JSON.parse(fs.readFileSync(p)),fp=p=>({path:p,sha256:createHash('sha256').update(fs.readFileSync(p)).digest('hex')}),verify=f=>assert.equal(fp(f.path).sha256,f.sha256,f.path);
const regression='docs/planning/ATTENTION_ACTIVE_REGRESSION_REV3.json',tests=read(regression);assert(tests.success);assert.equal(tests.numPassedTests,1159);assert.equal(tests.numFailedTests,0);
const concurrent='docs/planning/ATTENTION_CONCURRENCY_TESTS_REV1.json',extra=read(concurrent);assert(extra.success);assert.equal(extra.numPassedTests,1);assert.equal(extra.numFailedTests,0);assert(extra.testResults[0].assertionResults.some(t=>t.status==='passed'&&t.fullName.includes('concurrent settlement')));
const reference='docs/planning/ATTENTION_REFERENCE_REGRESSION_REV1.json',historical=read(reference);assert(historical.success);assert.equal(historical.numFailedTests,0);assert.equal(historical.numPassedTests,328);
const files=new Set(tests.testResults.map(r=>path.relative(process.cwd(),r.name).replaceAll('\\','/'))),inventory=[];function walk(p){for(const d of fs.readdirSync(p,{withFileTypes:true})){const n=p+'/'+d.name;if(d.isDirectory())walk(n);else if(/\.test\.[tj]sx?$/.test(n))inventory.push(n);}}walk('src');assert.deepEqual([...files].sort(),inventory.sort());assert.equal(files.size,136);
for(const r of tests.testResults)for(const t of r.assertionResults)assert.equal(t.status,'passed');
const failed=read('docs/planning/ATTENTION_ACTIVE_REGRESSION_REV2.json'),failures=failed.testResults.flatMap(r=>r.assertionResults.filter(t=>t.status==='failed'));assert.equal(failures.length,17);for(const t of failures)assert(t.failureMessages.every(m=>m.includes('timed out')));
const prior=read('docs/planning/CAMPAIGN3_EMBODIED_QUALIFICATION.json'),preserved=[...prior.evidence,...prior.verifiedFingerprints,prior.script];preserved.forEach(verify);assert.equal(preserved.length,453);
read('docs/formal/ATTENTION_PUBLIC_SHAPE_MANIFEST.json').files.forEach(verify);
const allocation=read('docs/formal/ATTENTION_ALLOCATION_FREEZE_AUDIT.json');allocation.artifacts.forEach(verify);verify(allocation.numericReview);
const cohortPath='docs/planning/campaign3-attention-model-rev2/FREEZE.json',cohort=read(cohortPath),old=read('docs/planning/campaign3-attention-model-rev1/FREEZE.json');[...cohort.sharedFiles,...cohort.models.flatMap(m=>m.files),...old.sharedFiles,...old.models.flatMap(m=>m.files)].forEach(verify);verify(cohort.supersedesSpecimenRuns);verify(cohort.correction.script);
assert.equal(cohort.models.length,32);assert.equal(cohort.runs.length,224);for(const m of cohort.models){const previous=old.models.find(x=>x.name===m.name);assert.equal(m.modelIdentity,previous.modelIdentity);assert.deepEqual(m.files.map(f=>f.sha256),previous.files.map(f=>f.sha256));}for(const r of cohort.runs)assert.notEqual(r.runIdentity,old.runs.find(x=>x.model===r.model&&x.scene===r.scene).runIdentity);
const mutants='docs/planning/ATTENTION_RUNTIME_MUTANTS_REV1.json',faults=read(mutants);assert.equal(faults.results.length,11);assert(faults.results.every(x=>x.detected));faults.sources.forEach(verify);
for(const p of ['docs/planning/ATTENTION_COMPONENT_MUTANTS_REV2.json','docs/planning/ATTENTION_REFERENCE_ORDER_MUTANT_REV1.json'])read(p).sources.forEach(verify);
const vectors={
 'AT2-A':'PASS:27 public denied-port role substitutions; observer-side outputs byte-identical.',
 'AT2-B':'PASS:224 model/scene runs; K0/1/2, equal and unlimited controls; all-denied branch; work8 atomic rejection.',
 'AT2-C':'PASS:public actual observed role swap and canonical tie scenes, independent selection oracle.',
 'AT2-D':'PASS:all public audit priorities/exclusions checked exactly across visibility/unit interventions; no strength claim.',
 'AT2-E':'PASS:shared actual observation across plural public units; duplicate binding/request component rejection; duplicate original rejection.',
 'AT2-F':'PASS split:missing/unsupported public; multi-role actual SEM/component positive with public one-role profile exclusion.',
 'AT2-G':'PASS split:public canonical tie/restore-original binding; actual component foreign observer/event/instant, stale/forged claim and strict-priority renaming rejection.',
 'AT2-H':'PASS split:public selected receipts; actual component direct/indirect resolver rejection; live consumer omitted/repeated/observation-read faults reject.',
 'AT2-I':'PASS:K-only SEM preservation; selector/consumer no-write faults; only tracking roots241/242 may change.',
 'AT2-J':'PASS:actual positive15-to40-to40 and empty10-to40-to40 graph; phase20 fault rejects; legacy regression preserved.',
 'AT2-K':'PASS:private exact parent-issued token, independent child/output carriage, allocation-table extraction and actual-slot freshness; no wrapper identities.',
 'AT2-L':'PASS:434 whole-prefix restores;58 injected boundary rollbacks;247 trace-field corruptions; edited saves/incomplete prefixes reject.',
 'AT2-M':'PASS:actual old Campaign2/EMB factories and codecs reject;453 EMB fingerprints and prior attention component faults preserved.',
 'AT2-N':'PASS bounded:no learning/encoding/retrieval writes or verdict; generic ATTN/corpus remains open.'
};
const sources=[...fs.readdirSync('src/campaign3').filter(n=>n.startsWith('attention')&&n.endsWith('.ts')).map(n=>'src/campaign3/'+n),'src/test/attentionPublic.test.ts','src/test/attentionBoundaries.test.ts','src/test/attentionPublicFixtures.ts'];
fs.writeFileSync(output,JSON.stringify({status:'BOUNDED PUBLIC ATTN QUALIFIED',reviewer:'Primary-agent adversarial self-review under autonomous authorization; no external verdict',contract:'attention-public-integration/0.1-candidate',declarations:'attention-public-declarations/0.1-candidate',selection:'attention-selection-component/0.1-candidate',models:32,specimenRuns:224,successfulRuns:217,workLimitRejections:7,wholePrefixRestores:434,boundaryRollbackWitnesses:58,traceFieldCorruptions:247,currentRuntimeFaults:11,activeTests:1160,activeFiles:136,execution:'Full1159 active run plus one new concurrency test; one worker and unchanged test timeouts. Previous unconstrained run retained with17 timeout failures.',preservedEMBFingerprints:453,vectors,correction:'Canonical set S0;32 model commitments unchanged,224 run commitments corrected forward; old bytes retained without alias.',limits:['Finite exact model cohort; no arbitrary model-authoring admission.', 'Multi-role/renaming and adversarial selected-view access use real components where the public profile excludes those inputs.', 'Adapter infrastructure owns omniscient source and transaction orchestration; the selector receives observer-safe SEM and the actual consumer receives only its selected capability.', 'ATTENTION intake is PARTIAL for active selection; encoding/later probe remains BLOCKED. No corpus member or whole ATTN PASS.', 'No residual-pool/salience reduction, character-state attachment, affect feedback, learning, memory or retrieval claim.'],sources:sources.map(fp),evidence:[regression,concurrent,reference,mutants,cohortPath,'docs/planning/ATTENTION_INITIAL_STATE_CORRECTION.md','docs/planning/ATTENTION_ACTIVE_REGRESSION_REV2.json','docs/planning/ATTENTION_COMPONENT_MUTANTS_REV2.json','docs/planning/ATTENTION_REFERENCE_ORDER_MUTANT_REV1.json'].map(fp),script:fp('scripts/qualify-attention-public.mjs')},null,2)+'\n');console.log({tests:1160,files:136,models:32,runs:224,preserved:453,vectors:14});
