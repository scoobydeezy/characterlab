import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const fp=p=>({path:p,sha256:createHash('sha256').update(fs.readFileSync(p)).digest('hex')}),read=p=>JSON.parse(fs.readFileSync(p));
const reviewPath='docs/planning/ATTENTION_ALLOCATION_REVIEW_REV1.json',review=read(reviewPath),draftPath='docs/planning/ATTENTION_ALLOCATION_DRAFT.json',draft=read(draftPath);
assert.equal(review.status,'NUMERIC REVIEW PASS; NOT YET PERMANENT');assert.equal(review.parityChecks,277);assert.equal(review.canonicalDeclarationRoundTrips,135);assert.equal(review.negativeControls.length,11);
for(const f of [...review.reviewedArtifacts,...review.priorAllocations])assert.deepEqual(fp(f.path),f);
const outputs=['docs/formal/ATTENTION_ALLOCATION_TABLE.json','docs/formal/ATTENTION_PERMANENT_ALLOCATION.md','docs/formal/ATTENTION_ALLOCATION_FREEZE_AUDIT.json'];for(const p of outputs)assert(!fs.existsSync(p));
const old=read('docs/planning/CAMPAIGN3_EMBODIED_QUALIFICATION.json');for(const f of [...old.evidence,...old.verifiedFingerprints,old.script])assert.deepEqual(fp(f.path),f);
const table={...draft,version:'attention-allocation/0.1-candidate',status:'PERMANENT AND FROZEN',acceptance:{date:'2026-09-11',reviewer:'Primary-agent separate numeric review under user autonomous-work authorization; no external verdict claimed',numericReview:fp(reviewPath)},acceptedProposal:fp(draftPath),permanence:['No renumbering, reuse or insertion by shifting.','Future families append through a separate accepted shape and allocation gate.','Numeric adjacency carries no semantic meaning.']};
fs.writeFileSync(outputs[0],JSON.stringify(table,null,2)+'\n');
fs.writeFileSync(outputs[1],`# Public attention permanent allocation\n\n**attention-allocation/0.1-candidate — ACCEPTED, PERMANENT AND FROZEN.**\n2026-09-11. Primary-agent separate numeric review under autonomous-work authorization.\nThis is not an external verdict, new semantic design or runtime qualification.\n\nThe [machine allocation](ATTENTION_ALLOCATION_TABLE.json) is authoritative together\nwith the [accepted symbolic shape](ATTENTION_PUBLIC_SHAPE_ACCEPTANCE.md).\nThe [reviewed Markdown proposal](../planning/ATTENTION_ALLOCATION_DRAFT.md) remains\nhistorical, with its exact bytes and draft disposition preserved.\n\n|Surface|Permanent assignment|\n|---|---|\n|Record types|516..541, schema1;26 records/89 fields|\n|SelectionOccurrenceId|1143; unsigned ordinal from the existing shared runtime allocator|\n|ProcessingOccurrenceId|1144; unsigned ordinal from the same allocator|\n|Fixed text members|52 exact payloads in their existing identity namespaces|\n|Union members|Four1024 unsigned-pair members; exact tuples in the table|\n|Role positions|62 scalar RecordField plus2 ObserverId StateMapKey|\n|Collection identity checks|3; owned by the finite compiler, not scalar VAL|\n|Occurrence mappings|11, including required214/215 field2 identities|\n\nAll closed tags and their field/container applicability are frozen by finiteEnums,\nfiniteFields and unionDefinitions. Capacity is the literal unsigned0/1/2.\nSource union and read union admit exactly their required/forbidden payloads.\nNo new identity is allocated for a view, wrapper, unit, audit row or read receipt item.\nObserver-file ordinals remain under existing SEM state ownership.\n\nThe separate [numeric review](../planning/ATTENTION_ALLOCATION_REVIEW_REV1.json) passed\n277 parity checks,11 negative controls and135 canonical declaration round trips.\nAll453 prior EMB fingerprints and prior allocation tables remained unchanged.\nThese counts establish allocation consistency only. No AT2 runtime vector passed.\n\nNo renumbering, reuse or insertion-by-shifting. Future families append through their\nown accepted gates; numeric adjacency does not imply semantic kinship.\n\nNext construct and review the32 exact model commitments, then implement the closed\nprofile and execute AT2-A..N and declaration/admission controls. No codec activation,\nmodel freeze, persistent learning, whole ATTN or corpus promotion is implied.\n`);
fs.writeFileSync(outputs[2],JSON.stringify({status:'ACCEPTED AND FROZEN; NUMERIC GATE ONLY',artifacts:outputs.slice(0,2).map(fp),numericReview:fp(reviewPath),shape:fp('docs/formal/ATTENTION_PUBLIC_SHAPE_MANIFEST.json'),priorAllocations:review.priorAllocations,preservedEMBFingerprints:453,script:fp('scripts/freeze-attention-allocation.mjs'),runtime:'AT2-A..N FROZEN NOT PASSED'},null,2)+'\n');
console.log({status:table.status,records:[516,541],namespaces:[1143,1144],next:'exact model commitments'});
