import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const out='docs/planning/ATTENTION_MEMORY_STATE_DECLARATIONS_REV1.json';assert(!fs.existsSync(out),'write once');
const roots=[
 ['AttentionTrackingWindowState','TrackingWindow','ObserverId','perception','authority/perception','accessor/attention-marker-window'],
 ['AttentionEpisodeState','EpisodeLedger','CharacterId','episodic-memory','authority/attention-episode-formation','accessor/attention-memory-episodes'],
 ['AttentionAssociationState','AssociationGraph','CharacterId','associations','authority/attention-association-update','accessor/attention-memory-associations'],
 ['AttentionPresentationState','PresentationLedger','CharacterId','episodic-memory','authority/attention-presentation-update','accessor/attention-memory-presentations'],
].map(([name,value,key,family,owner,accessor])=>({name,fields:[{name:'Entries',type:{kind:'map',key,value}}],family,owner,accessor,selector:'one exact mapKey',presence:'absent or complete value; no partial value',initial:'no leaf',removal:false}));
const retained=JSON.parse(fs.readFileSync('docs/planning/ATTENTION_SYMBOLIC_DECLARATIONS_REV1.json')).accessors;
assert(Array.isArray(retained));
const packet={status:'DRAFT SYMBOLIC ROOT/PATH/ACCESSOR DECLARATIONS; NO NUMERIC ALLOCATION',roots,retainedPerceptionAccessors:retained,
 subjectProjection:{definition:'consume exact accepted required PRJ/IDN descriptor from committed source registration',input:'required ObserverId scalar',path:'descriptor roster path with exact input ObserverId map key',returnedValue:'qualified CharacterId',readEvidence:'existing derived projection record plus exact roster source',authority:'immutable identity binding; no write capability'},
 stages:{track:['continuant-counter','continuant-active','event-counter','event-active','AttentionTrackingWindowState'],form:['IDN','AttentionEpisodeState'],associate:['IDN','AttentionAssociationState'],'seed-presentation':['IDN','AttentionPresentationState'],rank:['IDN','AttentionEpisodeState','AttentionAssociationState','AttentionPresentationState'],reinforce:['IDN','AttentionPresentationState']},
 reads:{character:'exact full leaf at actual projected CharacterId; no sibling-character access or entry iteration',rankUnavailable:'IDN only; no memory leaf reads',rankAvailable:'all three exact leaves, including absence; all absent is empty, partial presence invalid',perception:'read exact counters and required file membership paths; file keys remain full observer-relative records; window checked against source replay',cachedTracker:'replayed history must agree with canonical reads; never supplies unlogged owner facts'},
 roleRules:{ObserverId:'existing namespace1000, no character substitution',CharacterId:'existing SemanticReferentId role plus accepted character domain validator; not namespace-only',newRootMapKey:'one direct StateMapKey role per proposed root Entries field; qualified key type as declared',nestedValues:'existing scalar roles reused; new wrapper roles still part of whole symbolic packet',fileMembership:'existing212/213 composite records; observer sequence is opaque'},
 pending:['new root record/field and accessor numeric allocation after whole-shape acceptance','all wrapper scalar and collection role descriptors','source PRJ descriptor and registration dependency manifest','canonical persistence and public exact-read/sole-writer proof']};
function review(p){let checks=0;const check=(v,m)=>{assert(v,m);checks++;};check(p.roots.length===4,'root count');check(new Set(p.roots.map(r=>r.name)).size===4,'duplicate root');check(new Set(p.roots.map(r=>r.accessor)).size===4,'accessor alias');
 const expected={TrackingWindow:['ObserverId','authority/perception'],EpisodeLedger:['CharacterId','authority/attention-episode-formation'],AssociationGraph:['CharacterId','authority/attention-association-update'],PresentationLedger:['CharacterId','authority/attention-presentation-update']};
 for(const r of p.roots){check(!('typeId' in r),'premature root allocation');check(r.fields.length===1&&r.fields[0].name==='Entries','root shape');const t=r.fields[0].type,e=expected[t.value];check(e&&t.kind==='map'&&t.key===e[0]&&r.owner===e[1],'root key/owner mismatch');check(r.selector==='one exact mapKey'&&!r.removal,'broad selector or removal');check(r.initial==='no leaf','authored memory initial state');}
 check(JSON.stringify(p.retainedPerceptionAccessors)===JSON.stringify(retained),'changed existing perception accessor');
 for(const stage of ['form','associate','seed-presentation','rank','reinforce'])check(p.stages[stage][0]==='IDN','read without projection');
 check(p.stages.rank.join()==='IDN,AttentionEpisodeState,AttentionAssociationState,AttentionPresentationState','incomplete candidate leaves');
 check(p.reads.rankUnavailable==='IDN only; no memory leaf reads','unavailable cue memory read');
 check(p.subjectProjection.authority==='immutable identity binding; no write capability','identity mutation authority');
 check(p.roleRules.CharacterId.includes('domain validator'),'namespace-only character role');
 check(p.status.startsWith('DRAFT')&&p.pending.length===4,'premature acceptance');return {checks,newRoots:p.roots.length,newAccessors:p.roots.length,reusedPerceptionAccessors:retained.length};}
const baseline=review(packet),faults=[];
for(const [name,mutate] of [['character-keyed-window',p=>p.roots[0].fields[0].type.key='CharacterId'],['episode-owner-alias',p=>p.roots[1].owner=p.roots[2].owner],['broad-read-selector',p=>p.roots[1].selector='mapKey(*) runtime enumeration'],['seed-authored-memory',p=>p.roots[1].initial='authored episode'],['drop-rank-graph',p=>p.stages.rank.splice(2,1)],['unavailable-memory-read',p=>p.reads.rankUnavailable='read all leaves'],['namespace-only-character',p=>p.roleRules.CharacterId='namespace1002 only'],['renumber-sem-layout',p=>p.retainedPerceptionAccessors[0].root=9999],['allocate-root-now',p=>p.roots[0].typeId=9999]]){const copy=structuredClone(packet);mutate(copy);let reason;try{review(copy);}catch(e){reason=e.message;}assert(reason,'undetected '+name);faults.push({name,reason,detected:true});}
const fp=path=>({path,sha256:createHash('sha256').update(fs.readFileSync(path)).digest('hex')});
fs.writeFileSync(out,JSON.stringify({...packet,review:baseline,faults,sources:['docs/planning/ATTENTION_MEMORY_STATE_DECLARATIONS_REV1.md','scripts/review-attention-memory-state-declarations.mjs','docs/planning/ATTENTION_SYMBOLIC_DECLARATIONS_REV1.json','src/semanticBinding/semanticStateAuthority.ts'].map(fp),limits:['Static key/path/owner proposal; no canonical state codec or public read implementation executed.']},null,2)+'\n');console.log({...baseline,faults:faults.length});
