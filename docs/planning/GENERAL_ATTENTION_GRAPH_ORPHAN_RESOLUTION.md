# Zero-state graph normalization: A accepted

2026-09-12. **STRUCTURAL NORMALIZATION ACCEPTED by supplied planning ruling.**
Resolves the [orphan-policy packet](GENERAL_ATTENTION_GRAPH_ORPHAN_DECISION.md).

After the complete graph-owner reconciliation batch, remove membership for a node
with exactly zero incoming AND outgoing learned weights and no separately accepted
node-local learned payload. The present graph has no such node-local payload.
Any representable positive weight preserves both incident endpoints. No epsilon,
weak-edge threshold, importance or positive-edge survival rule is authorized.

This is zero-state normalization, not a claim of psychological forgetting. Run it
after all admitted learning, weakening and separately authorized pruning operations
have been reconciled; never during reads or between sibling contributions. The
association owner alone commits the result. Do not redistribute surviving weights
or invent a node-removal cognitive event/identity. Existing graph timestamp semantics
must be preserved within the eventual batch contract rather than silently refreshed.

Surviving episodic content and direct cue matching remain unchanged. A later lawful
learning operation may reintroduce the same semantic key without a new semantic
identity, dormant familiarity claim or recovery of forgotten evidence. Future
node-local familiarity/exposure/confidence/prior state explicitly reopens this
emptiness predicate; it cannot be discarded under the current rule.

Positive-edge scarcity/pruning, node/edge capacity, persistence and public owner
qualification remain separate open contracts. Dormant membership is not the first
model; no dormant learned-state field is invented. Prior component comparisons do
not by themselves qualify public normalization or forgetting. General Attention
remains OPEN, with acquired protection still a mandatory comparison.
