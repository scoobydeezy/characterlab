# Adopted bodily maintenance goal A accepted

2026-09-12. The user's ruling selects a separately governed prospective maintenance
goal as the first concern source. It targets experienced InteroceptiveSignalId and
a desired interval, never a hidden LocalReserveId. Identical bodily evidence can
coexist with different adopted goals. This does not decide Need ownership.

Satisfaction does not retire a maintenance goal. Withdrawal and expiry are explicit
lifecycle events. Assessment consumes the goal at consequence time; later changes
do not rewrite committed assessments. Durable bodily preference remains a distinct
later comparator (preference only, goal only, aligned, conflicting, neither).

[Lifecycle component](../formal/BODILY_MAINTENANCE_GOAL_COMPONENT.md) and
[six tests](BODILY_MAINTENANCE_GOAL_TESTS_REV1.json) implement a bounded trusted-owner
model. They prove no public adoption source, signal-domain provenance, canonical
goal identity or whole significance mechanism. Reads alone do not retire a goal;
no satisfaction-assessment integration was exercised because that law remains open.

The component holds at most sixteen goals, uses exact declared signal domains,
rejects same-key replacement/replay and requires expiry settlement before a boundary
read. The read basis is detached; querying changed state for a pre-change instant
rejects instead of manufacturing a historical snapshot. Committed assessment-history
storage is still a public successor obligation, not provided by this read component.

The prior boundary-only draft cannot represent worsening outside the desired range.
The user's5→7/[2,4] example exposes that limitation. The next concrete proposal is
[the assessment-law decision](GENERAL_ATTENTION_GOAL_RELATION_DECISION.md).
No importance variable, significance tier or retention ordering is accepted here.
