# Task packaging review2 — corrected key-role closure

2026-09-09. Agent construction review. Model materialization/activation remains pending.

The first source consumes task-commitment/0.2-candidate and
task-commitment-allocation/0.2-candidate. See the correct-forward explanation in
../formal/TASK_COMMITMENT_KEY_ROLE_CORRECTION.md. Original role-error artifacts remain
unchanged. The seven record schemas and14member payloads are identical to the earlier
allocation; only two invalid declaration positions are removed.

The new slot5 contribution is **ten RecordField roles, zero StateMapKey roles**.
TaskKey371 fields1/2 carry character/task predicates. Root373/1 uses exact record-key
grammar371. Existing IDN's identity-atom StateMapKey role remains unchanged. No new
meaning is assigned to CanonicalRolePosition264.

## Executed construction evidence

campaign2TaskDeclarations.test.ts:16 tests PASS. These call the actual new fixed
two-kind CONTENT component and existing compileCampaign2StateModel. They cover
three specimen declarations, exact qualified-character content projection, opposite
kind qualification failures, valid composite-key state, swapped C/task key failures
before absent lookup, non-record key rejection, both invalid StateMapKey additions,
either missing371 RecordField role, all seven spec-delegation fields and status-union
presence. This is construction evidence only, not live lifecycle or TC-A..L PASS.

The task content component is separate from the frozen character-only VAL compiler.
It adds the accepted kind/predicate through a closed build path, with no caller
callback. Generic CONTENT still commits references/cycles after exact task-field
checks. The qualified-character image is constructed from actual full content and
compared with the retained prediction source. No task is inserted into REG's domain.

## Exact proposed component bundle additions

Append to the prediction bundle, in this order:

1. task-commitment/0.2-candidate
2. task-content-kind/0.1-candidate
3. task-domain-validator/0.1-candidate
4. task-measurement-registration/0.1-candidate
5. task-deadline-registration/0.1-candidate
6. task-target-projection/0.1-candidate
7. prospective-transition-routes/0.1-candidate
8. prospective-family-topology/0.1-candidate
9. campaign2-task-commitment-trace-binding/0.1-candidate
10. campaign2-task-commitment-persistence/0.1-candidate

Registry171 entries for279 and284 select the respective new prospective specialization
versions. Their retained membership rows are unchanged; only the new route/two
transition memberships and prospective family row are appended. Task spec and the
executing seam fields select task-commitment0.2. The pending Rules/Registry/Numeric
tuple from packagingrev1 remains the proposed first model tuple, not a frozen alias.

Three finite specimen tables and six independent prediction controls produce192
source combinations. Initial adoption remains separate S0 data. The final review
must materialize these declarations, verify all inherited slots/entries and changed
root/role/route surfaces, compute canonical commitments and actual event ceilings.
No runtime transition, new original input kind or cognitive draft is activated yet.
