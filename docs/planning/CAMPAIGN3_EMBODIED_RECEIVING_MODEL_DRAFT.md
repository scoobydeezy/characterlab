# EMB receiving: finite model proposal

Revision2, 2026-09-10. SYMBOLIC MODEL SHAPE, no canonical byte freeze or activation.
Complements receiving revision2, closure revision3, inventoryrev3 and vocabularyrev2.

## Exact source envelope

Use the existing data-only nine-field model source: the six version strings plus
content, registry and parameter bytes. Proposed versions are
rules/campaign3-embodied-receiving/0.1-draft;
campaign3-embodied-receiving-registry/0.1-draft;
content/0.2-candidate; campaign3-embodied-receiving-parameters/0.1-draft;
numeric/embodied-receiving-exact/0.1-draft; and the unchanged
rng/sha256-addressed-128-v1-candidate. No caller semantic function, source generator,
world callback, body path or dynamic rules dictionary is admitted.

Registry retains the actual six-slot layout: descriptor/union/semantic entries;
existing ordering134; owned state155; read-only262 patterns; key grammars261; roles265.
Slot0 includes the complete admitted schema/union inventory required to decode nested
records, the exact sixteen stage registrations, accepted source/body/deadline declarations
and the explicit parameter definitions below. Presence of a schema descriptor does not
activate its transition. The only executable events are the declared closed graph.

The shared279 mapping has no character-learning route or transition membership in this
profile. Its output occurrence map contains the six task source schemas, ten new output
schemas,227,461,463 and464, with their exact existing families. No route is invented
for context state, expression or the truth-only terminal execution result. Original
459/377/478 and the private deadline have scheduler origin, not output occurrence rules.

The semantic bundle commits the actual dependencies and exact executing component
versions, including the pressure0.2 and task-source successor dispositions. Dormant
359 retains its actual measurement-prediction/0.2-candidate entry version; it does not
activate prediction producers or promise that forecasts exist. The bundle cannot be
an arbitrary set: materialization must publish the exact canonical version collection.

## Content and definitions

Retain exact character170 and task-a170 from the existing qualified task model,
verified in EMBODIED_RECEIVING_INHERITANCE_REVIEW_REV1. The body's qualified CharacterId
is exactly the subject of that copied359 definition. ObserverId is the existing
observer/embodied-subject; its one267 binding points to this actual qualified character.
This is a distinct committed model/run, not reinterpretation of either predecessor.

Retain copied359 with channel/regulatory-diagnostic-probe, diagnostic unit and max64.
Task-a370 retains its actual holder/prediction reference and diagnostic interval[4,6];
the new model declares activeFrom0 and deadline100. Timing is new immutable model
calibration under the same task semantics, not an edit to the old frozen task model.
The registry has exactly this one task spec and matching task content. Two existing389
instructions refer to the two existing391 count1/count2 actions. Definitions with
identical operational action counts are rejected: action identity does not multiply
lottery tickets by alias. Task adoption/instruction association remains independently
intervenable in S0, subject to its existing subset law.

Body definitions: existing reserve unit; capacity100; rate1; width10; H60; channel
available/permitted true; the old30/5/60 delivery magnitudes, now bound to the selected
character in this new model. The observer/subject/capacity/unit relations remain exact.
The source uses actual459/461/463 and accepted SEM current-lane settlement. Only the
pressure0.2 outgoing edge differs from the qualified terminal body profile.

Three new instruction definitions are exactly:

* definition/embodied-response-a: pressure definition -> existing protocol-contact-one.
* definition/embodied-response-a-copy: same pressure -> same action; a separate adopted
  description, expressly permitted to test redundancy within one ground.
* definition/embodied-response-b: same pressure -> existing protocol-contact-two.

All use the new exact registry/embodied-response-instruction kind and the instruction
contract version. Their definitions do not assert effect or success. No Unit, relief
quantity, confidence, reward or task referent appears in them. Two instructions may
describe one action; two operationally identical action definitions may not create two
choices. These are different admission rules with separate tests.

Task378: capacity3, task access true, forecast access false. This selects the one
eligible task with an honest Unavailable forecast. Concern385: gain1/enabled true;
its unavailable branch executes. Motive392: pressure1/4/enabled true. Candidate435:
plan access true. Raw436: standing access false, existing identityK1/10 declared but
unused and no identity read. Reason437 uses cutoffs1/20,1/10,1/5,3/10,2/5, threshold0
and modifier units1/caps3. Arbitration440 has thetaRoll=thetaPlayer=1/10. Execution434:
Permitted=true, delivered only to execution110. These are exact rational literals.

## Thirteen finite recipes

The public factory admits only these separately committed recipes after byte freeze.
It cannot accept an arbitrary in-range parameter edit. Each row changes only the listed
field(s) from baseline; content and all unrelated declarations remain byte-identical.

| Recipe | Exact change |
|---|---|
| baseline |None |
| slower |Reserve rate1/2 |
| coarser |Channel width20 |
| denied |Channel Permitted=false |
| unavailable |Channel Available=false |
| task-base-off |Task motive Enabled=false |
| task-plan-off |Task plan access=false |
| execution-blocked |Execution Permitted=false |
| quiet |thetaPlayer=1 |
| auto |thetaRoll=1 |
| weak-task |Task pressure1/20 |
| work25 |Settlement work25 instead of26 |
| no-coverage-control |Explicit alternative receiving version described below |

The quiet/auto models keep the same exact distributions and change the display/roll
decision law only through its accepted threshold operands. Do not force a particular
winner. Seeds are supplied through the existing run contract and exact addressed mapper.
No special seed chosen after observing a preferred behavioral result defines the law.

The no-coverage model is a **declared competing receiving implementation**, not a hidden
flag or an invalid reference-model run. It substitutes the committed version
embodied-task-reasons-no-coverage-control/0.1-draft for the reference receiving version
in both mixed-raw and mixed-reasons registrations and the exact semantic bundle. It
retains every actual source, origin, basis, ground, role and signed partition but sets
every contribution's overlap0/independence1/effective=raw magnitude; it sums then bounds
once. Body meaning uses that same unfiltered-total alternative. All other arithmetic,
read limits, graph, identities and control calibration remain unchanged. Its own trace
validator checks this declared alternative, rather than rejecting it for disagreement
with the reference law before a behavioral comparison is possible.

Consequently this model can execute and honestly fail the duplicate-description
phenomenon. With two body descriptions it predicts2/3 against the reference1/2. It is
not granted a reference-conformance PASS; its negative phenomenon verdict is evidence
for this bounded receiving distinction. Its one-description behavior is an equal-input
control. Ground deletion is separately witnessed by the actual base-off/adoption
interventions. One-die-per-description remains a mathematical/fault alternative with
no claim of a canonical public control model or valid new reason identity.

## Original input and S0 grammar

The paired-input/time/count rules are exact in the closure document. Canonical inputs
have at most100 original rows and empty dependency lists. No actor/action/pressure,
raw signal, candidate, intent, sample or join output can be authored as input. A paired
deliberation and sample use the one fixed observer and their exact agenda/channel.
The same timestamp permits the three distinct external deliveries. Repeated identical
rows reject. The compiler derives the one deadline after allocating all supplied rows.

S0: body anchor at0 with integer rational q in[0,100]; one observer binding; one required
BodyResponseContext with a subset of at most two of the three instruction definitions;
zero or one Open task status; zero or one task-plan binding (only for an adopted task).
The new receiving profile's initial body amount is integral; the qualified generic
body contract and its exact fractional controls are not narrowed globally. There is
no authored learned history, forecast, identity, persistent pressure or populated cache.
All unlisted state paths reject. At quiescence only body/deadline owners can have changed
their exact leaves. Task/body instruction contexts remain byte-identical throughout.

## Public witness plan

* Baseline q80 at45 gives q35, interval[30,40],p1/3. Task instruction A and body A/B yield
  independent task+body grounds on A against body on B, with probability221/256 for A.
* Remove body A adoption while retaining body B: A falls to task alone, probability1/2.
  Disable task base while keeping task origin and body A/B: the same analytical1/2.
  Compare real source/ground outputs, not only aggregate output counts.
* Task instruction B, one body A versus body A plus A-copy: two choices, probability1/2
  in both, one body nucleus, two origins only in the latter. Stacking raw duplicate
  strength predicts2/3; adding a die per description predicts221/256. Both must fail.
* Body adoption empty/remapped, with pressure fixed: exact body source output matches,
  while source-to-option associations change. World-blocked comparison leaves all
  pre-execution outputs fixed and changes completed count only. Missing adoption cannot
  become knowledge merely because the world would allow the action.
* Initial q71 versus79 at40 yields[30,40] in both under width10 and nonzero equal p1/3.
  Compare all character output after permitted-observation projection, allowing only
  explicitly different model/run commitments in omniscient trace. No truth-reader wins.
* q80 at35 gives q45: fine p1/6 versus coarse p0; actual body instruction access and
  choices reflect this genuine sensor contrast. Slower-rate trajectories provide the
  independent kinetics contrast. Neither initial pressure nor raw signals are authored.
* q80 at40/45/55/75 reaches body d6/d8/d10/d12; weak-task with no body adoption reachesd4.
  Baseline, quiet and auto contexts exercise mode boundaries; exact tie/draw obligations
  remain independent of a single observed winner.
* Empty task/body adoption gives NoOptions. Adopted task with base disabled and no body
  adoption gives NoActiveReasons. Both emit resolution and no intent/RNG.
* At100 use both body actions, all three deliveries and the task deadline:26 events,
  no task base after expiry, body choice remains, task status retires at140. Work25 fails
  atomically. Continue/restore before and after retirement and replenish boundaries.

Before freeze, materialize all thirteen exact models and input/S0 specimens, audit every
definition/role/reference and derive actual schema/union/model/run commitments. Before
qualification, execute all18 ER vectors, component exclusions, authentic stage/read
mutants, public continuation/rollback and prior preservation checks. This plan does not
pass them. The body/task comparison cannot settle general Need ownership, universal
cross-family consolidation, mixed identity learning or action-conditioned fuel efficacy.
