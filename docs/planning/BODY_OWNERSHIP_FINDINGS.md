# BODY ownership findings — 2026-09-21

Preserved by VER-C3-BODY-001; RO-C3-008/020. No canonical allocation:970/0.

## Storage is not psychological authority
MeterBehindSensor is a normalized stored level with its own passive transition and
bounded delivery writer. It matches the public reserve's experienced pressure in
every admitted run. IndependentNeed uses the same level but bypasses interoception;
its urgency differs at hidden41/49 while the public interval and pressure are equal.
Thus storage itself is not the rejected feature. Promoting hidden state directly to
experienced pressure violates the boundary. A stored body representation remains a
valid candidate; it is not evidence that a second psychological Need owner is needed.

CachedEvidence also matches every sampled pressure. Its only writer replaces the
cell from the current admitted output, including Unavailable. This is bounded
derivability, not a test of hysteresis, inference, between-opportunity retention or
different legitimate prior histories. It would be false to call this a universal
proof against independently acquired pressure or to delete a state root on its basis.

## Operating point is not current reserve
The existing REG compiler supplies authored80 under regulatory-reference/0.5-candidate.
It has no delivery writer and is not a substitute for replenished current physiology.
Its failure as that substitution leaves REG's intended reference/target role intact.
A more expressive authored trajectory or event-indexed lookup could match this finite
fixture, but would need to preserve information and governing inputs; none is tested.

## Scope and timing
All three positive representations share a scheduled observation domain. Same-instant
sample/pressure precedes delivery. Fast and slow share exact S0=80 at time0, not exact
positive-time truth: at1 they are79 and79.5. Both first pressures are known0; later
pressures differ. The contract's shorthand “equal initial sample80” refers to the
initial state; its following exact-domain clause specifies the actual positive-time
samples. No claim of an illegal time0 public original follows.

The component motive probe preserves Known(0) versus Unavailable, but is not a new
registered choice pipeline. The old receiving qualification remains separate.
Source prefix restores are genuine factory saves; component prefix tests reconstruct
meter/cache state from their original transition sequence. These are not new canonical
character-state saves, and the five candidates are not five new public ModelIdentities.

## Implementation corrections and preserved evidence
An initial TypeScript check rejected access to a canonical integer through an unnarrowed
union. The harness now uses its existing typed integer extractor. This occurred before
the frozen plan; no executed comparison or model cohort was replaced. First frozen
matrix and first test suite passed. No failed behavioral cohort is omitted.

Historical CTL-001 level, passive rate, bounded effects, deficit and K=1,p=1 urgency
were inspected in reference/src/model/needs.ts and re-expressed with active exact
arithmetic. Selected normalized values are exactly on its10^-6 lattice; historical
code is neither imported nor edited. Broad urgency laws, MPS mappings and historical
quantization outside this finite grid are not thereby qualified.
