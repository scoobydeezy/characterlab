# Source stage and transport closure

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic registration
proposal; not whole-profile acceptance or numeric allocation input.

The actual requested multimodal component establishes the conditional source products.
Public registration must preserve its internal phase boundaries. The machine packet
names15 lane-specific registrations and five transport records, reusing the existing
SEM outputs and previously proposed body/visual carriers. These records add no identity.
They have not yet been merged into the124-record identity crosswalk.

GeneralSamplingInput carries the exact admitted original and its optional actual
WorldEventTruth210. Its direct ObserverId field equals the request/profile observer
and supports the accepted field-based PRJ mechanism. Source binding membership is
verified before projection. Only the sampling stage may receive world data. Its body
read uses the PRJ/IDN-resolved character and the compiled local-reserve key. Neither
the original profile reference nor world records are forwarded to perception.

GeneralTrackingInput transports the completed safe samples and use plan, with a
direct ObserverId. The use plan is committed dispatch data, never a learned judgment
or authority to read arbitrary definitions. Its exact bytes survive transport; no
handler may turn cue-only input into a selector or invent a goal assessment. Goal
definition references resolve only at their separately registered consumer.

GeneralBindingInput adds actual tracks and an optional actual event transition.
GeneralFreezeInput adds the actual binding batch and serves both the classification
and freeze stages; a new wrapper is unnecessary merely to cross another phase.
The first homogeneous profile emits no classifications. GeneralUseInput contains
requested body/visual opportunity views, the preserved use plan and actual causal
role claims. The same record enters role derivation with an empty claim list and
leaves with its actual newly produced claims. Exact producer association distinguishes
these uses; shape equality does not authorize claims supplied at the earlier edge.

Body/visual views are present iff requested, even when unavailable. Any experience in
those views is the same actual frozen227, not another allocation. Panel-only sensing
still outputs its experience when justified; it need not fabricate a body/visual
view or an attention consumer. PerceptualEventEnd220 outputs remain actual perception
results and need not be copied through later wrappers that do not consume them.

World production occurs at0. The two chains are10/11/12/13/14/15 then40 and
120/121/122/123/124/125 then130. The world handler chooses exactly the lane admitted
by the original. A phase0 scene does not pre-sample physical reserves: consequence
sampling reads after any lawful110 replenishment. Each source chain costs eight
scheduled events including world and dispatch. This is a source-only work subtotal;
it is not the whole-instant settlement ceiling.

Only tracking writes the accepted observer-owned perception values. Sampling may
read the separately classified physical root through its authorized channel; later
stages cannot. No source registration is on route/character-learning. The distinct
phase130 AcquisitionFormationEvidence producer still owns that route entry. Immediate
encoding, recall, goal evaluation, owner batches and protocol hooks are not folded
into the source dispatcher. It emits their separately typed, admitted inputs.

Exact physical/state path patterns, accessor definitions, allocation/output rules,
producer attachment, selected-capability reconstruction, source-frame coverage,
canonical role recursion for these five wrappers and all downstream registrations
remain whole-profile gates. The machine graph is checked for topology and transport;
its named read/write values are not yet executable ReadDomain declarations.
