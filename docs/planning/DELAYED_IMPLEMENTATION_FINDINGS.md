# Delayed gratification implementation findings — 2026-09-25

LOCAL DISPOSITION. No owner ruling or reduction. Counters1400/24 pending verdict.

The preliminary allocation used ref142 for stage read/write patterns; the codec
rejected it before any model freeze or public run. DELAYED_SCHEMA_REJECTED_REV1.json
retains that complete rejected allocation and its digest. Corrected patterns use
inherited149. No already-frozen identity or schema bytes were changed.

The first24 scoped tests passed. Static review added direct choice-distribution
comparisons and rollback during actual due delivery, rather than counting different
appraisal labels as evidence that a law affects decisions. The expanded27-test
suite passes; both test receipts remain. The three frozen candidates retain exact
reason/dice computation. No empirical discount-curve selection is claimed.

An unknown future offer does not mean known zero; an absent offer is different again.
Missing receipt after a passed due time still supplies no outcome evidence. Known
zero requires an admitted receipt. Rereading the same visible receipt does not add
learning weight. This is retained outcome evidence, not learned forecast calibration
or a later adaptive choice. The one-shot eight-instant domain admits delays1..4 so
all choices at2 have a scheduled physical settlement within the source horizon.

Delay and waiting cost use the same benefit units in a finite control. They do not
establish a cross-benefit currency, rational/optimal patience or a personality trait.
An immediate-value intervention changes the promised immediate amount; actual amount
is independently authored and can differ even for an immediate transaction. The
primary promise failure comparison keeps the earlier choice and expression fixed.

The executor's delivery root is world state; character appraisal reads only its own
admitted offer, chosen plan and learned receipt. Late common false receipts deliberately
preserve the whole safe view despite changed physical delivery. Execution truth is
not back-projected into the character. Future trust, endogenous waiting effort,
satiety, renegotiation/abandonment and multi-goal temporal planning remain unqualified.

RO010/012/014/019/020 retain these distinctions and limits;021 historical reconciliation
remains mandatory before Campaign3 exit. No earlier task, identity, memory, BODY or
commitment mechanism is retired. The schema failure is structural, not a failed
psychological candidate; all three discount candidates remain serious comparators.
