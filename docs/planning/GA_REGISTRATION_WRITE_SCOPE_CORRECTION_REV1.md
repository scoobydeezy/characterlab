# General registration write-scope correction

LOCAL DISPOSITION — no owner ruling required. Bounded representational correction;
no runtime or model activation. Preserve the frozen704/1 wrapper and all prior bytes.

The current wrapper's WriteAuthorization aliases existing322/1, whose actual name
is WriteCapabilityV06. It names MutationAuthorityId and Campaign2StateFamilyId values;
it does not carry explicit StatePathPattern values. Its use by existing task wrappers
does not by itself force every such wrapper onto a learning route. Nevertheless,
perception and physical ownership already have a more appropriate accepted explicit
path representation: AttentionStageRegistration541 field11 and
ReserveReplenishmentRegistration480 fields8/9. There is no reason to invent new
cognitive family identifiers merely to serialize those scopes.

Select a distinct successor GeneralStageRegistrationV02 with identical fields except
WriteAuthorization becomes a distinct-record union of322/1 and proposed
GeneralPathWriteCapability. The latter contains one MutationAuthorityId and a nonempty
bounded set of StatePathPattern149. Its name describes storage authorization, not a
psychological capability. The fixed compiler, not the generic codec, admits exact paths.

Read-only stages require322 ReadOnly. Character-learning owners retain322 StateWrites
with their accepted family memberships. Current/consequence perception and physical
replenishment use explicit path scopes. The two non-learning goal lifecycle owners
may also use explicit path scopes for their existing MaintenanceGoalState; this does
not change that root's accepted classification or holder qualification. No transition
may name the FormationGovernanceState protocol root in either representation.

This avoids both a hidden extension of the Campaign2 family registry and a false
ReadOnly declaration on a writing stage. Preserve322-based family authorization as
the exact cognitive-owner path; preserve direct path authorization for the already
accepted non-learning owners. Do not reinterpret or revise704/1 in place.

The successor needs its own symbolic review and separate append-only allocation.
No old record, member, authority, route, output, mathematical law or model changes.
Exact write patterns, subject checks, model packaging and General Attention remain OPEN.
