# Explicit attributed-child credit A accepted

2026-09-13. User ruling accepts outcome-significance credit only for currently
surviving semantic children explicitly targeted by the authenticated observer-side
attribution result. Incidental siblings and consumed comparison evidence do not
receive significance automatically. Use protection and target credit stay distinct.

Duplicate target entries normalize to one opportunity. Missing/lost targets and
foreign observer/subject reject. False-but-supported attribution is allowed; hidden
causal truth cannot correct or veto cognitive targethood. Episode-context spread
remains a named later comparator requiring its own mechanism and proof.

[Boundary component](../formal/ATTRIBUTED_CHILD_BOUNDARY_COMPONENT.md) checks supplied
trusted projections, deduplicates exact addresses and validates current retention.
[Ten tests](ATTRIBUTED_CHILD_BOUNDARY_TESTS_REV2.json) pass, comprising six boundary
and four existing qualification vectors. REV1 is preserved: its one failed expected
order incorrectly assumed numeric order rather than canonical byte order. The
implementation's canonical ordering was unchanged; only the fixture expectation
was corrected. TypeScript and reference-boundary checks pass.

This is NOT authenticated causal-target production. The actual attribution component
still outputs Supported/Unavailable and consumed operands. It has no qualified
candidate/trial→target projection. Neither symbol equality, byte equality nor the
new boundary normalizer fills that missing source proof. No public target join,
significance write, persistence or false-attribution experiment is claimed.

The next representation decision is [retained significance state](GENERAL_ATTENTION_SIGNIFICANCE_STORAGE_DECISION.md).
It must stay independent of the still-required public target/source contract.
