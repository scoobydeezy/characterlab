# General attention role crosswalk — revision11

2026-09-13. LOCAL symbolic disposition; public compiler/allocation remain OPEN.

Add the three earlier-concern wrappers to revision10. TaskWorkspace381 and
TaskAppraisal384 inherit their accepted recursive record roles; neither becomes
an opaque blob or a new output identity. Each wrapper has direct ObserverId for
PRJ/IDN, while actual producer and nested-holder equality remain separate checks.
FeedbackDelivery recurses into its already inventoried narrow fields.

The resulting135 proposed records have134 scalar identity positions and192 record
boundaries. This declaration audit does not pass source authentication or public
AJB vectors. No permanent namespace/member/record allocation changes.
