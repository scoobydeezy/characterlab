# Campaign2 autonomous adversarial qualification

2026-09-09. The user explicitly authorized independent work and adversarial self-review
through Campaign2 completion. Entries below are agent self-review, not planning-agent
or user research verdicts. Previously accepted decisions and scope splits remain intact.
No test count alone qualifies a seam or the campaign.

## Authority and campaign gate

North Star owns capability/invariant requirements; CHARACTER_ARCHITECTURE owns
topology, epistemic separation and state ownership. Research Program Brief owns proof
and campaign method; accepted formal contracts own exact semantics. The Campaign
Plan's Campaign2 completion requires the full thin causal event, sole state authorities,
retained dice/identity pipeline, separated truth/evidence/intent/attempt/outcome/trace,
and no relevant P0. ADAPT, PHEN-ADAPT and FCT closure alone cannot satisfy that gate.

## AD-E7 self-review disposition

**PASS in the submitted finite public/component scopes.** Lifecycle correction
ACCEPTED by self-review. Evidence: CAMPAIGN2_AD_E7_CONSOLIDATION_REVIEW.md,
CAMPAIGN2_READ_EVIDENCE_PROOF_REV2.json; final targeted80 tests/eight files PASS and
TypeScript PASS. Existing read-evidence and binding-isolation corrections retain
their external acceptance, distinct from this new self-reviewed disposition.

Adversarial questions resolved:

- Does a sharing test merely swap trace data? No: the constructor returns one actual
  projection instance twice, and the test verifies identity before second-rule rejection.
- Can reentrancy advance the next admitted token while A is active? The reproduced
  failure is corrected before cursor advance; the test attempts B after A target and
  before A gate, and separately attempts finish during the last active evaluation.
- Do checks create a second state oracle or alter WRT precedence? No: instrumentation
  values come from real read returns; lifecycle checks grant no state access.
- Are generic permissions mistaken for public capabilities? No: exact-domain generic
  failures and private binding snapshots compose with named public hook rejection,
  existing one-character admission and fixed internal rule-binding construction.
- Are anonymous derive closure purity or arbitrary reflection claimed? No; both are
  explicitly outside qualification. ADAPT's fixed bindings are direct.

The correction conforms to frozen non-interleaving and capability requirements; no
new cognitive state, semantics, allocation or profile is needed. Whole B/FCT remain
OPEN for remaining vectors/composition and deliberate source-sensitive assay refresh.

## AD-E11 / AD-E12 evidence in progress

New route matrix swaps only the payload of four genuine generated/admitted consumer
events: learning evidence into each ADAPT consumer; AAI into each EVID transition.
All four reject INPUT_NOT_ADMITTED and preserve committed state/trace/output/clock.
Self-review corrected an initially confounded procedural case: it now uses a genuine
practice source and procedural child, not a regulatory child relabelled procedural.

New AD-E12 finite stage matrix: after real reads, real later out-of-range load candidate,
after five staged dispatch/evaluation outputs, after completed four-leaf patch union,
final trace, and committed work limit8. All six pass exact error and complete captured
runtime state/queue/allocator/output/trace/clock rollback with terminal Failed/save refusal.
Four removable injected faults restore a pre-failure save and match clean continuation
through both queued instants. The real-invalid and work-limit models remain invalid;
the tests do not pretend restoration repairs them. Combined with trace controls:
15 tests/three files PASS. Further adversarial dependency and mutation review remains
before recording these vector passes.

## AD-E11 self-review disposition

**PASS in finite route/dependency and accepted generic/public scopes.** Evidence:
five campaign2RouteSeparation tests, existing TRACE-C2-D/I paired immediate safe
OBS/SEM/EVID equality under count0/2 with divergent ADAPT state, and refreshed
CAMPAIGN2_FCT_C_PROOF_REV2.json. The refresh repeats both generic roster baselines
and public exclusion and adds ordinal/ancestry/perceived-success named capability
requests:18 inner ILLEGAL_READ / outer TRANSITION_FAILURE witnesses, no roster reads,
and rollback. The original12-case report remains historical.

Adversarial precision: occurrence identity is legitimately carried to its output
identity field; it is not asserted to be unreadable to arbitrary trusted JavaScript.
EVID exact output closure requires the reserved identity and unchanged predecessor
embedding, and exposes no state/event/trace/parent/content/registry or named success
operand. ADAPT's arithmetic receives only count, signed Step and actual scalar priors.
An additional public zero-count history changes real occurrence/event/parent IDs
without changing the later exact ADAPT patch or final state. Current
CAMPAIGN2_ROUTE_SEPARATION_PROOF.json detects use of event ordinal or causal parent
as a magnitude operand. No claim of universal host-code noninterference is made.

The route matrix uses genuine regulatory/practice sources and generated events;
only forbidden payload changes. All four INPUT_NOT_ADMITTED results therefore
exercise source/payload admission rather than a relabelled event shortcut. The
North Star's truth/evidence/adaptation boundary is preserved; no appraisal, reward,
success judgement or belief mutation is introduced.

## AD-E12 self-review disposition

**PASS for the frozen finite failure-stage matrix.** Six exact-code tests execute
after reads, a real later magnitude failure, staged outputs, patch union, final trace
and work-limit exhaustion. Real later overflow has more than one completed read
segment, so it is not an initial-state validation failure. Every negative asserts
Failed, save refusal and complete state/clock/queue/allocator/output/committedTrace
equality. A second queued instant is retained. Four removable faults additionally
restore the pre-failure save and match clean canonical continuation through both
instants. Invalid model candidates and insufficient work budgets are not repaired.

CAMPAIGN2_ADAPT_ROLLBACK_PROOF.json: six-test baseline PASS; publishing failed
working state, queue, allocators, or trace+outputs each DETECTED by the designated
final-trace witness. Runtime/scheduler/evaluator/test fingerprints are recorded and
unchanged across the assay. Existing C batch collision, REG pre-repair and EVID
failure controls retain distinct ownership. This qualifies the named matrix, not
all conceivable host failures. No persistent partial evaluation is introduced.

AD-E3/4/7/8/9/10/11/12/13 now have explicit accepted or self-reviewed dispositions.
AD-E1/2/5/6 and whole B/FCT still require the final current-source composition audit;
earlier informal remaining-vector lists do not substitute for that explicit receipt.

## AD-E1 / E2 / E5 / E6 self-review disposition

**PASS in the frozen finite controls.** Reviewed the actual fixtures and refreshed
current-source assays, not just their previous summaries:

| Vector | Distinctive evidence and adversarial result |
|---|---|
| AD-E1 | Batch uses one prepared model, Step=2, n=0/3, exact q=6 versus NoStateChange, equal rule/path/read topology and allocator/output identity positions, different RunIdentity. REV2 batch assay detects zero-count rule filtering. |
| AD-E2 | Actual nonempty resolver with wrong referent, separate empty procedural resolver, and nonbaseline false gate: one dispatch survives each; empty results have no evaluations/reads/patch, false gate retains its evaluation. Applicability REV3 detects ignored match, omitted empty dispatch and state-filtered applicability. |
| AD-E5 | Two committed identity orders yield exact tolerance1/load2 and three absent reads; model identities differ and actual output path order reverses. EVID/REG construction REV2 detects the gate reading an earlier staged write. This is a separate-model order control, not the fixed-model phenomenon pair. |
| AD-E6 | Zero-count and false-gate colliding pairs reject before any projection read and preserve complete captured runtime data. Separate noncolliding regulatory/procedural EventSequence permutations yield equal settled state. Batch REV2 detects removed collision preflight. Trace ordering need not be equal. |

The independent gate REV2 assay additionally compares72 cases with an independent
arithmetic implementation and detects six semantic substitutions. None of these
tests grants new state authority, treats a false gate as an inapplicable rule, or
changes the accepted frozen baseline. All AD-E1..13 now have explicit external or
agent self-review PASS receipts in their recorded finite scopes. Whole B/FCT still
requires the inherited REG/EVID and factory composition audit.

## REG-E key-preservation correction and self-review

The first new two-variable test distinguished different authored anchors under
identical scales/bounds, but numerical rejection alone did not satisfy the frozen
"before arithmetic" clause. This was an evidence and implementation gap, not a
reason to weaken REG-E. The ADAPT integration now compares the query character and
variable against the selected RegulatoryAdaptationKey before invoking REG. Both
candidate magnitude and retained-state reference validation use the same internal
guard. REG's public C,V,T,D contract, scalar grammar and authored reference stay
unchanged. No duplicate identity, domain metadata or provenance record is allocated.

The real generic component admits two variables with equal domain and anchors50/80.
The explicit mismatch test uses D=0, valid numerically for both, rejects INVALID_PATH
and observes zero calls to the actual REG arithmetic; the matching query calls it
once and returns Valid. Separate D=30 controls distinguish the exact references.
The bounded public first profile still excludes two variables. The current
CAMPAIGN2_REG_KEY_PRESERVATION_PROOF.json detects omission of the variable guard
and V1-selection/V2-query substitution. TypeScript PASS.

Self-review accepts this internal correction and REG-E's keyed integration scope.
It does not defend arbitrary replacement of both selected key and query in trusted
host code. The guard introduces no character-state read or write, no new failure
family, no model/profile version and no REG re-anchoring. Operand-schema negatives
and remaining REG-M construction checks continue to have separate proof ownership.
