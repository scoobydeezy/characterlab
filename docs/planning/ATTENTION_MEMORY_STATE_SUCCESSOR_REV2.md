# Ordinary memory state/read successor inventory

2026-09-12. PARTIAL SYMBOLIC DRAFT; NOT ALLOCATION INPUT. Correct-forward successor
to ATTENTION_MEMORY_STATE_DECLARATIONS_REV1; earlier bytes and receipts stay historical.

The machine packet retains four provisional root/owner names and exact existing
perception accessors plus PRJ/IDN requirements. It replaces invalid shared-presence
and graph-membership coupling. One empty or absent graph does not remove episodic
targets. A graph-only key may mediate spread but never manufactures a target. Direct
episodic cue matching uses actual surviving keys independent of graph membership.

Current ordinary content contains surviving semantic children, separately budgeted
by kind under one owner. Complete loss removes the target, not historical acquisition.
Presentation absence is not proof of episode absence; it contributes no presentation
history. Exact presentation seed/update/cleanup still needs the successor contract.
No reader may recover forgotten children through the source selection, formation
carrier, governance history or graph. Qualified CharacterId still comes only from
accepted source admission and PRJ/IDN; identities are never broad read capabilities.

Old nonempty EpisodeLedger/AssociationGraph layouts cannot represent complete loss.
Exact absent-versus-empty encoding and graph timestamp behavior must be resolved in
the full carrier revision. Do not reuse old max4/max12/max5 limits as new cognitive
or lifetime policy. Historical initial evidence is not a permanent current payload
archive. Public child fields, bounds, roots/accessors and role allocation remain pending.

The source replay representation is the next substantive choice. Acquisition IDs
remain nonreused, but allocator monotonicity alone does not remember which selections
formed. The machine review exhibits two different formed-source histories with the
same maximum source number. A high-water mark is insufficient without a newly accepted
contiguous admission/finalization protocol. Five static packet corruptions reject;
these are planning consistency checks, not runtime conformance.
