# Observed sparse/dense acquisition through learned recall

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Actual component
composition; public model, owner registration, persistence and corpus gates OPEN.

Four actual visual opportunities keep two observer files continuously tracked during
formation. At1 only A is focal; at2 only B is focal; at3 both are focal. Both objects
are genuinely sensed in the first three opportunities. The existing capacity2 selector
admits their actual role evidence, and the spatial model's zero residual pool gives
peripheral content KnownZero. Actual positive projection and the formation owner
therefore produce acquisitions containing A, B, then A+B. No complete raw-source
archive is supplied to recall. At4 only A is observed as the current cue; no selection
or acquisition runs for that cue.

The existing encoding law retains its homogeneous base3/10, giving dense strengths
3/13 and27/127. The existing association operator receives those actual positive
strengths, eta5, zero decay and scale1000. Its off-diagonal mass is245. Structurally
empty graphs after the first two sparse formations contain no nodes; the third
formation creates two nodes and two positive directed edges. These graph changes
do not rewrite earlier episodic contents.

Recall uses the actual canonical event adapter, beta1/2, scale1000, lambda1,
exponent1, omegaB11, omegaA1 and k2. Formation seeds one presentation at each actual
acquisition instant. Both models recall the joint acquisition first. With learned
spread, B's acquisition at2 is second; with learning disabled, A's acquisition at1
is second. The entire retained memory and protocol values are equal between those
models. The difference comes from the independently owned learned graph.

Complete graph removal preserves all episodes and restores the direct-only result.
Current cue absence prevents recall despite retained episodes and learned edges.
Hidden world referent renaming leaves the observer-side result unchanged. Recall
does not mutate memory or formation protocol and does not append presentation/use
feedback merely by returning results.

The learning-gain choice is an experimental calibration under the already accepted
operator, not new mathematics or a universal psychological parameter. Gain3 is an
explicit nondiscriminating control; gain4 also distinguishes the recall models.
The first development report incorrectly predicted strengths from base1 instead of
the actual3/10 calibration and did not obtain its proposed rank difference. Preserve
that failed report. Revision2 uses the actual strengths and stated gain5, and passes
six controls. No arithmetic implementation or source validator was changed to pass it.

This supplies an observed-source witness for MEC-007/008/009/010 composition and
preserves the accepted direct-cue/learned-graph separation. The fixture uses actual
component owners and canonical view bytes, but its enclosing retained carrier and
subject bindings remain trusted test data. Source-only finite schedule admission is
not public PRJ, a whole registered batch, graph-loss scheduling, complete-prefix
proof, retrieval feedback qualification or corpus promotion. Those gates remain.
