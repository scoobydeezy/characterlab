# General attention closure work plan

2026-09-11. User-authorized autonomous research, review and implementation. General
ATTN-001 remains OPEN. This plan specifies completion obligations rather than treating
the qualified finite selector as general attention. Existing accepted bytes and
corpus0.27.0 remain immutable. Every new public seam must pass symbolic acceptance,
separate numeric allocation, exact model packaging, execution and preservation gates.

## Closure obligations

1. Preserve AT2-A..N and the actual observer-safe source. Extend applicability only
   through explicit source contracts; missing/multiple roles never become Incidental.
2. Separate permission, active selection, continuous allocation, encoding strength,
   retained importance, current accessibility and retrieval reinforcement. Execute
   independent/shared/hybrid and retired flat-tag controls on the same safe inputs.
3. Form actual subject-owned episodes and associations from completed selected
   evidence, with accepted PRJ/IDN, one writer, bounded exact state and provenance.
   Keep formation separate from read-only recall and subsequent reinforcement.
4. Execute matched sparse/dense encodings followed by later cue/retrieval probes.
   Preserve recency/frequency/decay controls and distinguish associative pull from
   both encoding coactivation and historical importance. Prove no hidden truth read.
5. Bind real later affect feedback to attention/accessibility. The current transient
   TaskConcern is not persistent affect and cannot be silently read at another instant.
   Freeze the minimum later-feedback carriage/state seam and its authentic producer.
6. Qualify whole-prefix replay, each-stage rollback, exact read/write/identity closure,
   live-source negative controls and computational alternatives under frozen models.
7. Freeze an exact ATTENTION corpus fixture, comparison/coupling rules, ExperimentIdentity,
   aggregate manifest and promotion history. Run the retained corpus and record a
   coverage argument against North Star/Architecture/Brief. Close ATTN-001 only when
   all its actual obligations have a tested disposition; no universal cognitive claim.

## Inspection findings that constrain the design

The current measurement memory contract inserts an immutable measurement episode
and recalls an exact key. It has no semantic encoding strength, generic association
graph, decay, candidate ranking or retrieval reinforcement. Its existing record
shapes and meaning must remain unchanged. The attention source creates fresh observer
files per original; cross-event continuity is not established by equal truth objects,
port numbers or matching opaque ordinals. Later cues need their own admitted source.

Historical MEC-005 uses a residual1/5 pool for positively identified Incidental units.
Current SEM supplies no Incidental or Cause derivation. Unresolved and Beneficiary
inputs cannot be reinterpreted as that positive judgment. A historical math control
is expressible now; a public residual witness needs additional observer-safe semantics.
MEC-007's category priors also cannot be recovered from identity text. Use actual
admitted classification evidence or an explicitly restricted homogeneous category
control; do not claim general category sensitivity from such a restriction.

MEC-007 multiplies category, role, continuous attention and bounded relevance/surprise.
Its independent law is x/(1+x). Historical shared is x/max(B,sum(x)); it is **not**
B*x/max(B,sum(x)). Historical hybrid exempts important entries and uses the remaining
budget as the denominator floor for low entries. Thus neither its label nor the word
budget proves total allocation <=B. Preserve exact controls; any conserved-resource
alternative gets a distinct contract/model, never an unannounced correction.

MEC-008 decays and adds eta*z_i*z_j, rounds each entry to integer lattice mass, then
applies largest remainders only when row mass exceeds the scale. MEC-009 solves
a=(I-beta W)^-1 b for row-substochastic W and beta<1, quantizing after the exact solve.
MEC-010 computes decaying presentation-history contributions, averaged associative
pull and canonical top-K; retrieval appends a presentation without changing episode
facts. These are independently testable mathematical controls, not admitted public
record producers. Historical clamp-of-future-time behavior is not carried into new
admission: future history must reject.

## Explicit mechanism dispositions

MEC-005 CONTROL+CONTRACT; MEC-007/CTL-004 CONTROL; MEC-008 CONTROL with sole-writer
and largest-remainder invariants; MEC-009 CONTROL with exact solution and a separate
local alternative; MEC-010 CONTROL+CORPUS. EXP-007/015 remain required comparison
obligations. RET-001 remains retired, executable only as a named negative comparison.
RET-002 and RET-006 remain prohibited source shortcuts. No reference module is imported
into src. The Vivarium formula inventory is not being imported; these formulas are
restated directly from the inspected historical control sources.

## Immediate sequence

Qualify the exact mathematics component first, without identities, canonical records,
public ingress or state ownership. Then close the semantic carrier/subject/cue and
feedback inventory before choosing persistent layouts. A passing arithmetic kernel
does not close any of those expressibility gaps or authorize corpus promotion.
