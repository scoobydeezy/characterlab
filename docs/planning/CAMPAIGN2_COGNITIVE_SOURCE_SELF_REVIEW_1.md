# Cognitive source closure — adversarial review1

2026-09-09. Internal agent review. Directional decisions only; no whole shape,
allocation, profile freeze or canonical cognition implementation is accepted here.

## Findings that govern the next draft

The task lifecycle is now qualified separately. Existing prediction reads return
whole361/absence through a derived character/definition key; the new workspace must
declare its own exact source/read admission and cannot treat historical366 output
as an arbitrary live input. OutcomeEvaluation remains the accepted thin EVID carrier;
the new goal-relative appraisal is a different causal box and record chain.

Actual275 has only frozen SEM producer and registered-transition producer variants.
It does not express an original InputOnly opportunity. The workspace therefore needs
an explicit original-source registration/profile boundary, like the already governed
private clock/read opportunities, rather than a fictitious275 producer. Generated
appraisal/concern/context stages can reuse271/272 with exact274 registered-parent
admission,273 NoStateWrites,276 ingress and277 one-output closure.

Occurrence identities belong solely in279/3 via278. Do not repeat the old prediction
output-identity mistake by placing a second rule in a new output definition. Every
semantic stage emits one aggregate record, including empty branches. Individual
task items, plan origins and candidate keys require no independent occurrence.

## Original-input composition needs a successor profile

The current orderedInputs compiler admits only authored facts or probes at110.
Its probe uniqueness check counts every same-instant original entry. Simply adding
a40 opportunity to that decoder would either reject intended same-instant cognition
or silently widen the accepted old profile. Neither is acceptable.

Specify a new exact cognitive ordered-input version. Preserve the original entry
grammar and source authority; admit a declared DeliberationOpportunity at40 with
ObserverId and agenda only. Keep original fact/probe rules intact in the predecessor.
For the successor, separately define at most one cognitive opportunity and at most
one probe-compatible physical original source per instant. Original IDs/sequences
still allocate in manifest order, and all private deadlines allocate afterward.
No generated deliberation/workspace/appraisal/intent may be resubmitted as an original
source or injected through save editing. The same original-compiler authority should
authenticate the new root; do not create a parallel provenance or runtime allocator.

The exact treatment of authored facts, probes and a later generated attempt at one
instant must be explicit in the final profile. In particular, the existing local
probe output/child counters are not a license to exempt cognitive work from the
global settlement-work budget. The full event budget remains to be derived and run.

## Plan state decision in direction

Prefer a successor root373 schema with a new separately named, immutable-after-S0
instruction map over changing372 or adding a hidden second prospective root. Actual
stateModel supports disjoint read-only and writable patterns within one root.
Keep lifecycle field1 and its ownership untouched; add the new leaf explicitly to
the successor family/state/key-role declarations. Its field number remains unallocated.
The mixed topology needs construction proof and exact whole-profile closure.

The adopted instruction references a zero-argument protocol action through an exact
DefinitionId/1027 kind/schema/version. It does not reuse ProcedureId/1034's competence
meaning, import a world-effect table or imply learned efficacy. Action semantics
remain a real pending seam; an opaque unresolved definition is not activation-ready.

## Availability and motivation must survive separate interventions

The prior research draft could be read as constructing options only from emitted
base motives. That would confound base-source ablation with option unavailability.
The new plan/options draft instead carries the exact maintained task context through
the motive output independently of its base-source list. Candidate construction uses
that context and explicit adopted instructions. Thus options may remain available
when base motivation is disabled, while non-generating modifiers cannot create a
semantic reason from nothing. This is necessary for MEC-011 and the retained
activation wall, not an optimization.

Deduplicate complete actor/action tuples and retain their distinct task origins.
No-current-plan and no-active-reason are different absences. Empty outcomes cannot
be repaired by a default action, lowest ID or unscoped random choice.

## Evidence-overlap finding

The actual retained consolidator uses weighted Jaccard against union-max aggregate
coverage. It defeats the required collective-redundancy fixture, but it does not
eliminate every covered subset of a larger aggregate. TRC_004_EVIDENCE_COVERAGE_DRAFT
records the2/3 subset example explicitly. Preserve that mathematical limitation;
a directional coverage denominator is a competing model, not a silent correction.

Original observation support may receive unit membership coverage in the first
sensory source. Do not normalize it into invented confidence or use new appraisal
IDs as independent samples. Actual qualified-expression support remains necessary
before standing identity enters the same coverage framework. Atom/source admission
is still a blocker to whole TRC-004 acceptance.

## Calibration must be tested against generated operands

The earlier identity activation exploration used explicit component signals and is
not production calibration. With a permitted forecast3, the frozen overlapping
task intervals yield distances1 and2, not the exploration's arbitrarily supplied
context strengths. The eventual concern/motive/activation choices must be tested
from those actual operands. If a different scene or longer-lived task instances
are required to earn identity evidence, freeze a new exact model profile rather
than modifying the existing task model or merely preloading a trait.

Remaining agent-owned work: exact action instruction semantics, source/registration
closure, TRC-004 atom/raw-signal contract, retained Reason/dice/arbitration and DEC-001,
then joined allocation/profile/implementation. No user decision is currently needed.
