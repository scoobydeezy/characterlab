# General attention owner composition

2026-09-13. LOCAL DISPOSITION — no owner ruling required. General Attention remains
OPEN; component and symbolic review do not establish public qualification.

## Current evidence

- GA_PRODUCTION_BOUNDARIES_REGRESSION_REV4:235 tests/34 active-source files PASS.
- GA_PRODUCTION_BOUNDARIES_REVIEW_REV9:62 injected source faults detected.
- GA_ASSOCIATION_OWNER_TESTS_REV2:seven graph-owner controls PASS, including the
  strengthened asymmetric matrix/permuted-key check. Typecheck and boundary check PASS.
- GA_CALIBRATION_REVIEW_REV1:twelve definition shapes and12 structural faults PASS.
- GA_IDENTITY_ROLE_REVIEW_REV14:159 records,162 scalar positions,201 boundaries and31
  structural faults PASS. All newly added policy references reuse DefinitionId1027.

The REV3 regression records the discovered graph ordering defect. Correct-forward
REV4 verifies the fix; the old failed report is retained. The owner now reorders both
node keys and matrix axes canonically. REV8 mutation review predates that correction;
REV9 adds source substitutions for key ordering and matrix reindexing.

## Implemented

Observation policy binding checks exact requested-purpose slots before any model
lookup. Durable acquisition, body recall and goal-baseline recall remain independent.
It checks definition kind while leaving exact governed schema/value admission to the
public compiler. There is no second registry or runtime clock-selected policy.

Ordinary memory has separately named age-only, use-only and shared-protection controls
alongside significance-first. They share source/protocol validation and common-B0
batching. Ignored metadata survives; current credit cannot rescue current loss.

The main attribution fixture now combines the empty selector enrollment and actual
use in one71-instant batch, commits significance separately at72, and uses actual
owner results for75-instant retention/protocol loss. Negative source counts are intact.
The supplied later-use comparator remains explicitly labeled; intact actual-use needs
no supplied use of the fresh competitor.

Event presentation settlement seeds only actual event formations and removes history
only on actual complete acquisition loss. Both principal fixtures use this owner.
Recall/ranking/attribution use does not append history. Later presentation authentication
is still unqualified. Body history is not introduced.

Event association settlement derives decay elapsed from LastUpdatedAt, then runs the
accepted arithmetic and independent graph retention. Retention-only preserves that
clock; actual formation advances it even at zero graph capacity. The observed sparse/
dense fixture now uses this owner. No episode-loss operand enters the graph owner.

## Next local work

The symbolic retention original/dispatch/three-owner slice is in
GA_RETENTION_STAGE_CLOSURE_REV1. Complete the actual recollection publisher and explicit
presentation branch, then exact calibration instances, registration/input/output
patterns, owner-composition bindings and whole-model work ceiling. Separate numeric,
model, public execution, replay/rollback and corpus gates remain mandatory.

No permanent allocation, public model activation or corpus promotion occurred here.
Frozen corpus0.27.0 and all accepted Campaign2 commitments remain unchanged. There is
no demonstrated architectural fork requiring owner intervention.
