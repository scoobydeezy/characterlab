# General attention retention stage closure

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic registration
proposal; exact public patterns, registration instances and allocation remain OPEN.

A separately authored retention original references one immutable control definition.
That definition binds existing memory-retention, graph-retention and association
calibrations using DefinitionId1027. It carries no desired survivor, loss address,
memory payload, hidden truth or mutable capacity. The first recipe can therefore name
its retention instant explicitly rather than branch on a hard-coded clock value.

The original at0 schedules a dispatcher at130, which schedules ordinary memory,
event association and event presentation cleanup at140. No140 owner emits children,
outputs or occurrences. All three use required PRJ/IDN from field1 after exact original
and producer admission. All participate in route/character-learning. This slice adds
five events when independently scheduled, not five new selection sources.

Ordinary memory resolves its complete formation/use/significance/retention batch from
B0. Presentation cleanup uses the validated result descriptor to remove only histories
whose acquisitions actually disappeared. That descriptor is an owner-composition value,
not a sibling state read, observer evidence or a new persisted record. Graph retention
uses its own B0 graph and bound, with no episodic survivor operand. If a same-instant
event formation is admitted, the graph owner applies that one formation and retention
together; it does not run two sequential updates or decay twice.

The retained source/recall/component limits remain unchanged. Presentation cleanup is
not a later presentation: it appends no timestamp. The later actual-presentation
producer and its authentication remain a separate unresolved public branch. No
automatic recall/use-to-presentation edge is added by this packet.

Executed components now exist for all three operations. The main significance fixture
uses actual memory/protocol and presentation owner results. The observed association
fixture uses the graph owner for formation and independent loss. Those facts do not
pass runtime input authenticity, writable-state roles, owner-batch hooks or persistence.
