# General Attention source/protocol checkpoint — 2026-09-19

**Disposition:** LOCAL DISPOSITION; GA-owned source and formation-profile declarations implemented. Whole compiler/public GA closure OPEN.
**Stage (Brief §9):** A — declaration admission, with actual source-component checks.

## Counters

| Counter | Value |
|---|---|
| Highest allocated record type | 706 |
| Allocated since last verdict/corpus member | 0 |

## What changed

`generalSourceDeclarations.ts` materializes eight scene/panel frames, one source profile, two exact use policies and eight observation originals. Admission resolves references, qualifies scene content, invokes the actual source constructors and schedule/policy validators, requires exact requested-frame coverage and rejects ambiguous visual cues. `generalProtocolDeclarations.ts` binds all four current/consequence visual/body selection producers to their actual audit/view schemas, selection field, subject projection and acquisition kind; it checks the dedicated nonremovable protocol singleton against its substrate ownership declaration and the ordinary formation owner.

The concrete requests exposed a previous compiler bug: five `FixedCompilerCollectionElement` positions were incorrectly serialized as scalar 265 RecordField roles. They now retain their fixed carrier-grammar checks; sets/lists are not interpreted as identity atoms. Regressions cover valid channel/cue/loss collections and wrong namespaces/occurrence payloads.

## Local dispositions

- Preserve MEC-007/010 source, selection, encoding, recall and retention separation from `REFERENCE_MECHANISM_LEDGER.md`. No historical mechanism or comparator is retired.
- Use the proposal's receiving times 3..10: four combined body/visual formation opportunities followed by four combined cue-only opportunities. Eight observations therefore declare eight selection sources, even for zero-capacity or denied visual branches. Cue-only requests contribute no source slot. Initial clock is 0; horizon is the existing 100.
- The three authored markers occupy x=1,5,7 with alternating y=0,1. Two four-frame neutral panel cycles use Before/Motion/Motion/After. Cue sweeps physically display only scene-a; the consumer still must resolve the actual sensed/tracked file. No cuePort/index-to-perceptual-identity shortcut is introduced.
- These are bounded source-instance choices, not the larger historical four-trial attribution fixture. The current originals use the Current lane and request no goal assessment. Goal lifecycle/consequence/attribution runs still require their separate committed schedules.
- The formation profile names the ordinary formation owner. Runtime reconciliation of other terminal retention/use/significance batches, inherited probe originals/state, physical initial anchors, and whole-model ownership are still integration obligations. A passing descriptor is not a completed hook invocation.

## Evidence

`GA_REGISTRATION_COMPILER_PROGRESS_REV3.json`: **146 tests / 12 files PASS**, including 22 new tests, production build and reference-boundary check PASS. All 28 frozen-manifest file commitments match. The tests materialize admitted baseline/zero-capacity/denied/unresolved scene variants through actual source components; they reject wrong references, missing/duplicate frames, wrong marker kinds, ambiguous cues, policy substitutions, producer/field/owner substitutions and ownership mismatch. Existing 30 recipe declaration tests still pass.

The earlier full-suite 1,918/252 result belongs to the preceding increment. This pass ran the affected suite, not another full-suite or reference-tree run. Frozen contracts, allocations, model identities, corpus and historical receipts are unchanged.

## Stage C competitor

Named zero-capacity, denied-port and unresolved-role controls stay distinct. Existing independent/shared/hybrid/retired-flat encoding and concern A/B/C remain retained alternatives. These admission tests do not establish psychological necessity or a new verdict.

## North Star transfer

Supports observer-relative evidence and separate acquisition/accessibility: physically available cue evidence remains available when selection capacity is zero, while denied evidence cannot become a cue by referring to an authored identity. This prepares the §30 angry-person retrieval archetype without asserting public behavioral coverage or general Affect.

## Next gate

Compile inherited read access and actual runtime output slots, then integrate source/subject/owner authentication and whole-model ownership. Compose cohort identities and inherited work budgets only after those declarations close; 80/58 remains GA-local. The same primary owner retains steps 2–3.

## Owner decision pending

No. Counters **706 / 0**; corpus/0.28.0 has 21 members and the verdict ledger has 12 entries.
