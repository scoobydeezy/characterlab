# General attention spatial binding checkpoint

2026-09-13. LOCAL DISPOSITION — no owner ruling required. GA remains OPEN.

The [spatial binding component](../formal/SPATIAL_EVIDENCE_BINDING_COMPONENT.md)
joins a positive observation to its actual event detection, track outputs and SEM
bindings under exact observer/time/support and file coverage checks. Position is
attached through the resulting continuant, not a detection ordinal or array index.
The complete list goes through the existing spatial allocator before selection.

The [selected spatial encoder](../formal/SELECTED_SPATIAL_ENCODING_COMPONENT.md)
then consumes the actual one-use SelectedView, checks canonical selected binding bytes
and the selected claims' experience/event identity, and releases selected witnesses
only. It composes Raw=Base*Role*SpatialAllocation under a separately named candidate.
It does not change the old role-calibrated encoder, create an Incidental role, or
recompute the peripheral divisor after selection. Unknown allocation has no numeric
strength; known zero is distinct. The retired-flat control's different zero behavior
is explicit and tested, not silently corrected.

[52 focused checks](GA_SOURCE_SPATIAL_REGRESSION_REV1.json) cover the spatial join,
new/old encoding, source composition and transactional panel/body branches.
[Nine current-source substitutions](SELECTED_SPATIAL_ENCODING_REVIEW_REV1.json) are
detected: array-index attachment, changed event detection, missing visual support,
unbound file, role-as-spatial allocation, Unknown-as-zero, foreign experience,
reusable failed capability, and changed selected binding identity. TypeScript and
reference boundaries pass. No historical implementation or numeric allocation changed.

MEC-005 remains a preserved Incidental control, while this candidate uses positive
spatial membership. MEC-007 role, category, relevance and surprise factors retain
their distinct status; base is homogeneous and Need/surprise are disabled here.
No psychological reduction, new causal edge or owner decision is introduced.

The SEM products are structurally checked supplied products. Public source completion,
three-object source completeness, actual PRJ, canonical selected wrapper, positive-only
formation owner and complete-prefix persistence remain OPEN. The four-trial fixture's
motion carriers still await this actual selected spatial path. Component selection and
source expression do not substitute for those public gates or corpus promotion.
