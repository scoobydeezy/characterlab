# Costly reward integration findings — 2026-09-26

LOCAL DISPOSITION. Counters1400/0. `costly-reward-component/0.1-candidate`.

The compatibility audit found that CONTROL's single reward Boolean cannot represent
simultaneous positive benefit and adverse outcome. Keep that frozen law unchanged.
The new component maintains separate learned estimates and separate reason grounds;
it does not reinterpret harm as negative reward or invent an Addicted scalar.
The exact inherited grammar admits two options, two nuclei and up to three addressed
draws. A third abstention or alternative satisfier would require another admitted
profile. Withhold is an actual option; NoActiveReasons remains a separate no-choice
outcome. Prior physiological components cannot be imported as character evidence.

The complete exploratory roster is COSTLY_REWARD_EXPLORATION_REV1.json:99 trajectories
across all six laws and13 cases, with all eight main/noLoad/withheld mean-model seeds.
The final50-run roster freezes those24 seed cases, ten mean controls, fifteen other-law
main/noLoad/withheld cases and BeliefAccess/otherCue. Exploration informed the tests;
qualification is deterministic replay of that explicitly frozen design, not a new
independent statistical sample. No development test or build failure occurred.

Seven main seeds produce at least two actual costly actions during load; seed3
produces one. Every seed retains and maintains its protective goal. Every noLoad
run withholds after practice; all main seeds withhold before and after load. These
are finite witnesses, not addiction prevalence or an inevitable relapse claim.

Feedback updates harm estimates without rewriting past choice. Withholding receipt
leaves the estimate at1/4. It does not increase harm counts for every seed: main
versus withheld free costly counts are2/4,3/3,2/3,1/1,3/2,3/3,3/3,2/2 for seeds0..7.
Four action sequences differ and four match. Do not infer monotonic behavioral
improvement from higher harm belief. Exact dice brackets can plateau, and each run
consumes the draws required by its actual resolution; this is not a population-rate
estimate or a universal pathwise monotonic coupling law.

LatestHistory changes the primary seed7 result (one costly choice versus two).
BeliefAccess matches the primary mean sequence but also acts under the other cue,
where cue-history access withholds. Thus primary repetition alone does not prove
habit-history necessity. NoControl allows costly action with spare capacity; RewardOnly
keeps admitted harm but removes its competing reason. RewardOnly has no chosen action
when A is inhibited and B has no active reason; never relabel that as chosen withholding.
Harmless repeated reward has zero actual harm. No acquisition/NoLearning retain
unknown, unlike known zero benefit. Failed execution can follow a chosen A and then
admit false outcomes. False reports can preserve belief despite different actual harm.

Durable lesson: independently represented reward and harm allow acquired approach
and protective goals to compete without hiding the conflict inside one reward bit.
Actual choice gates evidence and later learning; learning does not guarantee a
particular later choice. Repetition, harm, knowledge of harm and goal conflict are
separate observable conditions, not a diagnosis.

RO008/010/011/012/015/019/020 retain broader physiology, natural acquisition, calibrated
belief/valuation, actual alternative relief, learned control, identity integration,
public source admission and whole addiction progression. RO009 remains separately
conditional for identity. No reduction, state retirement or historical import.
021 historical reconciliation remains mandatory before Campaign3 exit.
