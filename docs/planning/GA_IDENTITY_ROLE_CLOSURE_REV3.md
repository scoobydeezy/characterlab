# Joined visual, feedback and state identity positions

2026-09-13. Symbolic declaration closure, not public compiler qualification.

Revision3 combines the prior82 records with12 state/window records and eight narrow
earlier-concern records. ConcernOccurrenceId remains1130 and TaskConcernResponse
remains386. No new feedback identity or scalar continuant/event namespace is created.

The six new physical root map keys use actual StateMapKey dispositions from the
root inventory. They are not ordinary scalar record fields or collection-element
roles. Composite keys within the formation/goal values still recurse into their
declared records. Protocol singleton has no map-key role. Existing241/242 and all
other inherited root/key/field roles remain independently governed by frozen profiles.

This closes the previously pending **symbolic** new-root key positions. It does not
establish actual membership/holder equality, content-validator execution, authority
coverage, read/accessor closure, source completion, numeric allocation or canonical
model admission. All public gates remain open. Earlier role packets and fingerprints
are retained as history.
