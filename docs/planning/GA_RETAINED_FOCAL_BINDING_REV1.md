# Retained focal binding — first profile

2026-09-13. LOCAL DISPOSITION — no owner ruling required.

Remove the source loop's retained candidate-address handoff. The already selected
focal consequence has its actual ExperienceId, carried by the committed qualification
projection in the eventual public path. At the later read, reconstruct trial groups
from admitted surviving views. Exactly one group's After experience must equal that
consequence. Its end-position view supplies the first profile's prebound candidate
child address, before contrastive attribution executes. No final-row, newest-global,
occurrence-order or source-array lookup selects the focal child.

This is the narrow existing focal-stroke-trial decision realized under separate
position acquisitions. End-position child is the fixed first profile designation;
the start position remains an independently consumed operand and can earn use, but
does not gain outcome credit by sibling/trial membership. Targeting both is not an
implicit extension. A later alternate designated-child model remains an explicit
comparator, never an unannounced change to this profile's targeting rule.

Supported still requires that this prebound trial actually satisfy the stroke law.
The consequence identity does not assert causality, success, current concern or
survival. If no complete recalled group contains it, the lawful path is unavailable
with no target, not recovery from the source schedule. A forged mismatching public
result/input remains admission failure; ordinary forgetting itself need not be treated
as corrupt input. The earlier scheduler loss fixture exercises rejection at its
supplied-target boundary and does not prescribe a public failure for ordinary forgetting.

The controlled source test now derives the address this way and permits either stroke
trial to be the explicitly designated focal consequence. Changing which actual source
consequence is designated changes only the eventual credited child; both analyses use
the same recalled trial evidence and contrastive rule. Public completed-output
authentication and the same-consequence carry join remain separate OPEN obligations.
