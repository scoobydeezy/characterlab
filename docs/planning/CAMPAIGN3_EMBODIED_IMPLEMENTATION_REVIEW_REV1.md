# EMB implementation and adversarial review, revision 1

2026-09-10. Autonomous primary-agent implementation and review, as requested by the
user. **The bounded chain executes; whole vector qualification is still under review.**
This is not a whole EMB-001, BODY, MULTISOURCE or Need-ownership verdict.

## Governing commitments and corrections

The accepted 32-record reserve/current-observation/support-only-SEM/option-free-pressure/
external-delivery seam and its permanent allocation remain unchanged. Packaging shape
is accepted in `../formal/EMBODIED_MODEL_PACKAGING_ACCEPTANCE.md`. Two mechanical
materialization errors were corrected explicitly:

* Source input is opportunity459, not channel458. The historical packaging proposal
  remains intact; `../formal/EMBODIED_PACKAGING_INPUT_REFERENCE_CORRECTION.md` governs.
* Source PRJ selects field1 ObserverId; both pressure PRJs select field2 ObserverId.
  The first materialized cohort incorrectly reused field1. Real PRJ role checking
  rejects it. `../formal/EMBODIED_PRESSURE_PROJECTION_CORRECTION.md` supersedes that
  cohort without rewriting or aliasing its exact bytes.

Current cohort: `campaign3-embodied-model-rev2/FREEZE.json`, seven models including
work7, seven comparison runs. Baseline ModelIdentity:
`cae46fcb4da4c9711ee24153854ca2eb976ec9dcad9a2394acb5861489007844`.
The public factory requires exact membership in this corrected cohort. The shared
PRJ compiler is invoked during declaration admission, not deferred to first execution.

## Executed chain

`src/campaign3/` implements the named accepted seams. The public boundary accepts
only copied canonical model/run data. It exposes no callback, scheduling, cancellation,
projection token or mutable state object. Historical factories still exclude EMB.

Original source10 authenticates before PRJ/IDN, checks exact body ownership, reads
one anchor only when permitted and available, and emits a finite interval or absence.
Present samples traverse the real SEM reservation/assembly/freeze operations with
empty semantic sets and one observation support. Absence consumes private padding
and never creates SEM. Pressure60 requires the same live sample and its actual
settlement, then computes only max(0,H-U)/H. Its pure operator has no body reader,
truth resolver, appraisal, belief, action knowledge or learning-state capability.

External delivery110 uses the existing authoritative state patch machinery, exact
expected prior and sole owner. Two same-instant deliveries read staged predecessors.
The body anchor remains ordinary authoritative state. Querying never reanchors it.
No learned state family, analytical-anchor registry, RNG draw or character-learning
route is added. A separate trace-side verifier checks reads, bin operations, output
shape/identity, allocation budget, patches and trace metadata before commit.

`embodied-execution-rev1/REVIEW.json` records seven actual public executions against
the pre-implementation expectation artifact: 63 original inputs, 42 committed
boundaries, 273 domain events. Present profiles produce21 outputs, denied/unavailable15.
Each profile advances18 runtime ordinals and ends at event/sequence39. The record
stores exact output, trace and final-save bytes. Hidden reserve expectations are not
reconstructed in the unavailable runtime branch.

## Adversarial evidence and limits

Tests cover exact rational kinetics, partitioning, saturation, bin ties and aliasing;
live source authentication; actual PRJ field selection; same-opportunity SEM binding;
changed child coordinates; empty learning patches; bounded work rollback; data-only
factory admission; all seven whole-instant restore positions; and archive tampering.
The injected-failure matrix covers45 real scheduler boundaries, including a failure
after the earlier delivery has staged its write. State, outputs, trace, queue,
allocators and clock all revert. Mid-instant save attempts reject.

`EMBODIED_RUNTIME_MUTANTS_REV1.json` detects nine current-source alternatives using
isolated module substitutions. It is a source-specific historical receipt, not an
assertion that later source fingerprints remain identical. Stored-level kinetics,
direct lower-bound pressure, skipped padding, missing quantization, read-then-redact,
lost child binding, lost SEM settlement, consumption debt and lost overflow all differ
from the required behavior or reject at production. These are fault controls, not
reduction verdicts.

The two-body component uses actual VAL-qualified content and state machinery with
two equal-valued anchors. Exact body-target equality rejects substitution. The public
single-character model separately excludes that expanded content. Zero delivery,
rate1/3 partition arithmetic and capacity-at-sample endpoint likewise need their
explicit component/public scope; they are not silently advertised as frozen-cohort
public witnesses. Original-input timing/state controls use the admitted input grammar
and existing definitions and produce distinct RunIdentity commitments.

The earlier 1,072-test retry passed. The expanded full-suite receipt is being collected;
no pending result is counted as a pass. Type checking and reference-boundary checking
pass at this checkpoint. The old qualification receipt has439 unchanged entries and
seven historical references to the two explicitly changed source files (scheduler
failure-code typing and shared PRJ dispatch). This is not446 unchanged fingerprints.
All frozen historical data artifacts remain checked and intact.

## Remaining closure work

Finish the per-vector crosswalk and independent collection-member/state-key mutation
controls; refresh current-source fault evidence after implementation corrections;
review final full-suite and persistence/output evidence; then issue the bounded
qualification with explicit component/public limits. Do not infer receiving or action
knowledge from pressure production. EMB-M..O, final Need ownership and the BODY plus
MULTISOURCE composition remain separate open contracts after this bounded milestone.
