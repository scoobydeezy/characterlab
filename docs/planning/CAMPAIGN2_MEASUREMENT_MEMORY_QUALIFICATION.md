# Measurement-memory accepted research qualification

2026-09-08. User research verdict on
[runtime review revision1](CAMPAIGN2_MEASUREMENT_MEMORY_RUNTIME_REVIEW.md).

`measurement-episodic-memory/0.1-candidate` is **QUALIFIED in its accepted bounded scope**.
MEM-PACK-A..G and MEMR-A..P are **PASS**. Production activation and mandatory prefix-validation
persistence qualify under the exact frozen reference model and committed16-model control set.
Canonical bytes remain authoritative; no allocation or model change follows from qualification.

The accepted scope qualifications remain binding:

- MEM-PACK-E composes the canonical-set substrate permutation/duplicate witness with the
  singleton/empty exact memory profile. It is not a nontrivial permutation of a valid memory row.
- MEMR-B composes actual governed component R0/D variation (50+1 versus51+0) through production
  probe/carriage/memory with rejection of the changed anchor at the frozen public factory.
  Arbitrary public R0 variation is not admitted.
- MEMR-G composes public later adaptation without historical rewrite, a component D change
  between formation and recall, and recall's exclusion of current probe/REG/calibration/permission.
  Frozen permission/calibration contribute immutability/exclusion, not positive runtime-changing
  witnesses. This verdict does not transfer automatically to a future model with mutable controls.
- Persistence proves bounded prefix integrity, not antirollback security.
- MEMR-A/O/P qualify the thin content-conditioned episodic mechanism and later exact usable recall.
  The earlier different-adaptation-S0 pair did not satisfy the stronger parent PHEN-ADAPT setup.

The accepted validation accounting is599-test full fresh-source suite plus2 later focused witnesses,
not a601-test full run. Subsequent tests are recorded separately in the parent-pair consolidation.

At this verdict, ADAPT-9b was withheld pending one same-S0 actual-fact intervention pair.
The user authorized that joined witness without semantic redesign and explicitly conditioned
ADAPT-9b/control9 PASS on its success. The completed witness and current disposition are in
[the consolidation review](CAMPAIGN2_PHEN_ADAPT_CONSOLIDATION_REVIEW.md).

PHEN-MEM-001 remains OPEN / NOT CLAIMED. Search, recency, decay, retrieval reinforcement,
salience, top-K access and fuzzing/consolidation are outside this exact episodic seam.
This is a research qualification, not a reduction verdict or Campaign2 completion.
