# LONGITUDINAL implementation findings

## LONG-IMPL-001 — inherited workspace capacity is not a task count

The first construction exploration produced zero identity at every instant. The
inherited workspace capacity2 permits one task plus a prediction slot; capacity3 is
required for two tasks. The new contract already specified two biography alternatives,
but the adapter passed2. Corrected the adapter to3 before qualification. No model or
contract changed: this fixes conformance to the frozen two-alternative content.
Preserve a regression requiring two candidates, nonzero qualified public identity
and an effective downstream standing contrast, rather than accepting a changed trace.

The exploratory output is diagnostic only, not qualification evidence. No prior
Campaign2 workspace semantics were changed to accommodate this fixture.

Construction receipt: LONGITUDINAL_WORKSPACE_CONSTRUCTION_FAILURE_REV1.json.

## LONG-FIND-002 — symmetric identity modifiers are not selective preference

The conformant0.1 implementation acquired nonzero identity but its intact and
StandingOff runs had identical effective decisions. At the two-choice history's
strength, unit1/4 truncates the bounded standing modifier to zero. Even if larger,
adding the same integer to both active positive dice cannot change their relative
score ordering. This is a semantic matching issue as well as a calibration issue.
LONGITUDINAL_STANDING_FAILURE_REV1.json preserves the full16-instant paired run
inputs, identities, choices and terminal save hashes. All15 predecessor models and
their contract remain frozen; public factory admission retains them for replay.

LOCAL DISPOSITION: the separately frozen0.2 successor uses signed alignment with the
fixed positive/negative choice meanings and unit1/16 for standing (situation stays
1/4). Existing Architecture15 identity-to-matching-standing precedent supplies the
edge. The first raw/decision/expression is unchanged by StandingOff; later effective
probabilities discriminate. No universal identity gain or semantic channel law is
selected. Longitudinal memory/skill/relationship laws and allocations did not change.

## LONG-FIND-003 — capacity differences need a discriminating execution probe

The main fixture's practice difficulty1/2 leaves both post-interference capacities
below threshold. Different competence is real but does not alone prove different
performance. A prospective supplementary plan sets difficulty1/4 at the same final
practice opportunity for both retention models, keeping all other originals fixed.
It also removes only early practice opportunities in a history control to distinguish
reacquisition after lost episodes from fresh acquisition. Preserve the original
matrix unchanged; add independently frozen probe evidence, not a rewritten receipt.

## LONG-SCOPE-004 — state inspection is not knowledge of competence

LongitudinalView881 contains actual procedural competence784 alongside identity,
relationship and retained episodes. Despite the factory method name observerView,
this is a holder-scoped research diagnostic, not an evidence interface for the
character. It must not be described as proving that the character knows its actual
competence. Probe876 is likewise an inspection output with no downstream consumers.
Actual competence is read only by this diagnostic and by procedural execution/update;
biography cognition reads identity, not competence. This profile does not implement
capability self-belief. The closed-view test excludes physical interaction sources and
archives, not every latent state. Hidden-interaction-truth equivalence is valid only
under the declared intervention, with actual skill inputs held fixed.

No architecture edge or execution byte changes. Preserve this limit in qualification
and RO-C3-017; a future character-facing evidence view needs a separately admitted
projection instead of reusing this diagnostic as cognition input.

The added inspection regression passed at runtime, then the final TypeScript check
caught an unannotated array whose indexed closure use inferred implicit any. Added
the explicit runtime return type; no executable semantics, model or fixture changed.
