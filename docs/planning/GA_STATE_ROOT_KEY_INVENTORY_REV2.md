# State inventory for the tenth-source successor

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic revision2.

Preserve all revision1 roots, ownership, six scalar key roles, initial-value and
holder-equality rules. Its use of "physical root" meant storage layout, not bodily
classification: those roots remain cognitive/perception/protocol. The separate
LocalReserveState proposal alone adds the explicitly PhysicalBody composite key.

The explicit ten-sweep source can produce30 distinct continuant files, three per
sweep. Raise only AssociationGraph Nodes/Rows and AssociationMassRow Masses maxima
from27 to30. This is a model expressibility bound, not a psychological edge capacity
or permission to seed arbitrary nodes. Empty initial graph, actual source membership,
square row shape, zero diagonal and committed row-mass constraints still apply.

The prior nine-sweep inventory and its exact reviewed bytes remain historical.
The new source appends an actual competing acquisition after the recall cue; it
does not omit that source from the bound or reuse a retired identity. Public model,
work, serialization, allocation and persistence gates remain open.
