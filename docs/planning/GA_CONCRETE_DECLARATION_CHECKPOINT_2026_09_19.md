# General Attention concrete declaration checkpoint — 2026-09-19

**Disposition:** LOCAL DISPOSITION; concrete declaration admission implemented, complete registration/compiler and public GA closure OPEN.
**Stage (Brief §9):** A — executable declaration admission, not public Stage B execution.

## Counters

| Counter | Value |
|---|---|
| Highest allocated record type | 706 |
| Allocated since last verdict/corpus member | 0 |

## What changed

`generalBindingProfile.ts` constructs all 67 concrete 706 registrations with component versions, definition purposes, output schemas/cardinalities/ownership, subject projection, read patterns and write capabilities. `generalDefinitionProfile.ts` constructs calibration, task/prediction, body/channel and maintenance-goal instances plus content and predicate declarations. `generalDeclarations.ts` admits those bytes against compiler-owned bindings, validates the content commitment and cross-definition relationships, and compiles the eight fixed accessor descriptors with qualified subjects.

The goal check proves specification self-identity, content/spec delegation, authored referent kind, holder, signal and desired-domain/window coupling. Canonical roles compose GA and the relevant inherited attention, embodied, prediction, task and cognitive inventories. Concrete GA state-leaf admission restricts subjects beneath SEM wildcards and checks leaf grammars. Unconditional PRJ now runs even at stages with no owner-ledger read. Sampling's conditional physical PRJ remains the sampling adapter's obligation.

The GA codec now permits new records inside the inherited arbitrary-canonical definition-payload and map-key slots. It validates the enclosing inherited grammar and recursively validates the actual payload; it does not broadly exempt inherited records or modify prior codecs.

## Evidence

See `GA_REGISTRATION_COMPILER_PROGRESS_REV2.json` for validation results and source fingerprints. The 52 new tests include all 30 packaging-target calibration recipes, wrong goal holders/kinds/referents/signals/windows, content delegation, dangling/duplicate definitions, missing predicates, nested state roles, wrong component versions/read domains/PRJ/definition bindings, detached snapshots, foreign SEM subjects, and inherited codec exclusions. They exercise the concrete compiler, not a caller-selected expected-binding map.

Full active-source verification: **1,918 tests / 252 files PASS** with `npm test -- --maxWorkers=2 --minWorkers=1`, unchanged timeouts. Production build, final TypeScript check and reference-boundary check PASS. All 28 frozen-manifest file commitments verified. No reference-tree changes or reference test claim.

## Local dispositions and limits

- The bounded candidate uses one governed character, one task, one maintenance goal and three scene objects. Body recall capacity 2 and baseline capacity 1 stay distinct; event/body retention slots are 8 each, graph limits 30/870, and the goal's desired interval is [30,40] within explicit [0,100]. These are local fixture choices, not general psychological laws.
- The recipe proposal's `Priority` maps to the existing `RolePriority` tag. Missing-glyph/position component exclusions remain excluded. Recipe names currently identify calibration projections; source/calendar distinctions do not yet constitute separately committed whole models.
- Component version bindings and transport declarations are not 67 executed scheduler handlers. In particular, visual-cue and retention transport still need their concrete public adapters. The declaration compiler exposes no ModelIdentity, source-authentication token, occurrence allocator or runnable factory.
- Source/profile/scene/panel and formation-protocol instance closure, inherited accessor integration, actual runtime output slots and model-wide state ownership remain outstanding. The new state-leaf validator covers the GA stage domain only; it is not a whole-model restore validator.
- Runtime must authenticate producing events/outputs before calling the internal accessor constructors. The compiler's role and subject checks cannot substitute for that provenance boundary.

## Stage C competitor

Independent/shared/hybrid/retired-flat encoding and concern A/B/C, including DisabledFeedback, remain the named comparisons. This increment creates no allocation, psychological verdict or reduction. Rejection tests establish implementation sensitivity, not mechanism necessity.

## North Star transfer

Supports the separation of retained evidence from current accessibility, and the §30 angry-person archetype whose transient concern changes retrieval of prior insults. Qualified holder binding prevents one character's goal or memory from supplying another character's appraisal; canonical recall views continue to erase retention-protection metadata. This increment does not demonstrate the archetype or general Affect.

## Next gate

Finish source/profile/formation-protocol declaration closure and inherited accessor/output-slot admission, then compose cohort identities with inherited work and runtime slot budgets. The same primary owner retains steps 2–3. The GA-local 80 invocations/58 outputs remain only an input to the whole-model ceiling; source joins, rollback and replay remain separate public gates.

## Owner decision pending

No. Frozen contracts, allocations, historical receipts, corpus/0.28.0's 21 members and the 12 verdict entries are unchanged.
