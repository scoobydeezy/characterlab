# General Attention qualification — 2026-09-20

**Disposition:** QUALIFIED — the planned bounded GA work item is COMPLETE.
**Stage (Brief §9):** A–E complete; psychological law/reduction verdict UNRESOLVED.
**Owner decision pending:** no. LOCAL DISPOSITION under the Campaign 3 policy.

| Counter | Value |
|---|---|
| Highest allocated record type | **706** |
| Allocated since last verdict/corpus member | **0** |

## What changed

The primary owner completed registration/compiler, content/role/accessor, source,
runtime ownership and public model integration. All 67 templates bind through 706.
There are 43 distinct frozen public models, with 36 canonical comparison cases and
7,074 actual event traces across their complete runs. Exact source, required S0,
full original calendar, registration, component versions and budget are committed.
The earlier 35 models and 247 image files remain byte-identical in the successor.

The composed conservative per-instant bound is **92 invocations, 239 outputs and
228 fresh runtime slots**, including inherited work and nested output identities.
Maximum observed work was **21**, which is not substituted for the derived bound.
No permanent record allocation, old model identity or historical contract changed.

## Closure obligation coverage

| Closure-plan obligation | Tested disposition |
|---|---|
| 1. Preserve finite AT2 source/selection | Existing AT2-A..N public/component evidence remains intact; regression suite preserves it. GA adds actual denied-port, unresolved-role and K0/K1/K2 controls. Missing glyph/position remain explicit public exclusions with component controls. |
| 2. Separate permission, selection, allocation and strength | Independent/shared/hybrid/retired-flat, role/disabled/spatial and capacity models execute on actual safe source evidence. Earlier arithmetic verdict stands. Unlimited is an explicit negative capacity control, not a capacity-respecting model. |
| 3. Real owned formation, association and recall | Actual selected capabilities form episodes through sole owners. Actual cues read retained evidence; recollection publication and later presentation reinforcement are distinct transitions. No seeded learned episodes/graph or protocol provenance enters cognition. |
| 4. Footprint and later retrieval | Four sparse/dense pairs execute four formations and four subsequent cues; exact strength, graph and score results below. K0/K1/K2, local access, association decay and presentation decay remain explicit controls. |
| 5. Real earlier character-state feedback | Actual probe → M1 → prediction → workspace → concern producer → strictly later deliveries. Encoding-only and retrieval-only differ; known-zero, unavailable, no-task and disabled remain trace-distinct. This qualifies the ratified transient edge, not general Affect or Candidate A as final law. |
| 6. Identity, ownership, rollback and replay | 129 missing declaration controls; accessor/source/subject/owner negatives; 77 active event types each have whole-instant rollback witnesses, plus six final-commit faults. Two current baseline templates are replacement-only and retain compiler/component coverage. Public consequence and credit saves replay their complete prefixes and five subsequent instants each exactly. |
| 7. Fixture, identities, corpus and verdict | `GA_CORPUS_FIXTURE_1_0_0.md`, frozen manifests, public results, replay, successful `GA_PUBLIC_QUALIFICATION_REV1.json`, corpus/0.29.0 promotion and VER-C3-GA-001 discharge this gate. |

## Measured Stage C results

Dense scenes add two nonfocal Participants, of which K2 selects one with the unchanged
Actor. These Participants are irrelevant to the focal cue, not falsely classified
as Incidental. All numbers below are measured from actual public execution.

| Encoding law | Focal strength sparse → dense | Dense graph edge mass, scale 100 | First cue score of newest episode sparse → dense |
|---|---|---|---|
| Independent | 3/13 → 3/13 | 0 (pruned) | 3/2 → 1 |
| Historical shared | 1 → 50/53 | 20 | 3/2 → 211/200 |
| Historical hybrid | 3/13 → 3/13 | 92 | 3/2 → 57/40 |
| Retired flat | 1 → 1 | 100 | 3/2 → 3/2 |

Sparse graphs are empty. The two newest episodes win in every footprint pair.
Thus exact strengths, associations and scores discriminate models where winner
membership does not. Independent focal strength is footprint-invariant here, but
the later average over retained episode contents is not. Flat remains the already
retired negative control; a finite score equality does not rehabilitate it. Neither
the independent nor hybrid result establishes universal footprint immunity.

The baseline and K0 control have identical first-cue scores. At the next cue, the
episode formed at 6 has base accessibility **5/6** after retrieval versus **1/3**
without retrieval, down from **1/2** initially. The unselected episode receives no
reinforcement. With decay disabled, episodes formed at 5 and 6 tie at **83/60**;
canonical ordering selects the former. Graph access and recency remain separable.

Spatial encoding-only feedback lowers the peripheral factors and changes the graph.
Retrieval-only feedback preserves encoding and graph exactly while changing scores.
Both are needed to describe these two interventions; their numerical laws remain
UNRESOLVED under VER-C3-CONCERN-002. Known-zero, unavailable and disabled can have
equal multipliers while preserving different provenance and branch meanings.

Actual retained attribution consumes 16 experiences and targets one endpoint child.
Age-only retention finishes with 14 used children plus two fresh acquisitions;
use-only, shared protection and significance-first retain 16 used children and
exclude the fresh pressure. All retain one significant child. The three protection
models have identical final memory bytes here: significance-first has not earned
necessity over the simpler controls. Perceived attribution remains fallible;
exogenous replenishment is not a new psychological or objective causal law.

Other bounded equalities matter: priority K2 and equal K2 choose the same units in
this source order; independent budget floors 1/5 and 1 leave the same arithmetic;
local versus spreading access changes scores without changing these winner sets.
These are retained comparator limits, not reasons to collapse accepted state.

## Evidence and preservation

- `general-attention-public-comparisons-rev1/RESULTS.json`: 43 frozen runs, 36 exact
  ComparisonCases, corpus/0.28.0 ExperimentIdentity and per-run complete hashes.
- `general-attention-public-replay-rev1/RESULTS.json`: ten independent reexecutions,
  exact final state/output/trace/save hash agreement, eight captured footprint
  matrices, and public complete-prefix continuation for consequence and credit.
- `GA_MODEL_IMAGE_REVIEW_REV2_2026_09_19.json`: independently recommitted models,
  originals, S0, declarations and preserved predecessor images.
- Focused admission/spatial integration: **54 tests PASS**. Historical reference:
  **328 tests / 43 files PASS**. Production build and reference boundary PASS.
- Full source regression: **2,171 tests / 273 files PASS**, zero failed or pending.
  The prior September 19 mixed-module run is not a qualification receipt: it saw old recipe code with new footprint tests
  and one fixture that implicitly selected the changed baseline instead of spatial.
  The fixture now explicitly names allocation-spatial; no model bytes changed.
- Post-promotion corpus governance: **6 tests PASS**. The final ledger audit also
  repaired three earlier entries that the September 14 checkpoint reported as
  recorded but that were absent. Their existing evidence is now entered under
  VER-C3-SALIENCE-001, VER-C3-EMB-001 and VER-C3-ATTN-001 with an explicit September 20
  correction date. The ledger now actually contains thirteen named verdict entries.

Stage rollback uses the internal conformance fault hook, which is unavailable at
public ingress. Canonical trace tamper controls and public replay are separately
identified evidence; the report does not relabel internal fault injection as public
input. The qualification audit also checks that all ten public reexecutions agree
with the original runs and all frozen image hashes still match.

Corpus/0.29.0 digest:
`5dc8a6f23afe0c75b4b7d9fd0bf93c65f67bd4892ec7ec0e9e4d85d3a05a172d`.
PHEN-ATTN-001 advances to 1.1.0 with its requirements preserved; the other twenty
members are byte-preserved in identity/version/requirement section. The actual runs
retain their declared corpus/0.28.0 ExperimentIdentity. The promotion receipt names
the successor identity and unchanged evidence provenance rather than inventing a rerun.

The canonical encoder's large-value fix removes JavaScript argument-count overflow
without changing wire bytes. Extracted inherited arithmetic helpers preserve old
producer semantics. Frozen Campaign 2/ATTN/EMB fixtures and `reference/` remain in
their original scope. No reference module is imported into active source.

## North Star transfer and limits

This makes the attention/memory part of the §30 frightened/angry-person archetypes
experimentally traversable: limited selection, separate encoding and accessibility,
historical recall, and real earlier character-state feedback. It does not model a
whole frightened/angry person, and TaskConcern is not the general Affect state.

The required bounded GA obligations can close without declaring final psychological
laws. General Affect, Need, surprise, broad modalities, arbitrary role/position
inference, unguided episode segmentation and long-horizon cognition remain explicit
Campaign 3 frontiers. Other corpus members retain their previous bounded/PARTIAL/
BLOCKED dispositions; a full regression run is not a whole-corpus psychological PASS.

**Next gate:** select the next Campaign 3 phenomenon from the remaining frontier.
No further GA implementation gate is pending in this accepted bounded work item.
