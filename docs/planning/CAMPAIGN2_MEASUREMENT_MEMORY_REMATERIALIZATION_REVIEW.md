# Measurement-memory rematerialization review — revision 2

2026-09-08. **CONCRETE REVIEW CANDIDATE. NOT FROZEN.**

C2-MEM-PACK-002 is accepted and applied. The successor adds14 memory RecordField declarations plus one IDN StateMapKey declaration, totaling15 in slot5. Both frozen memory allocation tables, registry/rules versions, all wrappers, and IDN/PRJ meanings are unchanged. The accepted amendment supersedes the historical14-only accounting in earlier packaging documents.

## Reference commitment

Reference configuration: a=true, p=true, F=true, R=true.

```text
3396fa9887e6bed02f3d0cb72344e11a6e0fb23859d330cc45f779f931d01df9
```

The [reference packet](campaign2-measurement-memory-model/REVIEW_MANIFEST.json) contains canonical bytes, readable values, exact profiles,41-entry semantic bundle, six-slot delta and preservation evidence. [CONTROL_COMMITMENTS.json](campaign2-measurement-memory-model/CONTROL_COMMITMENTS.json) contains all16 complete registry/model-identity byte pairs with diagnostic a/p/F/R labels. The constructor receives canonical wrapper choices; no separate F/R metadata authority is added.

All16 commitments are recomputed from declaration constructors and differ from the previous blocked specimens. No ModelIdentity bytes were patched. The blocked reference58a49146718a42ec918787fbce5c5aac86b41b84baeedada7140ee685838ff46 and its other15 configurations remain in `campaign2-measurement-memory-blocked-review/` with their old blocked manifest and materializer source. They were never admitted or frozen and are not migration sources.

## Declaration and admission checks

The amended265 contains exactly StateMapKey position264(tag2,root268,field1), with RecordTypeId absent and263(namespace1000,validator absent). Exactly one matching declaration exists. The203/2 ObserverId RecordField role remains independently present. All14 allocation-derived memory role declarations remain byte-identical and unique, including the two344 key-field roles. No identity-atom role is added to the composite episode key and no wrapper role is added.

The existing `compileValDeclarations` and `compileCampaign2StateModel` execute the exact IDN component declarations: qualified267/1 role,268/1 read-only family, IdentityKey grammar and accepted map-key role. Positive closure passes. Missing role, wrong namespace, RecordField substitution, duplicate role, and unexpected validator reject. The full specimen validator also rejects erroneous role deltas. Each of the16 specimens carries the same checked IDN fragment.

Scope is explicit: the current substrate decoder does not admit new memory runtime schemas. The existing state compiler is exercised on the isolated exact IDN fragment; the review constructor validates the complete bounded successor bytes,17 descriptors, exact sparse episode ownership and remaining declarations separately. This is review admission plus an independently executed substrate prerequisite, not a claim that a full memory activation/runtime compiler now exists.

The589 consistency/component checks also cover the exact17-descriptor addition, typed wrapper matrix, bare inherited rows and opportunity350,41-entry bundle, signed RecallDelay1,16 distinct identities, wrong representations/profiles, projection changes and missing declarations, preservation, and bounded ordered-input reuse. MEM-PACK-D/G commitment sensitivity is separate from admission; MEM-PACK-E nontrivial permutation remains a substrate-level witness. No broader valid profile was fabricated.

## Preservation and runtime gates

150 prior files are fingerprinted, including frozen authorities/packets and the retained blocked review evidence. Content and parameter bytes/identities remain equal to the carriage parent. Registry/model identities change. No historical packet or numeric allocation is amended.

The script `scripts/materialize-campaign2-measurement-memory-model.mjs` supports --write for review generation and no flag for exact read-only comparison. Fresh-process completion is recorded below when verified.

Concrete ModelIdentity acceptance/freeze is the next user gate. Runtime formation, recall, stage scheduling, failure rollback, original-S0 admission and prefix restore remain future implementation/proof work. MEM-PACK-A..G and MEMR-A..P remain NOT PASSED as whole qualification controls. ADAPT-9b, parent control9, PHEN-ADAPT and Campaign2 remain OPEN.

Fresh-process verification completed: a second Node process without --write reproduced every emitted file byte-for-byte, all16 commitments and the review manifest. All589 checks and150 preservation fingerprints passed again. git diff --check passed. This does not promote the review candidate to frozen status.
