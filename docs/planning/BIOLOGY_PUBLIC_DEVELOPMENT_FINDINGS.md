# Biology public development findings

2026-09-26; pre-freeze, no qualification claimed. Counters1426/26.
The first TypeScript build rejected five accesses to an un-narrowed CanonicalValue
returned by compileReasonNuclei (TS2345, biologyPublicReasons.ts). Explicit Record407
admission now precedes field reads. This was a compiler failure, not a behavioral run.
The default Windows python alias was inaccessible; bundled Python was used instead.

Future-abstinence forecasting remains excluded, explicitly preserved under RO010/019;
current discomfort, learned consumption harm and future expected discomfort must not
be treated as equivalent. Public integration is not general biological identity learning.

## Preserved first public execution cohort

BIOLOGY_PUBLIC_PLAN_REV1.json and biology-public-development-rev1/PRESERVATION.json
preserve the first plan and every committed source. Four workers were interrupted
without completing their first qualification row after several CPU minutes each.
No failed behavior or successful public qualification is inferred. Restore had encoded
and decoded the entire growing trace at every replay instant just to count events;
the harness also saved all prefixes even where only three were selected. REV2 uses
the fixed14-stage contract to reject partial counts, replays exactly that many whole
instants, then retains the same whole-save equality. Only selected/next-boundary
saves are materialized. No native scientific formula or source/model identity changes.
The frozen plan/source identity changes are explicit. Vite concurrent HMR port warnings
were nonfatal and unrelated to simulation state. Tests are rerun after optimization.

The interim CURRENT index used prose for obligation counts. check:research correctly
rejected it because its exact required table row was absent. Counts themselves were
unchanged; the row was restored. Prior biological/costly-reward closure checks passed.

## ComparisonCase bookkeeping

The pre-execution plans committed ExperimentIdentity and every ordered model/run,
source, seed and prefix, but omitted the named canonical ComparisonCase wrapper.
BIOLOGY_PUBLIC_COMPARISON_REV1.json derives that wrapper during execution from the
unchanged frozen roster. Its timing is disclosed; no selection, replacement or extra
run was made. The wrapper script is separately hashed and rechecked at closure.

## Large native save admission failure

REV2 worker1 reached recovery/NoRecovery after twelve completed comparison rows, then
restore failed with SaveContractError: Too many properties to enumerate. The inherited
copyData helper calls Reflect.ownKeys on the save Uint8Array; the96-tick native save
exceeded the engine's property enumeration ceiling. This is an actual public seam
failure, not a failed biological candidate. Workers0/2 completed their partitions;
worker3 was interrupted. Their existing REV2 receipts and every frozen source are
preserved under biology-public-development-rev2/PRESERVATION.json and original paths.

REV3 uses a local byte adapter, leaving the old bounded helper unchanged. It copies
only intrinsic typed-array storage, never metadata/getters; exact wrapper and byte
prototype checks remain. A20-million-byte regression covers the former enumeration
ceiling. Restore validates the external save once then compares replay-generated
canonical bytes directly, avoiding redundant generated-save decoding while keeping
whole-byte equality. Completed run receipts now persist individually so a harness
interruption cannot discard completed evidence. No biological law or coupling changed.

## Final dispatch scheduling

After an ordinary worker completed, dispatch-biology-public-tail.mjs assigned it the
last precommitted rows of the long all-prefix worker in reverse order. The generated
harness changes only Vite module resolution, partition filtering/order and auxiliary
output name. BIOLOGY_PUBLIC_DISPATCH_REV1.json records the parent/derived hashes and
exact indices. The original worker reuses the same per-run receipts. No model, source,
seed, prefix check or biological computation is added, omitted or changed; this is
local scheduling, not a post-result cohort selection. Final partition aggregation
still requires every one of the58 precommitted RunIdentities exactly once.

The tail dispatcher and original worker overlapped on arousalControl/Full/seed1
(index35). Both completed the numerical and three-prefix assertions; the original
writer succeeded, and the auxiliary exclusive create rejected replacement with
EEXIST. BIOLOGY_PUBLIC_DISPATCH_DUPLICATE_REV1.json records the duplicate. The four
main partition receipts are complete. Resuming the dispatcher only aggregates
existing per-run receipts. Count58 distinct runs/59 row executions and270 unique
prefix tuples/273 executed checks; the duplicate earns no additional coverage.
