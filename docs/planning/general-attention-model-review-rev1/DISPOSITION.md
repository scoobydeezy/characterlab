# Review revision 1: rejected before freeze

2026-09-19, LOCAL DISPOSITION. These are historical unfrozen proposal bytes.
Do not activate them or infer equivalence from their aliased model identities.

The materializer exposed two omissions: role-calibrated and disabled allocation
recipes executed the spatial encoder, and the required S0 admission profile was
not committed even though the compiler admitted a sole recipe-specific S0.
Additionally, Windows slash normalization left the source-file inventory empty.

The corrected proposal binds the existing allocation controls through explicit
versions, commits the exact required S0, and inventories normalized source paths.
See SELECTED_ALLOCATION_CONTROLS.md. No frozen contract/model/corpus was changed.
Counters remain 706 / 0. No architectural owner decision is required.
