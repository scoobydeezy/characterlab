# Combined source and declaration checkpoint

2026-09-13. LOCAL DISPOSITION — no owner decision pending. GA OPEN.

The [combined source component](../formal/COMBINED_OBSERVATION_OPPORTUNITY_COMPONENT.md)
executes all eight panel/visual/body presence cases in both existing observation lanes.
Each actual observation allocates once; event detection exists exactly for panel or
visual presence, and one shared experience exists exactly when any present evidence
exists. Frozen support contains precisely the present observations. Panel/marker
publication is transactional across this local preparation, including failed fallback
after an existing context. The enclosing allocator/whole-instant transaction remains
a public-runtime obligation.

The component deliberately emits Unresolved visual role because neither its position
display nor panel supplies an observed role channel. The first four-trial fixture still
owes its separately authenticated role producer. Combined sampling does not authorize
silently requesting body evidence at every visual opportunity.

[Fifteen affected checks](COMBINED_OBSERVATION_TESTS_REV1.json) pass, including all16
presence/lane combinations and failure at every allocation in the full branch.
[Seven injected faults](COMBINED_OBSERVATION_REVIEW_REV1.json) are detected, including
fabricated Actor role, unavailable support, premature perception publication and
the two body-source SEM tag faults. Actual SEM codec round trips now cover emitted
body-only and panel/body experiences, which previously used an inadmissible umbrella
version. The correction is SEM-001H, without modifying the frozen codec whitelist.
[Thirty-eight affected checks](BODY_EXPERIENCE_CODEC_REGRESSION_REV1.json) pass.

The [state inventory](GA_STATE_ROOT_KEY_INVENTORY_REV1.md) specifies seven distinct
roots and six scalar map-key roles, preserving inherited SEM/measurement/task roots.
Eleven structural faults reject. The [earlier-feedback carrier](GA_PRIOR_FEEDBACK_CARRIER_REV1.md)
adds eight records and no identity family or persistent affect state; eight structural
faults reject. Its actual completed-parent/PRJ/delivery binding remains a public gate.

The [joined role crosswalk](GA_IDENTITY_ROLE_CLOSURE_REV3.md) now covers102 records,
98 identity positions and141 record boundaries; six positions are explicitly new-root
StateMapKey roles. Thirteen structural faults reject. This closes symbolic key positions
only, not actual model/authority/accessor/holder enforcement or inherited role coverage.

Type checking passes. Earlier reports retain their exact source fingerprints as
history. No public factory, numeric allocation, frozen model, corpus membership or
General Attention completion has been promoted. Continue the actual role-source and
registration/owner graph closure, followed by the remaining public and corpus gates.
