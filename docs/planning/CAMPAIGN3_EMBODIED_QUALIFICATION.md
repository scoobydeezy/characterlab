# EMB bounded experiment qualification

Date: 2026-09-10. Reviewer: primary-agent adversarial self-review under the user's
explicit authorization to work, review and iterate to completion. No external verdict
is claimed.

**Verdict: the bounded EMB experiment is complete and qualified.** EMB-A..O pass in
their stated public/component composition. ER-A..R close the previously missing
receiving obligations. The earlier 76 body implementation obligations and EPACK-A..I
retain their qualification, with shared-source extensions reconciled forward.

This closes the finite reserve → observer-safe interoception → pressure → adopted
body action association → shared task/body choice experiment. It does not close
Campaign 3 or establish general biological adequacy. The exact crosswalk, current
source fingerprints and immutable evidence commitments are in the
[qualification receipt](CAMPAIGN3_EMBODIED_QUALIFICATION.json).

## Implemented contract and authority

Implementation follows the [accepted receiving shape](../formal/EMBODIED_RECEIVING_SHAPE_ACCEPTANCE.md),
[permanent allocation](../formal/EMBODIED_RECEIVING_PERMANENT_ALLOCATION.md) and
[13 frozen model commitments](campaign3-embodied-receiving-model-rev1/FREEZE.json).
The public entry is `src/campaign3/receivingFactory.ts`, implementing
`rules/campaign3-embodied-receiving/0.1-candidate` and its named receiving admission,
ordered-input, trace and persistence contracts. Exact versions remain in the frozen
cohort; no historical model or schema is widened in place.

The North Star and Architecture Map require physiology, observation, evidence,
knowledge, motive, reasons, choice, expression and execution to remain causally
separate. Here physical reserve truth is readable only by the body-owned source and
external-delivery handlers. Actual support-only SEM feeds pressure. Body instructions
are adopted character knowledge, not registry membership or world efficacy. Task and
body grounds remain distinct even when they support the same complete action key.
The Research Brief's comparison burden is met by named public deletion/substitution
contrasts and explicit component controls. The mechanism dispositions in the accepted
[inspection](CAMPAIGN3_EMBODIED_RECEIVING_INSPECTION.md) remain in force; the qualified
task identity feedback loop is preserved rather than implicitly extended to body choice.

## Executed distinctions

- **EMB-M:** a real shared option carries both task and body origins. Independent
  support yields probability 221/256; removing either supporting ground yields 1/2.
  Collapsing the grounds is detected.
- **EMB-N:** removing or rebinding adopted body instructions changes candidate
  access/association while pressure and the original task candidate context remain
  byte-identical. World permission affects only the completed execution count.
- **EMB-O:** duplicate body descriptions retain both origins but one body nucleus and
  probability 1/2. The separately committed no-coverage alternative gives 2/3.
  The actual generic aggregate kernel also distinguishes `{a},{b},{a,b}` from a
  pairwise-only substitute. That three-description witness is component scope;
  the public singleton-support vocabulary stays closed.

[Public execution revision 4](embodied-receiving-execution-rev4/REVIEW.json) contains
28 runs and 688 checks, including 196 exact comparisons to the preceding artifacts.
[Association review](EMBODIED_RECEIVING_ASSOCIATION_REVIEW_REV1.json) adds 59 checks
of unchanged pressure/task inputs, adopted context preservation and expression-before-
execution ordering. All five dice and three arbitration modes execute publicly.
NoOptions and NoActiveReasons remain distinct; neither fabricates an intent or draw.
The modifier-only zero-base exclusion is separately exercised through the actual
component. Arbitrary negative/modifier public behavior is not claimed.

## Adversarial review and corrections

[Twenty current-source substitutions](EMBODIED_RECEIVING_MUTANTS_REV3.json) are
detected by production rejection or named canonical output differences. A separate
[aggregate-coverage substitution](EMBODIED_RECEIVING_COVERAGE_MUTANT_REV1.json)
distinguishes the retained aggregate from pairwise overlap. These are bounded
sensitivity results, not general reduction verdicts.

Review found and corrected four concrete weaknesses before this verdict:

1. The first Auto witness was symmetric, so it could not satisfy the accepted strict
   threshold. An asymmetric admitted context corrected the witness. Historical
   revision-1 evidence and its disposition remain intact; no model/math change was used.
2. Receiving traces gained independent reconstruction of all 19 fields, with 304
   corruptions across all 16 new stages rejected. No-write and single-allocation guards
   now independently enforce the receiving handlers' authority.
3. Phase 110 is shared by external body delivery and protocol execution. Body access
   is now handler-owned, revoked at every handler entry and checked again on return.
   A fault injected into execution after same-instant deliveries rejects with ILLEGAL_READ.
4. Concurrent settlement now rejects before replacing live ingress or RNG scopes.
   The first transaction produces the same save as an uncontended run.

Rollback is exercised at every new stage and before commit, including a prior
committed RNG prefix. The maximum instant executes 26 events; the work25 control
rolls back staged deliveries. Forged, missing, repeated and altered sibling bindings
reject through the actual ingress component. Public input remains original-only.

[Replay revision 2](EMBODIED_RECEIVING_REPLAY_REV2.json) restores all 86 whole-instant
prefixes across 29 specimens and all 13 frozen models, reproducing final save, output
and trace bytes. Factory tests additionally reject altered adopted state, originals
and RNG ledger bytes. This is exhaustive over the recorded finite witness set,
not every possible admitted sequence.

## Preservation and validation

[Preservation reconciliation](EMBODIED_RECEIVING_PRESERVATION_REV1.json) covers
**1,137 active tests in 133 files**, including the latest **27 receiving tests**, plus
**328 historical tests in 43 files**. The active count composes a broad passing run
with the latest receiving rerun; it is not represented as a single fresh full run.
The default reference command could not start because of sandbox ancestor access;
programmatic Vitest used the exact observed reference include/environment settings.
Type checking and the reference-import boundary check pass.

All 436 unchanged cognitive fingerprint references are preserved. Ten references to
three explicitly extended shared source files have new hashes recorded alongside the
old ones. The body admission and trace helpers gained separate receiving entry points;
the old runtime, model commitments and evidence artifacts remain unchanged. Historical
receipts have not been rewritten to suggest their old hashes described new source.

## Deliberately deferred

General physiology, biological calibration, constitutive adaptation, recovery kinetics
and final Need state ownership require later contracts and experiments. So do learned
efficacy, action-conditioned replenishment or relief, reward/success expectations,
mixed expression qualification, identity feedback and consequence learning. The intact
task feedback control remains available.

No general BODY/MULTISOURCE corpus winner, corpus promotion, attention/affect/habit/
relationship integration, longitudinal development or UI completion follows from this
verdict. The bounded EMB work requested here is complete; those frontiers remain
explicitly separate. Frozen draft documents still show their historical NOT PASSED
statuses; this forward qualification supplies the current disposition.
