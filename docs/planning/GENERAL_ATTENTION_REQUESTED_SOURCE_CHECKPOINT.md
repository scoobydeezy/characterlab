# Requested source and selected positive encoding checkpoint

2026-09-13. LOCAL DISPOSITION — no owner ruling required. GA OPEN.

The four-trial fixture now uses actual SEM-B binding materialization and role
projection rather than writing an Actor result directly. Hiding that observed role
prevents the qualifying retained operand despite unchanged position and panel.
Changing the hidden marker referent does not change observer files or downstream
memory. The producer's governed world/channel registration remains open: actual
component projection is not public authentication.

An actual empty attention pool handles completed zero-detection observations without
inventing a SemanticExperience. Empty and role-ineligible selections still enroll
formation protocol sources while producing no acquisition. The role/empty review
detects five faults; its source fingerprints record the pre-sampler fixture revision.

[Requested sampling](../formal/REQUESTED_SOURCE_SAMPLING_COMPONENT.md) distinguishes
seven nonempty request combinations from the eight P/V/B presence combinations.
Unrequested surfaces are never called and have no output. Requested unavailable
surfaces retain an observation identity. The combined and panel/body components
and the fixture's visual path now use this preparation. Cue-only visual sensing
requests neither panel nor body. Actual body-only cue sensing remains independently
admitted. No unsampled panel can close or create an event context.

The sampler integration exposed a fixture support-order assumption: panel now
allocates before visual. Sorting support by the existing SEM observation-ordinal
rule fixes it without aliasing identities or changing the frozen validator.
[Revision3](REQUESTED_SOURCE_SAMPLING_TESTS_REV3.json) passes33 affected tests.
Earlier failed report revision2 is retained as development evidence. Five sampling
faults are detected; the later direct-throw type refinement has identical behavior
and is covered by subsequent regression, not silently added to that fingerprint.

[Prepared positive encoding](../formal/PREPARED_POSITIVE_SPATIAL_COMPONENT.md)
connects the selected-only capability to positive child projection, including the
explicit prior-concern variant. Full earlier concern leaves focal encoding unchanged
and excludes the zero-strength peripheral child. Feedback diagnostics remain on
evaluation. Failed temporal admission revokes the capability.44 affected tests and
four injected faults pass. Test revision1's two failures were a missing rational
denominator in the new fixture; revision2 fixes that fixture and passes.

The [physical carrier](GA_LOCAL_RESERVE_PUBLIC_CARRIER_REV1.md) adds nine symbolic
records, reusing anchors454, parameters453 and numerical replenishment479. Its
composite Character/LocalReserve key remains distinct from safe signal identity.
Nine structural faults reject. The [joined crosswalk](GA_IDENTITY_ROLE_CLOSURE_REV4.md)
covers111 records,106 identity positions and152 record boundaries;16 faults reject.
Six scalar cognitive/perception key roles remain distinct from the new composite
PhysicalBody key and the protocol singleton. No permanent allocation is made.

Next: finish exact world/source request/output declarations and the actual public
producer/PRJ/owner/occurrence graph, then allocation, model and persistence gates.
The whole-source1827-test historical report is not current-source qualification.
No corpus bytes, prior accepted contract, model or GA verdict is promoted here.
