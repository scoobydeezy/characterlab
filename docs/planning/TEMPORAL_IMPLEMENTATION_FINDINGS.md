# Independent temporal-goal implementation findings — 2026-09-26

LOCAL DISPOSITION. Counters1400/0; no new allocation or owner ruling.

Both goals have independent adopted status, required progress and deadline. Exact
inherited reason grouping preserves separate task grounds even though the same actor
owns both. One losing a choice leaves its adoption intact; later pursuit consumes
that preserved state. DropLoser and SharedRetirement are explicit diagnostic failures,
not accepted lifecycle laws. NoTemporalBias remains a serious valuation competitor.
These comparisons test two distinct errors: choosing something else versus abandoning
this goal, and retiring one goal versus retiring the entire goal set.

HorizonRelative inherits a finite forecast-relative weighting from procrastination:
long value is reduced while a later opening is believed available before5. This is
not a universal temporal discount curve, urgency law, optimal scheduler or calibrated
exchange rate. Independent displayed strengths change exact choice probabilities
under NoTemporalBias. An unknown forecast and a reported unavailable opening are
separate, though this candidate can assign them equal current valuation.

The candidate does not estimate remaining completion capacity. Its long goal requires
three observed units by8, so some admitted contexts can make fulfillment impossible.
A believed opening never guarantees enough time or successful execution. Fulfillment
is character-relative and follows admitted receipts; hidden progress cannot close a
goal, while false positive receipts can produce mistaken fulfillment. Once terminal,
the profile does not reopen a goal or revise its historical outcome.

Current access and availability are explicit Boolean admission gates from a controlled
visible board, not a general missing/expired/conflicting feasibility belief model.
A missing board yields no current option; missing forecast preserves prior knowledge.
Source at120/lifecycle140 affects the next component step. This preserves same-instant
intent/expression if a cancellation or correction arrives later in that instant.

The actual campaign2 cognitive pipeline supplies complete409 arbitration,425intent,
426expression,431plan,432attempt and433execution. Meaning and draw records are retained;
there is no copied private dice implementation. Acquired identity remains separately
preserved and is not updated by this experiment. Component transaction faults discard
staged RNG and leave the complete prior save unchanged; exact retry and all-prefix
continuation tests exercise this boundary. No public scheduler ownership/source API
qualification is claimed from these component checks.

No scientific test failure or rejected source cohort occurred in this increment.
The prototype fixture's expired-short control was made distinct from inaccessible-short
before tests or freezing, so it checks late access after expiry rather than duplicate
coverage. The plan rejects duplicate run identities. The prior intention-forgetting
identity-binding lesson is applied before this freeze: resolved tasks, actions,
deadlines, progress requirements, calibration, dice and arbitration are committed
alongside source artifacts. No frozen model bytes were revised.

RO001/010/012/014/019/020 retain independent reasons, fallible forecasts, lifecycle and
broader source/integration limits.021 historical reconciliation remains mandatory.
No existing goal, plan, memory, belief, motive or identity distinction is retired.
