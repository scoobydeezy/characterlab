# Workspace/control checkpoint — 2026-09-21

**COMPLETE / QUALIFIED in the bounded PHEN-WORK-001 domain. Stage E.**
**LOCAL DISPOSITION — no owner ruling required.**

| Counter | Value |
|---|---|
| Highest permanently allocated record type | **781** |
| Allocated since this verdict | **0** (16 this increment) |

## What changed

[workspace-control/0.1-candidate](../formal/WORKSPACE_CONTROL_CONTRACT.md) makes
visible information → retained safe source → bounded maintained access → grounded
reasons → inherited dice/arbitration → intent executable. It preserves adopted task
state independently of active access and retires tasks through their existing sole
prospective-commitment authority. Previous GA, BODY/MULTISOURCE, BELIEF and AFFECT
contracts, frozen images and qualifications remain unchanged.

The public entry accepts physical instruction-board inputs, never selected sets or
reason values. Visibility excludes hidden cards, reminder lamps and private truth.
Three independently identified cards overload capacities one and two. A/B reference
two actually adopted tasks; neutral C can consume a slot but cannot create a motive.
Displayed priority affects admission, never reason intensity. The bounded identity-
establishing source is explicit: this is not a new theory of perception or recognition.

Source retention is an observer-owned journal shared by both serious candidates.
Its authority name identifies the implementation's source-retention role, not a
new psychological memory primitive. This experiment does not qualify ordinary
episodic encoding, retrieval, decay or arbitrary reminder recognition.

## Evidence and witnesses

The [main experiment](WORK_PUBLIC_EXPERIMENT_REV1.json) executes 70 preregistered
runs across 48 frozen models (240 model files). The [distractor supplement](WORK_DISTRACTOR_REV1.json)
adds eight runs, for **78 public runs, 624 complete-prefix restores and 624 continuation
save equalities**. Of these continuation checks,546 settle a next instant and78
verify the terminal no-op. Saves include exact state, queue, allocators, trace,
outputs and addressed random draws. No tolerance or post-hoc seed selection.

At time2 the goal is active. With support off, distraction delivered at2 displaces
it at3 while its retained Open status bytes remain unchanged. The competitor's
plan becomes the accessible ground and changes the chosen action. With support on,
the previously selected goal survives the same distractor. The matched supplement
holds goal and neutral-card priorities fixed, changing only the distractor's physical
priority1→3; exact pre-intervention outputs and preceding workspace are equal.

A reminder observed at4 restores the retained but inactive intention at5. Without
that cue it remains displaced. At6 the half-open active window excludes it; deadline140
then changes Open to DeadlineMissed. A reminder observed at6 cannot recreate it at7.
The same-time observation is unavailable to workspace40: observation120/append140
can affect only a later instant. Gapped inputs and deadline-only instants also pass.

UnlimitedWorkspace violates the capacity bound. AvailabilityEqualsAccess exposes
the unselected goal as a reason. StatelessPriority loses protection despite support.
ImmortalGoal admits an expired intention. These are explicitly labelled violating
controls, not admissible psychological baselines or general-purpose capabilities.

Hidden truth and physical card ordering preserve exact safe outputs and retained
state; invisible-card priority is inert. Missing board visibility admits no active
items. Schema, source, model-handle, initial-state, ownership and history bounds reject
invalid data. Every one of nine stage kinds plus commit has reached-fault rollback
checks, including deadline retirement after a committed prefix.

The [validation receipt](WORK_VALIDATION_CLOSURE_REV1.json) verifies all model hashes,
contract/allocation hashes, final implementation hashes and the four exact one-field
input pairs. **40 affected tests and328 preserved reference tests pass**; production
build, TypeScript and reference-boundary checks pass. A first build caught a test-only
TypeScript narrowing error; the corrected build passed. No full active-suite rerun
is claimed. Research bookkeeping and its ten corruption checks pass separately.

## Stage C competitors and verdict

**StoredSet** writes an independent active-set cache. **IndexedReplay** reconstructs
the prior selected set from the same retained safe display journal, with no active-set
state or cache reads. Every tested output—including exact candidates, access, control,
source, reasons, distributions, addressed draws and intent—is equal. Only the permitted
cache state/read/write diagnostics and model identity differ. Both remain executable.

The derivation is explicit: the initial set is empty; at each next opportunity the
previous committed frame, previous selection and immutable lifecycle interval determine
the next selection. Induction reconstructs that sequence from ordered safe frames.
It is valid here because every original supplies one frame, every original has one
workspace opportunity, and the fixed deadline is the only lifecycle transition.
New adoption, fulfillment, irregular cognitive opportunities or discarded history
would require a new derivation rather than silently extending this one.

VER-C3-WORK-001 is **RETAINED** for availability/access, bounded competition,
maintenance-sensitive protection, retained/active intention, reminder/expiry and
source-order boundaries. It is **DERIVED** for the independent active-set cache in
this retained-history domain. It is **UNRESOLVED** for universal maintenance,
indexing efficiency, control cost and wider workspace/control representation.
The cache is not removed globally; the derived model trades storage for repeated
bounded reconstruction. Stateless selection is not equivalent to maintained access.

## Findings and North Star transfer

[WORK-DESIGN-001](WORK_DISTRACTOR_DESIGN_FINDING.md) preserves the confounded main
priority fixture. Its frozen results are retained with a narrower claim; the matched
supplement discharges distractor-only causality. No failing hypothesis was erased.

North Star17.1–17.3 / Brief12.6–12.7: a character can keep an intention without having
it presently available as a reason, resist distraction through maintenance, remember
at a later cue, and fail to act after expiry. **Retaining something, selecting it,
and letting it influence a decision remain separate causal permissions. An active
set may be derivable without making those permissions interchangeable.**

## Remaining obligations and next gate

RO-C3-012 is **CONDITIONAL**: larger or changing candidate pools; longer or decaying
history; new adoption/fulfillment; ordinary cue recognition and GA retrieval integration;
alternative maintenance/inhibition policies; habitual inhibition and failure under
load; fatigue, rumination, error monitoring, strategy switching and deliberate
reappraisal; actual costs and control ordering. These wider Brief12.6 phenomena are
not passed by this closure. RO-C3-010/011 retain their prior belief/affect limits.
Registry counts: **0 active /12 conditional /0 unowned**; zero unowned is a structural
bookkeeping result, not a completed audit of all historical findings.

Next natural frontier: PHEN-SKILL-001 readiness—competence, believed competence and
temporary impairment—starting with its historical intake and accepted contract gate.
No skill implementation is started here. Corpus0.29.0 remains21 members with unchanged
digest `5dc8a6f23afe0c75b4b7d9fd0bf93c65f67bd4892ec7ec0e9e4d85d3a05a172d`.
No whole Campaign3 PASS, current-lane ORD-001 closure or general control theory.

## Owner decision pending

No. All bounded choices followed the escalation policy; no foundational ontology,
epistemic permission or untested architecture reduction was imposed.
