# Identity application registration

Status: DRAFT. No allocation, model activation or implementation. This completes the
symbolic write registration for the bounded identity-history proposal.

TaskIdentityApplicationRegistration ordered fields: ExecutingSeamId,
ExecutingSeamVersion, InputAdmission274, ReadDomain(set149), OutputDefinitions(set277),
WriteCapability322, Ingress276, IdentityRequirement(TaskIdentityReadRequirement).

InputAdmission names DecisionQualification and275 RegisteredTransitionProducer for
the actual qualification transition, with immediate generated-child relation. Ingress
names the exact identity-application event at140, same DueAt, exact source payload,
exactly once. Eligible and authentic rejected results both produce this child.
OutputDefinitions is exactly empty; application creates no second evidence output.

WriteCapability is the existing allowlist shape322 with the new symbolic
authority/task-identity-evidence and exactly existing1031 identity-disposition. The
new root/leaf is that authority's sole materialized target. It does not inherit ADAPT
write permission by using the same capability record shape. The compiler checks
the exact owner, root/field, record key/value grammar and no-removal policy.

## Reuse the same identity-prior operation

TaskIdentityReadRequirement already specifies captured subject plus fixed channel→
whole history-or-absence. That operation is the same for raw standing construction
and application; do not invent a duplicate write-prior accessor/record. The outer
registration supplies the exact input schema and preparation/eligibility rules.
The application subject path is the named chain:

    DecisionQualification.DecisionExpression
      .ChosenIntent.DecisionResolution.ReasonContext.RawSignalContext
      .CandidateOptions.MotiveContext.Concern.Appraisal.Workspace.CharacterId

With the current proposed field orders its positional illustration is
[2,2,2,3,2,2,2,2,2,2,2]. This is not permanent allocation. The numeric gate must derive
the field IDs from the accepted schema chain. The only other admitted instance is
the five-step raw-source chain already documented; arbitrary paths reject.

ReadDomain is exactly the identity evidence map pattern. The accessor remains
accessor/task-identity-history, with identical whole-prior/absence meaning. Capturing
an already admitted subject and building a key grants no state read before the stage
seal. It neither performs another IDN lookup nor exposes other-holder entries.

## Eligibility and target closure

The exact qualification Result determines applicability. Rejected resolves no key
and reads no prior, but executes as a mandatory no-write participant. Eligible validates
source subject/channel, occurrence identities, time and exact nonzero contribution
before resolving its single key. Only after all stage targets and collisions are
checked may the capability supply the prior.

The candidate appends the actual qualification reference, actual decision resolution
reference, frozen OccurredAt and signed contribution. It preserves every prior entry
byte-for-byte. History invariants,64-entry limit and repeated-source checks hold before
publication. It cannot replace a contribution, delete a key, write derived counters
as a second authority, or manufacture another source ID.

The joined source-row grammar also requires oneI alongside the chosen branch's actual
A. A component's ability to process one eligible record alone does not authorize an
I-only public instant. Rejection does not exempt I from completion or rollback checks.

## Route and trace

Add qualification and application to route/character-learning in the successor map.
The qualification schema inhabits the route's output closure; application has no
semantic outputs. No307 or execution truth joins that route. Existing EVID270 remains
its accepted separate source.

Trace160 records actual qualification input, subject, declared/read paths, actual prior
only when read, appended candidate and sole-authority diff. Its quantization/fold
provenance is distinct from draws, which belong to arbitration. Do not claim this
application performed a fresh random draw, appraisal or world-outcome evaluation.

Remaining gates: exact descriptors/field IDs, scalar/collection roles, authority/family
rows, occurrence table, joint stage registration, work and save/trace projections.
Runtime controls remain NOT PASSED.
