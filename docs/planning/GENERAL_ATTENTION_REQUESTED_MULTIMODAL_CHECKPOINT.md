# Requested multimodal source, schedule and public transport checkpoint

2026-09-13. LOCAL DISPOSITION — no owner ruling required. General Attention OPEN.

The main four-trial attribution/retention fixture now uses the actual
[requested multimodal source](../formal/GENERAL_SOURCE_OPPORTUNITY_COMPONENT.md).
The earlier separate body/visual cue calls at71 are one request and one conditional
SEM-H experience. Zero-capacity body selection and the two read-only cues remain
independent consumers. A body-only experience cannot replace a missing visual cue.
All visual roles now come from actual bounded scene materialization; the final
competitor also follows this source. Later qualified use remains a supplied component
premise, and retained fixture payloads are still test encoding, not public codecs.

The source executes all seven request masks and eight presence combinations in both
lanes. It supports up to three actual visual items, separately allocated bindings,
the existing perception owners and canonical SEM output. Six current-source mutations
detect unrequested scene/panel reads, a second body experience, reused binding slots,
early publication and erased role evidence. The standalone scene source and opportunity
retain their separate five/four-fault receipts and old single-point controls.

The [schedule component](../formal/GENERAL_SOURCE_SCHEDULE_COMPONENT.md) separately
counts sensing surfaces and completed selections. The fixture has18 observation
originals,10 visual sweeps,16 panel sweeps and18 selection slots. Two selectors on
one request count twice; cue-only use does not count as a selection. Four mutations
detect collapsed source counting, acquisition-gated cueing and invalid goal timing.
The first profile permits one observation original per instant; broader simultaneous
sources remain an explicit separately bounded extension.

[Physical schedule reconciliation](GA_PHYSICAL_SOURCE_SCHEDULE_REV1.md) replaces
hidden-state-driven preparation with literal earlier external inputs. The accepted110
writer precedes next-instant current10 sensing and same-instant consequence120 sensing.
[Lifecycle reconciliation](GA_PHASE_SCHEDULE_RECONCILIATION_REV1.md) uses adoption1,
withdrawal70 and deadline100. The deadline guard preserves withdrawal history; three
mutations protect it. Five actual scheduler controls prove proposal-before-terminal
ordering, pending deadline preservation, terminal-emission rejection, rollback of
queue/allocator/state/outputs and every quiescent prefix in this component schedule.
These are not public deadline authentication or whole persistence qualification.

The eight [source/profile records](GA_SOURCE_PROFILE_DECLARATIONS_REV1.md) have nine
structural fault controls. The [identity crosswalk revision8](GA_IDENTITY_ROLE_CLOSURE_REV8.md)
covers129 records,127 scalar positions and186 record boundaries, with23 faults rejected.
Scene objects reuse existing1002 and EventRoleId1003. No new numeric allocation exists.

The new [source stage proposal](GA_SOURCE_STAGE_CLOSURE_REV1.md) adds five transport
records and15 lane-specific source registrations. Ten structural faults reject.
World/profile data stop at sampling; later wrappers preserve safe products and the
admitted use plan. Source-only work is eight events per original. The five wrappers are included in the later role crosswalk below; executable
paths/accessors and exact
downstream registration remain open. This is not whole symbolic acceptance.

The [claim transport correction](GA_VISUAL_PIPELINE_CLAIM_TRANSPORT_REV2.md) adds the
actual phase15/125 claims to VisualSelectionInput; five structural faults reject.
Its allocation-stage wording now matches the accepted phase130 evidence producer.
The [actual role batch](../formal/GENERAL_SOURCE_ROLE_BATCH_COMPONENT.md) replaces
the fixture's eager claim reservation: unresolved input allocates zero claims, and
each produced claim receives its own actual shared slot. Four source mutations
reject phantom, placeholder, inferred-range and duplicate allocations. The old
source-stage packet's statement about pending wrapper roles is superseded by
revision8; its broader public registration limitations remain unchanged.

## Verification scope

[Affected current regression](GA_REQUESTED_MULTIMODAL_REGRESSION_REV2.json):119 tests
pass across17 files. Type checking, reference boundary and whitespace checks pass.
The [broader active-source run](GA_PUBLIC_PREPARATION_FULL_REGRESSION_REV1.json) passed
1679 tests across221 files. It began before the final multimodal integration and
contains the23-test earlier fixture, not the current24-test fixture or the two new
general-source files. The119-test report supplies the later affected-source proof.
These counts are not additive or a whole-GA qualification.

Correct-forward accounting: the historical1827-test report named
GA_SOURCE_INTEGRATION_ACTIVE_REGRESSION_REV1 included43 files from reference/src/test.
Its name overstated the active-only scope. Preserve its exact bytes and historical
results; do not compare its total directly with the current active-only count. The
current broad run explicitly includes src/test/**/*.test.ts. Default npm test startup
hit the sandbox's esbuild parent-directory restriction, so this run used the Vitest
API with that same source include and node environment, config loading disabled.

The initial new opportunity test report failed because its fixture used the wrong
signal-symbol prefix. Revision2 fixes the fixture; no production validator was relaxed.
Earlier failed development reports and older component fingerprints remain historical.
The frozen corpus0.27.0 and accepted numeric/model artifacts are unchanged by this work.

Continue locally through source wrapper roles, exact public producer/PRJ/read/write/
occurrence declarations, body audit and acquisition production, authenticated use and
prior-feedback joins, owner/protocol batches, full-prefix proof and corpus promotion.
No architecture-owner decision is pending; none of these remaining gates is waived.
