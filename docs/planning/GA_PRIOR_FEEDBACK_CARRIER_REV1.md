# Earlier-concern feedback carrier

2026-09-13. LOCAL symbolic reconciliation, not whole public shape acceptance.

Preserve the earlier TaskConcern feedback branch independently of the immediate
baseline and later goal-significance carriage. ActualConcernCarry reuses ConcernId
1130, qualified subject, actual producing instant and exact TaskConcernResponse386.
It excludes nested appraisal/workspace/forecast. NoConcernAvailable has no invented
concern occurrence or response; its reason is exactly NoSelectedTask.

FeedbackDelivery retains the zero-based unsigned original-input address and exact
target instant. It is generated work, never an original input and never a free-standing
character command. Existing scheduler allocation identifies the delivery event;
no new feedback occurrence namespace is proposed. Same-run completed-parent binding,
exact expected output bytes, receiving subject and strictly prior producer time
remain mandatory even when the numerical feedback model is disabled.

PriorConcernEncodingInput wraps the baseline selected input plus its exact completed
delivery. Its outer ObserverId must equal the selected input's observer before IDN/PRJ.
The encoder has no full source/preparation read. PriorConcernEncodingEvaluation wraps
the baseline evaluation plus the narrow delivered input and branch result, all trace
side. This preserves feedback provenance without duplicating it in durable children.

PriorConcernRecallInput similarly combines the actual visual cue with the delivery;
its exact cue instant is the receiving instant. PriorConcernRecallEvaluation wraps
the existing EventRecallResult and narrow delivered input/result. Ranking remains
read-only; subsequent actual recollection/presentation follows its existing path.
No body-recall graph/frequency law is added by this event-recall variant.

PriorConcernModulation records source status separately from EnabledKnown,
DisabledFeedback or BaselineWithoutAvailableFeedback, and exact residual multiplier
and associative weight. Its multiplier means1-q, not a second stored pool or reward.
The selected branch computes P*(1-q)/n from its bound P/n; the retrieval comparison
uses omegaA=1+q. Unsupported status/branch combinations and inconsistent values reject.
Source status KnownIntensity0 remains known even though its numbers match baseline.

No goal qualification becomes prior concern, no later consequence rewrites historical
encoding, and no callback or resolver is introduced. The machine packet supplements
the visual baseline and existing narrow two-parent proposal. Public producer/role/
registration, exact source cohort, allocation and save qualification remain open.
