# Earlier-concern producer and receiving stages

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Seven symbolic
registrations and three wrappers; public admission and whole packaging remain OPEN.

The first feedback variant preserves the existing TaskWorkspace40 → TaskAppraisal50
→ TaskConcern50 path and its1128/1129/1130 output families. It is a separately
registered terminal path. It does not truncate or weaken the frozen Campaign-2
choice adapter. Workspace retains the exact existing subject/task/prediction read
law. Later stages use completed operands and immutable definitions only. Direct
ObserverId wrappers allow PRJ/IDN before comparing the nested qualified subject.

The completed concern handler derives narrow delivery values from its actual output.
It schedules future phase15 deliveries only for declared consumer slots. Encoding
and recall deliveries use distinct event types, each bound to the same existing
original-address/instant format. Thus two consumers can receive the same earlier
concern through two independently consumed deliveries, without replaying one delivery
or inventing a feedback occurrence namespace. A no-selected-task projection has no
invented concern reference even though the upstream TaskConcern output exists.

Each phase40 join has actual selected preparation or cue completion as its direct
parent and its own completed delivery as an additional parent. Exact expected input,
run, target original, instant and consumer event type are checked before PRJ or
content reads. Allocated IDs alone are insufficient. Consume associations inside the
transaction; failure restores them, and successful settlement leaves no unresolved
expected target. Restore must independently reproduce the complete admitted prefix
and exact pending queue. An in-memory association is not persistent proof.

The encoder receives selected-only operands and the narrow response. The ranker
receives the actual cue and narrow response, then its declared available-cue reads.
Neither receives nested workspace, appraisal or forecast. The symbolic output of
rank is PriorConcernRecallEvaluation; its exact nested EventRecallResult supplies
the existing recollection publisher. No new result or presentation identity arises.

These current-lane registrations replace the baseline downstream encoding/rank
registrations in a distinct model. Both variants do not execute for the same input.
The selector remains identical. Empty selection and unavailable cue still consume
their expected joins; the former allocates no acquisition and the latter reads no
memory. Disabled feedback retains all delivery checks. No body-feedback extension
or body/event budget exchange is introduced.

Three producer events and at most two additional receiving delivery events are
bounded here. Existing encoding/rank are replaced, so they are not extra events.
This is not a whole-model work ceiling. Target schedule, model definitions, all other
consumer branches, exact accessors, codec roles, allocator tables and runtime
qualification remain required. AJB public vectors remain FROZEN, NOT PASSED.

The actual component formation test now composes an existing Workspace/Appraisal/
Concern result with selected-only visual formation: thirteen focused tests pass,
including unchanged selection/identity count, fixed focal allocation, reduced
peripheral allocation and explicit unavailable/current-time handling. The test's
producer-time and subject bindings remain trusted component premises, not AJB proof.
