# REASON joined comparison readiness —2026-09-21

LOCAL DISPOSITION; no owner ruling. Starting counters912/0. The exact corpus
PHEN-REASON-001 requires motive/referent separation, role/sign consolidation,
collective redundancy, zero-base exclusion, weak-base rescue and one die per
resolved reason, joined with pooled/pairwise/per-fact/identity-die controls.
Existing C2 and MULTISOURCE receipts cover separate subsets; preserve them.

Consulted MEC-012..018/022, EXP-009/014 and the inherited reason-evidence-coverage,
reason-dice and task-identity-evidence contracts. PORT exact keys and role/sign
partitions; CONTROL aggregate weighted Jaccard, bounded response and calibrated
dice/modifiers. Retain the larger-aggregate subset residual and source-key tie
dependence as limitations. No reduction or generic correlation law is selected.

Use a separately frozen bounded controlled-panel/task fixture: governed task
definitions associate permitted panel readings with option/motive/referent/source
role. This is an explicit identity-establishing symbolic control, not free event
recognition. Values and permission are public original physical inputs; no supplied
raw signal, basis, nucleus, learned identity or chosen result is admitted.
Aggregate descriptions derive from the same two actually observed panel samples.
Observer evidence IDs are allocated at runtime, never invented by descriptions.

Acquire standing through the existing public causal chain copied by composition:
task context -> raw signals -> actual C2 reasons -> addressed arbitration -> frozen
expression -> qualification -> identity owner. Reuse LONG's governed two-task
context and the accepted C2 math; no authored identity history. The probe uses
that retained history with its own explicit K calibration. Standing is a modifier
only in the lawful baseline. Its qualification provenance remains available.

Five frozen comparison profiles: exact semantic Aggregate, PooledChannel,
PairwiseOnly, PerFactDie, IdentityIndependentDie. The last two and pooled profile
are retired diagnostic alternatives; pairwise is a serious cheaper hypothesis.
No reinterpretation of motives by LLM or embeddings and no hidden target read.
Accept shape/allocation before source, model images and exact public inputs before
execution. Full joined gates, reached rollback and complete-prefix replay precede
qualification. Broader source/role/causal-referent generalization remains conditional.
