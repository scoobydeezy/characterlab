# Requested source output carrier

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic draft.

GeneralSourceSamplingRequest declares Observer, optional Body request, Panel and
Visual Booleans. At least one modality must be requested. An existing proposed
LocalReserveSamplingRequest supplies the body channel set and must have the same
observer. Body omission means not sampled; an empty channel set is invalid.

CompletedRequestedSamples binds the exact admitted request to its actual completed
outputs at one instant. RequestedBodySamples contains existing461/463 samples and
compiled safe signal declarations, without an experience yet. Sample/declaration
channels must biject the requested channels. Panel is a closed sum of the existing
proposed TrialPanelPresentObservation and the new unavailable header. Visual is
the existing proposed ExtendedObservation. Each requested product is present even
when observation is unavailable; unrequested products are absent. No unavailable
header discloses visibility, permission or hidden physical reasons.

EventDetection is present exactly when requested panel is Present or requested
visual detections are nonempty. If Visual is requested, its EventDetection equals
that shared value, including the empty-visual/present-panel branch. Panel-only
requests can therefore produce the same event detection without fabricating a
visual observation. Body alone never creates an event detection. Distinct actual
observations use existing1115; event detection uses existing215/1113. These are
allocated outputs, not new wrapper identities. No wrapper occurrence is invented.

The physical sampler component allocates observation headers only. Detection
allocation and independently permitted role projection are subsequent source-stage
obligations. ExtendedObservation's Role is safe223, never a truth binding reference.
Its source implementation must consume actual admitted world bindings and channel
policy. The current fixture's actual SEM-B projection supplies component evidence
only. Governed world/channel definitions and public producer registration remain open.

SourcePerceptionInput contains only Observer and the completed safe products, with
exact observer equality. It grants no physical-state, world210/binding211, registry
resolver or trace read. Tracking/event-file ownership and experience reservation
remain the existing separately ordered stages. One actual shared freeze later
constructs BodyOpportunityEvidence and VisualOpportunityEvidence. No reservation
is made for an all-unavailable batch. No synthetic experience stands in for absence.

Unlike a requested unavailable panel, an unsampled panel does not instruct the
perception owner to retire an active context. The bounded cue-only branch still
rejects an unsampled active context rather than inventing continuation or closure.
No observer-side general segmentation mechanism is introduced.

This five-record inventory is additive to111 earlier proposed records. It makes
no numeric allocation and does not constitute codec, role, source, model, queue,
runtime, persistence or General Attention qualification. Its exact request and
optional output equations must be enforced by the eventual closed compiler.
