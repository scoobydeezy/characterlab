# Governed bounded source and original-use declarations

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic draft.

The controlled source requires committed declarations rather than closure-captured
fixtures. PositionSceneItem names a source-side scene object, its authored EventRole,
grid position/glyph and visibility/permission with Preserve or Unresolved role mode.
SceneObjectReferent uses existing SemanticReferentId1002, with the existing governed
attention-scene-object kind and exact model membership checked by the compiler.
This is neither a new identity family nor a character/maintenance-goal qualification.
No scene object is a perceptual continuant-file or source of tracking continuity.

PositionSceneFrame contains0..3 items and an instant. PositionSceneDefinition contains
0..10 strictly increasing frames and an exact DefinitionId reference to existing
EventTypeBindingSchema248. Actual materialization produces existing WorldEventTruth210
and EventBinding211 through their existing allocators. Validation-only domain tags
come from the compiled content declaration, never an untyped runtime field. Role
cardinality and event-specific domain restrictions remain SEM-B's responsibility.
No authored frame may carry a causal-role judgment, stroke classification, significance,
retained target, body delivery provenance or outcome qualification.

TrialPanelFrame contains At/Glyph/Stage/Visible/Permitted; its definition admits0..16
strictly increasing frames under the existing bounded panel law. GeneralSourceProfile
links the exact compiled observer, body registry, channel registry, panel and visual
definitions. Its reference graph must resolve without callback bodies or arbitrary
registry access from cognitive stages. All source-side scene details remain outside
SourcePerceptionInput. Role projection may expose only the actually permitted role
sum; coordinates or panel stage cannot manufacture Actor.

ObservationUsePlan separately declares BodySelection, VisualSelection, BodyCue and
VisualCue, with optional goal-assessment DefinitionId. It schedules existing distinct
consumers; it is not a new psychological selector. A body use requires requested
body channels; visual use requires a requested visual source. Goal assessment requires
the consequence lane, actual safe consequence evidence, an actual adopted goal and
the separately admitted prior-memory read. The literal plan cannot supply a baseline,
assessment result, selected winner or acquisition identity. Cue and selection flags
may both be true; capacity belongs to their separate committed policies.

GeneralObservationOriginal contains DueAt, source-profile DefinitionId, the exact
GeneralSourceSamplingRequest, the use plan and Current/Consequence lane. Ordered-input
admission binds its existing original address, due instant and exact bytes. No new
original, source-batch, frame or use-plan occurrence is allocated. The first cohort
must close its exact schedules, consumer combinations, source limits and work maxima;
this grammar is not permission to accept arbitrary combinations from callers.

The ten-frame/three-item bound is the proposed maximum, not a claim that the current
single-point display component implements three-item frames. The four-trial plus
cue/competitor fixture uses one item. Multi-item source materialization, canonical
observed ordering and all public registration remain explicit implementation gates.
No hidden referent sorting may change an otherwise identical observer-safe detection
sequence. Existing one-point eight/nine/ten-frame constructors remain controls.

This eight-record source/original inventory is additive. It neither allocates
numbers nor qualifies a whole model, codec, source producer, memory route or corpus.
Separate bounded profiles remain possible; no inference about general event
segmentation, physiology, causal attribution or psychological finality is made.
