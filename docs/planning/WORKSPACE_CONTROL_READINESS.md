# Workspace/control intake — 2026-09-21

LOCAL DISPOSITION — no owner ruling required. PHEN-WORK-001 is the bounded target;
the wider Brief12.6 remains explicitly outside this qualification.

Architecture10/11, North Star17 and Brief9/12.6 were consulted. The existing
task-workspace/0.1-candidate and GA adapter qualify event-local access, but exclude
maintenance through interruption. The successor contract preserves them unchanged.

Selected first model: StoredSet, with IndexedReplay as a serious same-domain derived
alternative. A finite safe observation journal is shared by both; only StoredSet
maintains a second active-set leaf. This tests whether that cache earns independent
authority, not whether retained information equals active access. No Willpower or
control-cost scalar is invented. The controlled board is an explicitly identified
source; ordinary perception/recognition and retrieval remain separate frontiers.

| Historical mechanism | Disposition |
|---|---|
| MEC-011 / EXP-004 | Port availability/access distinction; AvailabilityEqualsAccess negative control |
| MEC-020 / EXP-013 | Port Open/window/deadline lifecycle; ImmortalGoal negative control |
| MEC-005 / MEC-007 | Preserve GA attention and salience controls; displayed card priority is a finite source operand, not replacement salience law |
| MEC-009/010 / EXP-003/006 | Preserve memory/retrieval mechanisms; board journal is admitted directly, not claimed as a retrieval implementation |
| MEC-012..016 | Port distinct task grounds, rolewise aggregation, inherited dice/arbitration, no motive from neutral card |
| MEC-017..019 | Chosen intent retained; execution, expression learning and identity-loop controls remain inherited, not reduced here |
| P3-011 | Port strict consequence-to-later-workspace order and partition/prefix tests; no same-cycle recursion |
| SUB-001/003/004/005/008/009/011 | Exact arithmetic, typed identity, addressed fair draws, complete-prefix persistence, matched interventions, preserve failed findings |

Prediction: distraction displaces an unprotected intention without deleting it;
support protects a previously active intention; a cue restores retained but inactive
intention, and cannot recreate an expired task. Cache-free indexing may reproduce
the same behavior at greater bounded reconstruction work. General control law and
wider cognitive-control phenomena remain conditional if this corpus passes.

Initial counters: highest765 / since verdict0. Contract and allocation precede code.
