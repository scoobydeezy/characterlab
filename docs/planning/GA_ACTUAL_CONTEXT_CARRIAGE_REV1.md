# Actual retained trial-stage provenance

2026-09-13. LOCAL DISPOSITION — no owner ruling required.

The component fixture previously stored a caller-supplied Before/Motion/After label
beside an actual experience. Although missing context blocked its positive grouping
controls, that label was still a supplied projection premise. It now stores the
actual observer-side panel companion produced by observeGeneralSourceOpportunity.
The stored role comes only from that companion's present panel sample. An unsampled
or unavailable panel contributes no stage; the standalone73 acquisition has null
stage and no context companion.

Retained grouping inspects only views with actually observed stages. It requires
exactly four complete context groups before focal lookup. Removing all panel evidence
therefore returns GroupingUnavailable, not a later missing-focal diagnostic. The first
new report exposed that empty-set fall-through; revision2 fixes the grouping boundary
and passes all28 controls. No source-array position or hidden episode key restores a
missing stage, and a missing stage does not undo otherwise lawful visual acquisition.

This implements the already proposed optional PerceivedTrialContextEvidence carriage
at component scope. The fixture still serializes test data and does not claim the
unallocated public retained-record codecs. Public exact-byte source attachment,
CharacterId projection, admitted retained reads and whole-prefix qualification remain
open. The actual-use comparison from GA_ACTUAL_USE_RETENTION_RESOLUTION_REV1 remains
the first discriminating witness; its older supplied-use comparator is preserved.
