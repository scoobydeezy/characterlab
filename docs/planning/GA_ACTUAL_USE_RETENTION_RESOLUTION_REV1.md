# Retention comparison without supplied use credit

2026-09-13. LOCAL DISPOSITION — no owner ruling required.

Select the already admitted earlier stroke as the focal episode for the first
discriminating actual-source retention witness. GENERAL_ATTENTION_FOCAL_TRIAL_RESOLUTION
explicitly permits either stroke and says focal-last is a fixture arrangement, not
a universal order. No fifth episode, new consumer, new motion projection or transfer
of significance is required.

The focal endpoint is acquired at3; its concern-relative qualification is completed
at4, while that adopted goal is active. At71 the actual retained-operand attribution
evaluator consumes all sixteen recalled body/position children, including the later
stroke endpoint acquired at63. Only the explicit focal endpoint receives the frozen
directional significance at72. Withdrawal70 does not rewrite the earlier assessment.

The actual tenth source still forms a new acquisition at73. It receives no use credit:
no qualifying consumer has used it. This retains the later-formation preservation
control while removing the unsupported premise from the discriminating witness.
At75, under the same per-kind retention challenge:

| Model | Survivor | Basis |
| --- | --- | --- |
| Significance first | focal endpoint acquired3 | actual use plus focal significance |
| Shared protection tier | later endpoint acquired63 | actual use, then existing recency tie |
| Significance disabled or nonqualifying concern | endpoint acquired63 | actual use only |

Both intact and comparator receive identical memory, actual read-use metadata and
retention capacities. The new73 acquisition remains unprotected in both. No memory
is labeled used merely to force this comparison. No other child receives significance.

Preserve focal-last with actual reads only as a named nondiscriminating control: both
retention laws keep the63 endpoint. Preserve the earlier supplied-use experiment as
a component sensitivity control, explicitly ineligible to prove public downstream
use. Its fixture-only mode is not a model option or a public authorization switch.

SC-Y/Z/AA execute these cases alongside the existing24 controls; all27 pass in
ACTUAL_USE_RETENTION_COMPOSITION_TESTS_REV1. This removes the supplied-use premise
from the new witness, not the remaining public producer, recall/read admission,
goal-result identity, owner, canonical carrier, scheduler and persistence gates.
The old source schedule's frozen64 qualification remains valid for its focal-last
control; the first actual-use cohort must bind4 explicitly rather than alias those
two different target schedules. Reopen only for a phenomenon that actually requires
an additional later consumer or a fifth episode, not merely to preserve fixture order.
