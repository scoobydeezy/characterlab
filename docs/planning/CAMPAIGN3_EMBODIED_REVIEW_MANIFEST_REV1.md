# EMB-001 bounded review manifest, revision 1

2026-09-10. **BOUNDED SYMBOLIC PACKET READY FOR SHAPE REVIEW.** This is a
dependency/role closure review, not whole EMB acceptance, allocation or runtime proof.
The companion JSON commits the exact packet files and inventories every local field
reference and identity position. Earlier receipts remain historical evidence.

## Governing packet

Read the motivation, observation admission, ingress scheduling, registration accessor,
replenishment profile, symbolic consolidation, field type review, vocabulary dispatch
and profile witness drafts together. Apply the explicit corrections in
[composed review revision 1](CAMPAIGN3_EMBODIED_COMPOSED_REVIEW_REV1.md), then this
manifest's role-owner clarification. The current inventory is **typed revision 2:
32 records, 128 fields**. The seven-case arithmetic receipt is augmented by the
composed-review quantization receipt; neither is a runtime golden trace.

## Role and reference closure

The field review's phrase “every nested field and map/set position as required by
VAL” is an obligation, not a claim that the current recursive VAL visitor checks
every such position. Inspection of `compileValDeclarations` establishes:

| Position | Owning check in the proposed profile |
|---|---|
| Direct ID field, including records nested inside sets or aliases | Declare its canonical record-field role; recursive VAL checks the record after decoding. Character1002 additionally requires the accepted authored-character qualifier. |
| ReserveState.Anchors map key | StateKeyGrammar first, then its StateMapKey role and character qualifier, then exact declared body coverage. The recursive record visitor alone is insufficient. |
| LevelSourceDefinition.ChannelDefinitions set member | Closed source compiler checks each ID's namespace1027 and resolves exactly a LevelChannelDefinition at the committed observation version. |
| ReserveReplenishmentRegistration.DefinitionIds set member | Closed replenishment compiler checks namespace1027 and resolves exactly a ReserveReplenishmentDefinition at the committed replenishment version. |
| Generic registry171 StableId | Registry-kind dispatch checks the required family and exact entry identity. Do not globally narrow the polymorphic field to DefinitionId; pressure registration IDs use TransitionKindId. |
| Schema references254 | Exact type AND schema version plus field-specific expected schema; merely resolving some known schema is insufficient. |

No new VAL selector or generic collection-role schema is proposed. Set checks belong
to the fixed profile compiler, not a caller-supplied validator. RequiredProjections
reuse266 and accepted PRJ/IDN; new source/pressure registration dispatch must explicitly
extract them before that component can consume them. Current unsupported-schema
rejection is correct and is not evidence that the proposed adapter executes.

All DefinitionId fields resolve by full typed identity, expected registry kind and
exact DefinitionVersion. Their targets are: reserve parameter, shared body registry,
level channel, pressure definition, source registration, or replenishment definition,
as named by the field. Both pressure admissions' SamplingProducerDefinitionId resolve
the same registered sampling producer. Both body consumers resolve the same singleton
body registry. Reject dangling references, duplicate stable IDs, wrong-kind same-family
references, and divergent body/parameter/channel unit or capacity bindings at compilation.
Version strings never substitute for identity or authenticate an occurrence.

Unit1039 remains namespace admission plus exact new fuel member at profile compilation.
No singleton DomainValidator is added. Observation1115 and experience1106 retain their
allocators; only pressure needs a new occurrence family. Neither the quantization record
nor replenishment result introduces another identity or character provenance path.

## Proof obligations added by this review — NOT PASSED

* EROLE-A: wrong namespace in either definition-ID set rejects before execution.
* EROLE-B: valid namespace but missing/wrong-kind/wrong-version target rejects.
* EROLE-C: unqualified character map key rejects after key grammar; valid different-body
  component control remains required separately from singleton-profile exclusion.
* EROLE-D: pressure registration's valid TransitionKind stable ID remains admissible;
  replacing it with a DefinitionId alias rejects under its registry-kind rule.
* EROLE-E: removing the collection-member check is caught independently of record-field
  VAL; removing state-key qualification is caught independently of recursive record VAL.

Retain all prior EOBS/EING/EREG/EREP/ECOMP and motivation comparison obligations.
Inventory/reference checks establish planning consistency only. They do not pass these
controls, first-profile activation, codec persistence or epistemic isolation.

## Scope and remaining gates

No additional symbolic expressibility blocker was found for the bounded chain:
reserve → permitted level sample → support-only current SEM → option-free pressure,
with external replenishment as the sole body writer. This subprofile can be reviewed
independently of non-task reason receiving and action knowledge because it claims
neither chosen action nor motive influence on choice. It cannot qualify BODY+MULTISOURCE
or decide stored Need ownership. CTL-001 remains the required stored-meter control.

Next is an explicit bounded whole-shape disposition against this exact packet. Then
separate permanent allocation, complete model/content/role packaging and implementation
qualification gates remain. Full170 content records, numeric role positions/schema
descriptors, canonical bytes/digests and public adversarial runs are packaging or
execution work, not silently supplied by the arithmetic witness. No accepted Campaign2
contract, frozen corpus, runtime source or permanent allocation changes here.
