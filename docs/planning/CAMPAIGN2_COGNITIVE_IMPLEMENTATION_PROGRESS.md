# Cognitive implementation progress after the power-cycle checkpoint

Work resumed under the user's continuing autonomous-work instruction. Campaign 2
remains OPEN; no new external review or whole runtime qualification is claimed.

The frozen shape, allocation and 21-model packet remain unchanged. Production
`cognitiveModel.ts` now reads complete data-only sources, validates declarations,
roles, references and structural ownership, computes their full commitments and
selects an exact frozen cohort member. `COGNITIVE_MODEL_ADMISSION_REV1.json` records
all 21 commitments admitted against the independent frozen declaration builder.

`cognitiveState.ts` adds task/plan membership, immutable instruction, initial-state
and ordered identity-history refinements while composing the retained prediction
state owner. A structurally valid retained history is not accepted in initial state;
complete-prefix restore must still authenticate later history.

`cognitiveTransforms.ts` implements restricted workspace, appraisal, concern, motive,
candidate and raw-source components. `cognitiveExecution.ts` currently authenticates
original and generated sources through arbitration. Its no-choice branch can finish
its own adapter transaction; chosen downstream scheduling still needs integration.
No public cognitive run factory exists yet. Shared ordered-input code now recognizes
the frozen cognitive profile with exclusive positive instants, cognition through 100,
probe through 99, and no authored-fact originals. Old profile branches are preserved.

`cognitiveArbitration.ts` uses the real addressed RNG and exact retained dice grammar.
Its provisional address ledger is discarded on abort; committed addresses and stale
capabilities reject reuse. `cognitiveChoice.ts` implements frozen intent/expression,
qualification, plan, attempt and execution components; execution permission is absent
from expression/qualification operands. These components are not yet a public run.

`protocolObservation.ts` projects actual execution into the generic observer and
clears truth references. `protocolBridge.ts` runs actual SEM consequence phases
120..124, reservation and freeze with no suppressed-branch padding. The observation
closure now recognizes the separately accepted protocol transformation version with
exact point 0/1 or lower-bound 2 and empty tokens/references. Generic measurement
mathematics and the predecessor fixed-pulse predicate remain unchanged.

## Executed checks and limits

- Model/state/upstream source tests: 15 PASS, including forged originals/children
  rejected before state reads, and mandatory binding of even empty child plans.
- Character transform tests: 11 PASS, including reserved workspace slot, Unknown
  versus known zero, option alias deduplication, and direct identity excluded from meaning.
- Arbitration/choice/protocol tests: 10 PASS, including genuine RNG transcripts,
  abort/retry equality, stale/duplicate rejection, authorship rejection order, all
  three observation counts, real five-stage SEM freeze and suppressed no-padding.
- Existing codec/math focused tests previously passed 23 cases; their implementations
  have not changed during this continuation.
- TypeScript compilation passed after adding the bridge implementation; run again
  after subsequent edits.
- Reference import boundary check PASS.
- A broad programmatic run selected both active and historical tests: 159 files,
  1,360 tests passed, 8 timed out. All eight were in five files; rerunning those files
  without file-level concurrency passed all 43 tests. No assertion failure remained.
  The broad run preceded the latest protocol tests and observation closure addition;
  current observation unit/integration controls and all protocol tests passed separately.

The initial audit/test harness mistakes were corrected rather than counted as
semantic findings: a text-only selector encountered union tuple identities; an
upward codec import in orderedInputs created a module initialization cycle. The
profile now uses its trusted supplied decoder without that import cycle.

Current-source fingerprint receipts for modified shared taskDeclarations,
orderedInputs, observation and earlier scheduler source must be refreshed where
applicable. Frozen historical reports are not rewritten or silently made current.

## Remaining implementation

Wire chosen intent/expression/plan/attempt/execution and both consequence branches
into the trusted scheduler; compose actual307 registered-producer ADAPT admission;
seal identity application alongside the prepared phase-140 owners; add full trace,
public data-only run factory and complete-prefix persistence with the rebuilt RNG
ledger; then execute all frozen generated witnesses, substitutions and preservation
checks before any whole Campaign 2 completion verdict. PERSIST-I remains unpassed.

## Integration update — 2026-09-09

This section supersedes the earlier implementation-progress descriptions above.
The public cognitive factory now exists. `cognitiveRuntime.ts` composes generated
cognition, protocol observation/SEM, EVID, ADAPT and the predecessor memory/prediction/
task adapters through the actual shared scheduler. `cognitiveIngress.ts` accepts the
frozen regulatory producer change while checking that every retained ADAPT rule and
all other registration fields match the accepted predecessor. Its registration view
is explicitly the inherited ingress slice, not the whole cognitive graph.

The first composed control caught the old ADAPT codec rejecting the successor's
frozen nested307 TransformationVersion. The trusted evaluator now takes its model
compiler's codec; arithmetic, staged output authentication and WRT checks remain
shared. Historical constructors still select the original codec.

`cognitiveTrace.ts` records actual sources, subjects, reads, draws, quantization and
separate identity WRT evidence. The common140 barrier joins A + I with optional task
deadlines against B0. Adversarial review caught a missing-I path failing too late;
source-owned participant cardinality now rejects missing/extra A/I before all priors.
No source, allocation, phase, numeric calibration or formal artifact was changed.

`cognitiveFactory.ts` exposes byte-only create/snapshot/save/restore. Restore rebuilds
the complete genuine prefix and requires exact equality to its freshly generated
whole save, then continues that replay instance with its private associations and
consumed RNG addresses. The state-derived RNG projection is the frozen canonical set,
separate from that private ledger. No caller-supplied cache or projection capability
is admitted. Public imported learned S0 remains forbidden.

Evidence so far:

- The actual baseline sequence probe1, cognition2/3/4, probe5, cognition6 runs from
  empty memory/prediction/identity and settles both later deadlines.
- COGNITIVE_RUNTIME_COHORT_REV1 records all21 frozen recipes executing through the
  public factory. It is a finite execution receipt, not whole-vector qualification.
  It predates the later participant-cardinality fix and additional test changes;
  preserve its exact fingerprints and issue a new receipt for final current source.
- Model/component/integrated tests:17 PASS at that checkpoint.
- Public create/restore tests passed full post-draw save continuation and final
  identity-trace failure rollback/reconstruction. An equal-criteria generated roll
  retains no identity contribution but its consumed addresses survive prefix replay.
- Independent retained-root/instruction/seed/draw mutation tests and common-stage
  cardinality controls are being completed; do not infer their final disposition.
- Affected predecessor preservation:100/100 PASS across task runtime84, independent
  ADAPT4, trace5, rollback6 and parent pair1. This run preceded the small cognitive-only
  participant check. No old model semantics were altered.
- TypeScript and reference boundary checks passed before the latest test additions;
  rerun after final changes. No git commit was created.

Remaining: finish and review the complete frozen TW/TP/RI/TRC4/CS and PERSIST-I
crosswalk; execute history-capacity and numeric/work boundaries and discriminating
source substitutions; refresh affected current-source receipts; run final full
active/reference preservation; then make the whole architecture/completion verdict.
Campaign2 remains OPEN. No new external decision is currently required.

## Campaign 2 completion — 2026-09-09

Campaign 2 is COMPLETE in the bounded thin-scaffold scope of
[CAMPAIGN2_COMPLETION_REVIEW.md](CAMPAIGN2_COMPLETION_REVIEW.md).
This internal adversarial verdict is made under the user's explicit autonomous-work
authorization. It supersedes the earlier OPEN/power-cycle/implementation-pending
checkpoint dispositions, without rewriting any frozen shape, allocation or model file.

`task-cognitive-path/0.1-candidate` is QUALIFIED. TW/TP/RI/IH/TRC4/CS controls pass
within the documented public/component/exclusion split; see
[CAMPAIGN2_COGNITIVE_QUALIFICATION.md](CAMPAIGN2_COGNITIVE_QUALIFICATION.md).
PERSIST-I now PASS only for `campaign2-task-cognitive-persistence/0.1-candidate`,
complete-prefix reconstruction with actual RNG and no external coupling maps; see
[CAMPAIGN2_COGNITIVE_PERSISTENCE_QUALIFICATION.md](CAMPAIGN2_COGNITIVE_PERSISTENCE_QUALIFICATION.md).
Earlier no-RNG qualifications, whole current VAL and bounded ADAPT remain unchanged.

The exact 21-model cohort executes;17 source substitutions are detected; historical
math and18 predecessor assays pass. Public64/65 and27/26 boundaries execute. Current
receipt/source and frozen-artifact consistency:24 receipts,446 fingerprints PASS.
No external planning-agent approval or general psychological/reduction verdict is claimed.

PHEN-ADAPT-001/1.11.0 and corpus/0.27.0 remain unchanged at digest
`3cb09115d90485d5975968ffca419a4960a398326b6b6339a5b1e8e8fdcec276`.
General DEC-001, ORD-001/002/005, future ContextModulating and the remaining expanded
phenomenon/research obligations stay open. Campaign 3 is next; it has not passed.

Final consolidated validation: COGNITIVE_FINAL_SUITE_RECEIPT_REV1.json records
118 active files /1,063 tests PASS and43 preserved-reference files /328 tests PASS,
with source/test fingerprints unchanged during both runs. TypeScript, reference
boundary and final whitespace checks PASS. This supersedes earlier test counts;
Campaign2 completion and its bounded research exclusions remain as recorded above.
