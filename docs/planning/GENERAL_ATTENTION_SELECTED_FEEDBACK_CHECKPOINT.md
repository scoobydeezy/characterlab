# Selected visual boundary and prior-concern checkpoint

2026-09-13. LOCAL DISPOSITION — continue autonomously. General Attention OPEN.

The [forward source integration](GENERAL_ATTENTION_VISUAL_RECALL_INTEGRATION_REV1.md)
now runs actual selected spatial formation and ninth-cue event recall. The SEM event
tag correction and the spatial consumer's version guard are implemented;32 checks
in GA_SPATIAL_SEM_VERSION_TESTS_REV1 passed after that guard.

The new [selected-only capability](../formal/SELECTED_SPATIAL_CAPABILITY_COMPONENT.md)
separates full source preparation from the encoder's actual inputs. Only selected
evidence, bound witnesses and the actual receiving instant remain reachable. Its
one-use/close behavior is executed; preparation does not retain a source resolver.
Four targeted faults were caught in SELECTED_SPATIAL_CAPABILITY_REVIEW_REV1. That
receipt precedes the explicit receiving-instant field and prior-concern extension.

[Prior-concern projection](../formal/PRIOR_CONCERN_FEEDBACK_COMPONENT.md) consumes
actual workspace/appraisal/concern transformation outputs in component tests and
returns only the existing concern identity, subject, producing instant and response.
Seven faults detect time/subject substitution, reversed modulation, unavailable/zero
collapse, ignored disablement, mismatched task and nested appraisal leakage. These
tests supply forecast/status/producer time; public source authentication is not earned.

The [selected spatial composition](../formal/PRIOR_CONCERN_SPATIAL_ENCODING_COMPONENT.md)
uses that narrow prior input without an encoder source read. Only peripheral allocation
changes; focal allocation and preselection count remain intact. Actual sensing time
comes from the prepared capability, preventing retrospective relabelling. Four faults
are detected in PRIOR_CONCERN_SPATIAL_REVIEW_REV1. The carried response and qualified
subject are component premises in that composition test, independently of the actual
concern-transform projection tests.

[One hundred affected checks](GA_VISUAL_PRIOR_CONCERN_REGRESSION_REV1.json) pass across
13 files. The subsequent test-only discriminated-union guard resolves a TypeScript
diagnostic without changing production behavior; type checking now passes. Reference
boundaries and diff whitespace checks pass. This is not a new whole-source suite or
public-runtime qualification, and old mutation fingerprints remain historical.

The [visual baseline field inventory](GA_VISUAL_PIPELINE_CARRIER_REV1.md) has16 records
and ten rejected structural faults. Its [scope correction](GA_VISUAL_PIPELINE_SCOPE_CORRECTION_REV1.md)
preserves lawful earlier-concern feedback as a separate variant. The [revised role
crosswalk](GA_IDENTITY_ROLE_CLOSURE_REV2.md) covers82 records,86 identity positions and
116 record boundaries, with11 rejected structural faults. RetainedEncodingUnit is
now explicitly traversed. Those numbers do not establish state-map key or public
compiler coverage.

Next: close the exact successor state-root/key/value inventory, preserve the separate
prior-feedback wrapper/registration, then assemble the whole public declaration
packet. Actual source/PRJ/owner/phase authentication, model/cohort closure, separate
numeric allocation, public execution, rollback/complete-prefix qualification and
corpus promotion remain OPEN. No owner decision is pending. Frozen corpus0.27.0,
accepted earlier models and permanent allocations remain unchanged.
