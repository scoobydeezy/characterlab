# Physical source schedule reconciliation

2026-09-13. LOCAL DISPOSITION — no owner ruling required.

The accepted external replenishment profile writes at110. SEM current observation
is10; consequence observation is120. The direct four-trial component fixture had
prepared the body immediately before a same-instant current sample, by calculating
the delivery from a hidden materialized level. That host call sequence is not an
admissible public stage schedule and is not a declared world controller.

Use explicit bounded external deliveries:7 at20,17 at40,17 at60, each at phase110.
The corresponding current before samples occur at21/41/61, phase10. Initial A is21
at0 with rate1, so the first current sample at1 already reads20 without a zero
preparation. Preserve the existing zero-delivery control independently; it is not
needed to create this first baseline. Outcome deliveries13/3/3/13 stay at4/24/44/64,
phase110, before the same-instant consequence observation at120.

These literals instantiate the declared bounded source, not a runtime policy that
reads physiology to force every new model back to20. They are held fixed across
the matched concern/significance and hidden-delivery controls. Rate/initial-state
interventions must not silently recompute these inputs to erase their effects.
No new Need, physiological controller, PRJ bypass or phase0 physical writer is added.

The component fixture now executes this schedule and records source-side inputs
separately from retained observer evidence. All four before intervals remain[20,21].
This correct-forward change leaves older direct-composition reports historical.
Public ordered-input/owner execution remains unqualified; do not infer a scheduler
PASS from a direct component test. The next public graph must use these exact phase
relationships and each original's normal admission, patch and trace rules.
