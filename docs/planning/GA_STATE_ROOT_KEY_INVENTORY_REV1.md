# General attention state-root and key inventory

2026-09-13. LOCAL representation disposition. Symbolic draft, no allocation.

The new cognitive leaves are whole bounded owner values under the actual qualified
character key. Retrieval enumerates children inside its explicitly read owner value;
it never scans arbitrary authoritative state entries. Existing346 measurement memory,
373 task commitments and241/242 SEM file state remain unchanged.

| New physical root | Leaf value | Canonical key | Classification / sole owner |
| --- | --- | --- | --- |
| GeneralEpisodeState | SurvivingEpisodeLedger | CharacterId | episodic-memory / ordinary-memory settlement |
| GeneralAssociationState | AssociationGraph | CharacterId | associations / association settlement |
| GeneralPresentationState | PresentationLedger | CharacterId | episodic-memory / presentation settlement |
| MaintenanceGoalState | MaintenanceGoalLedger | CharacterId | prospective-commitments / maintenance-goal lifecycle |
| GeneralTrackingState | TrackingWindow | ObserverId | perception / existing perception authority, explicitly extended |
| TrialPanelContextState | TrialPanelWindow | ObserverId | perception / existing perception authority, explicitly extended |
| FormationGovernanceState | FormationGovernanceValue | singleton, no selector | RuntimeProtocol / formation governance |

Perception in this table names the existing architectural owner, **not** a proposed
StateFamilyId1031 member. SEM tracking/context do not become learning or runtime
protocol merely to fit the ADAPT family classifier. Exact state/authority admission
already supports SEM241/242 separately. Additive learning-family declarations apply
to the four character-keyed roots; the protocol descriptor stays separate.

Six new scalar StateMapKey roles are needed: four1002 with the existing character
qualification validator, two1000 with no validator. Recursive record roles still
apply inside leaf values. MaintenanceGoalKey and QualifiedFormationSource are composite
record keys/fields inside their values, never substituted scalar map-key roles.
The compiler also checks exact closed model membership and key-to-contained-holder
equality. A namespace or predicate check alone cannot establish those relationships.

Each root has one symbolic field. The first profile admits exactly its one compiled
observer/character and initializes these six values empty at time0; the protocol
singleton is present with empty maps. Leaf removal is prohibited; complete child loss
produces a present empty episode ledger. No prior root or frozen model is reinitialized.
Goal finalization updates its existing entries rather than dropping lifecycle evidence.

The learned graph admits0..27 canonical continuant keys and matching square rows,
zero diagonal, bounded integer masses and row sums at most its committed scale.
The27 bound follows only from the first nine-sweep, three-detection source with empty
initial state and no external keys. Edges remain independently bounded and pruned;
memory loss does not remove a node unless graph reconciliation finds it structurally
empty. A broader source must declare its own complete bound, not truncate to27.

TrackingWindow contains only the latest sweep's actual observation, instant, file and
optional observed glyph for0..3 detections. TrialPanelWindow holds last processing
instant and an optional active glyph/event-file pair. An ended/absent panel leaves
no active context. Actual SEM counters/active membership stay in241/242; neither new
window copies those maps. Source replay/horizon/used-occurrence validation is rebuilt
from the complete admitted prefix, not a cognitive archive of all earlier observations.

Public restore must reconstruct and compare the windows and existing SEM files,
all model-bound lifecycle limits, exact pending queue and the protocol singleton.
These record shapes do not qualify reconstruction or grant any actor new access.
Canonical roles, grammar/removal rules, disjoint authority, expected-old reads,
recursive accessor exclusion and actual transition memberships remain separate checks.
