# Bounded perception ownership and real reservation checkpoint

2026-09-13. LOCAL DISPOSITION — no owner ruling required. General Attention OPEN.

The actual multimodal source now supports [marker-window tracking](../formal/MARKER_WINDOW_TRACKING_COMPONENT.md)
and [panel-window segmentation](../formal/PANEL_WINDOW_PERCEPTION_COMPONENT.md).
The main concern/significance/retention and observed-association/recall fixtures
now use both bounded owners. Reconstruction needs only existing SEM owner states
and the last windows proposed by the public inventory. It does not require old
sweeps, private observation archives or copied SEM allocation counters. The existing
history-bearing components remain differential controls.

The marker window preserves unique observed-glyph continuity, ambiguity and absence;
the panel window preserves actual observed context, ending and shared visual-event
rules. Both retain observer-relative fallibility. Source-time occurrence admission
still owns historical replay exclusion; cognitive state does not acquire that job.
Observation and detection families may have equal numeric payloads without aliasing.
The explicit source bounds imply at most30 continuant and26 event files. Those
ceilings do not replace the compiled ten-visual/sixteen-panel schedule limits.

[Reservation correction](GA_SOURCE_RESERVATION_CORRECTION_REV1.md) fixes a real
component ordering omission: the actual sample stage reserves ExperienceId before
tracking/binding, and freeze reuses it. GSO-K distinguishes this from a late call
merely labeled phase10/120. The [revision9 crosswalk](GA_IDENTITY_ROLE_CLOSURE_REV9.md)
contains129 records,128 scalar positions and186 recursive boundaries, with24
structural faults rejected. Only an existing1106 field was added to the proposed
completed-samples transport; no numeric allocation or reservation record was created.

The [observed association witness](GA_OBSERVED_ASSOCIATION_RECALL_REV1.md) connects
actual sparse/dense encoding and formation to graph learning and later current-cue
recall. Learned spread changes the second recalled acquisition with identical retained
episodes; removing the graph preserves direct matching. Actual source absence still
prevents cue-driven recall. Its earlier exact mutation report remains historical;
the current bounded-owner composition passes all six tests in the regression below.

The [actual-use retention witness](GA_ACTUAL_USE_RETENTION_RESOLUTION_REV1.md) and
[actual context carriage](GA_ACTUAL_CONTEXT_CARRIAGE_REV1.md) remain in force. The
discriminating earlier focal endpoint has actual downstream use and significance;
the new competitor is unconsumed. Stage labels now come from retained observed panel
companions. Supplied-use and focal-last arrangements remain named controls.

## Verification

- GA_BOUNDED_PERCEPTION_REGRESSION_REV2.json:149 tests,22 files, PASS, including
  current bounded-owner learning compositions and source reservation ordering.
- BOUNDED_PERCEPTION_SOURCE_REVIEW_REV2.json:11 exact source substitutions detected,
  covering ambiguity, namespace conflation, both file ceilings, candidate aliasing,
  skipped owner commits, unsampled panel context and late experience reservation.
- GA_IDENTITY_ROLE_REVIEW_REV9.json:24 symbolic structural faults rejected.
- TypeScript no-emit, reference import boundary and whitespace checks PASS.
- The earlier1679-test active-only full regression remains a prior-source checkpoint;
  the149-test report is the current affected-source run, not a full-suite claim.

## Remaining public gates

These are local owner candidates and source transactions. They do not qualify public
canonical window admission, registered paths/accessors, actual producer/PRJ joins,
runtime occurrence authentication, complete batch publication or save/restore.
Downstream registration, genuine delayed concern producer/delivery, exact model
packaging, public rollback/prefix proof and corpus promotion remain OPEN. Current
TaskConcern carriage does not add a persistent mood family. No owner ruling is
pending; continue through those gates under the decision policy.
