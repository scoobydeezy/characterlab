# Identity-role closure with visual carriers

2026-09-13. Symbolic crosswalk; no allocation or public admission claim.

Revision2 adds the16 visual carrier records to the prior66. RetainedEncodingUnit now
recurses into a declared shape instead of ending at a pending external boundary.
CurrentDetectionId214, CurrentEventDetectionId215, track217, event219, role223,
binding224, causal-role240, AttentionUnitKey530 and SelectedEvidenceView534 retain
their existing record-role authority. They do not become typed-ID namespaces.

All revision1 identity and domain dispositions remain: Character1002 requires actual
character qualification; GoalReferent1002 requires the separately proposed exact goal
predicate; Observer1000 is not Character; acquisition/result/recollection/signal
families remain symbolic. Collection elements use fixed compiler checks, not scalar
roles attached to their containers. Nested selected evidence never grants access to
the unselected source merely because both schemas are known.

The crosswalk proves field classification for82 draft records. Source authentication,
same-subject joins, selector capability ownership, state map keys and exact public
compiler enforcement remain open. The old role packet and its review remain historical.
