# Attention memory finite registration shapes

2026-09-12. Draft successor declaration shapes, not whole-contract acceptance.
Eight proposed records bind ingress, output counts/identity rules, allocation patterns,
read gates, committed definitions, stage authority, batch membership and the narrow
concern terminal. The accompanying22 rows instantiate the local admission/output
choices. Existing attention records538/539/541 remain unchanged.

The stage shape uses existing schema references254, state patterns149 and required
projection266. SubjectProjection is present exactly when IDN is declared; its ObserverId
position must match the required input scalar. MutationAuthority is present exactly
when write paths are nonempty. Writable paths never imply permission to skip projection,
expected-old reads, individual ownership validation or the common-B0 barrier.

Output identity and allocation are separate. A frozen experience consumes its earlier
reservation. The selection audit is fresh; its selected envelope copies the source
identity. Tracking uses the existing detection-keyed result identity. Pure transports
have no new occurrence. Exactly one finite allocation-pattern choice, or none, governs
each stage's shared-runtime calls. Role validity alone proves none of these relationships.

Ingress parent sets are alternatives for one direct parent, not a requirement that
every alternative executed. Encoding/recall joins separately require their one actual
completed delivery parent. Event-type membership does not replace exact expected-event
and payload authentication. Delivery retains exact future-original target binding.

ReadAccess has an explicit AvailableCue gate for rank's three memory leaves. Its IDN
projection is unconditional. Outputs have exact bounded count rules for detections,
claims, nonempty retention and actual winners. Phase140 batch members have no output
or runtime allocation, and remain terminal under the sibling correction.

The two new abstract input unions now have proposed tag/payload layouts: concern
versus no-concern delivery, and positive versus empty cue source. Exactly one payload
is present for its tag; the other is absent and unknown tags reject. Numeric tag values
and canonical field positions remain part of whole symbolic closure and later allocation.

The concern terminal declaration references the accepted workspace417, ordinary
appraisal272 and concern418 descriptors, adding an explicit outer stop-after-concern
and generated-delivery event binding. It does not edit inherited descriptors or make
the current whole-choice execution adapter configurable by omission.

Eight deliberately corrupted packets reject, including duplicated selected identity,
experience reallocation, missing projection, unconditional unavailable-cue reads and
ambiguous union tags. These are structural declaration checks, not production registry
compilation, actual admission or runtime qualification.

Remaining: exact per-stage definition bindings, finite calibration/model cohort,
inherited descriptor manifest, fixed symbolic members and complete schema/role/union
positions. Whole symbolic review precedes permanent allocation; no public implementation
is authorized by this partial packet.
