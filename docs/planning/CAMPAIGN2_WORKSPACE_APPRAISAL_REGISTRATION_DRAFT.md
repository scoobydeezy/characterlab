# Workspace/appraisal source and registration closure

Status: DRAFT; no shape acceptance, permanent allocation or implementation.
This specifies previously missing substrate surfaces for task-workspace/0.1-draft,
task-appraisal/0.1-draft and task-concern/0.1-draft. The transformation drafts retain
their mathematics. The following symbolic members/records remain unallocated.

## Original source and successor ordered input

Add a distinct cognitive ordered-input profile. It retains the canonical five-position
entry [DueAt, Phase, EventTypeId, Payload, Dependencies]. A DeliberationOpportunity
entry has positive signed DueAt, phase40, event/deliberation-opportunity, the exact
two-field ObserverId/AgendaDefinitionId record and empty-list Dependencies.
It supplies no state, psychological conclusion, option or ordinal.

At most one such original opportunity per instant is admitted for the sole holder.
The agenda must resolve to the profile's one TaskWorkspaceDefinition. ObserverId has
namespace1000; IDN qualification remains an executed projection, not an authored
CharacterId shortcut. Unknown agenda, malformed roles or another input schema reject
before any task/prediction access.

The first successor admits exactly the retained probe opportunity or the new cognitive
opportunity as its original source at an instant, mutually exclusively. It admits no
authored304 facts. This reconciles the initial overlap proposal with the later exact
stage profile and work accounting; predecessor adapters and their contracts remain
unchanged. A carried read generated by an earlier probe may coexist with cognition,
but is not a second original or a same-instant prediction writer.

All original EventIds and EventSequences allocate in manifest order, irrespective
of phase. Model-owned deadlines allocate after the entire original manifest, in
canonical task-key order. The original compiler's private branded schedule must
authenticate the new root. Return-value mutation cannot alter that authority.
Generated cognitive records, clocks, decision records and attempts are not original
input kinds. Restore authenticates their actual full-prefix derivations.

## Workspace source registration

New symbolic TaskWorkspaceSourceRegistration fields, in order:

1. ExecutingSeamId
2. ExecutingSeamVersion
3. InputRecordSchema (254)
4. ConsumerEventTypeId
5. AgendaDefinitionId
6. ReadDomain (set of149)
7. OutputDefinitions (set of277)
8. WriteCapability (273 NoStateWrites)
9. SubjectRequirements (set of266)
10. TaskRequirements (set of TaskWorkspaceTaskRequirement)
11. PredictionRequirements (set of existing363)

This is a direct original-source registration. It does not invent an InputOnly
variant in275. Its source gate consumes the compiler-authenticated opportunity.

TaskWorkspaceTaskRequirement fields: SubjectAccessor; TaskSpecIds; TargetStatePathTemplate;
OutputAccessor. Although the shape resembles374, this requirement owns a different
operation: finite context inspection, including absent/dormant/terminal tasks, without
the measurement dispatch's active-target or140 collision semantics. Do not reinterpret
TaskMeasurementTargetRequirement/374 or its accepted version.

The singleton266 selects ObserverId from input field1, targets IDN268/1, extracts
CharacterId from267/1, requires the existing character qualifier and names the existing
ResolvedCharacterSubject accessor. Its exact declaration must match the admitted
payload shape. No optional caller subject or guessed holder is permitted.

The task requirement names the complete declared task-spec set and root373's existing
Commitments field. Resolve finite keys from the derived holder, then inspect them
only when task access is enabled. Its new exact accessor is
accessor/workspace-task-status. TaskSpecIds is a collection, not a scalar265 role.

Reuse363's character/definition-to-whole-prior-or-absence operation for the prediction
requirement. Bind the same subject accessor, the agenda's one prediction definition,
root362/1 and its exact prior accessor. This does not reuse an old readout's producer
authority: it is a separately admitted workspace read, with its own actual-read
records and declaration. No prediction read occurs without the selected-task,
capacity and access conditions in the transformation draft.

The maximal registered ReadDomain is the roster, commitment and prediction patterns.
Controls determine actual reads, not a fabricated fixed log. Construction may retain
this superset while an execution reads none of one family. No extra root is admitted.

## Generated appraisal and concern

TaskAppraisalTransition uses an ordinary272 V04 registration:

* input274: TaskWorkspace schema,275 RegisteredTransitionProducer naming
  TaskWorkspaceTransition, RequiredSourceRelation=ImmediateGeneratedChild;
* ReadDomain={},273 NoStateWrites;
* one277 TaskAppraisal output, Multiplicity=ExactlyOne;
* ingress276 event/task-appraisal, same DueAt, phase50, exact parent output payload,
  one child for that output.

Appraisal's immutable task-spec projection accepts only the exact selected refs in
its authenticated workspace. It provides no general registry iterator and does not
claim a new character-state read. Holder, task order and criterion coverage cannot
be changed while appraising the source.

TaskConcernTransition consumes TaskAppraisal through the same V04 producer/relation
rules, at50 with strictly later allocated sequence and actual parent. Its output is
exactly one TaskConcern. A new symbolic TaskConcernRegistration wraps:

    BaseRegistration:272
    ConcernDefinitionId:DefinitionId/1027

The latter resolves to the single TaskConcernDefinition (Gain, Enabled). Its exact
schema/version/kind is checked at construction; it is not an arbitrary parameter
name. Source/calibration controls remain instrumentation, not new psychological fields.

The generated motive-context consumer at60 is separately declared by the plan/options
draft. Until that registration/output closure is accepted, the concern chain is not
activation-ready. No dummy terminal handler may fill the dependency.

## One source of occurrence allocation

Register TaskWorkspace, TaskAppraisal and TaskConcern output schemas in279/3, each
with278 pointing to its sole field1 occurrence role. Each output has a distinct
symbolic occurrence namespace, allocated only after the shape gate. Each valid
branch allocates one occurrence, including empty workspace or disabled concern.

277 remains only OutputRecordSchema and Multiplicity. No copied identity rule,
secondary forecast identity, task-item ordinal or private runtime allocator is added.
The existing parent event/source-record/trace160 conventions bind causality. These
transitions have no LearningRouteId and no persistent writes. Their records do not
become CharacterLearningEvidence or AutomaticAdaptationInput.

## Roles and closed unions

Opportunity.ObserverId uses1000; its agenda uses1027 with exact registry-kind checking.
Workspace.CharacterId uses1002 plus the unchanged character qualifier. Task items
reuse371's RecordField roles; TaskSpecRef uses1027 and the exact task-spec domain.
WorkspaceForecast's Known member contains the unchanged361. Unavailable contains no
number. Assessment Below/Above contain strictly positive exact distances; Within and
Unknown contain none. Concern Known contains exact intensity including zero;
Unavailable contains no number. These union tags and fields are not yet allocated.

No union discriminator is a reason ID, semantic weight or runtime occurrence.
Nested output validation recurses through existing role constraints and additionally
checks exact source list/key/order coverage. A known-zero forecast must remain distinct
from all unavailable branches after serialization and restore.

## Timing and phase controls

Original40 cognition reads the committed earlier state. An earlier physical probe
can generate M1 and prediction application at140; a later40 cognitive opportunity
can read that retained prediction. The first profile excludes a coincident original
probe. This preserves the intended ordering without deciding ORD-001 or inventing a30
producer. Same-phase50 forwarding is a finite two-transition chain, not a recursive
appraisal loop.

The source/appraisal/concern stages each add one output and one child. The final
joined profile must include the60/70/80 and later work before deriving its exact
whole-instant maximum. Every event counts against the shared work ceiling.

## Required pre-acceptance checks

Prove actual original-source exclusion, complete singleton/role/reference closure,
empty versus known-zero unions, finite selection/read semantics, shared occurrence
rules, exact generated source relation, zero writes and whole-instant rollback.
Source/child/event mutations must reject before the owning semantic operation.
Canonical prefix restore must reject replayed historical workspaces and altered
original agenda/observer inputs. Historical profile rejection remains a positive
preservation control, not a reason to widen old RulesVersion behavior.

Still open: exact action/plan and motive/Reason closure; all remaining aggregate
schemas and role inventory; trace/persistence version binding; profile control set
and budget; adversarial shape review; then separate permanent allocation. This draft
does not close those gates by describing a connected diagram.
