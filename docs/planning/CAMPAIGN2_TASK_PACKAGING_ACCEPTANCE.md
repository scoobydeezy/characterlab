# Task lifecycle first profile — internal packaging shape acceptance

2026-09-09. Agent self-review, not an external verdict.

Accept the lifecycle-only first profile defined by packagingrev1 with review2 and
task-commitment/0.2-candidate's key-role correction. The current canonical declaration
review is campaign2-task-commitment-review-rev2/REVIEW_MANIFEST.json. The earlier
review directory remains history. Both construct the same normal model bytes;
revision2 adds the current shared-criterion validation and17-test source fingerprint.

    normal model digest
    74aa96b751fb92dfaf693e6249ecc0abfe35df3493dd13919ddb254e4ee3d45e

## Concrete reviewed closure

192 distinct model commitments cover three finite task specimens and six inherited
Boolean controls. Actual two-kind content and state-role compilation executes for
each. The complete qualified-character image equals the predecessor content bytes.
All retained registry rows except the two explicitly extended topology/admission
rows remain equal. Their old family/transition memberships and the occurrence map
remain preserved. Slot1, slot3 and parameter bytes are unchanged.

The new surface is seven schemas,13 registry rows/descriptors, ten RecordField
roles, zero StateMapKey roles, one prospective physical root, eleven logical families
and three routes. Both lifecycle output closures are empty. No output occurrence
namespace or new original input kind is admitted. The task kind never qualifies as
Character, and the full content image is not passed indiscriminately into REG.

The17 executed construction tests include real state-map grammar and opposite-kind
negative cases, the two rejected historical role mistakes, missing key roles,
seven spec-delegation positions and an independently well-formed second prediction
criterion. These are component construction witnesses, not public runtime results.

## Scope and numeric binding

The numeric binding is ../formal/TASK_COMMITMENT_NUMERIC_PROFILE.md. Original profile
input behavior is preserved; both private deadlines are allocated after original
inputs regardless of adoption. The derived probe-instant ceiling17 fits the retained
100 budget; execution must reproduce that accounting. Arbitrary authored overload
retains its existing whole-instant failure. No deadline work is exempted from counting.

The exact bundle/tuple from review2 is accepted for materialization freeze. Task plan,
workspace, appraisal, affect, motives and option drafts are absent from this model.
No dummy handlers or authored psychological intermediates may fill those edges.

## Disposition

Freeze the reviewed canonical source/model/control commitments and construction
authority. Then implement the exact lifecycle-only public factory/runtime and run
TC-A..L with source, state, stage, rollback, deadline and restore adversaries. The
bounded profile must qualify before any live task runtime result is authoritative.

TC-A..L remain FROZEN NOT PASSED. Existing prediction/earlier qualifications are
unchanged. PERSIST-I remains deferred, and Campaign2 remains OPEN. No user decision
is needed to perform the authorized implementation and adversarial qualification.
