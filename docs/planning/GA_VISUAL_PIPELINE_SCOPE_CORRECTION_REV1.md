# Visual carrier baseline scope correction

2026-09-13. LOCAL DISPOSITION. Correct-forward review of the new visual draft.

GA_VISUAL_PIPELINE_CARRIER_REV1's statement that the older delayed EncodingJoinInput
layout is superseded is too broad. The owner-approved immediate-encoding ruling
prohibits later consequences from determining an earlier acquisition. It permits
information genuinely available at encoding time. The earlier
ATTENTION_FEEDBACK_JOIN_BINDING_REV2 explicitly delivers an earlier TaskConcern
before the receiving encoding event. That is lawful prior input, not future leakage.

Treat the16-record visual inventory as the **immediate baseline carrier**. Its encoding
join has no feedback. It replaces the obsolete positive/source field sketches for
that baseline; it does not retire the earlier-concern encoding or retrieval variant.
The ten structural review faults protect this exact baseline shape only. In particular,
the feedback-field fault does not prove that all prior-concern encoding is invalid.

Keep the delivered prior-concern comparison, authentic two-parent binding, selected
preselection-count rule and disabled-feedback control. A distinct composed input can
wrap the baseline selected source plus the already narrow authenticated delivery.
Do not smuggle future goal qualification into it, make a prior concern a condition
for every acquisition, or introduce a new identity simply for this wrapper.

The original proposal/review bytes remain historical; this explicit scope correction
governs their interpretation. Source authentication and whole public shape remain OPEN.
