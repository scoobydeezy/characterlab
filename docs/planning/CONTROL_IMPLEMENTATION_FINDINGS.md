# CONTROL implementation findings — 2026-09-22

LOCAL DISPOSITION; counters1024/17 before verdict. No architectural ruling.

The first development integration incorrectly reused HABIT's typed raw-to-execution
chain for a two-reason contest. HABIT admits only one raw reason/nucleus. Four tests
failed with HABIT_CODEC list bound;15 rollback checks and admission checks did not
exercise that later loaded contest. CONTROL_DEVELOPMENT_TESTS_REV1.json preserves
the complete failure. control-development-rev1/ preserves the original contract,
allocation and implementation; campaign3-control-model-rev1/ preserves all seven
initial identities. No public qualification plan or public PASS used that cohort.

Correct forward: control-public0.2 retains1008..1016 and adds1017..1024 for the typed
successor chain, admitting two reasons and at most three draws. The old HABIT grammar
remains unchanged and is explicitly tested to reject the new contest. Candidate
identities were refrozen under version0.2 before public execution. Do not weaken an
inherited codec to make a composition pass: finite arity is part of the seam contract.

The other important boundary is inhibition versus unlearning. EraseHistory may look
successful while the goal is active; goal retirement exposes its missing learned
candidate. NoHistory is a separate comparator. LoadBlind suppresses despite the
competing card, and RetainedEqualsMaintained hides loss of active access. The baseline
preserves history, goal retention, active maintenance and candidate inhibition as
distinct quantities. A neutral card does not emit a motive or alter competence.

RO-C3-012/015/019/020 preserve the resource-law, habit, broader family and compositional
schema limits. VER-C3-CONTROL-001 is bounded; fatigue, learned control, rumination,
monitoring, reappraisal and cross-domain effort costs remain unqualified.
