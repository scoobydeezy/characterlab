# General Attention public corpus fixture

**FixtureVersion:** `fixture/general-attention/1.0.0`. Declared 2026-09-20 before
closing the comparison review. This binds the already declared recipes and frozen
inputs; it introduces no outcome-dependent source change.

**LOCAL DISPOSITION — no owner ruling required.** Use the accepted finite source,
explicit observer roles, deterministic ordering and existing memory/feedback seams.
The fixture may qualify those seams without choosing a psychologically final law.
Counters: **706 / 0**. Stage C competitors remain independent, historical shared,
historical hybrid, retired flat, role/spatial/disabled allocation, local access,
selection capacity, presentation decay, source availability and retention policies.

## Exact execution domain

The 43 model images in `campaign3-general-attention-model-rev2/FREEZE.json` commit
the content, definitions, roles, registrations, parameters, required S0 and complete
ordered originals. Use the seed in that freeze. Public entry is the data-only
`generalFactory.ts`; invented S0, extra originals, callbacks and unfrozen recipes
must reject. Initial memory and learned associations are empty. Authored goal/task
and physical state are initial conditions, not invented learned evidence.

Ordinary runs encode at instants 3–6, cue at 7–10 and settle the task deadline at 21.
The retained-credit calendar additionally executes real goal baseline, four perceived
trials, recall/attribution at 37, significance at 38, retention at 39, fresh pressure
at 40–41 and goal expiry at 100. The consequence-lane control exercises its separately
registered source/encoding/rank path. All final outputs, state, trace and save are
hashed. No random draws occur, so coupling is deterministic rather than statistical.

## Comparisons and observation rules

Preserve the exact comparisons in `GA_COHORT_SOURCE_RECONCILIATION_REV1.json` where
both endpoints are frozen. Add the four sparse/dense pairs declared in
`GA_FOOTPRINT_COHORT_2026_09_19.md` and the three age-only versus protected retention
pairs declared in `GA_RETAINED_CREDIT_COHORT_2026_09_19.md`. Keep each canonical
ComparisonCase and ordered model/run identities in the public results receipt.

For footprint pairs, align the focal Actor by observed role and controlled source
position, not opaque identity magnitude. Dense adds two nonfocal Participants;
priority selection K2 admits one with the Actor. They are irrelevant to the later
focal cue but remain positively evidenced Participants, never invented Incidental
or Cause roles. Focal geometry, role, timing, permission and later cue match. Report
exact focal strength, learned association matrix and later rank scores, including
equality or quantization loss. Do not require a winner change when only scores change.

For PHEN-MEM, compare initial rank scores with recall K0/K1/K2, then subsequent
scores and presentation histories. Score computation precedes K selection: the
K0 counterfactual can expose reinforcement of selected memories without conflating
winner count with score. Separate decay of the base term from associative pull.
The no-presentation-decay control supplies exact ties; inspect canonical tie order.
Memory contents, presentation history, graph and retention credit remain distinct.

For prior-concern feedback, use allocation-spatial as the matched disabled control.
Compare encoding-only, retrieval-only and both, and preserve Known-zero,
Unavailable, NoTask and DisabledFeedback branches even when numbers coincide.
Candidate A is an executable witness of the ratified edge, not the general Affect
representation or a final law. VER-C3-CONCERN-001/002 retain the A/B/C comparison.

For retention, compare actual final retained acquisitions and used/significant
children after the same perceived attribution. An indistinguishable protection
policy does not earn necessity. Perceived trial context is not proof of objective
causality; the independent hidden-delivery/false-attribution controls remain required.

## Qualification and preservation

Require exact frozen admission, source/subject/owner negatives, actual read-domain
and protocol-state separation, one canonical trace per event, all active-stage
rollback, complete-prefix replay and bounds derived from the composed topology.
The two replacement-only current baseline templates remain compiler/component
covered; actual current execution uses delivered-feedback replacements. The separate
consequence baseline templates execute. Do not call this all 67 templates executed.

Run the full active source suite, historical reference suite and production build.
Retain AT2-A..N and Campaign 2/EMB corpus evidence in their original public/component
scope. This is regression preservation, not a claim that all 21 corpus phenomena
pass. General Affect, Need, surprise, arbitrary modality/position/role inference,
unguided event segmentation and unrestricted long-horizon cognition stay outside
this fixture. Missing glyph/position recipes are explicit public exclusions with
component controls, not invented source interpretations.

North Star transfer: the attention/memory part of the §30 frightened or angry
character archetypes becomes experimentally traversable through limited selection,
history-dependent recall and real earlier character-state feedback. This does not
qualify the complete frightened/angry character or claim that TaskConcern is anger.

The next gate is a measured coverage verdict and intentional corpus promotion.
