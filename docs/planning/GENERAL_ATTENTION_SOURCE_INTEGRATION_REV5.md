# General attention source integration — revision 5

2026-09-13. Continues revision4 locally. GA remains OPEN; no owner ruling is pending.

The [panel/visual event component](../formal/PANEL_VISUAL_EVENT_COMPOSITION_COMPONENT.md)
now preserves independently present visual perception when panel context disappears.
It uses the same actual SEM event-file owner/counter: present panel/visual share a
detection/transition; missing panel ends trial context and a visual-only fallback gets
a fresh immediately ended file. It never creates a trial-context companion. Wrong
observer/time, replay and inconsistent shared detections reject before state publication.
[30 affected tests](PANEL_VISUAL_EVENT_COMPOSITION_TESTS_REV1.json) pass at component scope.

The source composition no longer accepts a retained target pointer from the source
loop. It binds the [focal child through recalled grouping](GA_RETAINED_FOCAL_BINDING_REV1.md)
whose After experience equals the designated actual consequence, before evaluation.
The earlier stroke can be designated and becomes the survivor without changing the
contrastive operands. End-position child is this profile's explicit target designation.

The [focal position-pair consumer](../formal/FOCAL_POSITION_PAIR_ATTRIBUTION_COMPONENT.md)
then enforces that the prebound trial actually is one of the computed strokes. Naming
a control trial returns Unavailable, while the actual fully consumed operands still
earn use. Overall contrastive support alone cannot credit an unrelated/control target.
[42 focused tests](GA_RETAINED_FOCAL_BINDING_TESTS_REV2.json) pass; [nine source mutations](GA_FOCAL_AND_VISUAL_BINDING_REVIEW_REV1.json)
detect visual suppression/counter duplication, invented trial context, mutable focus,
control-trial credit, last-row targeting and silently switching to the start child.

The [recollection reconciliation](GA_RECOLLECTION_CARRIER_REV3.md) corrects the old
visual-only draft's selection-based episode addresses and two-winner bound. Actual
recollection uses acquisition identities and surviving content. Body recall retains
its accepted whole-acquisition behavior, including non-cued co-acquired groups; it
does not gain a graph, numeric score or group-local recall rule. Exact public record
and output registration closure remains work, not a qualified implementation.

The body-only source/SEM path and its41-check receipt remain as recorded in revision4.
Combined panel/visual/body source reservation, actual positional/selected evidence,
PRJ admission, whole public carrier/owner registration, historical-result join and
canonical persistence remain OPEN. These component integrations do not authorize
numeric allocation or corpus promotion. Current source TypeScript checks pass.
