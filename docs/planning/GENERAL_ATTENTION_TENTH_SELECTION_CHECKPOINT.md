# Actual competing acquisition and canonical visual completion

2026-09-13. LOCAL DISPOSITION — no owner ruling required. General Attention OPEN.

Self-review found that the final retention competitor used literal fixture bytes
and a fixed acquisition ordinal. It now comes from an actual tenth visual source,
SEM tracking and observed role projection, selection, positive encoding and shared
allocation. Hiding its position or role prevents formation; no fallback bytes exist.
The later qualified-use comparator remains supplied, explicitly not authenticated
public downstream use. Source world/channel/PRJ admission remains component scope.

The [ten-sweep successor](../formal/TEN_SWEEP_MARKER_COMPONENT.md) preserves old
eight/nine bounds, limits each sweep to three detections, rejects the eleventh,
and reconstructs every prefix with abort/retry controls. Four injected faults are
detected. The proposed [state revision2](GA_STATE_ROOT_KEY_INVENTORY_REV2.md) raises
the possible graph-key bound from27 to30 only; its11 structural controls pass.

The [canonical visual selector](../formal/CANONICAL_VISUAL_SELECTION_COMPONENT.md)
uses existing532/534 and1143. Audit and view reference one actual allocation and
are detached from each other and the selected capability. Positive, ineligible and
empty completion produce one audit; cue-only sensing does not. Each visual formation
source in the fixture references that actual audit rather than its acquisition ID.
Four injected faults reject extra view allocation, missing empty completion, shared
mutable output aliases and acquisition/selection substitution. Public registration
and protocol-hook qualification are still open, as is the proposed body audit.

The [requested-source carrier](GA_REQUESTED_SOURCE_CARRIER_REV1.md) supplies five
missing request/product records, including unavailable panel output. Nine structural
faults reject. CurrentEventDetectionId is existing215 with1113, not event-feature1102;
that unreviewed draft typo was corrected before review. The [joined role crosswalk
revision5](GA_IDENTITY_ROLE_CLOSURE_REV5.md) now covers116 records,112 scalar identity
positions and162 recursive boundaries;16 structural faults reject. These are symbolic
declarations, not public codecs or numeric allocation.

[Current affected regression](GA_TENTH_SELECTION_REGRESSION_REV1.json):79 tests pass
across10 files. Type checking passes. Intermediate failed reports remain development
history: tenth-source revision1 retained one obsolete1000 assertion; canonical
selector revision1 incorrectly removed required claims while preserving exact roles;
integration revision2 incorrectly assumed lexical order for a source-membership check.
All are corrected in the later passing reports; no production validator was weakened.

Continue the world/channel definitions and full public stage/owner/PRJ/output graph.
The older22-stage inventory remains a historical hypothesis: its selection-as-
acquisition identity, two-winner bound, old memory roots and mandatory delayed
encoding dependency cannot be imported into the new model. Actual goal lifecycle,
body selection, authenticated use/qualification joins, canonical pending queue,
whole-prefix proof and corpus promotion remain open. No owner ruling is pending.
