# Bounded public attention qualification

2026-09-11. **BOUNDED PUBLIC ATTN QUALIFIED; AT2-A..N PASS in the scopes below.** Primary-agent adversarial
self-review under the user's autonomous-work authorization; no external verdict.
The [machine qualification receipt](ATTENTION_PUBLIC_QUALIFICATION_REV1.json) verifies
1,160 active tests (full 1,159 plus one concurrency test), 328 historical tests,
eleven runtime fault substitutions and all 453 prior EMB fingerprints. TypeScript
and the reference-boundary check pass. The final full run retains every original
test timeout; the preceding timeout-only failure receipt remains historical.

This implements `attention-public-integration/0.1-candidate` and
`attention-public-declarations/0.1-candidate`, with the unchanged
`attention-selection-component/0.1-candidate`. The accepted symbolic packet and
permanent allocation remain unchanged. This checkpoint concerns a finite public
profile; it does not close general ATTN-001 or promote a corpus member.

## Implemented boundary

The data-only factory compiles the exact 32 committed models, including the actual
three-object governed content manifest, scalar/map-key roles, existing perception
state ownership and committed occurrence table. It accepts one positive-time scene
original. Private scheduler-issued child bindings admit the ten registered stages.
Actual SEM tracking, binding, experience reservation/freeze and role derivation
produce the selector's observer-safe input. No psychological intermediate is authored.

The positive branch executes nine events, at most 15 outputs and 18 shared runtime
slots. The all-denied branch executes four events/four outputs/seven slots without
an experience reservation. K0 still retains the positive source history. Selection
and consumption occur at phase 40 after role derivation at 15. They write no state.
Only the producer's Track stage writes the four existing perception leaves in
roots 241/242 under `authority/perception`.

The actual selected-only component holds copied canonical binding/claim bytes in a
revocable private capability. The processing receipt records the complete unique
selected read set. It has no observation, parent-audit, experience or archive resolver.
Transaction orchestration infrastructure necessarily owns the truth-side producer;
this is not a claim that every adapter closure is an epistemically restricted actor.
The selector receives safe SEM; the consumer component receives only the selected
capability. Observer identity suffices for this accepted diagnostic refinement;
future character-state consumers still require accepted PRJ/IDN.

Persistence uses the existing canonical save 132 and trace 160. Restore reconstructs
the original committed run and compares every byte of a complete prefix. There is no
new continuation format, historical alias or public internal-event admission API.

## Adversarial findings resolved

1. The earlier image builder and image review shared a wrong empty-list S0 assumption.
   Actual state restore rejected it. The [correct-forward correction](ATTENTION_INITIAL_STATE_CORRECTION.md)
   preserves all old bytes, leaves all 32 ModelIdentities unchanged, and recomputes
   224 specimen RunIdentities with the actual empty set of StateLeaf records. The
   [revision 2 cohort](campaign3-attention-model-rev2/FREEZE.json) is current. Old
   list-S0 run commitments remain historical and inadmissible, without aliasing.
2. Allocation counts alone do not prove output freshness. Runtime validation now
   extracts identity through the committed occurrence table and checks actual slots,
   including nested truth bindings/detections and the separately reserved experience.
   Reusing a well-typed selection ordinal fails and rolls back.
3. A valid parent edge alone does not prove child carriage. The independent trace
   validator reconstructs each producer's child payload from completed outputs and
   admitted input, including selected binding/claim copies. It also checks exact
   registered phase, event type and input schema. Reordered carriage and moving
   selection into recognition phase 20 are detected.
4. An apparently plausible receipt can omit a real read. The live consumer enforces
   complete, unique selected reads. Missing/repeated reads and shared-observation
   lookup faults reject. Selection and consumer state-clear faults also reject.
5. A suspected concurrent-reset defect did not survive inspection and substitution.
   The existing scheduler's `getPendingQueue()` rejects nonquiescent reads before
   private reset. Removing an experimental extra guard preserved both immediate and
   phase15 reentrant rejection. The redundant guard was removed; the regression test
   remains. This surviving substitution is not counted among the eleven detected
   faults, and no concurrency bug fix is claimed.

## Executed proof scope

The public oracle covers all 224 frozen model/scene combinations: 217 successes and
seven work8 rejections. It checks exact RunIdentity, branch/output bounds, each row's
roles, exclusions, evidence-reference count and rational priority, and the selected
result for role-priority, equal-priority and unlimited models. Every denied-port role
substitution preserves observer-side output bytes (27 comparisons).

All 434 successful zero/full-prefix restores are compared byte for byte. The boundary
matrix injects 58 failures across every stage on both branches and final commit,
checking state, queue, trace, outputs and allocators. Both branch traces supply 247
independent field-corruption controls. Canonically edited save fields, incomplete
prefixes, changed originals, duplicate originals, forged handles and executable
input properties reject. Old Campaign2/EMB factories and codecs reject this profile.

[Eleven current-source substitutions](ATTENTION_RUNTIME_MUTANTS_REV1.json) detect
visibility/capacity bypass, reused occurrence slots, omitted/repeated/indirect reads,
selector/consumer writes, child-carriage corruption, phase reassignment and issued
event mismatch. The earlier six current-source component faults remain preserved.

| Vector | Evidence and deliberate public/component limit |
|---|---|
| AT2-A | Actual denied-port hidden-role substitutions preserve safe history. |
| AT2-B | All models/scenes, K0/1/2, empty branch, equal/unlimited controls and work8/9. |
| AT2-C | Actual observed role swaps and canonical ties change the selection oracle. |
| AT2-D | Exact fixed role priorities across changes in the admitted unit pool; no encoding-strength claim. |
| AT2-E | Plural units share a real supporting observation; duplicate semantic bindings and read requests reject in the real component. Duplicate originals reject publicly. |
| AT2-F | Missing and unsupported roles execute publicly. Multiple-role exclusion is component-positive; the public source admits one role per port. |
| AT2-G | Public ties and original/restore binding; component foreign observer/event/instant, forged/stale claims and strict-priority renaming. Arbitrary renaming is outside the fixed public vocabulary. |
| AT2-H | Public complete selected receipts; live consumer omission/repetition/observation faults; actual component rejects parent-audit/experience/archive lookup. |
| AT2-I | K-only source preservation and no-write faults. Producer tracking ownership is separate. |
| AT2-J | Actual 15→40→40 positive order, 10→40→40 empty order and rejected phase20 reassignment. Legacy recognition remains unchanged. |
| AT2-K | Exact issued event, independent parent/output carriage, allocated-slot freshness and no wrapper identity. |
| AT2-L | Every successful specimen prefix, every new stage rollback, trace/save corruption and empty-versus-K0 distinction. |
| AT2-M | Actual old factory/codec rejection, full active regression and preservation of 453 prior EMB fingerprints. |
| AT2-N | Selection exists without learning state. No encoding, retrieval, whole ATTN or corpus verdict is inferred. |

The initial unconstrained regression run is retained with 17 timeout failures in
older suites. It is not passing evidence. The final regression uses one worker with
the original test timeouts; source and assertion semantics are not weakened.
The one additional concurrency test is recorded separately from that full run.
`npm run test:reference` encountered an esbuild config-loading sandbox error before
tests started. The equivalent programmatic Vitest run uses the same node environment
and reference test glob with `config:false`; no reference source/config was changed.

## Scope after this checkpoint

ATTENTION intake has partial evidence for active selection. Its encoding footprint,
two matched encodings and later learning/retrieval probe remain blocked on their
own accepted seams. The whole intake has no PASS and is not a corpus member.
`corpus/0.27.0` remains unchanged at
`3cb09115d90485d5975968ffca419a4960a398326b6b6339a5b1e8e8fdcec276`.

Deliberately deferred: MEC-005 residual allocation; MEC-007/CTL-004 independent
multiplicative salience and shared-budget alternatives; irrelevant-footprint effects
on encoding strength; later accessibility/retrieval/learning; affect feedback;
character-state subject attachment; arbitrary model authoring, modalities, source
cardinality and multiple-role public profiles. RET-001/002 remain retired. No
historical mechanism is imported, reduced or silently discarded.

The next research checkpoint is an encoding/footprint expressibility inventory:
inspect accepted episodic/association/access contracts and historical controls,
identify the minimum permitted strength/readout seam, then freeze its symbolic
shape and comparisons before allocation or implementation. A hard top-K selector
does not establish a continuous salience law. Campaign 3 remains open.
