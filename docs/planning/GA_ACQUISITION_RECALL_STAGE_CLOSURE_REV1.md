# Acquisition and recall stage closure

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic registration
proposal, not whole public acceptance or numeric allocation input.

This closes the immediate acquisition and read-only recall part of source dispatch.
Three missing transports are explicit: VisualAcquisitionEvidenceInput wraps the
actual positive candidate; EventRecallInput wraps the actual cue; and the shared
GeneralFormationOwnerInput wraps the actual AcquisitionFormationEvidence. Each has
direct ObserverId field1 for required PRJ/IDN and exact nested-holder equality.
Sharing an owner input shape does not share mutation authority or admit body evidence
to event-only graph/presentation owners.

The21 registrations preserve distinct current40 and consequence130 selection,
encoding, cue, rank and recollection stages. Both lanes reach phase130 evidence
producers. Each completed selector owns exactly one1143, including empty selection.
Protocol source enrollment uses that actual completed audit independently of whether
formation succeeds. The evidence producer alone allocates one acquisition on the
positive branch, or zero for empty/zero/unavailable content. Its140 owners reuse it.

Visual encoding consumes the selected-only carrier and produces both its evaluation
and positive projection from that one calculation. It never reloads source windows
or derives replacement causal-role claims. Cue admission remains independent of
selection. Rank requires PRJ even when unavailable, but an unavailable cue performs
no memory reads. Available event rank reads exactly the subject's episode, graph
and presentation owners; body rank reads only the episodic owner. Neither writes.

One recollection publisher per rank result allocates only actual winner occurrences;
zero winners allocate none. This is an evidence-output stage, not presentation or
qualified use. Subsequent consumer/presentation/use registrations remain separate.

Event formation schedules ordinary memory, association and initial presentation
owners; body formation schedules only ordinary memory. All140 obligations are
emitted by the130 producer before the barrier. They have no output, allocation or
children. Ordinary memory folds its entire formation/retention/use/significance
batch against common B0. Other owners consume validated same-batch descriptors where
required, never sibling writes. Protocol reconciliation is a private before-commit
hook over that validated owner result and has no character read or learning route.

With both modalities selected and cued, this slice adds at most15 scheduled events
to the eight-event source chain: body formation3, visual formation6, body recall3,
event recall3. This23-event subtotal excludes goal lifecycle/assessment, retained
attribution, use/significance/retention, feedback production/delivery and other
same-instant originals. It is not a whole-model work ceiling. Existing32-source and
separate recall bounds remain independent of this event count.

Exact public patterns/accessors, definition references, expected-event/parent binding,
output occurrence descriptors, all omitted consumer branches and complete barrier
composition still need the final whole packet. The three new wrappers must enter
the next role crosswalk before numeric review. No existing typed identities, EVID
consequence path, model bytes or corpus commitment change.
