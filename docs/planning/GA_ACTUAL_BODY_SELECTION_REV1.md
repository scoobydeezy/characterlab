# Actual body selection before acquisition

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Component correction.

The main four-trial source fixture previously decoded actual body observations but
formed its eight body acquisitions directly, using a fixture source key derived
from acquisition identity. That was useful earlier component evidence, but did not
implement the proposed actual1143 audit-to-acquisition relationship. The zero-capacity
cue selector likewise used a separately supplied fixture source identifier.

The [body selection occurrence adapter](../formal/BODY_SELECTION_OCCURRENCE_COMPONENT.md)
now executes the actual local-reserve selector, allocates one1143 per completed
selection and binds its one-use selected capability to that same identity. Body
formation derives retained intervals only from the consumed selected sample bytes.
The current live consequence remains independently available for lawful goal
assessment; selection does not become the concern or cue gate.

All nine body selections now have actual audits: eight positive before/after
acquisitions plus one capacity-zero cue selection. Together with the nine visual
audits they account for the existing18 qualified sources. The empty body selection
enrolls without a new acquisition. No fixture-selection fallback remains in the
main composition, and acquisition identities remain separately allocated later.

The first development test fixture violated the accepted three-reserve mapping by
omitting mappings or conflating two distinct reserves. Preserve its failed report.
The corrected fixture keeps all three independent mappings and obtains two views
of A through two declared A channels. No source validator or physical contract was
weakened to support the control.

ACTUAL_BODY_SELECTION_COMPOSITION_TESTS_REV1.json passes34 tests:29 main-composition
and five adapter controls. BODY_SELECTION_OCCURRENCE_REVIEW_REV1.json detects four
faults: skipping the empty audit occurrence, converting a missing sample to absence,
using experience identity for the view and reusing acquisition identity as its source.
TypeScript no-emit passes. The earlier149-test bounded-perception run remains a
prior-source affected regression; current composition changes have this focused run.

The audit has the proposed typed data shape, but its new canonical record is still
unallocated. This does not pass public producer/PRJ admission, protocol-hook wiring,
owner batch registration, canonical body codecs or persistence. Symbolic acquisition
and recall registrations remain the proposed21-stage slice, and whole GA remains OPEN.
