# BODY/MULTISOURCE expressibility audit

**Disposition:** LOCAL DISPOSITION; Stage A source gap identified, Stage C numerical
comparison executed. Public PHEN-MULTI-001 remains PARTIAL, with the common-fact
producer/comparison path BLOCKED pending its successor contract. GA remains complete.
**Counters:** highest allocated **706**; allocated since last verdict/member **0**.
**Owner decision pending:** no. Behavioral differences are not an architectural blocker.

## Existing path and precise gap

The bounded EMB receiving experiment already has actual body and task sources,
shared options, independent grounds, duplicate body descriptions, real dice,
expression and execution. Rebuilding it would not advance the corpus obligation.

| Surface | Actual authority/code | Consequence for the next experiment |
|---|---|---|
| Task commitment base | `cognitiveTransforms.rawSignalOutput`: role1, adopted pressure, empty observation basis | It cannot be relabelled as a second description of body evidence. |
| Task situational source | Same function: role2, concern derived from an admitted diagnostic forecast's observation basis | A common body observation needs its own accepted task-evidence interpretation. |
| Frozen receiving task branch | `receivingRuntime.ts`, workspace prediction accessor throws `forecast disabled` | Existing thirteen public models cannot produce that shared fact. Merely changing a raw basis would bypass the producer. |
| Body source | `receivingTransforms.rawOutput`: role1, actual positive pressure, singleton atom referencing that pressure's actual461 observation | Two body instructions can duplicate a fact within one body ground; this is already qualified. |
| Grouping/coverage | `reasonOutput`: candidate/ground, then role/sign; `coveredRole` uses union-max overlap | Shared evidence across two genuine grounds is not deduplicated. This preserves independent motives and leaves the cross-family research question open. |
| Evidence keys | Existing399 atom containing237 CharacterEvidenceRef | Safe shared occurrence identity is available as a representation. A typed occurrence alone does not authenticate a producer or prove statistical independence. |
| Numeric units | Diagnostic task forecast has the closed0..10 diagnostic domain; body461 has its declared reserve unit and interval | No body-to-diagnostic relabelling or invented unit conversion is allowed. |
| Learned body history | GA now supplies safe retained body evidence and recall | It is an available future source, not automatically admitted into the frozen EMB task branch. |

MEC-012 semantic grounds, MEC-013 source roles, MEC-014 one final bounded transform,
EXP-009 independent motives and EXP-014 collective redundancy remain PORT/CORPUS
obligations. This pass adds no reduction and touches no reference implementation.

## Executed comparison

`multisource-coverage-comparison/0.1-candidate` is a pure, separately named comparison
contract. `MULTISOURCE_COMPARISON_REV1.json` records **13 fixtures, 52 coverage-law
runs, 10 description-dice controls and 22 exact reference partition checks**.
It uses the actual existing receiving coverage, reason-dice and analytical choice
kernels. Synthetic component inputs are expressly not public source evidence.
Each option is compared with a fixed independent d8 opponent; probabilities are
enumerated exactly, with no RNG sampling. This is not a whole Decision qualification.

| Witness | Reference result | Competing result | Interpretation |
|---|---|---|---|
| Duplicate body description | One nucleus; probability2/3 | DescriptionDice89/96 | One die per description overcounts this duplicate. |
| Collective {a},{b},{a,b} | Third contribution0; bounded total5/9 | Pairwise third1/8; total11/19 | Pairwise maximum misses collective coverage. Both yield2/3 under this die calibration, so probability alone hides the error. |
| Shared versus disjoint bases across grounds | Two distinct grounds;89/96 in both | FamilyNormalized221/256 shared,89/96 disjoint | Cross-family normalization is a substantive hypothesis, not a serialization fix. |
| Shared fact in different roles | Body Base and task Situation remain separate | FamilyNormalized changes89/96 to349/384 | Overlap alone does not settle how different psychological roles should interact. |
| Task modifier without task Base | Body probability3/5, unchanged when modifier is removed | FamilyNormalized1/2 with modifier,3/5 without | Role-blind normalization introduces an indirect effect from a source that cannot form its own reason. It is not selected for the first public baseline. |

The normalized law preserves distinct grounds in representation but can still erase
part of their motivation numerically. The aggregate reference is the conservative
first baseline; this does not establish it as a universal cross-family law. Retain
the exact failed comparator and require an explicit active-ground/role/sign scope
for any proposed successor. Do not silently repair it in place.

No owner escalation follows: existing architecture already preserves motive/role
distinctions and supplies the narrower baseline. Adopting a new cross-domain exchange
rate or causal edge as canonical would be a different decision.

## Minimum next source contract

See `MULTISOURCE_COMMON_EVIDENCE_SOURCE_DRAFT.md`. The missing seam is an
authenticated task interpretation of the same permitted body fact, with an explicit
task criterion and unchanged independent commitment Base. It is not a new Need state,
a second private dice system, or an unrestricted observation-to-reason adapter.
Both shared and independently sourced facts must be produced, not authored as
coverage maps. A genuine independent-fact control must name independent source
operands; different opaque IDs or resampling the same hidden quantity are insufficient.

The one-next-gate is **complete the bounded common-evidence producer shape and
comparison admission before any permanent allocation or public implementation**.
The numerical experiment identifies the distinctions that shape must preserve.

## North Star transfer and validation

The target is a character with a bodily concern and an adopted commitment addressing
one action: shared knowledge must remain visible without deleting either legitimate
motivation. This serves Brief12.2/12.5 and the North Star's separately traceable motives,
correlation handling and reason compilation. It does not qualify a complete embodied
person, general Need ownership or cross-family normalization.

New comparison and preserved receiving transformation tests pass:21 tests/two files;
build and reference boundary pass. `MULTISOURCE_CHECKPOINT_2026_09_20.json` records
exact counts and fingerprints; `VER-C3-MULTI-001` records the scoped verdict.
The earlier2,171-test GA full suite is historical, not relabelled as a new full run.
