# Attention memory finite comparison recipes

2026-09-12. Exact draft parameter recipes, not canonical model commitments. The packet
names a baseline and matched interventions, with explicit per-stage definition-purpose
bindings. No model or run digest is computed, allocated or accepted here.

Baseline uses role-calibrated independent encoding, small budget floor1/5, association
scale100/rate1/no decay, activation scale100/beta1/2, presentation decay1/exponent1,
and at most two winners. The unlimited selector preserves three-unit sparse/dense
controls. Spatial comparison uses rectangle[0,3]×[0,7], focal1 and residual1/5. Source
workspace capacity2 and REG anchor0 on scale10 produce the earlier proposed below-target
case through the actual probe; changing the anchor to50 or100 supplies in-range or
above-range controls only through that same actual observation/prediction path.

Budget variants cover the four accepted laws at both floors. Independent and flat
floor variants are explicit null controls; their existence cannot prove sensitivity
to a parameter those laws ignore. Separate allocation, encoding-feedback, retrieval-
feedback, beta0, association decay, presentation decay, selector and winner-capacity
variants avoid attributing a combined intervention to one mechanism.

Feedback pairs preserve all source/schedule parameters. Source-unavailable and channel
variants deliberately change their named source intervention instead. Missing position
does not become zero; a denied port contributes no features. Need and surprise remain
Disabled in every recipe. No category, Cause/Incidental or capacity-narrowing claim is
earned by this bounded cohort.

Every local stage now has an exact set of definition purposes, with Spatial added to
evaluation only under spatial allocation. Inherited fixed SEM rules stay in the source
manifest rather than being disguised as new calibration DefinitionIds. Dynamic scene
references come only from admitted originals and must resolve against the exact
governed scene catalog. That catalog and matched input specimens remain unfinished.

The static review checks all recipe pairs for undeclared parameter changes, finite
bounds, complete stage binding coverage and feedback/source separation. Six deliberate
corruptions reject. This is not execution of the comparisons or proof that each recipe
has a discriminating specimen. Model identity, run coupling, scene content and full
inherited descriptor closure still belong to whole symbolic/model packaging gates.
