# Canonical protocol root: A accepted

2026-09-12. **ACCEPTED by supplied planning ruling.** Resolves
[placement decision](FORMATION_PROTOCOL_STATE_PLACEMENT_DECISION.md).

Formation governance is current authoritative runtime protocol state, not cognitive
memory, Knowledge, evidence, trace or presentation. It records distinct admission and
successful-formation facts; enrolled/no-success, enrolled/success and unenrolled must
remain distinct. No cognitive evaluator can read it as evidence or learned feedback.

A separately declared admission authority owns exact canonical paths. Authenticated
source admission occurs before positive eligibility, while the phase130 evidence
producer keeps WritableStateFamilies={}. Successful formation and any required loss
disposition derive from the ordinary-memory owner's authoritative result, under the
same atomic instant. Governance does not decide whether content survives.

Successful admission without positive formation commits enrolled membership. Actual
whole-instant failure commits neither enrollment nor new success history. No rows
are added for queries, rejected attempts or failed lookups. Source-success mappings
remain immutable through cognitive loss; the later disposition is protocol metadata
updated only from the governed memory-owner result.

Persist through normal AuthoritativeState, without changing SchedulerSave132, using
ContinuingRunInputs as a mutable bag, or reconstructing authority from diagnostic
logs. Fixed N belongs to immutable model/run commitment. No save/load capacity reset.
Exclude the new root from psychological state classifiers; extend explicit protocol
classification where needed rather than labeling it episodic memory.

Given the same committed profile, canonical protocol root and cognitive/world state,
protocol decisions must not depend on incidental diagnostics, output retention or
private caches. This does not waive existing whole-save archive integrity checks or
authorize arbitrary deletion from frozen saves. Preserve authoritative log/replay B
as a future alternative when protocol chronology/compaction itself matters.
