# LEARN readiness — 2026-09-21

LOCAL DISPOSITION under the Campaign3 escalation policy. PHEN-LEARN-001 requires
four scalar censored-bound contrasts, not binary trial censoring. The existing
observer-evidence/SEM/consequence130/sole-belief-owner140/later50 pattern supplies
the accepted topology. No new architectural box, truth read by learning, Need
ownership law, covariance or current-lane application is required.

Consulted MEC-001/002/003/006, SUB-001/002/008/009/011, EXP-002/005, CTL-002 and
RET-003..006 in REFERENCE_MECHANISM_LEDGER. Preserve the weighted mean/prediction
error identity; informative-bound gating is a named candidate, not Bayesian truth.
Historical lattice arithmetic remains a separately committed comparator. No imports
from reference into src and no historical source edits. No surprise consumer added.

The historical CaseD starts at mu=0,tau=0. Its first bound1/10 sets mu=1/10;
the next five are uninformative by strict equality. The corpus's repeated-bound
clause is qualified over that frozen domain, not every prior: with established
mu=1/20,tau=50, repeated bounds can remain strictly above the mean and continue
adding precision. Preserve this limitation, and the full-point precision assigned
to an accepted bound, as unresolved scalar inference approximations.

Acquire the exact established priors through25 public point observations at
precision2 from empty S0. This avoids authored learned state while reaching the
required precision50 exactly. Compare Gated, UnconditionalPrecision (retired
negative control), and PointOnly (safe but ignores informative censoring), under
exact rationals and a separately named millionth lattice. No decay.

First freeze contract/allocation, then components, model images and public plan;
run exact four-case comparisons, hidden-overflow/missingness, later probes,
rollback and every-prefix continuation before any qualification. Starting counters
883/0. The broader final-history gate RO-C3-021 does not block this experiment.
