# EMB-M..O receiving proposal

Revision2, 2026-09-10. SYMBOLIC DRAFT, NOT SHAPE ACCEPTED. No permanent numbers,
model activation, runtime implementation or behavioral qualification. Inspection:
CAMPAIGN3_EMBODIED_RECEIVING_INSPECTION.md. The author will adversarially review and
close the remaining composition gates under the user's autonomous instruction.

Proposed components: embodied-response-instruction/0.1-draft;
embodied-task-options/0.1-draft; embodied-task-reasons/0.1-draft;
embodied-task-arbitration/0.1-draft; embodied-choice-expression/0.1-draft;
embodied-protocol-attempt/0.1-draft. Existing body mathematics and task source
mathematics retain their exact accepted versions.

## Source promise and adopted context

An EmbodiedResponseInstructionDefinition contains exactly PressureDefinitionId and
ProtocolActionDefinitionId. Both use1027 and exact registry-kind/schema admission.
The instruction means "try this protocol when this perceived deficit is present."
It asserts no success, expected relief, amount, efficacy, reward or causal truth.
The existing391 action still means one or two fixture contacts and has no fuel effect.

EmbodiedResponseContext contains a canonical set of adopted instruction IDs, size0..2.
EmbodiedResponseInstructionState has a map from qualified CharacterId to that context.
The first profile has exactly one character entry; an empty set is valid and distinct
from malformed/missing required state. The root is a new read-only character procedural
context, admitted through262; it has no writer or learning route. Initial adoption is
an explicit fixture premise analogous to accepted task instruction adoption. Registry
membership alone never creates adoption. Dynamic instruction acquisition is deferred.

At70 authenticate actual464 from the matching current observation/SEM chain. The
subject is exactly its PRJ/IDN-derived character. Unavailable or Known(0) performs no
instruction-state read and emits an empty BodyResponseOptions. For Known(p)>0, read
the character's one immutable context leaf once. Resolve only its adopted definitions,
in canonical order; require their pressure reference to match the exact registered
producer's460 definition. Each creates BodyCandidateOrigin(candidate395,instructionID).
No full registry, body, world, REG, memory, prediction or identity capability is supplied.
One BodyResponseOptions output contains an occurrence, exact464 and ordered origins.

Changing adoption can change options while leaving464 byte-identical. The model may
contain an unadopted instruction without making it accessible. Repeated literal IDs
in a set are invalid canonical input, not a duplicate-description witness. Two distinct
adopted definitions with the same pressure/action are permitted descriptions of one
response ground; neither changes the complete action key or adds a motive by itself.

## Genuine task branch and shared options

Use actual task373/schema2 adoption/instruction state, task381 workspace,384 appraisal,
388 concern,394 motive,398 options and403 raw signals. Do not construct a TaskKey for
the body. The first fixture can select one genuinely adopted task and have an unavailable
forecast; its commitment pressure remains genuine under the accepted source law.
The existing deadline lifecycle must still execute and preserve instruction bytes.

MixedCandidateContext contains its occurrence, actual398, actual BodyResponseOptions
and a canonically ordered list of MixedCandidateOption. Each option contains395 plus
separate canonical sets of actual396 task origins and body instruction IDs. Compute
the union by395; origins must equal the exact complete source union. No filtering by
pressure magnitude, world permission or expected success occurs at this join. There
are0..2 actions, at most one task origin and at most two body origins in this profile.
Task options may exist with disabled base motivation; body instruction access is
explicitly deficit-triggered. These are distinct source contracts, not a global
availability-equals-motivation law. No-options and options-with-no-reasons stay distinct.

## Semantic grounds, raw causes and evidence admission

MixedMotivationGround is a closed union: Task(actual371), or
Body(EmbodiedGround(CharacterId,PressureDefinitionId)). The matching motive channels
are existing Commitment and proposed EmbodiedFuelDeficit respectively. The body ground
does not contain an instruction, observation or occurrence ID. Such IDs describe
sources/support; they do not define independent motivational concerns.

MixedSignalKey contains Candidate395, Ground, SourceRole and an optional body
InstructionDefinitionId. Task keys require the optional field absent; body keys require
it present and role=MotiveGenerating. SourceRole retains1 motive,2 situational,3 standing.
ContextModulating remains an explicitly absent source, not relabeled as base pressure.
Every task key and value is the exact deterministic receiving projection of a genuine
402 in the actual403; body keys must match actual adopted candidate origins. Caller raw
signals are never admitted as originals. MixedRawSignal contains Key, exact Strength
and existing400 Basis. MixedRawContext nests both actual403 and MixedCandidateContext,
its occurrence, canonical raw set and per-ground unfiltered meaning maps.

Every body origin contributes the same positive p from its actual464. Its basis is
exactly one existing399 observation atom, weight1, containing237 for that464's actual
present461 observation. The new consumer expressly admits only the exact461/schema1,
embodied-level-observation/0.1-candidate producer and matching observer. Its authority
comes from live464/source settlement, not from dereferencing an arbitrary237. No new
evidence identity, pressure-as-evidence atom, world ID or instruction-as-observation
is created. The nested sample is already observer-safe. No body/trace reader is granted.

Task signals retain their original basis, strength and roles. Task commitment base
has empty observational basis; it is an independent adopted motivation, not an inferred
second description of the body's observation. Never consolidate across different
grounds, candidates, source roles or signs. Repeated complete source keys reject.

## Receiving hypothesis and exact dice

Within each candidate/ground/role/sign, apply the accepted aggregate weighted-Jaccard
algorithm: sort descending magnitude then full canonical mixed source key; accumulate
pointwise maximum raw basis; overlap is sum(min)/sum(max), zero for empty union;
effective=magnitude*(1-overlap). Fully discounted sources still enter raw coverage.
Subtract negative total from positive total and apply x/(1+abs(x)) once per role.
This is an explicit new receiving domain for that mathematics. Old401 admission stays
unchanged; no fabricated old key may be used to call the old canonical adapter.

MixedReasonKey contains candidate395, Ground, exact matching MotiveChannel and resolved
direction. MixedReasonNucleus retains the old semantic fields: role summary, relevance,
die, standing integer, situational integer and424 exact distribution. A zero net base
cannot create a nucleus. Relevance, threshold, die bands, modifier truncation/cap and
polarity are the accepted reason-dice mathematics with explicit finite bounds. Up to
three nuclei are possible: one task plus the one body ground for each of two actions.
Up to five raw signals are possible: three task roles and two body descriptions.

Per-ground meaning maps cover every candidate, zero where that ground has no source.
Task meaning is exactly its accepted403 map, completed with zeros for body-only options.
Body meaning is its consolidated non-standing raw base, bounded once, per candidate.
Repeated body descriptions cannot inflate meaning after their evidence contribution
has already been removed. No scalar combined trait alignment is inferred across grounds.

MixedReasonContext nests the raw context and nuclei. MixedDecisionResolution preserves
NoOptions / NoActiveReasons / Chosen, exact probabilities and Margin/Contest/Stake/
AuthorshipPotential/mode. Reuse generic exact convolution and arbitration, not weighted
sampling. Options with no own reason retain their zero-score distribution when other
options have active reasons, as in the accepted task mathematics.

Reserve the resolution occurrence once before RNG. A new scoped address contract uses
the existing oracle,411 draw record,1042 purpose family and1043 subject-role family.
Task draws bind actor/action/task; body draws bind actor/action/pressure-definition.
Distinct purpose members separate task and body ground kinds. Each root permits at
most three reason draws and one fair tie draw; DrawIndex=0. Bind every draw to the exact
live nucleus; no caller oracle, unscoped random key or private body lottery. Auto and
both no-choice branches draw nothing. Transaction abort discards provisional addresses;
complete-prefix restore reconstructs their committed use.

## Expression and execution scope

Only Chosen emits MixedChosenIntent. It forks separate phase90 MixedDecisionExpression
and phase100 MixedActionPlan. Expression nests actual intent and freezes each ground's
choiceAlignment from its complete non-standing meaning map, plus an exact scope version.
It is contextual expression, not qualified trait evidence. Do not manufacture a428
task-only scope or silently send this output to429. This bounded pilot makes no acquired
identity write. The qualified task identity loop remains the preserved MEC-018 control;
mixed identity learning requires a future qualification/learning seam.

The plan resolves only the already selected391 action, records its requested count and
actual intent. At110 an actual attempt is emitted before its separate execution child.
Only execution receives the immutable world permission: completed=requested if permitted,
otherwise0. MixedExecutionOutcome is truth-side and contains actual attempt and completed
count. It never changes frozen expression, pressure or adopted knowledge. It has no body
effect. No generated478, invented unit conversion or action-conditioned relief occurs.
This scoped protocol wrapper does not claim the existing task execution contract's
complete ADAPT/EVID successor closure. Those outgoing integrations remain absent in
this receiving experiment and the qualified intact task profile preserves their control.

This scope is sufficient to test genuine shared choice and failed execution without
claiming learned efficacy, behavioral replenishment, mixed identity development or a
complete general BODY loop. Any broader verdict must first supply those missing paths.

## Proposed scheduling, identity and persistence envelope

A composed public original-input contract pairs one existing459 sample at10 with one
existing377 deliberation at40 per chosen positive instant, same observer/subject. No
psychological intermediate is authored. At most one pair occurs per instant. Existing
external478 deliveries may follow at110; task deadline events remain at140. Their exact
compiler origin, allocation order and work budget are still packaging gates.

Body chain10/11/12/13/14/60 emits BodyResponseOptions at70. Task40/50/50/60 emits398 at70.
Because body60 is generated at14 and task60 at50, canonical scheduler sequence places
body70 before task70. Task70 schedules TaskRaw80 and the mixed option join70. The join
consumes the actual already-produced body70, not a reconstructed copy; its70 output
is terminal. Actual TaskRaw80 then emits mixed raw80, pairing its completed403 with
the already-produced mixed candidate70 output. Both joined sources therefore exist
before the mixed raw child is allocated; no future403 placeholder is admitted.
Then reasons80 -> resolution80 -> intent90
-> expression90 and plan100 -> attempt110 -> execution110. Every join is one-shot and
binds exact model, instant, subject, outputs, parent and allocated child coordinates.
No inter-instant cache, arbitrary pairing, stale pressure or public merge API exists.

Retain one occurrence per context/result output via shared279/278. Proposed reuse of
the existing generically named1132..1137 and1139..1141 families requires explicit review
of their semantic domain extension; it does not make old decoders admit new records.
No item-level ground, option, instruction-origin, coverage or draw identity is allocated.
Original-source namespace/subject admission remains PRJ/IDN-owned. New immutable context
needs exact roles and state grammar; no new body identity lookup or truth handle.

All cognitive writes are empty; only accepted body deliveries and task lifecycle write
their respective leaves. Trace must record actual restricted reads, output/child/RNG
closure and body truth only at its owning stages. Persistence compares exact model/run,
S0, original prefix, pending deadlines, archive, allocators and RNG history. Whole-instant
rollback includes live join state and both branches. Exact schema/registration/trace/
failure/finite-work inventories remain required before shape acceptance.

## Proposed adversarial obligations (all NOT PASSED)

| Code | Distinction |
|---|---|
| ER-A | Real same-instant SEM/pressure and adopted task converge to one395 with both origins and two independent grounds. |
| ER-B | Remove body adoption: pressure unchanged; body origin absent; task origin preserved. Unadopted registry definitions cannot repair access. |
| ER-C | Rebind adopted body instruction to the other admitted action: pressure unchanged and only association changes. |
| ER-D | Two adopted descriptions of one pressure/action preserve origins but yield one body nucleus and unchanged body meaning/distribution. |
| ER-E | Removing either independent task/body source changes the shared option's reason distribution; global maximum/one-source collapse fails. |
| ER-F | Actual independent grounds remain separate even with same action. No world-ID overlap join or fake TaskReferent exists. |
| ER-G | Genuine generic aggregate {a},{b},{a,b} component catches pairwise-only coverage; singleton public body support is explicitly narrower. |
| ER-H | Known0/Unavailable perform no body-instruction reads; missing and forged binding fail at their owning stage, not fallback. |
| ER-I | Body state aliases and channel-resolution contrasts propagate through actual mixed choice without cognitive truth access. |
| ER-J | NoOptions versus NoActiveReasons; no phantom intent/draw; zero-base modifiers cannot create motivation. |
| ER-K | Two choices/three nuclei, exact fair ties, all admitted dice/mode branches and finite RNG address closure. Public/component scope must be reported. |
| ER-L | World permission changes actual completed count but not preceding cognitive outputs or adopted associations. Missing knowledge remains missing. |
| ER-M | Expression precedes outcomes, excludes standing from meaning and produces no task qualification or identity patch. Old task feedback control is preserved. |
| ER-N | Actual deadline retires commitment pressure without erasing body origins or immutable task/body instruction context. |
| ER-O | Forged/cross-instant/cross-subject/repeated sibling outputs and child coordinates reject before receiving; actual two-branch join only. |
| ER-P | Failure at every new stage restores both branches, live bindings, state, output, trace, allocation and provisional RNG. |
| ER-Q | All complete-prefix restores match continuation; altered instruction S0/model, original pairs, archives and random keys reject. |
| ER-R | Old task and EMB factories reject new records/models; all prior frozen artifacts retain their meaning and bytes. |

Open before acceptance: exact typed inventory and role refinements; occurrence-family
scope ruling; registered joins; final finite recipes/calibration; exact trace/failure/
work/restore closure; review of the intentionally absent mixed learning/outcome edges.
Permanent allocation and exact model freeze follow whole shape acceptance separately.
