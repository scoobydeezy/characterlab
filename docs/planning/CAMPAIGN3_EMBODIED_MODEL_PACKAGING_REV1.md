# EMB-001 model packaging, revision1

2026-09-10. **PACKAGING PROPOSAL; NOT A MODEL FREEZE OR ACTIVATION.** Consumes
[accepted shape](../formal/EMBODIED_RESERVE_SHAPE_ACCEPTANCE.md) and
[permanent allocation](../formal/EMBODIED_RESERVE_PERMANENT_ALLOCATION.md).
No change to those mathematics, records, numbers or proof-vector dispositions.

## Inspection findings

Existing model factories snapshot nine data-only source fields and recompute identity
from complete canonical content, registry and parameters. Cognitive admission requires
its exact frozen cohort; submitting an EMB image to that factory must reject.
The six-slot registry layout is expressible without a new wrapper record. Generic
state ownership accepts an independently declared455/1 map; ADAPT's logical-family
topology is not a prerequisite for a physical writer outside its routes.

The current `numeric/exact-1` profile explicitly excludes new saturation transforms.
It therefore cannot be claimed unchanged for this profile. A new version below binds
the already accepted exact stock/sensor laws; this is profile closure, not new arithmetic.
The existing shared admission compiler only dispatches older registration shapes.
Its279 occurrence collection remains usable as data, but the EMB source/pressure
adapter must validate its own complete graph; a V04 compiler call cannot prove it.

MEC-003/004, P3-009/010/011 and CTL-001 retain the accepted intake dispositions.
SUB-003/008/011 preserve identity, whole-instant restore and correct-forward history.
No stored-meter mechanism or historical factory is imported as the new authority.

## Exact proposed compatibility bundle

| Field/profile | Exact value |
|---|---|
| RulesVersion | rules/campaign3-embodied-reserve/0.1-candidate |
| RegistrySchemaVersion | campaign3-embodied-registry/0.1-candidate |
| ContentSchemaVersion | content/0.2-candidate |
| ParameterSchemaVersion | campaign3-embodied-parameters/0.1-candidate |
| NumericProfileVersion | numeric/embodied-reserve-exact/0.1-candidate |
| RandomAlgorithmVersion | rng/sha256-addressed-128-v1-candidate |
| Ordered-input profile | campaign3-embodied-ordered-input/0.1-candidate |
| Trace profile | campaign3-embodied-trace/0.1-candidate |
| Persistence profile | campaign3-embodied-persistence/0.1-candidate |

Version operands stay independently present in ModelIdentity. The fixed compatibility
bundle validates their combination and selects input/trace/persistence interpreters;
no caller override, implied version, latest selection or digest-only source is admitted.
The nine input fields remain rulesVersion, contentSchemaVersion, registrySchemaVersion,
parameterSchemaVersion, numericProfileVersion, randomAlgorithmVersion, content,
registry and parameters. Reject extra keys, accessors and mutable aliases as in the
accepted data-only boundary. No semantic callback or prepared handler enters it.

## Registry closure

Propose exactly six slots, preserving the existing collection forms:

| Slot | Complete proposed contents |
|---|---|
| 0 | Canonical set of329 trusted172 schema descriptors (297 inherited declared record/schema pairs plus32 new), inherited union171/259 declarations plus four new union declarations, two existing VAL entries, the11 EMB instance entries, and one existing transition-admission singleton. |
| 1 | Exact existing134 ordering-phases/2-candidate, including non-schedulable150. Execution remains limited to10,11,12,13,14,60,110. |
| 2 | 155 with one154 authority: authority/embodied-reserve owns455/1/mapKey(*) with canonical-record454 value grammar and removal=false. |
| 3 | One262 read-only IDN family268/1/mapKey(*) with canonical-record267 value grammar. |
| 4 | Two261 identity-key grammar rows, exactly matching455/1 and268/1 patterns. No orphan grammar, overlap or writable roster. |
| 5 | Forty new direct record roles,455/1 character map-key role,267/1 qualified character role,268/1 observer map-key role,227/1 ExperienceId role and227/2 ObserverId role:45 distinct265 positions. |

The inherited descriptor/union set is taken from the frozen cognitive declaration
image, not generated by importing its executable entries. It retains the declared373/schema2 only; historical373/schema1 stays decoder-supported
but is not a descriptor in this model. The inventory receipt commits that exact input and enumerates inherited
union StableIds. Build-supported schemas do not admit old tasks, REG, ADAPT, learning,
recognition, RNG or arbitrary inputs into this model's execution closure.

Eleven EMB entries are the nine DefinitionIds and two TransitionKindIds in the
accepted witness. VAL entries keep existing character-kind329 and validator330.
The existing `definition/transition-admission` / `registry/transition-admission` entry
retains transition-admission/0.4-candidate and279 grammar: Routes=set([]),
TransitionRoutes=map([]), and exactly four schema-to278 occurrence entries:
227/1→1106 at field1;461/1→1115 at field1;463/1→1115 at field1;464/1→1142 at field1.
No character-learning or automatic-adaptation membership is inferred from the
presence of an occurrence row. No occurrence row for a source input, trace-only result,
quantization operation or private carrier is added.

All descriptors and unions must equal trusted allocated layouts. Each171 entry must
match its exact StableId, registry kind, version and payload. New source/pressure
registration dispatch extracts existing266 requirements without changing PRJ semantics.
The two DefinitionId set-member checks remain fixed compiler checks, not extra265
roles. Schema references and optional union fields need exact version/branch checking.
VAL validator coverage includes nested266 and278 roles, not just slot5.

## Complete content, parameters and state

Content is a singleton canonical set of170: StableId1038(character/embodied-subject),
SemanticKind1004(semantic-kind/character). Its remaining14 fields are explicit empty
canonical lists, including both reference lists. This uses the existing qualified
authored-character semantics and does not author knowledge, pressure or world effects
into character content. The body parameters/interventions live in their own registry
definitions; empty character content fields do not duplicate those definitions.

Parameters are exactly list([OrderingParameters133(MaxSettlementWorkPerSimulationInstant=8)]).
The largest baseline instant is T75: six sample-chain events plus two deliveries.
No hidden initialization/phase0 event or settlement sentinel consumes work. The limit
is a parameter commitment, not an adapter argument. A separately committed limit7
control must fail that instant atomically; passing arithmetic counts is not execution.

S0 has exactly two materialized maps: immutable IDN268 containing the accepted O→C
binding, and ReserveState455 containing the accepted anchor80@0 for C. Body bindings
remain immutable registry data. No empty cognitive/ADAPT state root is manufactured.
Map keys, value roles, exact key coverage, anchor bounds and anchor time are all checked.
S0 clock/counters and the nine original inputs are exactly the accepted witness.

The baseline and six comparison specimens preserve that input sequence. Hidden89
changes initial state only and therefore RunIdentity, not ModelIdentity. The slower,
coarser, denied, unavailable and overflow cases change model definitions. They require
six distinct model commitments in total (baseline shared with hidden89), seven runs,
and no recipe-name dispatch in place of bytes. This refines the witness's “future
ModelIdentity/RunIdentity” wording without changing any comparison operand.

## Numerical and run-profile closure

The new numeric profile admits exact canonical rationals, arbitrary-precision integer
arithmetic, checked nonnegative Int64 SimInstant, exact elapsed subtraction, max/min,
and nonnegative rational floor for bin index. It binds precisely q=max(0,q_a-r*dt),
bounded refill decomposition and p=max(0,H-U)/H. No floating point, debt, hidden tick,
wraparound, SI conversion or REG clipping. Capacity endpoint takes the last bin;
trace quantization is one484 per present sample, none otherwise. Time/amount domain
errors precede arithmetic. RNG algorithm remains an identity operand with zero draws.

Ordered inputs contain only sampling458 and replenishment478 at their fixed10/110
phases, ordered as the witness. Reuse the existing five-position canonical list entry:
list([DueAt:signed, Phase:unsigned, EventTypeId:typed1001, Payload:exact458/1 or478/1,
Dependencies:list([])]), within the outer canonical list. Each time is strictly after
initial clock. Event IDs/sequences and empty causal parents are compiler-owned, absent
from these entries. The new input version changes the exact admitted rows/time domain,
not this outer encoding; the old compiler still rejects it. Both source families are original InputOnly, no runtime
creation/cancellation. One sample per observer/instant; delivery ties retain input order.

Trace mapping and committed-boundary persistence consume their accepted EMB contracts.
Save validation must derive empty RNG/coupling metadata from admitted no-draw closure;
empty initial metadata alone proves nothing. The reserve anchor belongs to ordinary
authoritative state, not a second run-owned TIME analytical-anchor registry. Full
history and both pending original-input families remain mandatory. This model does
not inherit cognitive pending inputs, task RNG histories or old factory activation.

## Packaging controls and remaining gate

EPACK-A..H, proposed and NOT PASSED:

1. Change each registry slot: identity changes or exact closure rejects; no split snapshot.
2. Inject an old executable entry despite a supported descriptor: reject before activation.
3. Omit either key grammar, alter owner or widen roster rights: reject at state closure.
4. Omit a nested validator or set-member target check: reject at its owning boundary.
5. Replace new numeric/input/trace/persistence profile with an old one: reject the bundle.
6. Baseline/hidden89 share model identity but have different initial-state/run commitments;
   each actual model-definition comparison changes ModelIdentity.
7. Work8 admits the busiest witness instant; work7 fails and rolls back that whole instant.
8. Wrong arity/content fields, missing occurrence row or extra route entry rejects.

This revision defines the receiving composition. Before model bytes freeze: review
this packaging shape, materialize all six
complete declaration images and seven run specimens, validate cross-slot/reference
closure through the new trusted declaration interpreter and freeze exact digests.
No placeholder digest, callback-based predicate or widened old factory can close that
gate. No new permanent record/member is requested by this packaging revision.
