# Bounded measurement prediction — autonomous qualification

2026-09-09. **Agent self-review under the user's independent-work authorization.**
No external planning-agent or user verdict is attributed to this review.

**measurement-prediction/0.2-candidate is qualified for the frozen first profile,
with the explicit component/public scopes below. Campaign2 remains OPEN.**

The governing model is rules/campaign2-measurement-prediction/0.1-candidate,
digest `f272823f9c3aca5894e7366d646cdfcad050b4062db084219860403324008278`.
The frozen model directory and correction allocation are unchanged. The earlier
0.1 record-reference error remains documented history:367/1 and368/1 never become
production-admitted schemas;367/2's retired field2 remains absent.279/3 alone owns
the readout identity rule. No runtime correction required another allocation.

## Implemented causal path and authority

Actual permitted diagnostic203 passes accepted carriage337 and live M1/342.
M1's existing memory formation child is followed by a prediction application at140
and an independent later read cue at T+1/40. Application consumes342; its sole state
authority sets one362/1 key360 value361. Later read emits366 only when that owned
value exists. It is not a new learning-evidence producer.

The owner comes through the accepted nested343 subject projection and IDN267/268,
not observation.SubjectId. Prediction reads only that roster source and its own
prior leaf. Its evidence basis reuses the existing237 observation variant and1115
occurrence. No trace resolver, current REG query, copied truth provenance, appraisal,
confidence, reward, value, identity or action efficacy is added.

The model compiler checks the exact six-slot frozen source after real CONTENT/VAL
declaration validation. Existing memory components retain their original registration
bytes; the new combined state owner/content and whole ModelIdentity are bound by
the successor compiler. Every public trace carries the successor model/run identity.
Old-profile activation is not reused under a relabelled model.

The paired140 dispatcher accepts exactly the authenticated memory/prediction pair
or their matched padding pair, separately from unchanged ADAPT batches. Both operate
on B0. Their independently authorized patches combine without overwriting either
family. Output closure rejects extra application/formation outputs. Pair lifecycle,
final trace validation and private association commit preparation precede commit.

## Frozen-vector crosswalk

| Vector | Executed evidence and bounded disposition |
|---|---|
| PRED-A | Public application ablation versus learned0: the latter remains present with one support and a later366. PASS. |
| PRED-B | Public matched5 versus51/10 intervention changes forecast through203→337→342; qualitative EVID remains equal. PASS. |
| PRED-C | Actual source components produce the safe point; holding that complete input fixed while changing hidden retained D changes neither actual cognitive reads nor patch/diff. This is a component intervention, not a public model/REG override. PASS in that scope. |
| PRED-D | Actual producer-component histories0,10,10 and10,0,10 yield20/3, distinguished from last10 and half-gain15/2. Earlier1296-case independent algebra/historical-control exploration remains separate supporting evidence. PASS for exact-point candidate. |
| PRED-E | Forged event identity rejects before any state read; public ordered-input language and exact model source exclude raw, archived and wrong-route entry. Source-specific validation is not itself a generative credential. PASS. |
| PRED-F | Distinct ObserverId/CharacterId path, actual IDN read and qualified key; missing roster rejects with no prior read. Foreign path is excluded by fixed target/WRT checks. The first profile has one qualified character; no second-character public positive is claimed. PASS in this split. |
| PRED-G | Wrong channel, observed subject, point bound, precision and unit fail source admission before state access; accepted carriage validation still executes. PASS. |
| PRED-H | A new342/337 wrapper carrying the retained original203 fails PREDICTION_OBSERVATION_ALREADY_APPLIED, including at the full support limit. PASS. |
| PRED-I | Actual pair rejects missing/mixed/reused/late execution and premature/reentrant finish. Component duplicate target rejects after IDN but before a second prior read. Public profile excludes multiple prediction targets. Both real pair consumers receive B0; independent-key numerical freeze discrimination is not claimed for an own-key-only profile. PASS for this bounded stage, not a generic multi-target qualification. |
| PRED-J | Exact one prediction patch, unchanged other entries, wrong authority and foreign target rejection. Public64-control matrix independently retains or suppresses episode and belief writes. PASS. |
| PRED-K |64 distinct actual component-produced observations admitted;65th fails the named evidence-limit code, with duplicate failure retaining precedence. No forgetting/clipping. PASS in component scope. |
| PRED-L | All64 committed controls preserve M1 and later cue while independently ablating application. No forecast is recovered from retained episode. PASS. |
| PRED-M | Independent read ablation preserves ordinal slot and IDN but performs no belief read and emits no366. PASS. |
| PRED-N | Final prediction trace failure after staged writes restores state, queue, ordinals, outputs, trace and pending associations. Seven adversarial stage variants also preserve the pre-instant transaction. PASS for named failures; not arbitrary host failure coverage. |
| PRED-O | Public exact S0/input prefix restore, real and padded pending chains, consecutive instants and before-first-event save. Forged mean/support/owner/cue, dropped queue and partial prefix reject. PASS, zero RNG. |
| PRED-P | Public consecutive probes preserve40-before140 and12/14/2 event counts. Component changing-reading witness reads committed5 before its instant updates mean to11/2, and emits the next cue only at T+1. PASS. |

Component tests compare permitted operand/read/patch projections; their synthetic
envelope placeholders are not claimed as public canonical trace or replay witnesses.
Public factory tests separately exercise the actual model/run/trace/save envelope.

## Adversarial self-review and verification

The first compiler attempts correctly failed on heterogeneous registry identifiers
and the old generic record helper's schema1 assumption; both were corrected before
runtime qualification. The output-rule schema correction was not bypassed. Further
review added accepted237 union validation, explicit source-domain failure ownership,
two-phase private commit preparation, terminal-output rejection and scheduler-owned
trace construction. None changes the frozen symbolic mathematics or model bytes.

MEASUREMENT_PREDICTION_RUNTIME_PROOF_REV1.json records five current-source
substitutions: repeated source counted fresh, mean replaced by last reading,65th
support permitted, same-instant read back-edge, and extra application output.
All are detected by named controls. No undetected independent-target freeze mutant
is presented as a success.

Executed combined suite:147 files/1124 tests PASS, comprising104 fresh files/796
tests and43 reference files/328 tests. Added stage suite:1 file/8 tests PASS.
Added persistence-forgery suite:1 file/1 test PASS. These are separate executions,
not a fabricated single1133-test run. TypeScript and reference boundary PASS.
The application shell build passes; it is not a completed character interface.

MEASUREMENT_PREDICTION_RUNTIME_AUDIT_REV1.json verifies exact production/frozen
model and source bytes,190 preservation entries, the five substitution fingerprints
and18 refreshed inherited report fingerprints. The prior166 frozen artifacts remain
intact. CAMPAIGN2_CURRENT_EVIDENCE_INVENTORY_REV2.json supersedes the earlier source
inventory for current-source claims, preserving the old inventory as history.

## Deliberately outside this qualification

Immediate phase30 learning and ORD-001; bounds-only evidence and MEC-002; calibrated
precision/confidence, decay and unbounded retention; action-conditioned prediction,
causal attribution and efficacy; appraisal/affect/goal meaning; multi-character or
multi-target public profiles; general workspace control; stochastic arbitration and
PERSIST-I; identity formation; intent/attempt/world-outcome generation. MEC-001's
historical estimator is a comparison control, not a replacement imported into src.

Next work is explicit goal-relative workspace/appraisal and lifecycle-bearing motive
semantics, then the mandatory reasons/dice/DecisionExpression/identity path. A bare
forecast of5 has no negative valence until a new contract supplies the relevant
character goal and its source. Campaign2 cannot close on this diagnostic pipeline.
