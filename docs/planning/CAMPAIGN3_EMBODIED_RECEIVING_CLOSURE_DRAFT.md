# EMB receiving registration, roles and finite closure

Revision3, 2026-09-10. SYMBOLIC DRAFT. Refines receiving revision2 and self-review1.
Not whole acceptance, allocation or implementation. Inventoryrev3 is the current
31-record/110-field proposal. Revisions1/2 are superseded drafting evidence; revision3
uses the accepted family name DefinitionId instead of an unsupported registry alias.

## Executing versions and preservation

Propose embodied-pressure/0.2-draft: same464 schema and exact p mathematics; same
sample/SEM/PRJ/IDN admission; exactly one generated BodyResponseOptions70 child.
The pressure's TransformationVersion names this successor. Frozen pressure0.1 and
its terminal profile remain unchanged and are not aliases of the new bytes.

Propose embodied-task-source/0.1-draft for the six task source transitions below.
It consumes accepted task-commitment/0.2, immutable task-plan-context/0.1, and the
accepted workspace/appraisal/concern/motive/candidate/raw formulas and schemas. Its
profile has forecast access disabled and standing access disabled, with no forecast
or identity read. Their unavailable/zero-source branches execute honestly. This is
not a claim that the old complete task transition registrations license new children.
The old intact cognitive model, earned standing loop and consequence bridges remain
qualified preserved controls. The new profile cannot activate those old consumers.

Remaining executing versions are the six receiving proposal components; the bundle
also commits embodied-receiving-admission/0.1-draft for the exact graph. Candidate
versions follow only whole shape review. No runtime branch chooses a contract by an
uncommitted callback or arbitrary string.

## Closed stage table

EmbodiedReceivingStageRegistration has the11 ordered fields in inventoryrev3.
Stage is a closed discriminator whose numeric values remain unallocated. The table
below supplies its complete alternatives. Event member suffix is the stage label,
prefixed `event/embodied-receiving-`. The registry compiler requires exact field
equality to this table, the named component and the exact compiled read/projection
patterns. An arbitrary combination of Stage, input, output or source events rejects.
WriteCapability is existing NoStateWrites for all16 rows.

| Stage | Phase | Input | Exactly-one output | Actual source / outgoing children |
|---|---|---|---|---|
| workspace |40|377/1|381/1|Original paired deliberation; appraisal50 |
| appraisal |50|381/1|384/1|workspace; concern50 |
| concern |50|384/1|388/1|appraisal; task-motive60 |
| task-motive |60|388/1|394/1|concern; task-candidates70 |
| body-options |70|464/1|BodyResponseOptions|pressure0.2 present/absent; terminal |
| task-candidates |70|394/1|398/1|task-motive; task-raw80 then mixed-candidates70 |
| mixed-candidates |70|MixedCandidateJoinInput|MixedCandidateContext|parent task-candidates plus actual body-options sibling; terminal |
| task-raw |80|398/1|403/1|task-candidates; mixed-raw80 |
| mixed-raw |80|MixedRawJoinInput|MixedRawContext|parent task-raw plus actual mixed-candidates sibling; mixed-reasons80 |
| mixed-reasons |80|MixedRawContext|MixedReasonContext|mixed-raw; resolution80 |
| resolution |80|MixedReasonContext|MixedDecisionResolution|mixed-reasons; intent90 iff Chosen |
| intent |90|MixedDecisionResolution|MixedChosenIntent|Chosen-only resolution; expression90 then plan100 |
| expression |90|MixedChosenIntent|MixedDecisionExpression|intent; terminal |
| plan |100|MixedChosenIntent|MixedActionPlan|intent; attempt110 |
| attempt |110|MixedActionPlan|MixedActionAttempt|plan; execution110 |
| execution |110|MixedActionAttempt|MixedExecutionOutcome|attempt; terminal truth output |

SourceEvents contains exactly the actual listed producing event kinds, or the empty
set for original workspace. For the two joins it contains both producer kinds even
though there is only one scheduling parent. The fixed Stage discriminator, not set
membership alone, specifies which is the parent and which actual sibling is required.
InputSchema is exactly the table's254 reference. OutputDefinitions is singleton277,
with the appropriate shared279/278 occurrence rule. RequiredProjections is singleton
IDN266 only for377 workspace; all other new rows inherit authenticated subject from
their actual sources and have an empty set. Pressure0.2 retains its original separate
PRJ registrations and field2 ObserverId selectors.

Both pressure alternatives produce body-options even for unavailable/zero. Every
task stage and both joins produce their one context even with empty task/options/raw
sets. Resolution absence has no intent and no downstream record. No dummy successful
action, default candidate or padding intent occurs. Body sampling retains its accepted
present/absent reservation-padding rule. Other new stages add no padding ordinal.

## Sibling authenticity and allocation order

The paired originals are independently admitted real459/377 sources. They have equal
instant and ObserverId, the exact model channel/agenda, and empty dependencies; no
second caller CharacterId exists. At each new source transition, live input authority
binds the complete actual event and registration before any projection/read.

At task-candidates70, body-options70 must already have completed at the same instant
and subject. The trusted host constructs MixedCandidateJoinInput from these two exact
published-in-the-staging-transaction outputs. It emits task-raw80 first and the join70
second. Phase ordering executes join70 before task-raw80. At task-raw80 the trusted
host constructs MixedRawJoinInput from its actual403 and the already-completed mixed
context. The latter must contain the exact same398 as403, not merely the same subject
or occurrence family. Both joins allocate children only after both sources exist.

Private associations authenticate every byte, model/run, instant, source event and
actual output occurrence. Validate allocated child dueAt, phase, event kind, payload,
dependencies, parent IDs, EventId and Sequence; require one-shot consumption. Source
collections equal the actual source set, not only a subset of admitted IDs. Repeated,
foreign, copied, altered, unbound, prematurely delivered or leftover sources abort.
Both branches' state is discarded on abort, sealed at commit and never persisted as
an independent provenance graph. Restore rebuilds associations by executing originals.

SourceRecordIds in a join trace list the two actual output occurrences in canonical
order. The input already nests their exact permitted data. The scheduling parent and
causal source list serve distinct existing purposes; no extra causal identity is minted.
No next-instant pressure cache or archived-output admission is allowed.

## State and read ownership

The new context instantiates candidate prospective instruction state under Architecture
section11. Its record root, exact262 read-only pattern and StateMapKey role suffice
for this model, as with the existing read-only roster. Do not allocate1031/1032 logical
family/leaf members without a consumer: this profile has no ADAPT family-map route
for the new context. It is distinct from procedural-skill competence and commitments373;
no fake task or skill state is introduced. Read-only admission gives it no writer.

The model has one qualified character, one observer binding, one body and one declared
task. S0 adopts0..1 task and0..1 task instruction; instruction keys are a subset of
adopted task keys. It always has one BodyResponseContext leaf with0..2 adopted IDs.
All roles/refinements are checked before run creation. The body instruction map is a
new read-only procedural family, not a writable belief, Need or identity family.

| Consumer | Exact admitted reads |
|---|---|
| existing level source |IDN roster plus selected body anchor iff channel permitted/available |
| pressure0.2 |Existing IDN projection only; sample/SEM admission remains exact |
| workspace |IDN roster plus declared task status373/1; no prediction read |
| task-candidates |Selected task's immutable373/2 instruction binding iff access enabled |
| body-options |Projected character's BodyResponseContext iff Known(p)>0 |
| task-raw |None: standing disabled, no identity or forecast lookup |
| other new stages |None; restricted actual source and immutable selected definitions only |
| existing delivery |Bound455 anchor, owned expected-prior write through authority/embodied-reserve |
| existing task deadline |Declared task status, owned write through authority/prospective-commitments |

Every direct read has a fixed model-compiled path and accessor. The new body accessor
is proposed `accessor/embodied-response-context` in1028. It selects the exact character
map key inherited from actual464, never a path supplied by the input. Trace actual
presence/value; malformed/missing required context rejects, whereas a valid empty
adoption set is an honest result. No skipped read is reported as executed.

The frozen one-task spec retains its actual370 prediction-definition reference. Its
target remains the accepted **diagnostic regulatory reading** domain; it is neither
contact measurement nor body level. Preserve an actual existing359/1 declaration from
the qualified task model, including its channel, qualified subject, diagnostic unit and
64-support bound. The new body's binding uses that same actual qualified subject.
Retain the corresponding actual character/task content and one task-spec declaration;
the new model need not retain unrelated task content or activate the old whole model.
This closes the dormant reference without inventing a producer, casting462 to204, or
assigning protocol contact semantics to359. Source access is disabled, so no prediction,
memory, REG or identity state/read is admitted. The profile compiler checks the exact
copied declaration and its content roles, and excludes prediction event registration.

Deadline compiler produces one private original clock fact per declared task, including
an unadopted task, after allocating supplied originals. At its exact140 deadline it
performs the accepted absent/terminal no-op or Open->DeadlineMissed expected-prior
write. No source output/occurrence is allocated. Workspace excludes an expired task
before the deadline stage by its accepted strict active interval. No measurement
settlement producer exists here, so no PerceivedSatisfied status is fabricated.

## Role and field refinements

All CharacterIds use1002 with the accepted qualified-character validator; all task
referents retain371's accepted qualified task role. Body map StateMapKey must have
that character role independently of recursive field validation. Context IDs and
instruction/pressure/action references use1027 without an invented DomainValidator;
the model compiler separately enforces exact referenced kind/schema/version and
adoption/source equality. Scalar membership is not a substitute for these relations.

Occurrence fields use the existing1132..1137 and1139..1141 families named in inventory;
they have no domain validator. This successor explicitly adds new record mappings to
its own279. Old279 mappings and old closed consumers are unchanged. New ground/items
are inline values, never independently allocated identities. Motive1040 is exactly
Commitment for Task and proposed EmbodiedFuelDeficit for Body; mismatched branch/channel
rejects. Event1001 and Seam1036 fields require their exact closed members and version.

MixedMotivationGround has exclusive Task/Body branches. MixedSignalKey's optional body
instruction exists iff Body, whose role is1. Task keys forbid it and exactly reconstruct
the original401 candidate/task/role. Candidate character equals ground character;
body pressure reference equals the authenticated producer and adopted instruction.
Every signal must have a corresponding actual candidate origin. Task signals, including
their basis/strength, exactly cover403; body signals exactly cover positive body origins.

Every basis is400, at most64 positive-weight atoms. Public body basis is singleton
399/237 from actual461; public task basis is empty because forecast/standing are absent.
Collection roles and live equality checks apply to adopted sets, option origin sets,
signal sets, ground maps, candidate probability lists, source event sets and random
subject bindings. Opaque occurrence ordinals are not interpreted as causal similarity.

Ground meaning list covers exactly the grounds of actual candidate origins, in full
canonical ground order, including zero-valued task grounds when base is disabled.
Each map covers all current candidates, in canonical map order. Expression covers
exactly this same ground list; touched-zero alignment is retained. Zero maps are not
invented identity channels. ScopeVersion names only embodied-choice-expression.

Coverage traces exactly reproduce sorted contribution keys and arithmetic. Nuclei are
canonical-key ordered, at most3, no duplicate key; active direction follows net base.
Probabilities cover the complete candidate list and must equal exact analysis, not
merely sum to1. Auto has empty draws/tie; roll has one draw per nucleus and a tie iff
actual score maxima tie. ReasonDraw face, contribution, span, address and chosen key
must all agree. Positive public base sources do not claim a public Avoid witness.

## Finite work and archival commitments

Proposed bounds: signed time1..100; at most100 supplied original rows; unique paired
sample/deliberation per instant; at most three distinct admitted deliveries per instant.
Delivery-only instants are valid. Task activeFrom0, deadline100. Original ordering is
canonical dueAt, phase, then full payload; no repeated complete row. Allocate original
EventId/Sequence in that order, then the one private deadline. Runtime ordinal starts0.

One pair executes6 body events + body-options +6 task source events +4 mixed receiving
events =17. Chosen adds intent/expression/plan/attempt/execution =5, total22. Three
deliveries plus one coincident deadline give exact maximum26. NoChoice still executes
17. Present pair publishes14 no-choice outputs or19 chosen; absent publishes one fewer
because no SEM exists. Runtime allocations are14 or19 in either observation branch,
including the accepted one reservation/padding ordinal. Deliveries add one output but
zero runtime ordinals; deadlines add neither. Work25 must fail at a reachable26-event
instant, rolling the whole instant back. No hidden phase0/init/sentinel event is counted.

At most50 pairs imply at most950 runtime allocations and1101 total events including
the deadline and all supplied delivery rows. Three reason dice plus tie bound draws
at4 per pair/200 per run. The accepted mapper permits at most three candidate hashes
per draw, so at most600 such computations. Three d12 dice give at most1728 elementary
score combinations before exact convolution. All arithmetic is rational/integer;
there is no new rounding or stochastic physiological operation.

Trace remains trace/0.2 with RecordKind=actual EventTypeId, full model/run identity,
exact read domain/actual reads, canonical input/output, actual emitted events and
occurrence allocation consequences. Only resolution owns random draw records; body
source retains484 quantization and truth-side reads. No new trace-only arbitrary raw
payload, provenance registry or success flag becomes a cognitive capability.

Public save is quiescent/whole-instant only and uses complete-prefix reconstruction,
including both paired originals, private deadline origin, initial adoption state,
pending inputs, exact output/trace/state/allocator bytes and RNG ledger. Validate run
seed, model/RulesVersion and original sequence. A copied generated join cannot be
supplied as an original. Changed action knowledge cannot reinterpret a prior save.
Failures after RNG are terminal for that run; authenticated reconstruction, not retry
on partially restored caches, is the recovery path. The general PERSIST-I lifecycle
continues to retain its prior scope; no new generalized lifetime semantics are inferred.

Still open: independent inherited-schema audit, exact new member/role machine inventory,
finite recipe materialization plan, concrete copied359/content byte audit and whole self-review.
All ER-A..R remain NOT PASSED, including these newly precise work/trace/restore clauses.
