# Campaign-2 amended consequence bridge checkpoint

2026-09-06. C2-BRIDGE-OBS-001 acceptance with sole-occurrence clarification is implemented as
an incremental component beneath adaptation-input/0.31-candidate. No integrated FCT-4/5/6 or
global PHEN-ADAPT-001 qualification is inferred.

## Implemented behavior

`bridgeObservation.ts` calls the unchanged observation/0.1 compiler and checks its actual raw
candidate. The private projection has only that candidate and its fixed version as operands;
fields 1..9 are copied exactly, field 10 becomes list([]), and field 11 becomes
authored-fact-observation/0.1-candidate. Only the projected record leaves the function. No raw
type-203 occurrence is staged, output, registered, or independently cited. The existing observation
validator dispatches the two versions separately; the new closure requires exact [1,1], Point,
precision 1, empty tokens and empty references. Canonical byte validity does not authenticate origin.

`consequenceBridge.ts` compiles the exact bridge singleton and fixed channels, including matching
channel key/ID, namespace-1005 channels, namespace-1006 modality, namespace-1000 observer, unique
observers, and the accepted precision/polarity/missingness/visibility rules. UnitId retains the
inherited generic field grammar at this component boundary; production vocabulary is the separate
C2-OBS-UNIT-001 decision. No test unit is accepted as a production allocation.

The live bridge context binds complete scheduler-allocated children to actual staged parents.
R allocates its one pulse truth occurrence after AAI, emits bridge children in canonical channel
order before the AAI consumer, then invokes the real bounded compiler at phase 120. One ObservationId
is allocated and preserved. The sole projected observation supplies a real type-216 support and
accepted consequence ExperienceId reservation. The 121/122/123 operations have their accepted empty
work sets and phase checks; 124 calls assemblePreRecognitionExperience and the accepted SEM freeze.
Successful settlement verifies the reservation/frozen-envelope bijection and complete bridge work.

`adaptationRuntime.ts` composes the committed optional bridge with the existing source/ingress
mechanism, observes the actual SEM freeze, executes the exact EVID phase-130 chain and only then
the exclusive ADAPT batch. It rejects duplicate fixed handler assignments and unsupported V04
adapters. The bridge receives no AAI, basis, count, rule or adaptation state as measurement operands.
No new admission issuer, observer-side provenance graph or character-state family is introduced.

## Controls exercised

- Actual raw candidate retains its legacy truth reference; projected fields 1..9 match byte-for-byte.
- Vary only FixtureConsequenceTruthId: projected bytes remain identical. Legacy compiler bytes and
  version remain unchanged. A nonmatching raw precision rejects instead of inventing a measurement.
- Scoped wrong kind/precision/version/tokens, truth references or substitute tokens reject decoding.
- Empty/duplicate-observer channel sets reject; forged, altered, wrong-parent, repeated and expired
  bridge work rejects. A generated observation with unfinished SEM reservation cannot settle.
- The composed paired control uses the same channel/subject/timing with hidden count 0 versus 2.
  Both have one R with direct bridge and ADAPT children, exactly one published type-203 observation,
  and byte-identical observation/X/E/L outputs. Only the adaptation path's state/result differs.
- The actual phase sequence is 110→120→121→122→123→124→130→130→140. Shared occurrence allocation
  yields AAI, pulse truth, observation, experience, E, L, dispatch, evaluation: eight occurrences.
- A phase-140 magnitude failure after the complete character-evidence branch restores queue,
  allocators, committed trace and outputs to the pre-instant snapshot.

BRIDGE-OBS-I/J have component evidence from the sole-output boundary, copied-field checks and real
paired runtime. Full independent semantic mutants, complete canonical transition trace/diff adapters,
all inherited E/ADAPT branches, fresh-process factory/restore and release comparison remain pending.
The tests deliberately retain local UnitId vocabulary; they do not activate a first production model.

## Validation and next decision

Full suite: **48 files / 375 tests PASS**. TypeScript/Vite build and reference boundary: **PASS**.
No reference-source changes, new numeric allocation or changes to frozen allocation tables.

C2-OBS-UNIT-001 is ready in CAMPAIGN2_OBSERVATION_UNIT_DECISION.md. The accepted REG inspection
already identified the missing UnitId home; D now requires that existing observation-channel operand
in a production inventory. Proposed resolution is a symbolic ObservationUnitId family and one
abstract fixture-pulse member, with no unit mathematics or REG redesign. Numeric allocation is later.
The separate concrete RulesVersion/bundle/profile binding remains required before FCT-5.


## Observation-unit continuation — 2026-09-06

Namespace 1039 and unit/fixture-pulse are permanently frozen. First-profile bridge tests now use
the permanent unit; generic/raw OBS controls retain their original fixture vocabulary. The bridge
checks canonical unit text and exact membership; compileFirstCampaign2Bridge also requires the
committed namespace-only 201/5 role and runs declared record-role checks. See
[unit implementation evidence](CAMPAIGN2_OBSERVATION_UNIT_IMPLEMENTATION.md). The earlier unit gap
is closed. Whole FCT-4/5/6, VAL closure and PHEN-ADAPT qualification remain pending.
