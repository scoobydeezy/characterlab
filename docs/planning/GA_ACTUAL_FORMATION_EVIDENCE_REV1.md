# Actual selected acquisition evidence

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Component qualification;
public registration, canonical acquisition codecs and General Attention remain OPEN.

Both acquisition paths now use actual producers. Visual preparation binds the
observed context and transfers selected-only operands, encoding calculates the
positive projection, and the later producer allocates one acquisition iff positive
children exist. Body production consumes the actual contextual selected capability
and allocates one acquisition iff a selected signal group exists. Multiple visual
children or multiple views of one body signal do not allocate extra acquisitions.
Both main causal fixtures now use these outputs and their actual selection IDs.

The shared perceived-trial-context validator rejects mismatched experience, event,
observer, instant, supporting observation and producer version. It admits exact
plain-data fields and detaches accepted context. It is a structural source join,
not an independent authenticity certificate. The actual source adapter remains the
producer; no new provenance graph or truth handle is introduced.

[Current affected regression](GA_FORMATION_EVIDENCE_REGRESSION_REV1.json):169 tests
in25 active-source files PASS. [Current adversarial review](FORMATION_EVIDENCE_REVIEW_REV2.json):
16 distinct injected faults detected by named witnesses. Removing the outer selected
map deletion alone remains blocked by the independently consumed inner capability;
that defended substitution is recorded separately and is not counted as a detected
fault. Earlier reports describe their earlier source versions.

The missing-coordinate case remains a supplied projection control: the actual first
scene profile has complete positions. Actual capacity-zero, known-zero and unresolved
role cases execute separately. Component tests do not pass scheduler rollback, PRJ,
public source authentication, save validation or corpus promotion. No permanent
allocation or frozen corpus/model byte changed.

Next: finish the remaining public consumer/feedback/owner registrations, exact model
calibration definitions, and whole-graph admission before the separate numeric gate.
