# Bounded Campaign2 factory qualification

2026-09-09. **Agent adversarial self-review verdict: QUALIFIED for the original
bounded no-RNG factory profiles.** The user authorized independent work and review;
this is not attributed to an external planning agent. It is not Campaign2 completion.

## Exact scope

The reviewed factory implements the accepted first-model profile and its trace
successor: rules/campaign2-bounded-bridge/0.1-candidate and
rules/campaign2-bounded-bridge/0.2-candidate, with campaign2-registry/0.1-candidate and
campaign2-parameters/0.1-candidate. Permanent allocations and frozen declaration
packets are unchanged. Generic PRJ/IDN, roster, multiple-variable and family-scope
controls retain their explicit public-profile exclusions. They do not enlarge the
factory's accepted input language.

Previously qualified probe, carriage and memory successors retain their separate
contracts, packets, research verdicts and persistence scopes. In particular the
memory prefix-replay path is not reclassified as the original no-runtime restore
validation path. PERSIST-I remains DEFERRED / NOT PASSED; a real production RNG
consumer requires its extension and qualification before use.

## Work-package disposition

| Gate | Disposition and evidence basis |
|---|---|
| FCT-1 | PASS: fixed record/union codecs, raw decoder negatives, full descriptor inventory and closed model intake. Structural qualification does not imply domain truth. |
| FCT-2 | PASS: accepted whole-current VAL; closed CONTENT/domain-role compilation, all three reference positions and finite independent CONTENT/role/declaration comparisons. No callback or public identity override. |
| FCT-3 | PASS in recorded generic/public scopes: PRJ/IDN/WRT ordering and isolation; current REG review; exact ADAPT topology/domain/definition checks. Named family-scope exclusion and REG-C future body obligation remain explicit. |
| FCT-4 | PASS for bounded bridge/EVID/ADAPT execution: current inherited review and ADAPT composition map, actual source/child admission, inhabited outputs, route isolation, common frozen batch, exact writes and full failure-stage rollback. Fixed pulse is not general recognition or appraisal. |
| FCT-5 | PASS for original facade: detached intake, immutable receiving model, committed work bound, exact original pending-source set and accepted bounded no-RNG restore preparation. No original initial-state handoff or archival output re-admission. |
| FCT-6 | PASS for the finite comparison and mutation program: independent CONTENT, roles, REG arithmetic and gate arithmetic; assertion-based lineage/authority/rollback controls; complete current source/report inventory. No independent general character implementation or universal bigint proof is claimed. |

FCT-A/B/D/E pass the precise scopes in CAMPAIGN2_FCT_QUALIFICATION_CROSSWALK.md.
FCT-C retains its externally accepted generic-positive/public-exclusion disposition.
FCT-F passes the fixed bridge profile: public callback exclusion composes with actual
accepted OBS/SEM operations and six fixed-declaration bridge substitutions, including
hidden-count-dependent pulse, truth provenance, omitted observation, missing/duplicate
support and shifted time. Shared Campaign1 semantic conformance runs unchanged in
the fresh-source suite; this does not claim the pulse exercises its full vocabulary.

## Inherited dispositions

**Current EVID and ADAPT are QUALIFIED in the mapped finite scopes.** EVID-A..T are
accounted for in CAMPAIGN2_INHERITED_CURRENT_REVIEW.md; ADAPT main controls, AC-A..L,
AD-D1..15, AD-E1..13 and AD-F1..7 in CAMPAIGN2_ADAPT_COMPOSITION_REVIEW.md and the
individual accepted/self-reviewed receipts. Whole current VAL, bounded PERSIST-A..H
and PHEN-ADAPT keep their previously accepted verdicts.

The EVID no-state theorem is a capability/composition argument plus executed named
negative controls, not a fabricated belief/value-state fixture. DecisionExpression
is not an admitted evidence producer in these profiles. New body, belief, value,
DecisionExpression or other producer/state semantics must bring the corresponding
positive intervention and exclusion controls before their own qualification. The
frozen negative epistemic requirements are not waived by absence of those families.

## Final evidence

- Fresh source: **102 files / 721 tests PASS** after all current production corrections
  and the seven-case stage suite. TypeScript PASS.
- Application build PASS using the repository's React plugin and explicit Vite
  configuration; this builds the present shell, not a completed character UI.
- Reference boundary PASS;166 preservation fingerprints PASS. No historical source
  is modified, imported or promoted. Existing reference tests are not newly claimed
  rerun by this fresh-source result.
- CAMPAIGN2_CURRENT_EVIDENCE_INVENTORY.json verifies18 selected current report
  fingerprints against source and records71 production files. The final magnitude
  assay was refreshed after the REG key guard changed adaptationDomains.
- Earlier failed fixtures, stale-anchor runner attempts and historical proof reports
  remain distinct from passing results. No old digest, model or report is aliased
  to a changed byte sequence.

## Limits and next work

This receipt permits authoritative use of the already implemented bounded profiles;
it does not add an API, turn test interception into caller authority, or enable new
psychological transitions. The implementation corrections enforce frozen contracts
and require no new model/profile/allocation version.

Campaign2 remains OPEN under the Campaign Plan. The next work is the complete thin
topology specification, starting with ORD-001 and the belief frontier, then the
preserved motives/reasons/dice/DecisionExpression/identity pipeline and independent
intent/attempt/outcome path. CAMPAIGN2_REMAINING_TOPOLOGY_WORKPLAN.md records those
dependencies. No unsupported canonical seam may be implemented as a shortcut to
the campaign completion gate.
