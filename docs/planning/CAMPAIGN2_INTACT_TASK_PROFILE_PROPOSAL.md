# First generated task-cycle profile proposal

Status: DRAFT; research scene and finite packaging constraints only. No model digest,
RulesVersion, allocation or phenomenon PASS is created. Prior frozen task/prediction
models remain byte-identical. This replaces no accepted scene.

## One complete causal scene

Use the same one observer/qualified character and admitted regulatory fixture domains.
Initially adopt both declared concrete tasks and their respective procedural bindings.
The new profile proposes taskA desired[4,6], taskB[5,7], both active from1, deadlines10
and11 respectively. These longer windows require a new model; do not edit the frozen
task model's existing5/6 windows. The desired intervals overlap. The earlier sketch's
"incompatible intervals" is not a requirement of this witness: different lower bounds
already generate distinct concern while both tasks remain genuinely adopted.

Both instructions are zero-argument fixture protocols, requesting1 and2 contacts.
The model declares their procedural association; it does not assert either action will
satisfy a task. Initial displacementD=-50 is the already admitted control value. All
other retained adaptation state starts under the predecessor's absent/zero semantics.
Prediction and identity start absent. Workspace capacity3 admits both tasks and the
one shared forecast slot. Other cognition definitions are the declared shared calibrations.

Original input schedule:

| Time | Original opportunity | Required causal purpose |
|---|---|---|
|1|diagnostic probe110|Generate safe measurement0, M1, memory/prediction application140.|
|2|deliberation40|Generate the first actual workspace→appraisal→concern→motive→options→reasons→choice→intent→plan→attempt→outcome.|
|3|deliberation40|Read actual retained identity from the previous choice; generate another choice and outcome.|
|4|deliberation40|Generate the third history-bearing choice; no authored psychological intermediate.|
|5|diagnostic probe110|Read the actual adapted regulatory state; generate the next safe measurement and update its forecast140.|
|6|deliberation40|Read that changed forecast and actual earned identity; test later reason activation and arbitration.|

Private task deadlines10/11 remain scheduled under the accepted lifecycle bootstrap.
The predecessor's generated prediction readouts may also occur; they are not original
deliberation input and cannot author a workspace conclusion. Complete event work must
include these overlaps, e.g. the original40 opportunity and generated40 read at2/6.
The workspace reads the retained prior prediction itself through its accepted accessor.

Each permitted choice completes1 or2 actual contacts. Thus the first three completed
choices yield3..6 and the fourth yields a total4..8. This stays below the existing
tolerance/load maximum10, without clipping or widening. There are no original authored
exposure controls in this profile. Every count comes from actual generated execution.

At5 the completed-contact totaln gives the proposed regulatory readingn/10; together
with the first0 observation its exact retained mean is n/20. This arithmetic must be
proved using the actual compiled probe and ADAPT rules, not asserted from the scene
table. Interference=false changes the actual count and may keep the reading0; it
does not alter the previous voluntary intent or expression.

## Calibration proposal, not a frozen result

Research candidate: raw task base1/10; concern gain1; reason activation37/100;
die thresholds1/5,2/5,3/5,4/5,1; standing/context modifier unit1 and cap3;
identityK=1/10; ThetaRoll=1/2; ThetaPlayer=1/2. The current historical direct-standing
exploration found later mode migration for32 sampled histories. This justifies testing
the configuration; it does not freeze these constants or prove a canonical witness.

Commit the first profile's fixed seed independently of whether its cosmetic choice
sequence is attractive. An intended boundary witness must execute under that seed
with the new accepted RNG. If it fails, report the failure and revise/calibrate with
an explicit new research record rather than silently searching until a trace passes.
No requirement that canonical new RNG reproduces the old hash's selected actions.

## Required interventions and isolation

* Prediction unavailable versus retained: Unknown is not forecast0; no direct current
  truth read replaces the missing forecast.
* Workspace capacity and task access: omitted context cannot contribute motives,
  origins or reasons through a hidden task-list scan.
* Concern disabled: preserve the adopted base and plans, while removing that modifier.
* Base disabled: preserve available options but produce no active reasons or chosen
  intent, with no RNG, expression, attempt or automatic-adaptation branch.
* Plan unavailable: live tasks still generate context/pressure, but no executable
  options appear through an implicit default.
* Identity read disabled at the later decision: keep its retained history byte-identical;
  compare the changed activation/mode to the ordinary path. This is a source ablation,
  not erasing already-earned biography.
* Execution permission disabled: identical frozen choice context/intent precedes the
  different outcome; perceived consequences obey the observer permission boundary.
* Equal task criteria: equal semantic meanings yield NoExpressedChannel rather than
  awarding fidelity to whichever protocol happened to win.
* Task retirement: at exact deadline no workspace eligibility or new task source;
  preserve prior plan/history and do not resurrect it from identity or forecast.

Only one cognitive opportunity per instant is admitted. The profile forbids an original
physical probe on an instant containing a cognitive opportunity capable of execution;
separate1/5 versus2/3/4/6 times make ordering unambiguous. General coexistence remains
excluded here even though the symbolic input grammar could represent it. No schema
or predecessor source rule is globally narrowed.

## Completion claims this scene must earn

All state/read/write/producer/occurrence/trace boundaries must close before allocation
and activation. Executed tests must demonstrate genuine generated psychology, the
retained reason/dice/identity mechanism, independent world interference, safe outcome
carriage and distinct automatic/character-learning routes. Full-prefix persistence,
random-address rollback, current PERSIST-I qualification and counterfactual inspection
must cover the joined route. Existing no-RNG and bounded task PASS receipts are inputs,
not an aggregate PASS for this new profile.

This free fixture-choice scene does not settle general fear/cost/coercion-sensitive
DEC-001 or every Campaign3 phenomenon. Any Campaign2 completion verdict must state
that scope and reconcile the still-open register decisions against the exact slice.
It cannot call a hand-built reason context or hypothetical later mean an intact event.
