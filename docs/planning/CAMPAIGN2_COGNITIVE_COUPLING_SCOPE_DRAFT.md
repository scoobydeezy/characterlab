# First cognitive coupling scope

Status: DRAFT scope choice before allocation. This resolves the earlier optional
language in arbitration/persistence drafts for the first twenty-one model recipes.

The first cognitive ordered-input profile excludes comparison maps. Its actual draw
EffectiveKey is always existing NaturalRandomKey112. Generic CoupledRandomKey113,
ComparisonDrawMap116 and ComparisonDrawKey117 remain accepted substrate controls,
not newly admitted original input or emitted draw data in this profile.

Allocate no RandomComparisonRoleId or ComparisonDrawKeyId family for an unused hook.
Earlier symbolic inventory mentions of RandomComparisonRoleId are deferred, not
part of the current allocation request. Only new purpose and subject-role vocabulary
is needed for natural addressed draws. This keeps every proposed production identity
family tied to an actual current consumer/output.

Counterfactual tests must demonstrate equal complete natural addresses at each draw
they couple. The later standing-read ablation is designed for this: its previous
history remains identical, while the later source omits only the standing read/signal.
At that boundary the reason binding identifies corresponding tasks independently of
which nuclei remain active. The actual test must verify equality; design intent is
not a substitute. A no-choice side has no corresponding draw to fabricate.

Execution/observer interventions can compare the already frozen choice before their
downstream effects diverge. They do not claim equal future roots after branches have
different occurrence counts. A study needing those later matches must first define
and qualify a coupling-input successor; it cannot smuggle a comparison map into the
closed input list or change the random key after a draw.

The completed first profile's PERSIST-I claim will therefore be natural-address,
complete-prefix scope, with the accepted substrate's independent coupling tests
preserved. No coupled-runtime or map-persistence PASS is inferred. This is an explicit
bounded scope, not a reduction or retirement of SUB-009's comparison-map capability.
