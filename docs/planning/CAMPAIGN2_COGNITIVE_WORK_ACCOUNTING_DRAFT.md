# First cognitive profile work accounting

Status: DRAFT; derived proposed graph counts, not executed runtime controls. This
must be checked against the final materialized registrations before model freeze.

## Chosen, observed protocol instant

The new scheduled stages execute exactly once each:

    Workspace Appraisal Concern Motive Candidates RawSignals Reasons Arbitration
    Intent Expression Plan Attempt Execution ActualFactBridge Qualification
    IdentityApplication

These are16 events. The existing one matching RegulatoryExposure consumer adds one
ADAPT140 event. The permitted observation/SEM chain adds five events120..124, and
EVID adds its two130 events. Thus the chosen observed branch has24 same-instant events.
The ordinary first-model exposure rules are exactly four, verified in the actual
firstModelCandidate builder; practice is a distinct unmatched basis. There is one
dispatch plus four evaluation outputs, not one event per evaluation.

The14 new semantic outputs each allocate one runtime ordinal. ActualFactBridge adds
existing307 (one), ADAPT adds dispatch+four evaluations (five), and observation/SEM/
EVID adds four (observation, experience, evaluation, learning evidence). Thus the
branch allocates24 runtime ordinals. Tracking/binding/classification in this empty
scalar binding profile allocate no additional domain occurrences. The200 truth view
uses the existing execution occurrence, so there is no extra truth ordinal.

All23 non-original branch events are generated at this same instant. Identity
qualification rejection does not alter these counts: its application remains one
required no-write event. Completed count0 also preserves all counts and rule resolution.
Neither case is permission to omit a branch or inflate observer precision.

## No chosen intent and observer suppression

NoOptions/NoActiveReasons stops after the first eight events/outputs/ordinals. It
generates seven children and consumes no RNG. It does not schedule a dummy intent,
qualification, attempt or automatic-adaptation event.

For the new protocol bridge only, choose the thinner observer-suppression policy:
no observation/SEM/EVID events or runtime occurrence padding. A chosen suppressed
branch therefore has17 same-instant events,16 generated children and20 ordinals.
This is an explicit successor policy; it does not alter the predecessor probe's
accepted padding. Suppressed and observed protocol histories are not claimed to
have equal later runtime IDs. Comparisons needing such identity alignment must
establish natural equality at the specific boundary or use an admitted coupling map;
they cannot silently compare unrelated random faces.

Suppression is an observation-access intervention, not a missing always-present
measurement. No reservation occurs, and source/experience closure is genuinely empty.
The actual execution and count-bearing307 are still produced independently.

## Joined maximum per instant

At most one predecessor memory recall and one prediction readout can be due from
the previous instant's sole probe. Inspection of memoryExecution shows recall at20,
while predictionExecution fixes its read at40. These are two events and at most two
additional runtime ordinals, including the predecessor's reserved absent read slots.
They are not additional physical roots or supplied psychological inputs.

At most two task deadlines add two more same-instant events and no runtime output
ordinals. Therefore the proposed maximum is28 same-instant events and26 runtime
ordinals for a chosen observed instant with both carried reads and both deadlines.
This is a structural ceiling; a first-profile fixture may not reach it because both
deadline-expired tasks are ineligible at40. The final audit must distinguish a loose
valid ceiling from an actually inhabited boundary witness. A live remaining task plus
one deadline is an available tighter counterexample for stage integration.

Applying that eligibility invariant tightens the profile bound to27: a chosen branch
can coexist with at most one of the two task deadlines. With both deadlines, neither
task is eligible and the branch stops after arbitration. A named deadline-overlap
control with windows[1,2) and[1,3), a probe at1 and deliberation at2 can inhabit27:
the first task expires, the second remains live, the prior probe supplies both reads,
and the one-option settled choice still schedules rejected qualification/application.
The main10/11 scene alone does not prove this boundary. The final profile must either
admit that exact control model or keep the looser ceiling explicitly uninhabited.

The predecessor probe has15 events across its generation graph, of which its two
future reads execute at T+1. Its same-instant13 plus at most two prior reads and two
deadlines yields the already qualified17 bound. Its14 generated children include those
two future events; do not add them again as same-instant work.

Each chosen roll has at most two reason draws and one tie draw. Each draw has at most
three SHA candidates. These at-most-nine candidate computations are arithmetic work,
not scheduler events, outputs or runtime ordinals. PMF controls enumerate at most144
elementary die combinations for the at-most-two nuclei.

No scheduler maxWork value is frozen here. The receiving numeric/model audit must
recompute all row counts, prove the chosen ceiling bounds every admitted control
combination, execute reachable near-boundary cases, and reject a too-small ceiling.
The historical default100 is not evidence of this accounting and must not conceal
an omitted edge. Allocation acceptance alone cannot pass any of these controls.
