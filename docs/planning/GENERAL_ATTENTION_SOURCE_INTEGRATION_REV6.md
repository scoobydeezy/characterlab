# General attention source integration — revision 6

2026-09-13. LOCAL DISPOSITION — no owner ruling required. GA remains OPEN.

The [bounded positional display](../formal/POSITION_DISPLAY_SOURCE_COMPONENT.md)
now produces one actual observed position per sampled frame. The attribution fixture
uses those returned positions for its separate start/end acquisitions. Hidden or
denied frames cannot form positional content, and hiding either endpoint prevents
grouping while the authored motion schedule remains unchanged. Display outputs carry
no motion classification, trial/target identity, body key or causal assertion.

[33 focused tests](POSITION_DISPLAY_COMPOSITION_TESTS_REV1.json) pass, including the
prior source/goal/focal consumer distinctions, five display controls and seven root
classification controls. [Seven position-source substitutions](POSITION_DISPLAY_SOURCE_REVIEW_REV1.json)
are detected, including two fixture bypasses that recover hidden positions directly
from the authored schedule. Those two are coordination evidence, not public admission.
Earlier mutation receipts retain their exact historical source fingerprints.

The [additive root descriptor](GA_ADDITIVE_ROOT_DESCRIPTOR_REV1.md) preserves existing
measurement and task roots while permitting new physical leaves under their existing
logical families. [Six source substitutions](ADDITIVE_FAMILY_ROOT_REVIEW_REV1.json)
are detected. An actual StateAuthorityRegistry test denies the old memory owner's
write to a new root despite shared logical classification. Classification itself
grants no reads or writes; public descriptor/state/authority joins remain OPEN.

The current body pipeline field inventory is [revision3](GA_BODY_PIPELINE_CARRIER_REV3.md),
including optional actually perceived context. It has ten records and ten structural
fault controls. This corrects the prior body-only shape without adding sample content
to a recall cue or routing cue admission through acquisition selection.

TypeScript validation passes. No numeric allocation, frozen schema, runtime factory,
corpus commitment or historical implementation changed in this pass. The new source
is a controlled component, not general visual perception or the final public source.
The fixture still uses local JSON views, supplied subject/selected-evidence premises,
and panel-only experience support for its original formations. It does not yet prove
combined panel/visual/body observation binding. That exact source join, canonical
carrier/owner registration, qualified result delivery and complete-prefix persistence
remain the next closure work. The expanded tests do not upgrade those OPEN gates.
