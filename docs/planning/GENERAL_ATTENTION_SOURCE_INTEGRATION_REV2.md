# General attention source integration — revision 2

2026-09-13. LOCAL DISPOSITIONS; no owner ruling required. GA remains OPEN.
These changes make the already authorized causal distinctions executable without
promoting supplied fixture admission to public qualification.

## Corrections and current evidence

1. Context is read from the [neutral panel source](../formal/TRIAL_PANEL_SOURCE_COMPONENT.md).
   Permission, visibility and exact sampling time gate its glyph/stage observation.
   No stroke/control, delivery, result or target identity enters the display output.
   Four [source faults](TRIAL_PANEL_SOURCE_REVIEW_REV2.json) are detected.
2. The inspected spatial-source proposal stores one position per observation. The controlled
   fixture forms separate start/end positional acquisitions instead of encoding a
   whole motion in one retained view. The [pair consumer](../formal/RETAINED_POSITION_PAIR_ATTRIBUTION_COMPONENT.md)
   reuses the old exact attribution/use kernel. Its [eleven current-source faults](RETAINED_POSITION_PAIR_ATTRIBUTION_REVIEW_REV1.json)
   include five pair-specific faults and the six prior consumer protections.
3. [Two-position context grouping](../formal/RETAINED_POSITION_TRIAL_GROUPING_COMPONENT.md)
   supplies actual operand bindings. Observation times order endpoints; carrier and
   occurrence order do not. The focal source key is explicit before evaluation,
   and the focal target is its end-position acquisition, not both positions.
4. [Historical-result handoff](FOCAL_HISTORICAL_HANDOFF_CHECKPOINT.md) executes an
   actual scheduler delivery across goal withdrawal, rollback and trusted continuation.
   It remains a separate scheduler fixture with supplied Supported attribution.
5. The old formation owner implements recency-only retention and strips newer metadata.
   It remains a control. The [significant formation owner](../formal/SIGNIFICANT_FORMATION_SETTLEMENT_COMPONENT.md)
   preserves prior bytes/use/directions, creates fresh uncredited children, runs
   significance-first retention and derives protocol loss from its actual result.
   [17 owner/protocol tests](SIGNIFICANT_FORMATION_SETTLEMENT_TESTS_REV1.json) pass.

The source composition has four trials × four acquisitions =16; one later competitor
is the seventeenth admitted formation source. Its local source bound is17. This is
fixture construction, not a permanent public model parameter allocation. Body history
is continuous, with explicit preparation replenishment. Each trial has Before, two
Motion observations and After. Three elapsed decay units are offset by delivery13
for stroke and3 for control, preserving observed20..21 versus30..31. The final
consequence is64, goal withdrawal70, recall71, focal credit72, competitor formation73,
supplied qualified competitor use74 and retention75. Grouping binds16 retained operands.

SC-A/B preserve the significance/shared-protection distinction and concern-only
counterfactual. SC-C/F remove all or one panel cue without changing body/motion inputs,
preventing grouping. SC-D prevents body recall capacity bypass. SC-E permutes retained
access. SC-G compares stroke-coupled versus independently scheduled hidden delivery
with identical observed evidence: both produce the same Supported judgment and
retention. The source laws differ under a motion intervention, so this preserves a
controlled false-attribution possibility, not authoritative causal truth.

[47 pair/source/scheduler tests](POSITION_PAIR_SOURCE_COMPOSITION_TESTS_REV2.json)
pass before the metadata-preserving formation substitution; [40 formation/source/state
tests](SIGNIFICANT_FORMATION_SOURCE_COMPOSITION_TESTS_REV1.json) pass after it. Earlier
receipts retain their earlier scopes. TypeScript, reference-import boundaries and
tracked whitespace checks pass. The earlier broad suite's1,827 PASS is historical
regression evidence, not relabeled as a run of these later source changes.

## Open public gates

The panel and body sources are controlled components. Public observation, positional
role/selection admission, PRJ/IDN joins, focal/consequence/result authentication,
ordinary memory typed carriers, owner declarations and adversarial save loading
remain open. Position values are fixture-produced observations; a passing retained
consumer does not qualify a public visual source. Event read admission and the
competitor's qualified use remain supplied premises. The source composition's
historical qualification is a local value; the separate scheduler fixture does not
turn that composition into one public committed causal chain.

No public factory, numeric allocation, frozen model or corpus changed. Next: consolidate
exact source companions and surviving-child carriers, then close producer/result and
state-owner registration before allocation. No architectural choice is pending.
