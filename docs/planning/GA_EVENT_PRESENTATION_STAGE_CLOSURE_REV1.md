# Explicit event presentation stage closure

2026-09-13. LOCAL DISPOSITION — no owner ruling required.

Select the explicitly requested event-winner presentation path as a bounded MEC-010
control. Preserve unpresented recall as the matched comparator. This implements the
already accepted historical retrieval/presentation mechanism without making every
rank evaluation, body baseline, cue or cognitive use a presentation.

A governed GeneralEventPresentationPlan value contains TargetOriginal (the existing
ordered-input address, not an identity). Its enclosing DefinitionId1027 is supplied by
the existing registry envelope. The compiler admits at most one plan for an original,
requires that the target requests event recall, and binds it into that original's
exact event-recollection publisher registration. No plan means no presentation branch.
This is immutable model packaging, not a runtime user command or hidden clock branch.

The actual event-recollection publisher alone schedules event-presentation-dispatch
at130, only if the plan exists and at least one actual winner was published. The
existing PresentationInput carries the exact emitted recollections and observer.
Dispatch authenticates actual parent/output membership before PRJ/IDN, then schedules
event-presentation-owner at140 with the same admitted payload. Empty publication
schedules neither stage; body publication never reaches either stage. Both stages
are in route/character-learning and allocate no occurrence or output record.

The owner reads/writes only GeneralPresentationState, folds its whole batch against
B0 and validates actual event acquisition membership. It uses the validated ordinary
memory result only to reconcile complete loss. It changes no episode bytes, graph,
use bit or significance direction and cannot change the already completed current
rank evaluation. No140 output or child emission is allowed.

Each requested presentation branch adds at most two scheduled events regardless of
winner count. Existing32-winner and32-history bounds still apply. This subtotal is
not a whole-model work ceiling. GeneralEventPresentationPlan must enter the final
record inventory before separate allocation review.

Component evidence exists: recollection-production/0.1-candidate publishes actual
winners with private one-use binding; takeEventPresentation snapshots only their
actual occurrence/acquisition pairs. The observed association fixture explicitly
compares publication-only with this handoff and actual history settlement. Public
original/parent authentication, exact schemas, transitions and persistence remain OPEN.
