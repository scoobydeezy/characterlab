# Reliance history findings - 2026-09-24

LOCAL DISPOSITION. RO-C3-014/016/019/020 preserve these findings and boundaries.

Six existing frozen rel-dimensions models express the new learning horizon.
No production module, model content/parameters/registry, codec, allocation or
contract is amended. The experiment adds fixtures, tests and public comparisons.

The first test receipt has6 passes and1 default5-second timeout in the multi-model
comparison. No scientific assertion failure was reported there. The first plan
was frozen before that test process finished and was never publicly executed.
RELIANCE_HISTORY_PRESERVATION_REV1.json preserves the old test/harness and receipts.
The successor gives that comparison60 seconds and changes harness receipt paths
only. All7 tests then pass. Original plan artifacts can be resolved through the
preservation manifest's originalPath mapping; frozen production bytes are unchanged.
This is a harness runtime limit, not evidence of model failure or a new learning law.

Fulfilled then failed commitments give retained fulfillment estimates1 then1/2;
reverse order gives0 then1/2. The inherited mean intentionally converges at equal
counts. This does not qualify recency, asymmetric betrayal, forgetting or repair.
A mixed known estimate1/2 and no estimate both produce an unbiased distribution1/2.
Their evidence/state remain different: equality of one consumer cannot license
collapsing unknown into neutral belief.

A denied occurrence creates no history. An admitted incident without commitment
opportunity can still enter general dyadic history but contributes no promise sample.
No opportunity is not failure. The source fixes own participation and independently
observed task/assistance/threat fields. It supplies no ordinary chosen promise,
normative commitment recognition, enforcement or reciprocal interaction.

SplitLenses keeps prospective commitment reliance stable under separately manipulated
task evidence; SingleScore and PersonOnly do not. PersonOnly also misses promise-only
differences. These are results for the deliberately separated commitment/task classes,
not a claim that competence can never inform practical reliance in richer contexts.

The consumer is an existing prospective entrust-versus-retain reason/dice distribution.
It does not sample an action or execute delegation. No general interpersonal trust,
attachment, affection, verified reliability probability or promise semantics follows.
Prior REL_DIMENSIONS and REL_ATTRIBUTION failures, models and closures stay preserved.
