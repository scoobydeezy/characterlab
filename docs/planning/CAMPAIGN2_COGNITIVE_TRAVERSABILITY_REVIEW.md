# Traversability review before cognitive allocation

Status: internal adversarial review; no completion verdict.

The Campaign Plan's completion gate requires a complete causal path without a
hand-authored psychological intermediate. The proposed original DeliberationOpportunity
contains only ObserverId and agenda, not a forecast, motive, option, reason or choice.
It can legitimately schedule cognition, but its mere presence proves no traversability.

The first profile must trace the real retained prediction read back through the prior
probe's M1 and observation. That state dependency is a causal edge even though the
later opportunity is an independently scheduled root rather than a generated child of
the probe. Scheduler parenthood and retained-state causation are different relations;
the complete trace must show both. The phenomenon is not literally one scheduler event:
its distinct phases already execute as multiple events under accepted ordering.

The test may not reset state between probe and cognition, splice an exported361 into
an input, patch identity between choices, supply selected options in an opportunity,
or replace later execution counts with authored facts. It must run one actual profile
from S0 through the whole prefix and compare the read operands and source IDs to the
earlier committed writes. Save/restore must retain the same causal history.

The diagnostic readout366 is not the workspace's source in this proposal. The workspace
performs its own declared prediction access. A coincident generated readout is useful
inspection but cannot stand in for that actual read or receive a fabricated parent
edge. The original40 opportunity and generated40 read remain separately identified.

After the choice, the complete generated branch includes frozen intent/expression,
plan, attempt, actual execution, admitted307, observer-safe consequence SEM/EVID and
independently owned consolidation. The next probe reads the actual resulting adaptation
state and its safe measurement changes the retained prediction used by later cognition.
Identity uses actual prior qualified choices. This closes the repeated loop through
state, rather than pretending all operands were produced in one same-instant chain.

No ORD-001 current-lane write/read is needed: prediction changes at140 and cognition
reads only at a later instant. This is a scoped omission of immediate-current belief
application, not a general ordering decision. The register must retain ORD-001 OPEN
and state why it is not relevant to this bounded completion slice.

DEC-001 likewise remains a broader P1 obligation if only free fixture-choice qualification
is implemented. The Campaign2 completion review must explicitly reconcile the bounded
qualifier with the retained pipeline and preserve general cost/fear/coercion qualification
and Campaign3 phenomenon obligations. It cannot silently mark the entire DEC decision
closed because one action sequence acquires a scalar. A relevant unresolved P0 or an
uncontracted used edge still blocks completion regardless of successful traces.
