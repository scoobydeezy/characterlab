# COMMIT implementation findings and limits

2026-09-21. Preservation record, not an independent verdict. Local disposition;
no architecture-owner question arose. The first frozen public cohort passes its
27 focused tests and registered lifecycle/social matrix; no failed model/test
cohort required replacement. Earlier failed cohorts from other seams are untouched.

## Earned identity is held fixed, not replaced by an authored trait
The prelude executes actual reasons, addressed choice, frozen expression and
qualification, then appends the accepted quantized contribution to identity414.
All four models share that exact contribution and every subsequent probe retains
the identical history. K1/100 and modifier unit1/10 deliberately expose strong
standing+3. That is a receiving calibration, not a claim about learning speed.
Probe decisions are Auto when a live option exists; no further identity updates
occur. The experiment tests persistent identity versus changing motive existence,
not new general identity reinforcement, coercion or biography laws.

## Lifecycle is causal rather than retroactive
Command source30 schedules the owner transition at140. The decision at40..80 and
truthful communication at110 use the prior committed instance status. Cancellation
therefore first removes pressure on the next instant. A recipient can correctly
observe Active before cancellation and later hold a stale Active belief; the
observer must receive a retirement message to update it. The implementation does
not silently synchronize belief to private state when retirement commits.

## Occurrence opacity in the observer inspection view
Private actor decisions can allocate different numbers of research occurrences.
Observer probe968's own allocator ordinal is not knowledge of a commitment. Its
inspection projection is the tuple(kind,at,observer,known claims), omitting that
research occurrence ID. This conservative projection was implemented before public
testing. Actual observation/evidence support occurrences remain intact for admitted
messages; no authoritative state is rewritten or fabricated to force equality.
Closed output rosters reject unknown/private original records in observer views.

## Controls and retained reopening conditions
CoreNeed is the declared fixed recurring-drive proxy, using a separate Need referent
through the common reason receiver even before any commitment exists. It is not a
fresh qualification of embodied physiology or every possible Need formulation.
Immortal keeps created instances effective after retirement. ReusedId explicitly
reactivates A/version3 rather than creating B/version1; baseline A is terminal.
The distinctions are behavioral/source-identity findings, not label comparisons.

Communication is truthful, controlled and identity-establishing. Instance-specific
LastObservation extends the bounded SOCIAL precedent; source trust, deception,
hearsay, uncertain identity, partial fulfillment, conflicting beneficiaries,
delegation, recurring-series identity and lifecycle consequences remain open under
RO-C3-014/020. No general memory or social synchronization law is earned. The
remaining BODY ownership, BIO and Brief-family debts remain separately active.
