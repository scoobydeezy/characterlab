# General attention calibration definitions

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Twelve symbolic definition
records complete the missing parameter shapes; exact model instances remain OPEN.

Reuse DefinitionId1027 through the existing governed registry envelope. These value
records do not copy that identity or create a separate parameter registry. Selector
policy reuses519, including its already frozen EqualPriority and Unlimited algorithms.
Unlimited can select three eligible units without expanding the capacity enum0..2.

Separate spatial allocation, encoding budget, association update, event access, body
recall, episodic retention, graph retention, feedback switches and run bounds. This
keeps similarly named decay/capacity parameters in their actual owning mechanisms.
Body recall gains no graph/frequency parameters, and episodic retention gains no
physiological quantity or exchange rate.

Encoding keeps the accepted bounded homogeneous prior3/10 and role factors. Public
recipes use floor1/5 or1 and threshold1/4. The new calibrated component entry now
executes that floor; the old convenience entry keeps its original fixed parameters.
Actual three-unit sparse/dense tests distinguish historical shared at the small floor,
while independent and retired-flat remain floor/footprint null controls for a fixed
unit. Retired-flat assigns1; historical hybrid may sum above1. Neither formula is
silently replaced by a conserved resource law.

The proposed graph carrier has30 nodes/rows, derived from ten sweeps times three
files. Use a30-node/870-directed-edge public ceiling, not the math kernel's32-node
ceiling. Graph loss still targets directed positive edges weakest first and removes
structurally empty nodes. Memory budgets remain independent child-slot counts and
read prior committed protection. Whole-source enrollment is separately bounded at32.

Definition-purpose bindings in the machine packet are required, but do not yet
constitute concrete registry entries, exact stage wrappers or model identity bytes.
Every parameter must resolve to the declared record kind/version and be compared
under a matched recipe. No permanent record/namespace/member allocation is made.

The final inspection found no body gate limits in GeneralSourceProfile. The packet
therefore includes GeneralBodySelectionCalibration and an immutable
ObservationExecutionPolicy. ObservationUsePlan carries its DefinitionId through
the existing source transport. The compiler checks purpose-specific references and
selects the corresponding exact registration; it cannot choose capacity from a
hard-coded instant. Body recall and goal-baseline recall have independent definition
slots. GeneralGoalQualificationCalibration preserves the both-directions intact law
and its deterioration-only comparator. Existing component schedule validation still
consumes its old five-field projection, after the new outer policy binding is checked.
