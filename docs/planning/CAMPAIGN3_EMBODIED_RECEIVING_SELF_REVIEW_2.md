# EMB receiving whole-shape review

2026-09-10. Primary-agent adversarial review under the user's autonomous instruction.
**Internal whole symbolic shape acceptance, bounded receiving experiment only.**
Formal disposition and executing versions are in EMBODIED_RECEIVING_SHAPE_ACCEPTANCE.
Numeric allocation, exact model freeze and runtime qualification remain separate gates.
EMB-M..O and ER-A..R are FROZEN, NOT PASSED. No external review is claimed.

## Corrections made before acceptance

The two-source joins now allocate only payloads whose actual constituent outputs
already exist. A six-variant abstract run with the actual scheduler confirms the70/80
ordering,17/22/26-event branches and work25 rollback. It does not authenticate semantic
sources; the actual binding/occurrence mutants remain implementation obligations.

The successor explicitly names pressure0.2 and embodied-task-source for changed
outgoing edges. Old terminal/body and intact/task contracts are not widened by an
implementation convenience. Pressure mathematics, current SEM support, PRJ/IDN,
genuine task adoption and exact task source transformations remain dependencies.

The dormant task359 reference was initially described too broadly as contact-domain
prediction. Actual inspection rejected that interpretation: the qualified record is
measurement-prediction/0.2-candidate, channel/regulatory-diagnostic-probe, diagnostic
unit, max64. The inheritance receipt identifies exact canonical declaration bytes
and matching character/task content. Those are copied as dormant model data, with the
new body bound to that same qualified subject. Forecast access remains disabled.
No contact-count forecast, finite-interval cast or body efficacy meaning is inferred.

Independent schema checking corrected StatePathPattern to149/1 and the identity family
name to DefinitionId/1027. Inventoryrev3 is current; earlier drafts are not numeric
inputs. The live codec registry verifies all16 inherited names/versions and the accepted
table verifies all14 identity-family aliases. No new occurrence family is necessary.

The new immutable prospective instruction root requires262 and its StateMapKey role.
Two initially proposed1031/1032 members had no consumer in this profile and were removed.
Vocabularyrev2 has49 actual proposed members, not51. The root does not pretend to be
procedural competence or a fabricated commitment. Its initial adoption is a declared
fixture premise; no learning writer or authored efficacy estimate is introduced.

## Receiving distinctions and competing model

The reference preserves task and body grounds on the same395, but consolidates two
descriptions of the same body ground using the actual shared safe observation basis.
Its task source is the real adopted/lifecycle branch. The exact arithmetic exploration
shows a choice-probability difference, not merely a different number of trace rows:
221/256 versus1/2 for independent-ground deletion;1/2 versus2/3 for duplicate stacking.

A criticism of relying only on invalid-source mutants was legitimate. The thirteen-model
proposal therefore includes an explicitly committed no-coverage receiving control. It
has its own exact version, declared sum-then-bound semantics and truthful corresponding
trace validation, while preserving sources/grounds/roles. It can run publicly and fail
the duplication phenomenon. It is not falsely labeled reference conformant, and the
reference's validator does not preemptively reject its behavioral output. Actual base
and adoption interventions separately exercise deletion. One-die-per-description and
generic three-support aggregate cases retain explicit mathematical/component scope.

This supports a bounded future comparison; no broad cross-family normalization or
Need-ownership winner is declared now. Observer safety is a required architecture
boundary, so a hidden-body reader remains a forbidden shortcut, not an eligible
character model whose good fit could overrule that boundary.

## Ownership, expression and absence of downstream learning

Body instructions depend only on actual perceived pressure and explicit adoption.
They can be wrong about what to try. World permission is confined to the separate
execution adapter; the contact protocol has no fuel effect. An outcome cannot become
a relief estimate or retroactively qualify a choice. Generated478 remains prohibited.

Every task deadline executes even if no choice or no task adoption exists. It retires
the actual task status while preserving both kinds of instruction context. All new
cognitive stages have NoStateWrites; only accepted body and commitment owners write.
No pressure cache, belief, identity, reward or independent provenance store is created.

Mixed intent, frozen per-ground expression, plan, attempt and outcome remain distinct.
Mixed expression does not claim task-only428 qualification or create429/identity state.
The already qualified task feedback and ADAPT/EVID bridges remain mandatory preserved
controls. This receiving fixture omits their mixed successors deliberately. These
missing paths prevent a broad BODY/learning/identity conclusion; they do not erase its
explicit EMB-M..O shared-choice/knowledge/duplication distinctions. Any later claim about
learned relief, mixed identity development or general body regulation must supply them.

## Final shape checks and limits

The packet specifies31 new records,110 ordered fields, two closed tagged unions,
16 exact stage alternatives, ten new output kinds,19 scalar roles, four direct
collection-identity checks and an independent StateMapKey role. Conditional field,
reference-kind, collection completeness, source equality and role/ground relations
are mandatory refinements beyond primitive VAL. Existing recursive roles remain in
force for nested schemas. Registry presence never authenticates an original/source.

The exact finite recipes, timing/initial adoption grammar, one-shot joins, positive
resolution instant, four-draw limit, borrowed occurrence families, exact trace owner
and whole-prefix RNG reconstruction are specified. Implementation must use actual
restricted projections and real output/child association, not a data-only simulator.
All canonical models, source cases, sibling/role/trace substitutes, failure stages and
complete-prefix boundaries must execute before qualification. The earlier body and
task model bytes are frozen preservation inputs, never rewritten by this acceptance.

No genuine higher-authority decision is needed from the user to proceed within this
bounded accepted shape. Next: separate one-to-one allocation audit, materialize/review
the thirteen model recipes, implement, then adversarially qualify the actual public path.
