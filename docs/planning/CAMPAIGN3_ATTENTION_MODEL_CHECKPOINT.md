# Public ATTN exact model commitment checkpoint

2026-09-11. The [32-model cohort](campaign3-attention-model-rev1/FREEZE.json) is
**ACCEPTED AND FROZEN** by primary-agent independent image review under autonomous
authorization. This is model packaging closure, not runtime qualification.

All models contain323 schema descriptors and105 registry entries in the exact six-slot
recipe. There are27 channel triples at RolePriority/K1, plus all-visible-exact K0, K2,
equal/K1, unlimited/K0 and work8. Each admits the same seven scene originals. All224
specimen RunIdentities were independently recomputed with the empty initial state,
DueAt1 and an exact32-byte seed. The seed is committed; no RNG operation is implied.

The independent reader does not invoke the builder. Its
[review receipt](ATTENTION_MODEL_REVIEW_REV1.json) records65 checks,32 independent
ModelIdentity reconstructions and224 RunIdentity comparisons. Seven corrupted images
fail: channel override, substituted policy, wrong causal-role basis tag, truth as
consumer input, selector writes, absent scene-object registry kind and a missing
nested detection occurrence rule. This is image/declaration evidence; it does not
qualify a production compiler or public ingress rejection.

The actual three170 object records pass generic governed-content commitment. The
observer-only kind is registered through329/170; no character qualification is used.
All new scalar/map roles, collection ownership, occurrence mappings, state ownership,
input/producer/output stages and inherited descriptors/unions match the accepted
shape and allocation. Actual SEM model identifier payloads retain their existing
codec representation. In258, causal-role domain and basis kind both use their own
accepted tag1; basis kind is not CharacterEvidenceRef tag4.

Registry/parameter/numeric/rules version strings are the exact accepted ATTN labels.
ContentSchemaVersion remains content/0.2-candidate, and RandomAlgorithmVersion remains
rng/sha256-addressed-128-v1-candidate with no draws. Frozen bytes and their hashes are
copied exactly from the reviewed proposal; the original proposal remains unchanged.

The [first incomplete materialization](attention-model-review-rev1/INCOMPLETE.json)
is retained as history. A four-byte specimen seed failed the existing32-byte
RunIdentity check before that cohort completed. Revision2 corrects the fixture seed;
no seam mathematics, allocation or previously accepted artifact was changed. The
incomplete directory is not an eligible model cohort.

All453 prior EMB fingerprints remain unchanged. No active runtime code was added by
this checkpoint. AT2-A..N and declaration/admission execution controls remain FROZEN,
NOT PASSED. Whole ATTENTION corpus intake remains BLOCKED, and the residual-pool,
encoding, retrieval, affect-feedback and learning debts retain their recorded scope.

Next implement the closed declaration/content compiler and data-only public factory
against this exact cohort. Then execute both source branches, selected-only processing,
trace corruption, all-stage rollback, work8/9, complete-prefix restore and preservation.
