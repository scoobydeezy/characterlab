# Protocol consequence observation cut

Status: DRAFT. Version: protocol-consequence-observation/0.1-draft. No shape acceptance,
allocation or implementation. This is a required addition to the protocol source
proposal, not permission to weaken authored-fact-observation/0.1-candidate.

## Actual substrate finding

compileBridgeObservation accepts only the predecessor's raw point[1,1], precision1,
and one exact truth reference, then copies fields1..9 while clearing203 field10 and
changing field11 to the accepted fixed-pulse observation version. It must continue
rejecting0 or2. Generic compilePermittedEvidence emits a private raw truth reference;
publishing that candidate directly would violate the Campaign2 observer boundary.

The new protocol bridge therefore requires an explicit receiving transformation
version and exact source admission. It may reuse canonical200/201/203 and SEM stages,
but cannot reuse the predecessor's fixed-pulse acceptance predicate or version string.
The old observation version and its bytes remain frozen.

## Truth source and one identity

The authoritative ExecutionOutcome already has its one occurrence ID and complete
actual completed count. Construct a private BoundedEffectTruth/200 view with before0,
potential=completed, applied=completed, overflow0, after=completed, minimum0, maximum2,
empty EffectProvenance, and TruthRecordId equal to that exact ExecutionOutcome ID.

Here200 is an observation operand projection of the existing truth record, not a
second published truth occurrence. Allocate no ProtocolEffectTruthId, no1121 fixture
pulse alias and no second ordinal. Only ExecutionOutcome has an output occurrence
rule. The generic truth-reference field names the actual source truth record from
which this exact projection is derived. Validate every projected field against the
live source before observing it; an arbitrary200 with the same ID is not authentic.

The outcome's attached attempt carries requested count; it is not exposed by this
view. Post-interference completed count is the physical potential before bounding.
Thus all accepted bounded-effect identities hold without treating interference as
overflow, goal failure, reward or a competence estimate.

## Exact first channel and raw measurement

Declare one observer channel for this first profile with Polarity=Increase,
MeasurementMode=BoundedStateChange, precision1, AlwaysPresent, no visible provenance
slots and no experimental-control ID. Its UnitId is exactly1039 unit/fixture-pulse.
Observer/subject/modality/channel identities are complete committed values and must
satisfy their accepted roles. The profile's permission control can suppress observer
emission through an explicitly declared outer bridge branch; it cannot fabricate a
Missing observation where the AlwaysPresent channel promised a present value.

Call the actual generic observer compiler with the exact200 view, channel, one new
1115 ObservationId and current instant. Its required results are:

| Completed count | Permitted interval | Evidence kind |
|---:|---|---|
|0|[0,0]|Point|
|1|[1,1]|Point|
|2|[2,+unbounded)|LowerBound|

The upper endpoint saturates the bounded-state-change Increase channel. Do not turn
it into a point merely because the fixture source knows requested count≤2. Source
knowledge is not extra observer precision. All other raw identity/time/precision
fields must match the exact compiler operands. There are no perceived concept tokens.
The private raw result has exactly one source reference: the actual ExecutionOutcome ID.

## Authoritative observer projection

Copy203 fields1..9 exactly. Set SafeSourceReferences field10 to the empty list and
TransformationVersion field11 to the new protocol-consequence-observation version.
No other field may change. Stage/publish/cite only this projected value under the
one allocated ObservationId. The raw candidate and its execution truth reference
remain private and trace-side; they are never staged as a second observation.

The successor observation closure validator explicitly admits this new scoped version
with these exact rules. The general observation contract is unchanged. No safe-reference
alias to the outcome, attempt, intent, model permission or plan is admitted indirectly.
The observer cannot reconstruct a truth handle by following a permitted token because
this profile emits none. Trace retains the actual source/projection relation independently.

If observer emission is suppressed, the bridge must consume the declared padding/work
policy without creating an observation, experience reservation or EVID source. The
exact numeric padding policy remains part of model accounting, not inferred here.
Execution and automatic adaptation still occur independently of observer access.

For a present projected observation, retain the accepted conditional experience
reservation, supporting observation216, stages121..124, empty event-file/classification
collections, immutable SEM freeze and EVID130 ingress. Measurement reading/carriage
337 and M1 are not admitted from this new observation version: that separate route
continues to require its exact diagnostic source. The contact observation supports
the consequence experience without becoming a regulatory measurement by relabeling.

## Required PC-A..J, all NOT PASSED

Actual compiler outputs for0/1/2; endpoint censoring; wrong raw identity/time/version;
exact1..9 preservation; truth-reference removal; no second observation identity;
source alias/substituted count rejection; observer suppression with continuing ADAPT;
SEM reservation/ingress closure; full rollback and prefix replay. The first explicit
mutants are publishing the private raw candidate, turning2 into a point, using requested
count when blocked, and allocating a shadow truth occurrence. No existing pulse PASS
qualifies this new projection.
