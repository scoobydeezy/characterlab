# Intention forgetting implementation findings — 2026-09-26

LOCAL DISPOSITION. Counters1400/0, no new allocation. Composed component scope.

The accepted recall law uses top-K even at zero score. With the neutral competing
acquisition removed, a present nonmatching cue retrieves the sole retained intention.
The character acts at3, and fulfillment prevents the test's incorrectly expected
second action at5. INTENTION_ZERO_SCORE_FINDING_REV1.json and TESTS_REV2 preserve
14 passing tests plus that failed assumption and exact source. The correction changes
the expected action time, not recall mathematics. Do not add a positive-score cutoff
from this fixture or call a wrong cue equivalent to no recall in every memory pool.
Actual absent cue remains different: the kernel returns UnavailableCue without reads.

The first build needed explicit TypeScript annotations around a loop with changing
goal state. INTENTION_BUILD_FAILURE_REV1.json preserves the rejected source. An
initial test attempt was interrupted because repeated identical calls and rebuilding
invariant reason configuration made prefix checks unnecessarily slow. Its source
and interrupted status are preserved; no successful result is claimed for that attempt.
The final15 tests pass after annotation, local configuration reuse and nonredundant
prefix checks. No psychological candidate changed as a result of those corrections.

The first experiment completed54 runs/486 component prefixes, but its identity bound
source artifacts without directly committing the resolved inherited dice/content.
INTENTION_IDENTITY_FINDING_REV1.json preserves that first plan, outputs and source.
The corrected model commits exact task/actions/dice/calibration content as well as
artifacts. Fresh REV2 model/run/experiment/comparison identities precede reexecution;
all final and safe-view hashes and component-prefix hashes must match the prior
behavior. This corrects reproducibility evidence, not the model's psychological law.

RetainedCue versus NoLoss compares actual loss under capacity1 using existing
significance-first retention; empty protection means recency decides. PersistentAccess
recovers an uncued retained intention but cannot recover removed content. Neither
proves a universal capacity, decay, cue or importance law. All component source,
retention pressure and key identities are explicitly controlled. No new public
scheduler, source-authentication API, ownership registry or public save qualification.

This witness forgets the future action operand while retaining goal adoption. It
does not equate goal cancellation, goal expiry, current inaccessibility, unavailable
opportunity, failed physical execution and instruction loss. A reminder has no action
payload; both routeA/B witnesses prevent a hidden fixed-route reconstruction. There
is no erased-content recovery, learned association, protection credit or re-adoption.
RO005/006/007/012/014/019/020 preserve these distinctions and broader receiving,
retention, prospective-lifecycle and integration limits.021 history gate remains.
