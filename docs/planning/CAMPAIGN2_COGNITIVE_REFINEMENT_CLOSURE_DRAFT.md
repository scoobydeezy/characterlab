# Cognitive refinement and adversarial admission closure

Status: DRAFT. These are receiving predicates for the proposed field grammar, not
runtime passes. Primitive types and union tags alone do not establish any predicate
below. Every generated source must first pass its actual private parent binding.

## Source and collection predicates

| Surface | Required refinement beyond field types |
|---|---|
|Opportunity|Exact agenda,1000 observer; original40; positive signed time≤100; no supplied subject or intermediate; no coincident original probe.|
|Workspace definition|One exact prediction definition; capacity0..3; both access flags Boolean.|
|Workspace|Character only through266/IDN;0..2 distinct task keys in selected deadline/canonical order; task spec matches each key; forecast Known iff the admitted read returned361; capacity2's forecast slot is not donated.|
|Appraisal|Exactly one item per selected workspace task, same order and key; Unknown iff unavailable forecast; Within iff closed desired interval contains the mean; otherwise correct side and strictly positive exact distance.|
|Concern|Same task coverage/order; disabled or unknown produces Unavailable; otherwise Known(Gain*distance/10), including known0;0≤Gain≤1; no registry or truth-derived extra factor.|
|Motive context|Enabled: one exactP item per selected task in order,0<P≤1; disabled: empty; preserve exact concern in either case.|
|Candidates|0..2 candidates sorted by whole key; each has1..2 canonical origins;≤2 total origins; every origin is an actual selected retained plan binding; no repeated task origin; deduplicate whole actor/action tuple.|
|Raw signals|≤6; canonical set with unique complete raw key; exactly the admitted nonzero source for each origin/role; all keys match candidates; no omitted generating source when enabled; strengths/bases recomputed from admitted operands.|
|Evidence basis|≤64 atoms; unique complete atom keys; exact unit weights in this profile; observation arm only361 support; qualification arm only matching retained history; zero weights omitted; no dereference.|
|Meaning map|Exactly the candidate-key domain, including zero values; b(sumP+concern over actual origins); no standing, role-bounding, floor, probability, outcome or winner term.|
|Reason context|0..2 nuclei in canonical full-key order; precisely those passing nonzero base then inclusive activation; exact role/sign partitions, bounds, relevance, die/modifiers and normalized positive discrete mass.|
|Resolution|Exact source/time; NoOptions iff candidates empty; NoActiveReasons iff candidates nonempty and nuclei empty; otherwise Chosen with exact probability domain, ranking, derived scalars, mode and authenticated draws.|
|Intent/expression|Intent only actual Chosen; exact nested source; channel list0..1 in canonical order; touched channel retained even at zero alignment; expression has no outcome or later-state operand.|
|Qualification|One actual expression; exact frozen profile scope; ZeroAuthorship tested first; then zero/absent meaning; otherwise weight=authorship>0 and nonzero e=alignment*weight.|
|Plan/attempt/execution|Plan count from selected action1or2; attempt exactly that plan; completed=count iff permitted, otherwise0; one execution ID owns both bridge sources.|
|Identity history|At most one key;1..64 nonzero contributions per present value; strictly increasing positive times; unique qualification and decision refs; ordered quantized fold; append only actual eligible evidence; empty S0.|

Exactly-one output rules cover empty workspace, empty candidates and no-choice
resolution. They do not license invented missing children: chosen branches require
the full intent/expression/attempt and qualification/application path. Rejected
qualification is a valid result; an unauthenticated expression is an admission failure.

## Arithmetic and transcript refinements

All role strengths lie strictly inside[-1,1] after b; base rawP may equal1. Concern
intensity is0..1 because the accepted prediction/criterion range is0..10. Nonidentity
pressure is in[0,1), alignment in(-1,1), weight in(0,1] when eligible, and e in(-1,1)
excluding0. Generic history's weaker[-1,1] bound does not authorize fabricated S0.

Each active die is4/6/8/10/12, modifiers are integers within their declared cap≤3.
ExactScoreDistribution has nonempty positive rational masses summing to1; support
must equal the actual signed dice expression, not merely an arbitrary normalized map.
CoverageResult fields must reproduce raw magnitude, overlap,1-overlap and their exact
product. Results use fixed role order MotiveGenerating, StandingDisposition,
SituationalEvidence; within each role positive partition precedes negative, each
with descending magnitude/canonical source ordering. Empty partitions emit no rows.
This list ordering carries no semantic weight.

Natural draw EffectiveKey112 must contain exactly LocalAddress110. DrawIndex is0;
root is the already reserved current resolution; purpose and actor/action/task
bindings equal the authenticated nucleus, or actor alone for a tie. No comparison
map, extra binding or alternate root is admitted. Candidate attempts have consecutive
indices starting0, at most3, with exact128-bit values and rejection/fallback flags
matching the accepted mapper. Span/limit/result must reproduce that transcript and
the run-seed derivation. A plausible face alone is insufficient.

Quantization operations retain scale1000000 and exact ties-to-even Input→Integer→Output.
An n-entry history fold produces2n operations in contribution order, Support before
Opposition. Raw read and eligible application can together produce at most256 such
operations on a successful instant. Rejected application produces none. Trace logs
are provisional until the whole instant commits; no failed65-entry candidate trace
is published as successful evidence.

## Component-positive / public-exclusion scope

The actual first task source emits at most one raw contribution per role per nucleus.
Consequently nontrivial multi-source aggregate coverage cannot be exercised honestly
through that public source. TRC4's {a},{b},{a,b}, unequal weighted bases, sign partitions
and subset residual require tests of the real pure consolidation component with its
explicit component domain, paired with public rejection of forged extra raw sources.
Do not widen task-source admission just to manufacture multiple sources in a partition.
Shared support across distinct task nuclei remains the explicitly retained historical
behavior; it is not silently pooled across tasks to obtain a nicer result.

Likewise Avoid is a real component branch, while this public adopted-task source has
positive generatingP. Integer modifiers, each die size, Auto/Quiet/Player modes,
equal-meaning rejection, absent plans and earned standing are included in public
recipes and must execute publicly. Component tests cannot substitute for those
reachable witnesses. Freeze this split in the eventual qualification crosswalk.

## Required adversarial substitutions

Before whole runtime qualification, substitute independently: caller-authored subject;
truth-backed appraisal; missing forecast treated as zero; donated reserved slot;
base ablation deleting options; plan alias yielding duplicate lottery tickets;
new appraisal IDs as evidence; duplicate empty-basis source; pairwise-max coverage;
modifier resurrecting zero base; omitted active floor; Avoid sign applied only to die;
standing in nonidentity meaning; rawI replaced by I*alignment; all-zero random fallback;
winner-only probabilities; wrong top-two conflict pair; sampled face with omitted raw
transcript; qualification based on execution success; independently rounded increments;
same qualification applied twice; identity write under ADAPT authority; skipped
prepared140 sealing; missing rejected application; post-draw failure poisoning retry;
protocol truth reference escaping203; completed2 treated as exact point; and a restore
projection that omits retained decision roots. Each test must name its owning boundary
and show detection by current production source when that source exists.

These proposed controls preserve the architecture's distinct evidence, belief,
workspace, appraisal, concern, motive, option, reason, decision, intent, expression,
attempt, outcome and learning owners. They do not claim general planning, appraisal
theory, fear/cost semantics, task efficacy learning or whole DEC-001 closure.
