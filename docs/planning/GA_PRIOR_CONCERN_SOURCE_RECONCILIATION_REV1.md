# Prior concern source and calendar reconciliation

2026-09-13. LOCAL DISPOSITION — no owner ruling required.

`GA_PRIOR_CONCERN_PUBLIC_SOURCE_TESTS_REV3.json` passes three tests using the actual
frozen Campaign-2 workspace-two model. The public factory accepts its exact existing
cohort bytes, runs the probe at1, commits prediction at140, then runs cognition at2.
No prediction record is supplied by the test.

The results constrain the successor rather than qualifying a GA join:

* Initial D=-50 produces committed prediction0. The later actual one-task concern
  supports the existing narrow projection and exact2/5 intensity component result.
* D=0 is represented by an absent displacement entry. An explicit zero entry is
  invalid, as required by existing ADAPT. The actual prediction5 commits, but the
  already-active frozen tasks are fulfilled by measurement settlement. The later
  workspace has no selected task; this is **not** a known-zero concern witness.
* D=50 produces committed prediction10. The full frozen choice continuation at2
  subsequently rejects with `ADAPTATION_REFERENCE_OUT_OF_RANGE`. Its state, outputs
  and trace roll back to the committed prefix. A transient concern from that failed
  instant cannot be exported as committed feedback.

The original baseline cognitive model also selects two tasks, which the narrow
single-task concern projection correctly rejects. The test selects the already
frozen workspace-two cohort; it does not weaken the projection or modify frozen bytes.
Failed development reports preserve these discoveries and the corrected assertion
of the public ADAPT error code.

## Successor disposition

Retain the separate terminal workspace/appraisal/concern adapter already proposed.
Do not truncate the frozen choice adapter or export records from a failed instant.
Place the actual probe at1, task activation and concern at2, and receiving observations
strictly later. Activating the task after the probe avoids treating the warmup sample
as fulfillment of an already-active task. Exact lifecycle/deadline admission and this
known-zero witness still need execution under the new model.

The source recipe proposal preserves intended probe points0/5/10 using authored
R0=50 and initial D=-50/absent/+50, respectively. This is an explicit source-model
choice, not a claim that the old draft's differently named R0 field or hypothetical
model bytes are identical. REG remains immutable authored reference; D remains
separate initial adaptation state. The public probe witness establishes these
points, not their whole later GA continuation.

Missing-glyph/position controls in the old32-recipe draft remain component controls
with first-source profile exclusion: the current admitted scene-item grammar requires
both fields. Do not label those controls materialized public models or infer missing
facets from hidden scene indices. Existing unknown-role and denied-observation cases
remain independently expressible. A later source extension would need its own exact
contract and tests, not an incidental optional-field change.

Whole GA registry/model composition, same-run future delivery authentication and
public encoding/access effects remain OPEN. No architectural escalation is needed:
these are source sequencing, profile-scope and existing boundary preservation choices.
