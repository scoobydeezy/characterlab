# EMB receiving adversarial review 1

2026-09-10. Primary-agent self-review; no external approval is claimed.
Disposition: DIRECTION RETAINED, WHOLE SHAPE WITHHELD pending registration, roles,
finite model, trace and persistence closure. No implementation or allocation yet.

## Challenges and corrections

1. **Future input at a sibling join.** Revision1 scheduled mixed raw80 from candidate70
   before the exact403 existed. Even though phase order could make the value available
   at execution, that was not a complete canonical child payload at allocation. Corrected
   revision2: mixed candidate70 is terminal; actual task raw80 allocates the mixed raw80
   child containing both already-produced sources. No placeholder/future output ID.
2. **Terminal old contracts.** The old EMB pressure transition is terminal and the old
   task candidate/raw transitions have different child closure. Reusing their full seam
   versions for the new graph would be false. The receiving closure must separately name
   `embodied-pressure/0.2-draft` (unchanged pressure mathematics, one70 child) and
   `embodied-task-source/0.1-draft` (accepted task source mathematics, explicitly narrower
   forecast/standing availability and new registered outgoing joins). Old0.1 pressure
   records/profile remain exact. New464's transformation version names the executing
   pressure0.2 candidate once accepted, not an alias of0.1. No record schema change is
   needed merely for that already explicit version field.
3. **Invented efficacy.** An adopted response instruction is legitimate procedural
   context but does not establish relief knowledge. The proposed contact protocol is
   allowed to be ineffective for fuel; execution does not acquire a fuel effect by name.
   ER-L proves world permission isolation and no world rescue of absent adoption. It
   must not be reported as an intervention on a learned fuel-efficacy estimate.
4. **Identity overclaim.** Reusing428/429 would silently qualify a different choice
   domain. Keep separate ground expressions, exact frozen choice context and no mixed
   identity application. MEC-018's qualified task loop is preserved as a control, not
   retired. This omission prevents a broad BODY/identity/learning-loop verdict; it does
   not invalidate EMB-M..O's bounded receiving contrasts. Cost/fear/intervention are
   absent representational paths, not inferred experienced zeros.
5. **Vacuous overlap comparison.** Two copies of the same canonical instruction ID are
   invalid set data. Use distinct adopted descriptions for the same pressure/action.
   Compare against a genuine task on the other action so duplication can change actual
   probabilities. Three catalog definitions and at most two adoptions suffice; no
   duplicate operational action definition or second option identity is required.
6. **Observable independent grounds.** A sole shared action would conceal the choice
   effect of deleting one reason. Include task+body on A against body on B. The actual
   exact analytical arbitration component gives221/256 for A, versus1/2 after deleting
   either A ground. A separate duplicated-body-A versus task-B scene gives1/2 under
   consolidation,2/3 under raw stacking and221/256 under one die per description.
   These are research arithmetic results, not public runtime qualification.
7. **Aggregate scope.** The single body observation cannot furnish public {a},{b},{a,b}
   evidence. Preserve the actual generic aggregate component control plus explicit
   singleton-profile exclusion; do not invent extra observation atoms or truth keys.
8. **Inherited schema error.** Inventoryrev1 mistakenly named StatePathPattern160/1.
   The accepted allocation table gives149/1. Inventoryrev2 corrects this; rev1 is
   historical drafting evidence, never an allocation input. Every inherited reference
   still requires independent name/number validation before acceptance.

## Proposed finite calibration has discriminating power

EMBODIED_RECEIVING_ARITHMETIC_REV1 uses the actual shared exact arbitration kernel.
Task pressure1/4; body pressure1/3; die cutoffs1/20,1/10,1/5,3/10,2/5;
activation threshold0; thetaRoll=thetaPlayer=1/10. Both source bases compile tod8.
Incorrectly stacking two body descriptions compiles tod12. The kernel's distributions
and fair-tie analytical results distinguish all three principal receiving alternatives.
Bodyp1/6,1/3,5/6 reachd6,d8,d12. Other admitted dice/mode branches need named public
recipes or an honest component-positive/profile-exclusion qualification.

No random sampling was used in this exploration. Random execution and address binding
remain frozen-test obligations. Mathematical option labels in the exploration are not
new canonical option records, permanent IDs or model inputs.

## Scope ruling for occurrence reuse (symbolic proposal)

The accepted table names candidate, raw, reason, resolution, intent, expression, plan,
attempt and outcome occurrence families generically; their payload is an unsigned
runtime ordinal. The earlier per-output table closes the old profile, not a rule that
every future representation of the same semantic output kind invents a namespace.
Propose explicit successor registration of the new records under those same families,
one runtime allocation per actual output. BodyResponseOptions and MixedCandidateContext
are different actual candidate outputs and therefore receive distinct ordinals. They
are not two IDs for one output. No old output/reference rule is changed or generalized.
The numeric review must confirm no new occurrence family was silently introduced.

## Remaining acceptance gates

The symbolic inventory has31 proposed records,110 fields and10 new output shapes.
It is not yet the whole typed closure: exact stage values, inherited domains, scalar
and collection roles, source/ground refinements, state grammar and model reference
closure remain open. Complete the stage table, exact read/write ownership and live
join obligations; close fixed-member vocabulary and no-new-identity reasoning; prove
finite workload including task deadlines and replenishment; specify complete-prefix
RNG restore and public model recipes. Then review the composed shape again before the
separate numeric and model gates. Earlier kernel qualification is unchanged.
