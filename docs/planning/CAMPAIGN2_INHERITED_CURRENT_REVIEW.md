# Current inherited REG / EVID qualification review

2026-09-09. Agent adversarial review under the user's independent-work authorization.
This corrects forward from CAMPAIGN2_INHERITED_VECTOR_CROSSWALK.md; its historical
remaining-work column is not rewritten. The accepted contracts remain
regulatory-reference/0.5-candidate, character-learning-evidence/0.5-candidate and
their admitted transition versions. This is a bounded review, not a new model or
a general character architecture qualification. Whole factory accounting follows.

## REG disposition

Current REG implementation is **QUALIFIED in the bounded no-body profile and named
generic component controls**. The selected-key guard correction is self-reviewed
in CAMPAIGN2_AUTONOMOUS_ADVERSARIAL_REVIEW.md. No new authored reference semantics,
state root, conversion or cache is introduced.

| Vectors | Evidence and exact scope |
|---|---|
| A | Repeated exact C,V,T queries; public anchor-only, rate-only and scale-only registry/model commitment changes in PersistenceDerivations. No change to other identity fields is inferred. |
| B/L | RegExactWitnesses uses the same compiled provider and retained D=0/10 histories; R0 remains80 and effective sums80/90. The reference provider receives D only as a validation operand and never retains it. |
| C | Source closure: REG owns only decoded immutable declarations and the accepted CONTENT qualifier; no body/state/trace capability is supplied. FCT public callback exclusion and qualified VAL closure protect that qualifier. The current profile has no materialized body family: no executed body-state intervention is claimed. Such a future profile must supply that positive intervention before qualification. |
| D | Permissive wire descriptor injects Unit into280; production decode rejects INVALID_CONFIGURATION before any materializeLinear call. Generic OBS units remain independent. |
| E | Real two-variable equal-scale/bounds component; selected-key/query guard rejects even D=0 before REG arithmetic. V1/V2 substitution and removed guard detected. Public one-variable profile exclusion is separate. No operand-side domain metadata exists. |
| F/G/H | Exact Scale1, [0,100], R0=80: -81/21/30 fail the named carrier; -80/-30/20 pass, including both endpoints. Public AD-E10 additionally checks adjacent candidates and no clipping. |
| I/J/K | Exact signed-time, repeated/permuted queries, authored remainder preservation, 80→81 at Int64.MaxValue and constant80 control. Same-clock create/save/restore and actual retained histories preserve provider and validation bytes. REG_PERSISTENCE_MUTATION_PROOF_REV2 compares252 cases with arithmetic independent of production TIME. |
| M | Nine frozen semantic-pass mutants plus family, domain, role declaration, role-before-local-resolution and inert-content-owner substitutions detected. Wrong-key guard and arithmetic mutants are separate reports. Public callback rejection remains separate from internal component substitutions. |
| N | Missing character, noncharacter, orphan, malformed anchor/remainder and endpoint negatives. Duplicate character keys cannot be encoded by cenc; this is structural rejection before REG, not a fictitious later REG carrier. Role/local-resolution dual defect proves the phase2 check owns precedence. |
| O/P | Real time-only retained D invalidity aborts before ingress/repair; early-validation omission detected. Repeated same-T queries across actual phase140 D writes preserve complete save/allocator data. AD-E12 owns the independent six-stage rollback matrix. |
| Q/R | Equal/unequal cross-variable parameter reuse rejects; multiple characters sharing one variable's parameter succeeds. Wrong-family key/value/governing anchors reject. A schema-admitted inert content copy remains non-owning; its erroneous recursive ownership interpretation is detected. |

REG-C's future body intervention is a retained conditional obligation, not a new
body contract or a relaxation of REG's no-state-read rule. Component qualification
does not make a two-variable model publicly supported. Finite arithmetic comparison
is not universal bigint verification.

## EVID review and remaining closure

The source-boundary inventory is now materially stronger. Final EVID disposition
still requires the last crosswalk below to be checked against current reports;
this section does not silently pass all EVID-A..T.

| Vectors | Current concrete evidence / scope |
|---|---|
| A/I/S | Both-stage zero/two-distinct/two-identical/wrong-schema exact output carriers; generated-child missing/duplicate/unbound checks; cardinality/occurrence/child mutations. Terminal L has required output even without a downstream child. |
| B/C/H | Forged/current-lane/injected work rejects before allocation; altered source bytes/observer/time/occurrence and parent relation reject; repeated producer occurrence with equal/unequal payload rejects; missing/wrong occurrence role and extraction tests. Trusted freeze adapter is an internal authority, not a public label assertion. |
| D/E/F | Unbound first-profile observer succeeds with zero reads. Generic roster A/B/absent invariance and accepted IDN projection rejection; separate later PRJ absent-source carrier. Distinct observers remain distinct across two shifted allocation ranges; no CharacterId is added to E/L. Top-level ObserverId injection rejects. |
| G/L | Paired safe-output invariance; Campaign1 producer-derived missing/false/continuity/segmentation evidence survives exact nested copies. Raw opaque extra fields reject at each inhabited record position in the tested carrier, including nested typed-identity records. This is not a claim to test every possible future schema. |
| J | Both-stage no-write precedence, including a real globally valid unchanged-value patch; no dummy authority. Exact source/output completion does not consume an execution on rejected completion. |
| K | Production EVID has only detached safe payload, allocated identity and stage selector. Eighteen named state/trace/parent/content/registry/ordinal/ancestry/success capability requests reject. Current profile has no belief/value materialization; it does not manufacture those states to claim an intervention. ORD-001 remains open. |
| M/P | Empty input/restore yields no reservation or E/L; zero-count admitted consequence still yields evidence. Generic no-bridge ADAPT fixture has phases110/140 and no E/L; actual fixed-profile both-route trace preserves phase130 evidence under phase140 state changes. The fixed pulse is not a general sensory-opportunity model. |
| N/O | Actual four EVID failure stages preserve full runtime structure; archived E/L native and disguised initial/pending work reject before construction; exact continued allocation and archive preservation. Accepted bounded persistence and separate-process continuation retain their own scope. |
| Q/R | Inhabited route closure269/270, closed registry/role/phase/event declarations; fixed output identity bijection preserves exact predecessor chain. Canonical declaration reordering preserves bytes/identity. Generic scheduler owns backward-phase and150 rejection; no new phase convention. |
| T | Exact immediate E predecessor and output embedding; raw/AAI routes cannot qualify. No DecisionExpression record is allocated/admitted by this profile. A future family requires its own source/registration and an explicit masquerade control; this review does not invent such a family. |

The bounded substitutions compare actual runtime results or exact rejection stages.
They are not an independent general EVID interpreter. No appraisal, expectation,
success inference, truth comparison, belief learning or identity update is introduced.

## Review provenance and preservation

Prior externally accepted ADAPT vectors and whole VAL/bounded persistence/phenomenon
verdicts remain unchanged. New self-review is identified as such. The first full
fresh-source run after the key guard passed101 files/712 tests; subsequent additions
have targeted passing runs and must not be attributed retroactively to that count.
Historical mutation reports remain available alongside their numbered refreshes.
Campaign2 remains OPEN for whole factory accounting and the full thin topology,
including its mandatory dice/identity port.

Subsequent final disposition: CAMPAIGN2_BOUNDED_FACTORY_QUALIFICATION.md accepts
the current EVID composition in the finite scopes above. Final fresh suite102/721,
TypeScript/build/boundary checks and166 preservation fingerprints PASS. Earlier
pending-review prose is checkpoint history; future-family exclusions remain live.
