# Absence-deficit findings and preserved corrections — 2026-09-26

LOCAL DISPOSITION. Counters1400/0. Read ABSENCE_DEFICIT_CONTRACT.md for scope.

The existing R0+D probe reveals an adapted reference. It does not compare that
reference to an independently maintained current physical condition. Therefore
neither displacement alone nor the preceding tolerance witness proves an absence
deficit. This increment adds that comparison as a world-only diagnostic component.

The first draft incorrectly borrowed R0=80 from BODY ownership's different control
model. The unchanged firstTraceModel actually declares50. This was caught by source
inspection before tests or identity freeze. ABSENCE_DEVELOPMENT_FINDING_REV1.json
and absence-development-rev1 preserve all original draft files. The corrected
harness checks the actual constant reference and zero rate before freezing identities.
No old public model or verdict was changed; illustrative constants are not portable
between models merely because both use the same regulatory contract.

The initial four scientific tests passed, but production build found TS7034/TS7005
in the test's unannotated states array. ABSENCE_BUILD_REV1.json preserves diagnostics
and hashes of every original frozen artifact in absence-development-rev2.
ABSENCE_PLAN_REV1.json remains unexecuted. An explicit Uint8Array[] annotation fixes
typing; no physical-law or production-source change. ABSENCE_PLAN_REV2.json freezes
the successor before qualification. Both test receipts are retained; only the
successor build and plan support closure. No favorable run was discarded.

For the unchanged acquired source, D progresses0,1,2,3,4. With current level50,
LinearGap response progresses0,1/10,1/5,3/10,2/5. ThresholdGap stays zero through
D2, then gives1/10,1/5. FixedReference stays zero despite acquired displacement.
With physical support4, the final acquired reference54 and current level54 match;
response vanishes without erasing D4. Naive absence has neither acquired displacement
nor excess deficit. Ordinary level48 gives LinearGap response1/5 even without
adaptation; a positive deficit alone is not evidence of withdrawal.

Other-leaf and unrelated-variable controls isolate D from tolerance, sensitization
and load. Spaced zero exposure preserves D; this is not a recovery experiment.
The support is an exogenous physical challenge operand, not a chosen dose or learned
expectation. No new sensor admits the computed gap, and no character consumes truth.

Three candidate laws and27 composed runs reuse four distinct public input/state
conditions.135 restore comparisons are repeated checks across component candidates,
not135 distinct public saves or27 new public source models. Preserved public ADAPT
boundary proofs remain prior evidence; this adds no receiving-horizon qualification.
General physiology, source specificity, natural decay/recovery, perceived distress,
craving and compensatory behavior remain RO008/010/011/015/019/020;021 stays mandatory.
