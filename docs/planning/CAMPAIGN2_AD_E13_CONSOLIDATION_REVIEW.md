# AD-E13 construction and runtime collision consolidation

2026-09-09. Propose AD-E13 PASS in the finite matrix below. Whole B and FCT remain
OPEN; no production implementation, model grammar or permanent allocation changed.

## Accepted checkpoint

AD-E4 and AD-E8 are ACCEPTED/PASS in their submitted bounded and split scopes.
The registers and ledgers record the ruling without promoting remaining vectors.
For evidence precision, E8's same-model zero-count exposure control has one dispatch
and four regulatory evaluations, five outputs total. It does not execute the fifth,
procedural rule. The review's prose saying five evaluated rules is not used to
change the fixture or its execution accounting.

## Frozen AD-E13 obligation

Same-consumer rules with overlapping Match domains and identical target recipes
must fail INVALID_CONFIGURATION at construction even when Gate or Step differs.
Both exposure and practice matches and every compatible key variant must be exercised.
Disjoint matches or distinct target recipes are legal at this boundary. Different
exposures targeting the same displacement key may still collide when both actually
execute in one instant; construction rejection cannot replace the runtime guard.

## Direct controls

`src/test/campaign2RuleOverlap.test.ts` uses separately committed declaration
specimens compiled by the actual bounded model compiler. The frozen artifacts are
not edited and no factory validation is bypassed.

| Cases | Result |
|---|---|
| Five leaf/key forms, duplicate Match and target recipe, same Step/Gate | Construction INVALID_CONFIGURATION: guaranteed overlapping rule target collision |
| Same five forms with different nonzero Step | Same construction failure |
| Same five forms with mixed Gate pair: original Always, duplicate FrozenBaseline reading the same target; identical Step | Same construction failure; the required ReadDomain remains valid |
| Disjoint exposure Match for each of the four regulatory key forms | Construction succeeds with six total rules |
| Original five distinct target recipes under the exposure/practice matches | Construction succeeds with five total rules |
| Two distinct exposures, same Character/V displacement key, same instant | Construction succeeds; runtime ADAPTATION_TARGET_COLLISION before any ContractReadProjection read |

The five forms are tolerance, sensitization, displacement, accumulated load and
procedural competence. The first four use Exposure matches; the fifth uses Practice.
The matrix is15 negatives +4 disjoint positives +1 original positive +1 runtime case
=21 tests. It is not an exhaustive product of every possible declaration.

The runtime case preserves public state, output, trace and clock and enters Failed.
An initial fixture assertion incorrectly expected Active status after the failure;
the corrected assertion requires Failed while preserving the other snapshot fields.
No production behavior was changed to satisfy that assertion. Full queue/allocator
rollback is separately evidenced by the existing batch controls and is not newly
claimed from this public snapshot.

## Current-source substitutions

[CAMPAIGN2_RULE_OVERLAP_PROOF.json](CAMPAIGN2_RULE_OVERLAP_PROOF.json), generated by
`scripts/prove-campaign2-rule-overlap.mjs`, records:

- Baseline21 tests PASS.
- Remove construction overlap check, effectively deferring rejection: DETECTED by
  the construction-negative tests, which never start a runtime.
- Reject identical target recipes regardless of disjoint Match: DETECTED by the
  disjoint-exposure positive tests.
- Remove runtime target-collision check: DETECTED by the distinct-exposure runtime
  collision case.

The runner requires the named test category to fail for each substitution; setup or
unhandled errors do not count as detection. Each in-memory replacement must match
exactly once. Construction/evaluator/test fingerprints are checked unchanged during
the assay. TypeScript PASS.

This is new source-sensitive evidence, not a relabeling of an earlier batch report.
Other historical reports still require deliberate refresh before whole B/FCT claims.

## Requested disposition

Accept AD-E13 for this bounded construction/runtime matrix. It exercises both Match
kinds, all five key forms, Gate/Step invariance, legal disjoint/distinct recipes and
the later runtime collision distinction. No broader match language, extra procedure
profile or universal matcher equivalence is claimed.

## Mixed-Gate clarification following the narrow withholding review

The review accepted the construction/runtime direction and all other named clauses,
withholding AD-E13 solely until literal Gate difference was made explicit. The
existing five `gate variation` cases already have the requested mixed pair:

| Operand | Original rule | Duplicate rule |
|---|---|---|
| Consumer | Same consumer | Same consumer |
| Match | M | M |
| Family / leaf / key derivation | T | T |
| Step | signed1 | signed1 |
| Gate | Always (tag1) | FrozenBaseline (tag2), compatible source at T |

`firstModelCandidate.ts` creates every original rule with Always and Step1.
In `campaign2RuleOverlap.test.ts`, `specimen(name,'gate')` builds a replacement
record for the local duplicate's field5 only. The original entry remains unchanged
in the entries array. The duplicate receives its new RuleId and replacement rule
when appended. The separate Step branch is not taken. Thus this is **Always versus
FrozenBaseline**, not two FrozenBaseline rules.

The gate source copies the same family/leaf/key derivation, so it adds no new required
read pattern. Both declaration validation and exact ReadDomain comparison precede
the overlap predicate. The tests require the exact guaranteed-overlap message and
INVALID_CONFIGURATION, not merely any configuration error.

On 2026-09-09 the existing proof's construction/evaluator/test fingerprints were
verified against current files. All five `gate variation` baseline rows are PASS
in `CAMPAIGN2_RULE_OVERLAP_PROOF.json`. No source, test, proof report or frozen artifact
was changed or rerun for this clarification. The ambiguous original table wording
is corrected above; the historical execution evidence is unchanged.

Requested disposition: accept the already-executed mixed-Gate witness as satisfying
the sole withheld clause. AD-E13 remains pending that final disposition; whole B/FCT
remain OPEN regardless.

## Final disposition received 2026-09-09

AD-E13 is ACCEPTED/PASS for the submitted finite construction/runtime matrix.
The mixed-Gate clarification closes the sole withheld clause; both Match kinds,
five key forms, Step/Gate invariance, legal distinct/disjoint targets and runtime
contingent collision controls are accepted. All three policy mutants were detected.
This forward receipt supersedes the pending disposition above without rewriting
the reviewed evidence. Whole B/FCT remain OPEN; AD-E4/E8 PASS and the accepted
VAL, bounded persistence and PHEN-ADAPT dispositions remain unchanged.

Keep whole B, AD-E3/7/9/10/11/12 and whole FCT OPEN. Retain AD-E4/E8 PASS, VAL
QUALIFIED, bounded no-RNG PERSIST-A..H PASS, conditional/deferred PERSIST-I and
PHEN-ADAPT PASS. No accepted scope is widened by this proposal.
