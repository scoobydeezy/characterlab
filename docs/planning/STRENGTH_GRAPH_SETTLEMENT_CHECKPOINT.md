# Strength graph scheduler checkpoint

2026-09-12. [Twenty combined tests pass](STRENGTH_GRAPH_SETTLEMENT_TESTS_REV2.json):
six scheduler/weakening cases, seven strength-priority cases and seven edge-plan cases.
Revision 1's nineteen-test receipt is preserved. The supplied
[split scope ruling](GENERAL_ATTENTION_GRAPH_WEAKENING_SCOPE_RESOLUTION.md) is recorded.

The fixture uses the real deterministic scheduler and adaptation-settlement/0.2-candidate
terminal adapter. One fixed coactivation input passes through actual EAM learning and
strength-graph-retention-component/0.1-candidate. Both terminal participants read B0;
one final result updates the graph and a separate peer counter. Reversing participant
emission order preserves final state. Zero capacity commits real complete graph loss.
A late finish failure restores state, queue, clock, allocators, committed outputs and
trace. The failed scheduler remains terminal under its existing contract.

Active quiescent snapshot continuation reproduces all committed surfaces after a later
learning/loss barrier. Reintroduced domain keys come from the fixed fixture input and
zero padding, not forgotten graph content. This is trusted in-memory continuation,
not canonical save/load, public source replay/idempotency or runtime package admission.

SS-E exercises 135 actual uniform-weakening combinations. Exact order remains strict;
quantized order is nonincreasing with ties allowed. This is separate from supplied-
operand selective reversal and does not close the conditional producer obligation.

## Public closure remains

The fixture does not implement PRJ/IDN, admitted CharacterLearningEvidence, real root
write capabilities, public graph record allocation, multi-source coactivation aggregation,
canonical persistence or model/corpus promotion. Phase130 only stages fixture work;
no public learning producer is inferred. Equal final state does not imply identical
event traces for differently ordered emissions.

The older ATTENTION_MEMORY_STATE_DECLARATIONS_REV1.md's all-three-leaves-presence and
membership coupling must not govern the successor: independently bounded episodic,
graph and presentation state cannot require equal membership. Retain that packet as
history, and revise the complete symbolic owner/read/state inventory before public
implementation. Graph-only and episodic-only membership, payload-free acquisition
history, retention fragmentation and body-kind coverage must all survive that revision.
Public persistence qualification follows accepted complete shape and numeric gates.
