# Composed general-attention logical topology

2026-09-13. LOCAL DISPOSITION — no owner ruling required. This composes the reviewed
stage slices, not the final public TransitionDefinition instances.

The63 named stages plus three independently bound goal-baseline recall stages yield
66 logical stage templates. The latter reuse body cue/rank/recollection contracts,
but bind GoalBaselineRecall rather than ordinary BodyRecall. They are required to
preserve the accepted independent capacities when both uses share a consequence.
They add no record, identity, mathematics or physiological source occurrence.

Conditional edges explicitly connect the source dispatchers to their requested uses,
the feedback alternatives to actual selected/cue parents, and actual recollection
publishers to joins/presentation. Join edges mean completion of the last required
parent with the others already completed, never three independent evaluations of one
join. Baseline recollection cannot become an ordinary recall/presentation branch.

The static audit proves immediate edges stay within declared phases, remain acyclic,
and never emerge from140. Future deliveries are separately marked; their strict later
instant requirement remains in the owning contract. Same-phase edges still require
actual causal scheduling and expected-input binding; this audit is not that proof.

Do not sum66 templates into a work ceiling. Some templates run twice for different
recall purposes; many are mutually exclusive, and inherited probe/task production
still contributes separately. Exact first-model originals, inherited producer closure,
registration multiplicities and pending deliveries must determine the whole bound.

Remaining gates: exact model recipes/instances, all public patterns/accessors and
producer/output associations, admitted-input capabilities, owner-composition hooks,
numeric review, model bytes, public rollback/replay, comparisons and corpus promotion.
General Attention remains OPEN. No architectural ruling is required by this graph.
