# Bounded public BODY/MULTISOURCE experiment — 2026-09-20

**Large checkpoint COMPLETE: the bounded public experiment is QUALIFIED, with
preservation and final decoder reexecution verified.** See `VER-C3-MULTI-002`.
**LOCAL DISPOSITION:** no architectural owner ruling is required. General Attention
remains complete in its earlier accepted bounded scope.

## Contract, identities and source boundary

`multisource-public/0.1-candidate` implements
[the accepted contract](../formal/MULTISOURCE_PUBLIC_CONTRACT.md) and
[allocation707..733](../formal/MULTISOURCE_PUBLIC_ALLOCATION_TABLE.json), with
namespace1149 and existing physical649/task373 owners. The two counters are currently
**733 highest / 0 allocated since VER-C3-MULTI-002**;27 were allocated in this increment.

[Successor images](campaign3-multisource-model-rev2/FREEZE.json) contain **43 distinct
ModelIdentities and 55 named model/S0/input scenarios**. Known-zero, no-task and no-body
controls change initial state, not model identity. The legacy `models:55` receipt
field counts scenario entries; [the explicit counting clarification](MULTISOURCE_COHORT_COUNT_CLARIFICATION_REV1.json)
preserves that distinction without rewriting an image or receipt.

The public factory accepts only frozen data. One governed character and observer have
two independently adopted tasks, two protocol actions, three controlled local reserves,
and at most four descriptions from selected safe observations. Task B produces the
actual competing option. Body descriptions require adopted instructions; the task
Situation requires an actually selected instruction even when task Base is disabled.
The registry binds instruction, action, criterion, channel, threshold and source.

Sensing, conditional experience freeze, signal selection, task production, family
assessment, raw receiving, reasons, arbitration, intent, expression, execution plan,
attempt and outcome run through separate registered scheduler stages. The inherited
task workspace/appraisal/concern/motive/candidate outputs remain explicit in their
source ancestry. No synthetic old receiving ancestor is used by the public path.
Only permitted safe samples enter character calculations. Denied channels authorize
no physical-anchor read. Original replenishment is an actual owned state transition.

## Experiments and discriminating results

[The main experiment](MULTISOURCE_PUBLIC_EXPERIMENT_REV1.json) executes55 scenarios
and restores all220 whole-instant prefixes, including initial prefixes. Seven
[matched interventions](MULTISOURCE_PUBLIC_INTERVENTIONS_REV1.json) freeze their
additional S0/original/seed bytes and pass complete-prefix restoration. Total:
**62 public runs / 227 prefix restorations**.

| Contrast | Measured result | Scope |
|---|---|---|
| Shared fact, separate motives | Body and task Situation contain the same actual observation support; commitment and body retain distinct grounds | Adopted commitment plus body criterion, not general Need |
| Independent facts | Separate A/B support; replenishing B changes task Situation1/2→0 while body remains1/2 | Independently controlled reserves, not independent sensors |
| Orphan modifier | GroundAggregate keeps body Base1/3 with/without task Situation; FamilyNormalized changes1/5→1/3 when the orphan is removed | Task A has no Base; task B supplies the actual opponent |
| Behavioral consequence of orphan | FamilyNormalized probability for action two changes1/2→3/5 after orphan removal | Exact analytical arbitration, not sampled win frequency |
| Collective descriptions | Third effective contribution0 under aggregate versus1/6 under pairwise; bounded body totals5/11 versus1/2 | Singleton/singleton/hull support; declared equal-magnitude ordering |
| Calibration can hide difference | Both collective models produce probability349/384 with the adopted task present | Intermediate values remain decisive evidence |
| Matched duplicate DescriptionDice | Identical raw bytes; two total nuclei versus three; probability3/5 versus73/80 | Task A adoption removed in both runs to obtain matched Base-only sources |
| Matched collective DescriptionDice | Identical raw bytes; two nuclei versus four; probability2/3 versus2511/2560 | Same controlled Base-only comparison |
| Hidden within-bin intervention | Levels21 and29 produce identical complete safe output bytes | Fixed shared-model bins; trace/state truth may differ |
| Availability, zero, capacity, adoption | Unavailable and known-zero remain distinct; capacity excludes all groups; absent adoption produces no body motive | No missing coverage is converted to a zero assessment |
| Execution failure | Requested action remains traceable; disabled execution returns actual count0 | No body-relief efficacy or new learning law |

GroundAggregate, GroundPairwise, GroundUncovered, FamilyNormalized and Base-only
DescriptionDice are separately committed competitors. FamilyNormalized is preserved
unchanged as the role-blind negative candidate. No role-aware repair is silently
substituted. The main DescriptionDice scenarios alone are not a fully matched raw
comparison because they omit Situation; the additional matched interventions above
establish exact raw-byte equality before comparing the dice unit.

Analytical distributions are the comparison measure. A shared seed does not guarantee
cross-model address equality when observation/description allocation differs. Polarity
interventions preserve signed source and receiver values even where the inherited
modifier quantization produces the same die result. Neither identical winners nor
identical probabilities erase a measured intermediate difference.

## Validation and preservation

- All current-lane stages, the final commit barrier, replenishment and deadline faults
  leave state, queue, allocators, outputs, trace and the committed RNG ledger unchanged.
  Each fault test verifies that its intended boundary was actually reached.
- Public checks reject edited model data, the rejected first cohort, generated
  originals, duplicate opportunities, mismatched task adoption, future anchors,
  undeclared state and run-data getters. A locally decodable save with an erased RNG
  ledger fails exact prefix reexecution. Conformance observers receive detached event
  copies and cannot alter the actual generated parent through those copies.
- The overlap-domain controls match the preserved coverage kernel, dice bands,
  modifiers, relevance and distributions. Public source/receiver ancestry is tested
  independently of those synthetic mathematical controls.
- [Final focused validation](MULTISOURCE_FINAL_FOCUSED_TESTS_REV2.json):22 tests pass.
  [Reference preservation](MULTISOURCE_REFERENCE_TESTS_REV1.json):328 tests pass.
  Production build and reference-boundary checks pass. The full run recorded2,235
  passes and one failure, the codec regression added during that run. Its failing
  receipt is preserved; the subsequent22-test focused run covers all five affected
  new files and passes that regression. [Validation closure](MULTISOURCE_VALIDATION_CLOSURE_REV1.json)
  accounts for all2,236 active tests using their latest results, without claiming that
  the original full-suite process passed. No predecessor source or reference code changed.
  [Final current-code reexecution](MULTISOURCE_FINAL_REEXECUTION_REV1.json) reproduces
  all62 complete-save byte sequences after the decoder correction.

The original construction mismatch is preserved in
[MULTISOURCE_CONSTRUCTION_FINDING_REV1.json](MULTISOURCE_CONSTRUCTION_FINDING_REV1.json):
the copied body instruction named a different action. Revision1 images remain intact
and inadmissible; revision2 corrects the new registry copy. The codec audit is preserved
in [MULTISOURCE_CODEC_BOUNDARY_FINDING_REV1.json](MULTISOURCE_CODEC_BOUNDARY_FINDING_REV1.json):
new records may cross inherited validators only through explicitly open canonical-value
slots. This validation correction changes no model, input, output or formula bytes.

## Research disposition and remaining denominator

The immediate common-fact source and receiving experiment is discharged in this bounded
domain. Retain separate motive grounds, source-description identity, role/sign partitions,
selection/permission and encoding boundaries, and exact intermediate witnesses.
FamilyNormalized is not selected as the general baseline; DescriptionDice does not earn
an evidence-to-motive collapse. GroundAggregate remains a bounded candidate, not a
universal psychological law.

RO-C3-001 is now CONDITIONAL: the immediate public source/receiver debt is discharged,
while the broader receiving-law question is retained. Reopen it for a receiving reduction, role-aware
normalizer, new motive family, wider source domain, or renaming/order invariance claim.
General physiology, final Need ownership, additional source families, learned efficacy,
arbitrary identifier renaming and unrestricted horizons remain unqualified.

This is evidence against the shared/independent/duplicate clauses of PHEN-MULTI-001
1.0.0-draft in corpus0.29.0, digest
`5dc8a6f23afe0c75b4b7d9fd0bf93c65f67bd4892ec7ec0e9e4d85d3a05a172d`.
The corpus remains21 members. No corpus promotion, whole PHEN-MULTI-001 PASS,
whole Campaign3 PASS or general BODY reduction is inferred.
