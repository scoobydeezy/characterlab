# General attention additive root descriptor — revision 1

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic descriptor;
production numeric allocation and public compiler activation remain OPEN.

The old Campaign2StateFamilyRegistry284, definition285 and storage286 remain intact.
The successor's additive definition contains a set of AdditionalFamilyRoot records:
Family (existing StateFamilyId1031), RootStateTypeId (canonical unsigned), and
LeafFields (canonical map of existing StateLeafFamilyId1032 to positive field ordinal).
The root set is bounded by32; each map contains1..16 entries. The composite total of
base, additional and RuntimeProtocol roots is also at most32. Root and field ordinals
refer to the exact admitted schema inventory; they are not occurrence identities.
The enclosing registry uses the existing governed DefinitionId and schema/version
machinery. No new identity namespace, route override or content callback is needed.

The additional declaration is valid only for a family already in the base registry.
Existing measurement memory346 and task commitment373 keep their exact roots and
owners. General episodic content and event presentation history add distinct physical
roots under episodic-memory; adopted maintenance goals add a distinct physical root
under prospective-commitments. Associations retain their independently declared
family. Each leaf identity has one physical address within its logical family.
Formation governance uses its already proposed RuntimeProtocolStateDeclaration,
never AdditionalFamilyRoot. Neither classification grants read access or ownership.

The compiler must cross-check every root/field against actual StateModelDeclaration,
value and key grammars, removal rule and exact authority coverage. The additive
descriptor cannot widen an existing transition's authority, ReadDomain, accessor
dependencies or WritableStateFamilies. Missing, surplus, conflicting or overlapping
declarations reject the model. A family route is inherited from the base declaration;
it is not a second editable copy. Protocol roots must be disjoint and excluded from
cognitive direct and recursively derived reads. Public validation of these joins
remains OPEN; the trusted classification component does not claim to perform them.

The selected representation preserves the existing psychological distinctions and
frozen record schemas. A replacement multi-root base registry is a possible later
representation comparator, with no proposed behavioral difference. Reopen only if
the fixed additive inventory cannot express required disjoint authority/read scopes.
No historical mechanism is retired or imported; MEC-007/010 and the earlier memory
and association controls retain their recorded obligations.

[Seven component tests](ADDITIVE_FAMILY_ROOT_TESTS_REV2.json) and
[six source substitutions](ADDITIVE_FAMILY_ROOT_REVIEW_REV1.json) pass. The tests
include actual StateAuthorityRegistry denial of an old owner's write to a new root.
This is component and state-substrate composition evidence, not public registration,
save/restart, or General Attention qualification.
