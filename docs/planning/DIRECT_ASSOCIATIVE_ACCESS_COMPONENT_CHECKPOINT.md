# Direct and associative access component checkpoint

Follow-up: [six actual source faults detected](DIRECT_ASSOCIATIVE_ACCESS_REVIEW_REV1.json).
The [orphan-policy review](GENERAL_ATTENTION_GRAPH_ORPHAN_DECISION.md) adds four
component cases without changing this source or its original eight-test receipt.

2026-09-12. Implements
[direct-associative-access-component/0.1-candidate](../formal/DIRECT_ASSOCIATIVE_ACCESS_COMPONENT.md)
with the actual EAM exact solver and ranker. No public cue admission is qualified.

[Eight tests pass](DIRECT_ASSOCIATIVE_ACCESS_TESTS_REV1.json): matching isolated graph
membership does not alter direct contribution; beta0 contributes no learned spread;
a graph-only cue mediates activation to a real surviving target; absent-both cues
create no rows; input order is irrelevant; invalid domains reject; outputs are detached
immutable values. Graph-only rows do not manufacture episodic targets.

The reference factorization is exact learned spread a-b, direct cue on episodically
retained keys, and a single final rounding of their sum. Two numerical witnesses
protect ordering: quarter direct plus quarter learned at scale2 must yield half,
not separately rounded zero; an exact fifth cue with W=0 must yield zero learned
spread even when total quantization rounds down. No clipping is used to hide a
negative residual. Returning influence through positive-length graph cycles remains
learned spread under the existing solve.

Unit direct/spread coefficients are an explicit component reference choice, not an
accepted psychological weighting or public model parameter. Component strings,
supplied cue maps and32-key union limits do not establish producer authenticity,
graph capacity or canonical identity allocation. Working rows create no persistent
nodes, learned history or identity. Earlier EAM and graph-only access adapters remain
unchanged under their respective contracts.

Next: source-substitution review of this composition, then independently governed
graph node/edge lifecycle and orphan policy. Public cue matching, owner reads,
persistence and the acquired-protection comparison remain open. General Attention
is not completed by the direct/spread arithmetic.
