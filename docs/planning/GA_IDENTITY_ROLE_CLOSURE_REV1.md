# General attention identity-role closure — revision 1

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic role inventory,
not an allocated role table or public compiler qualification.

The five current carrier inventories contain66 records. Their scalar identity fields
use existing CanonicalRecordField positions after allocation. Optional scalar fields
retain the same role when present. An identity inside a collection requires the fixed
compiler's element check; a scalar role must not be attached to the enclosing list,
set or map as if it were itself a TypedIdentifierValue. Composite record keys recurse
into their actual fields. PerceptualReferentId212 and PerceptualEventReferentId213
are records, not new namespace IDs. Their existing SEM role declarations remain
authoritative, as do nested227/461 records and the selected encoding evidence.

CharacterId requires namespace1002 and the existing character-qualification validator.
This is a domain check, never a replacement for actual PRJ/IDN subject projection.
ObserverId uses1000 without a character validator. The GoalReferent scalar uses1002
with a proposed exact maintenance-goal qualification predicate; it is neither the
holder nor the immutable DefinitionId1027. Propose the fixed symbolic kind member
semantic-kind/bodily-maintenance-goal in1004 and predicate
validator/maintenance-goal-qualification in1021. These append to the successor's
closed kind/predicate set, leaving the old two-kind/two-predicate task compiler intact.

The goal predicate requires the actual declared content kind. The enclosing fixed
compiler additionally proves the bijection of goal content, its exact spec DefinitionId,
the authored-origin SemanticReferentId derived through1037/1038, and the holder's
CharacterId. Passing the generic predicate alone cannot prove that coupling. Goal
content/spec cardinality is bounded by16. No arbitrary caller predicate or resolver
is accepted. Satisfaction does not retire a maintenance goal.

AcquisitionOccurrenceId, InteroceptiveSignalId, GoalOutcomeAssessmentId,
RetainedAttributionResultId and general Recollection occurrence remain their already
proposed distinct symbolic families, unallocated. This pass introduces no additional
family. Existing Observation1115, Experience1106 and Selection1143 roles remain
provenance references with exact producer/version admission; none resolves an archive.

The machine crosswalk identifies direct scalar roles, collection element checks and
inherited record-role boundaries. It verifies field coverage in these66 records only.
It does not close the remaining visual wrapper inventory, state root map-key roles,
model cohort membership, source/result relations or runtime authentication. StateMapKey
roles are still required for actual scalar root keys; record-valued GoalKey and
QualifiedFormationSource keys must use recursive record checks, not a fabricated
scalar namespace. Protocol history remains excluded from cognitive read domains.
