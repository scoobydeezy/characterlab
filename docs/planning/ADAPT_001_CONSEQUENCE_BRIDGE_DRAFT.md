# ADAPT D — fixed consequence bridge

2026-09-06. **SHAPE ACCEPTED AS COMPOSED**, adaptation-input/0.31-candidate. Receiving recipe for D revision 4;
no implementation, allocation or proof pass. A/B/C remain internally shape-ready without redesign.

## Witness and inspected receiving shape

The common source R is the admitted InputOnly phase-110 actual-fact intervention. Besides its AAI
output, R produces an observable fixture pulse. The pulse means the lab intervention occurred;
it does not report contact count, completed repetitions, success, harm or acquired adaptation.
Zero/nonzero count changes the stipulated hidden exposure/practice only. Both runs include R and
the same pulse. This is a controlled simultaneous consequence of the intervention, not evidence
that the hidden contact itself was perceived.

Existing observation/0.1-candidate supplies BoundedEffectTruth (200), ObservationChannel (201),
PresentObservation (203) and compilePermittedEvidence. SEM supplies SupportingObservationId (216),
PreRecognitionSemanticExperience (227), assemblePreRecognitionExperience, and the Consequence
lane reservation/freeze/settlement operations in phaseOrdering.ts. Inspection confirms an experience
may contain supporting observations with empty event-file/binding/classification collections.
No fabricated event classification or instance recognition is required for this scalar witness.

## Exact committed fixture declaration

```
AuthoredFactConsequenceBridgeDefinition
    Channels                nonempty canonical map<ObservationChannelId, ObservationChannel>
```

One singleton: RegistryDefinitionId member definition/authored-fact-consequence-bridge,
RegistryKindId member registry/authored-fact-consequence-bridge, DefinitionVersion
adaptation-input/0.31-candidate and the record above. Required in the both-affected admitting fixture;
absence is permitted only in a separately declared adaptation-only fixture, which cannot claim
the both-affected control. No Boolean on the source payload selects a branch.

Map key equals embedded channel ID; each ObserverId occurs once; all identity fields have their
accepted observation/SEM roles. Complete channels are committed model data, not invented from R's
subject. Require PolarityId.Increase, MeasurementModeId.BoundedStateChange, precision 1/1,
MissingnessRuleId.AlwaysPresent, empty VisibleProvenanceSlotIds and absent ExperimentalControlId.
Modality, SubjectId and UnitId are the complete explicitly authored channel fields under the existing
observation control, unchanged across runs. This does not introduce or imply REG physical units.
No conversion, token vocabulary or interpretation of channel identifiers is added.

The fixed pulse uses exact rationals:

```
Before=0/1  PotentialEffect=1/1  Applied=1/1  Overflow=0/1
After=1/1   Minimum=0/1         Maximum=2/1
EffectProvenance.slots={}
```

This satisfies the accepted bounded-effect decomposition, has an interior After value and yields
the point interval [1,1]. A new symbolic FixtureConsequenceTruthId occurrence family occupies
type-200's existing typed TruthRecordId field; no existing scheduler/world-event identity is cast
into that role. Allocate once per R, after AAI's occurrence allocation. It remains truth-side only.
This is a bridge-local construction requirement, not a global PRJ RecordField role restriction
on BoundedEffectTruth.TruthRecordId. Other admitted producers retain their own governed identity
domains. F must not narrow the accepted type-200 field to this fixture's namespace.
The bridge introduces no WorldEventTruth (210), no claim of an event-file and no body-state mutation.

## Exact source and receiving lifecycle

```
FixtureConsequenceObservationInput
    Truth                   complete immutable BoundedEffectTruth (200)
    Channel                 complete immutable ObservationChannel (201)
```

R's closed source executor derives the pulse only from the fixed constants, model channels and
actual DueAt. It has no access to Basis count, AAI value/ID, RuleId, adaptation state or previous
outputs when constructing this branch. Staged source context checks that exact derivation.
Generate one event/fixture-consequence-observation child at phase 120, DueAt=R.DueAt, per channel
in canonical channel-ID order, carrying the exact input above. Its actual scheduler parent is R.
AAI's generated ADAPT child and these observation children have R as the same parent independently.
For deterministic event allocation, emit bridge children in that order before the shared extension
generates AAI consumer ingress; both runs use that same closed ordering.

At phase 120 require genuine staged R→bridge generation, exact source input equality and
assertTruthAvailableAtLaneEntry(Consequence,110). A seeded/lookalike observation event or unrelated
same-T source cannot substitute. Call compilePermittedEvidence with the exact truth/channel, a new
existing namespace-1115 ObservationId and T. The fixed inputs must produce the exact private raw PresentObservation candidate with
MeasurementInterval=[1,1], EvidenceKindId.Point, precision 1, no perceived concept tokens and the
single exact FixtureConsequenceTruthId reference, with exact observer/channel/subject/time and
observation/0.1-candidate version. Apply authored-fact-observation/0.1-candidate: copy fields 1..9
exactly, set SafeSourceReferences to list([]), and set TransformationVersion to that scoped version.
Only this projected value is staged/published/cited as the sole authoritative observation for the
one allocated ObservationId. The raw candidate remains private, non-staged and truth-side.
Reject unexpected raw/projection results; never fabricate a substitute measurement.

Then call admitObservationLane({observerId: channel.ObserverId, lane: Consequence, dueAt:T,
emitsCharacterAccessibleEvidence:true}) using the accepted allocator for ExperienceId namespace
1106. The reservation is transaction-local SEM staging, not a new domain certificate.
Use the existing SupportingObservationId(ObserverId,ObservationId) type 216 as the immutable
payload for the 121→122→123→124 continuation. Each child has the previous actual event as parent,
same T, empty dependencies, and the exact fixed phase for its SEM operation.

The bridge schedules those operations; it does not replace their semantics. At 121 tracking/event
segmentation receives no detections and performs no state changes. At 122 there are no binding or
feature requests. At 123 both classification work sets are empty and pass their accepted canonical
ordering checks. No absence is converted into a classification, recognition or truth binding.
Each step validates its phase with assertLaneOperationPhase and the same observer/reservation/
supporting-observation relation from actual staged context. Arbitrary payload IDs cannot forge it.

At 124 construct exactly type 227 with:

```
ExperienceId                    the reserved ExperienceId
ObserverId                      channel.ObserverId
OccurredAt                      T
PerceptualEventReferentIds       {}
PerceivedBindings               {}
PerceptualClassifications        {}
PerceptualEventClassifications   {}
SupportingObservationIds        {the actual type-216 support}
TransformationVersion           semantic-binding/0.1-candidate#SEM-001H
```

Use accepted assemblePreRecognitionExperience, canonical SEM encoding and
freezeAndStageSemanticExperience(reservation,experience,124). The last operation is the actual
accepted SEM freeze, recorded under seam/event-truth-to-pre-recognition-experience and
semantic-binding/0.1-candidate#SEM-001H, Consequence. The bridge cannot fabricate a producer tag
or directly call EVID. The shared interpreter observes this actual freeze and generates the
accepted phase-130 EVID transitions, with their unchanged registrations and output schemas.
No causal-role/recognition requests exist in this scalar fixture, so 125/126/127 have no work;
it does not weaken required work in models with such requests. Validate the accepted reservation/
frozen-envelope bijection at settlement. All outputs/queue/context/allocations roll back together.

The fixture endpoints are closed bindings to these named accepted operations, with event members
for observation, tracking, binding, classification and freeze. They are not generic arbitrary
handler callbacks or V06 ADAPT consumers. Their complete operation/phase/payload recipe is fixed
by the bridge registry version and participates in ModelIdentity. Existing EVID sees a genuine
SEM producer, not an AuthoredAdaptationFactProducer. No phase-140 handler is added.

## Epistemic cut and proof gate

The pulse is independent of every fact-record field, including CharacterId/Exposure/Procedure/count;
only R's actual T and the committed channel definitions enter the consequence recipe. Subject and
observer therefore cannot accidentally be copied from an adaptation semantic key. All those fields
and T are held fixed in the paired count intervention. Allocation topology is fixed, so supporting
observations, X, OutcomeEvaluation and OutcomeLearningEvidence are byte-identical across the pair.
The root's EventId and pulse TruthRecordId remain in omniscient scheduling/truth context only;
neither is copied to supporting references or character evidence. No common-root token is introduced.

AD-D11: one actual R has both direct children; replace either with an unrelated same-time event and
trusted source/ancestry validation fails. Deleting one branch cannot satisfy the both-affected case.
AD-D12: independent AAI/observation/experience/evaluation/evidence occurrence identities; no branch
is derived from the other's output. Runtime identity/queue topology and rollback are checked.
AD-D13: zero/nonzero count yields byte-identical immediate observation/X/E/L, with equal allocation
positions. Mutating the pulse or channel from count, source subject or adaptation state is detected.
AD-D14: injecting AAI ID, EventId, pulse truth ID or a root token into character-accessible records
fails exact schema/SEM evidence admission. Trace ancestry remains available omnisciently.
AD-D15: wrong lane, late truth availability, missing/duplicate reservation/freeze or forged SEM
producer relation fails the inherited phase/admission/ingress boundary. An empty experience with
fabricated support does not count as the positive witness.

These controls supplement AD-D1..10 without renumbering them. They are frozen for review, not passed.
F inventory adds the bridge singleton kind/member, its one-field Channels record, the two-field
phase-120 input record, FixtureConsequenceTruthId family and five fixture event-type members.
Reuse existing observation records, type 216/227, ObservationId/ExperienceId and all SEM operations.
No AAI ID or scheduler ID field is added to consequence or evidence schemas.

This closes the proposed receiving recipe at shape level for review, not by implementation proof.
It is a fixed observable intervention marker, not a general consequence simulator or adaptation
kinetics model. Later revealing challenges and E/F retain their own obligations.

Composition pass 7 (2026-09-06): bridge direction is internally shape-ready. DefinitionVersion and
ADAPT-owned version references advance together to adaptation-input/0.31-candidate; no bridge redesign.
Accepted observation/SEM/EVID versions remain unchanged. FixtureConsequenceTruthId is narrowed
only by this fixture construction rule, never globally on BoundedEffectTruth.TruthRecordId.

Pass 8: owning ADAPT version synchronized to 0.31 for E revision 2; D semantics unchanged.


F composition update (2026-09-06): [symbolic packaging](ADAPT_001_PACKAGING_DRAFT.md) records
the supplied domain decisions and ownership audit. All ADAPT-owned semantic definitions target
adaptation-input/0.31-candidate; independent V06 and settlement targets remain 0.6-candidate and
0.2-candidate. Candidate version spelling is not whole-contract acceptance. A–E behavior is
unchanged; shared PRJ-owned ProjectionAccessorId now closes the accessor allocation-home gap. No allocation
or implementation. Historical status entries above remain review history.


Accessor resolution (2026-09-06): [F packaging revision 2](ADAPT_001_PACKAGING_DRAFT.md) and PRJ
record shared ProjectionAccessorId with three symbolic members, no numeric allocation and no
global type-147 restriction. No known identity-home gap remains; A–E and their versions are
unchanged. Final packaging/composition review remains before allocation or implementation.


## Current whole-contract disposition — 2026-09-06

2026-09-06 whole-contract verdict: SHAPE ACCEPTED at adaptation-input/0.31-candidate with
transition-admission-extension/0.6-candidate and adaptation-settlement/0.2-candidate. A–D are
shape accepted as composed, E revision 2 unchanged, and F packaging revision 3 shape accepted.
Permanent allocation is now authorized as the next specification step; canonical implementation
is NOT authorized. ADAPT-001 stays OPEN in the formal register until PHEN-ADAPT-001 and required
proof/mutation gates pass. VAL-001 does not block shape acceptance or allocation; close it before
canonical reliance on affected governed executables, including the CONTENT-001 character-kind
validator. AD-F1..7, AD-E1..13, AD-D1..15, AC-A..L, main ADAPT controls, REG-A..R, EVID-A..T and
applicable inherited PRJ/WRT gates remain frozen; this verdict passes no implementation gate and
does not rescind previously recorded WRT substrate proof. PHEN-ADAPT-001 remains NOT PASSED.

Earlier dated restrictions and open-status notes are history; this verdict governs the current gates.

2026-09-06 C2-BRIDGE-OBS-001 amendment: the receiving recipe above now explicitly composes the unchanged observation compiler with the accepted bridge-owned projection. No equation, allocation, phase, source, route, or visible phenomenon changes. See CAMPAIGN2_BRIDGE_OBSERVATION_DECISION.md for sole-occurrence and no-remeasurement controls. Earlier direct-safe-output assumption is superseded by this amendment.
