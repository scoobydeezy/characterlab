# Common-evidence source join checkpoint — 2026-09-20

**Disposition:** LOCAL DISPOSITION; trusted transaction implemented, public model OPEN.
**Stage:** A/B source composition; C controls preserved. No public D/E qualification.
**Owner decision pending:** no. **Counters:** highest allocated706; since verdict/member0.

## What changed

Accepted `multisource-source-join/0.1-candidate` for trusted transaction implementation
and symbolic successor shape. `multisourceSourceJoin.ts` compiles a detached private
profile and reads qualified observer-holder mapping, task status, selected task
instruction and adopted body instructions from exactly four state paths. It produces
the inherited task chain and real body observation/selection itself at one instant.
No external raw signal, forged parent output or selected capability enters execution.

The active task's instruction and body instruction must match their compiled bindings.
The narrow workspace has one selected task and disabled forecast; Standing is disabled.
The inherited motive definition supplies the Base-off control. The join enforces the
task time window, actual source channel/signal mapping and uniqueness of allocated
occurrences across both paths. It makes no authoritative state writes.

The component still receives an externally supplied opaque source and trusted compiled
declarations. Registry-to-definition/action/criterion binding, source parameter/state
commitments and scheduler event authority remain public model obligations. The local
allocator's uniqueness check is not transactional allocator rollback.

## Evidence

`MULTISOURCE_SOURCE_JOIN_TESTS_REV1.json`: **49 tests/five files PASS**. Eleven new
join tests cover exact read paths/no writes, absent/foreign holder mapping, inactive
task, absent/rebound plans, body adoption, task time window, Base-off, forged profile
and source, mismatched channel mapping, allocation reuse, detached declarations,
deterministic repeat runs, hidden-within-bin invariance and capacity exclusion.
Static compiler checks reject invalid windows, channels, thresholds and motive pressure.
Build/reference boundary PASS. The test fixture was corrected to explicitly declare
the new capacity1 workspace and a valid inactive status union. Full suites not rerun.

Repeat component execution is deterministic, but this is not complete-prefix replay
or execution under a new public ModelIdentity. Source audit includes all actually
observed samples; only selected samples reach the character-side source assessment.
The inherited state-read provenance is recorded separately from observation support.

## Stage C competitor / North Star transfer

GroundAggregate, GroundPairwise, GroundUncovered and role-blind FamilyNormalized keep
their prior numerical scope; DescriptionDice remains Base-only. Base-off keeps actual
adoption/plan but removes motive generation, preserving the orphan constraint without
pretending that deleting task adoption is the same intervention. Separate body and
commitment motives share actual evidence without acquiring a common motivational
accumulator. MEC-012/013/014/015/016/020 remain preserved; no new Need owner or edge.

## Next gate and research obligation

**RO-C3-001 remains ACTIVE.** Complete the exact public record/registry and receiver
inventory, including actual duplicate and third-aggregate sources and a declared
normalization competitor, before permanent allocation. The symbolic join/output
envelope must preserve real parent ancestry; frozen499 cannot represent this graph.
Then compile model commitments and admitted originals, register reads/output ownership,
compose arbitration/execution and prove rollback/replay. These gates remain open;
PHEN-MULTI-001 stays PARTIAL and the general law verdict remains UNRESOLVED.
