# Visual selection's actual role-claim input

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Correct-forward symbolic
revision2 of the visual carrier; no new role semantics or numeric allocation.

The source stage proposal transports actual phase15/125 CausalRoleEvidence240, but
revision1's VisualSelectionInput could not receive it: VisualOpportunityEvidence
contains observations, tracks, event, experience and context, not role claims. Add
Claims:list(CausalRoleEvidence,0..3) to VisualSelectionInput. The source dispatcher
copies the actual completed role batch with exact observer/experience/binding
association. It may not recompute claims, allocate replacement occurrences or use
role labels from the original world definition as cognitive claims.

Three is the first scene's bound: three visual bindings with the accepted one-role
mapping per binding. Empty/unresolved/unsupported source distinctions remain intact.
An unavailable visual source has no claims even when another modality produced the
shared experience. Selection authenticates that batch before constructing its pool.

The selector may inspect the safe source, actual claims and compiled spatial
calibration. Spatial preparation computes the preselection divisor. The emitted
ExtendedSelected contains exactly the selected evidence, selected witness subset and
actual optional context. The encoding input stays selected-only; this field addition
does not grant its consumer a read of VisualOpportunityEvidence or unselected claims.
The source proposal's GeneralUseInput supplies the previously missing edge.

Also correct revision1's machine invariant that said the formation owner allocates
the acquisition. The accepted acquisition protocol assigns that single occurrence
to phase130 AcquisitionFormationEvidence production after positive eligibility. The
phase140 owner consumes that exact candidate and commits through its actual batch.
It allocates no replacement identity. Successful whole-instant commit makes both
evidence and retained acquisition authoritative; failure rolls them back together.

All other16-record fields and the prior-feedback scope correction remain unchanged.
The machine packet remains symbolic. Public producer matching, roles, phase130
registration, owner/hook composition and persistence remain open.
