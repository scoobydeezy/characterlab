# Visual selection through actual event recall

2026-09-13. LOCAL DISPOSITION — no owner ruling required. GA remains OPEN.

The four-trial source fixture now uses actual observed positions, SEM marker tracks,
selected spatial encoding and positive-only acquisition candidates. The ninth visual
cue is a separate completed sensing opportunity. It does not sample the panel or run
an acquisition selector. Body cueing remains its separately admitted opportunity;
neither path grants the other sensing permission or memory admission.

Actual canonical event recall now determines the event read set. The fixture no longer
hands every retained event acquisition directly to attribution. Seven recall slots
cannot deliver eight operands, and a hidden current cue cannot be replaced by an old
marker file. Missing positional acquisitions fail at recall availability before the
grouping stage. Body and event read sets then feed the existing focal-pair consumer.

The source's Actor evidence is still an explicitly supplied observer-role-channel
premise. Coordinates and panel stages do not derive Actor. Fixture JSON payloads,
subject identity and formation-source labels are not public canonical admission.
Each event acquisition in this first fixture has one child; its legacy consumer label
does not establish a general canonical multi-child mapping. Public closure remains due.

Self-review found panel/event outputs stamped with component-version strings outside
the frozen SEM codec's admitted set. Those records now name SEM-001C, which owns their
event-file transformation. Component contracts keep their own implementation labels.
No accepted SEM version, schema, allocation or codec whitelist is widened. The spatial
join also rejects an unadmitted event transformation before producing a witness.

[Thirty focused checks](GA_VISUAL_RECALL_COMPOSITION_TESTS_REV3.json) cover the joined
source, panel/body, panel/visual and cue-only paths. [Five injected faults](GA_VISUAL_RECALL_REVIEW_REV1.json)
are detected: invalid SEM tag, borrowing an unsampled active context, replayed cue
observation, bypassed recall capacity and manufactured current cue. Type checking
passes. These reports precede the additional spatial-consumer version guard; its
forward test receipt is recorded separately. Historical receipts remain unchanged.

Next work is the explicit visual-carrier inventory and its identity/owner joins,
followed by whole public shape, separate allocation and exact packaging. Public
runtime, rollback, complete-prefix persistence, affect feedback and corpus promotion
are not inferred from this component fixture. The frozen corpus remains unchanged.
