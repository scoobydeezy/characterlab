# Attention memory source and calibration definitions

2026-09-12. Draft closed definition shapes. Every source/channel/calibration choice
must enter committed model content; no public runtime default or callback is proposed.
Eight definition records separate observation, spatial allocation, encoding, association,
access, feedback and schedule. Their nonfixed numeric values still need an exact cohort.

Each of three physical ports has independent detection permission, role readout,
glyph availability and position availability. Denying detection suppresses all its
features regardless of other settings. Missing admitted features remain unavailable.
Role mode derives Exact or Unresolved evidence from the actual binding; there is no
authored Actor/Incidental/importance input. A closed CuePort designates a physical
port before permission and tracking, never a learned episode or perceived-file key.

Spatial calibration commits a bounded rectangle, focal weight and residual pool.
Encoding separately names role-calibrated, spatial or disabled allocation and the
four accepted budget laws. The homogeneous base3/10, role weights, threshold1/4 and
budget floors1 or1/5 remain explicit controls. Need and surprise are explicitly Disabled;
these records do not create evidence that those quantities were observed as zero.

Encoding feedback may select spatial residual modulation only with spatial allocation.
This avoids a model labelled feedback-enabled that has no compatible modulation target.
Retrieval feedback separately changes associative pull weight; it does not rewrite W,
retained factors or presentation history. Unavailable feedback has an explicit baseline
branch and retains its status. The existing selector policy and capacity are unchanged.

Association and access calibration expose the parameters already governed by EAM:
lattice scale, learning/decay rates, beta, presentation decay/exponent and winner capacity.
No hidden query-time association decay is introduced. The exact model cohort must
bound the otherwise component-admissible nonnegative parameters and positive scales;
this declaration inventory does not authorize an open configurable public factory.

The schedule definition makes the proposed probe0, concern1, distinct receivers2..9,
four-formation/four-cue limits and deadline20 explicit. Formation precedes cue in the
bounded series. Counts must match the actual admitted original sequence, not merely
metadata. Automatic source reads and deadline work remain in the queue and budget.

The review checks16 channel-configuration combinations and rejects ten malformed
configuration specimens. It does not execute channel permission, spatial arithmetic,
source provenance or a canonical rational codec. The evidence is configuration-design
review only. Existing exact component contracts and frozen models remain unchanged.

Next is the finite registration-record shape tying these committed definitions to
stage inputs, outputs, projections, state paths, batch membership and occurrence rules.
Whole symbolic review and an exact finite cohort remain prerequisites to allocation.
