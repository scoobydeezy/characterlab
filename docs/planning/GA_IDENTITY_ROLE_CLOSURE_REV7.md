# Joined identity roles through source-stage transport

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic revision7.

Add the five source-stage transport records to revision6's124 records. Each new
wrapper has direct ObserverId1000 at field1, matching its nested admitted source
observer. That supports existing field-based PRJ; it does not introduce a new roster
or make source-provided CharacterId authoritative.

GeneralSamplingInput.World inherits exact WorldEventTruth210 identity roles. Only
the source sampling edge may receive this field. Later wrappers recurse through
declared safe samples, actual SEM tracks/bindings, the committed use plan and body/
visual opportunities. The graph's epistemic prohibition remains separate from this
structural role inventory; a correct identity namespace alone cannot grant a read.

The joined inventory has129 records,127 scalar identity positions and185 record
boundaries. Six new-root scalar key roles and composite physical-key recursion remain
unchanged. No new identity family, permanent number, lookup authority or public
registration is created. All downstream input, occurrence, owner/hook and persistence
gates still require their actual contracts and executable qualification.
