# Campaign 2 cognitive qualification

Disposition, 2026-09-09: **task-cognitive-path/0.1-candidate QUALIFIED in its frozen
bounded profile**, by internal adversarial review under the user's authorization to
finish Campaign 2 independently. This is not a planning-agent or external verdict.
Historical shape/allocation/model receipts remain unchanged. Their NOT PASSED labels
describe those checkpoints; this later record supplies runtime disposition.

## Executing commitment and comparison

The executing versions are exactly those in formal TASK_COGNITIVE_PATH.md and
campaign2-task-cognitive-model/FREEZE.json. Baseline ModelIdentity digest:
`abe15a4f6bb6ec13a3ccc825dc5e9d755c4444998fa178dbb8683969a1a2a498`.
The separate 21 recipes retain their own frozen commitments. No implementation fix
changes a model, numeric allocation, calibration or accepted mathematical contract.

Public evidence means execution through cognitiveFactory's data-only model/run
boundary from admitted S0 and original inputs. Component evidence means the actual
production transformer or compiler executes with explicitly supplied mathematical
or retained-state operands; it does not establish public reachability. Static
exclusion and capability arguments are identified separately. Exact canonical bytes,
not chosen action or a digest alone, are the continuation comparison relation.

The baseline starts with empty episodic memory, prediction and acquired identity,
an immutable IDN roster, D=-50 and two actually adopted tasks with immutable
instructions. Probe1, cognition2/3/4, probe5, cognition6 and later deadlines execute
without supplied psychological intermediates. Four choices and four eligible
identity contributions are generated; actual completed counts are 2,1,2,2. The
baseline has 128 committed rows, 114 outputs and nine draws. Its final save digest
is `c85c9f2d8a8741d4675b7696a17b9da109c094bf51a1dbb78a1977d759dd9bac`.

## Evidence inventory

- COGNITIVE_RUNTIME_COHORT_REV2.json: all 21 frozen recipes execute through the
  public factory. Baseline PlayerFacingRoll, context-integer QuietRoll and
  workspace-two Auto occur. All five die sizes execute. Equal criteria produces
  genuine rolls with NoExpressedChannel and no acquired identity entry.
- COGNITIVE_HISTORY_BOUNDARY_REV2.json: 64 genuine public contributions succeed;
  a novel 65th rejects atomically. No imported learned history. Failed run terminal.
- COGNITIVE_SOURCE_SUBSTITUTIONS_REV2.json: 17 finite production-source substitutions
  detected by executed assertions, not compilation failure. No source file was
  rewritten to run the mutant. These are component/sealing controls, not 17 public
  counterfactual models or an exhaustive mutation result.
- COGNITIVE_MATH_REFERENCE_COMPARISON_REV2.json: actual historical compiler parity
  on 270 dice cases (140 active) and 11 ordered identity histories. Dice operands
  here have empty evidence bases; this receipt alone does not prove coverage.
- COGNITIVE_COVERAGE_REFERENCE_REV1.json: 12 external comparisons with the actual
  historical aggregate-coverage implementation, including weighted, collective,
  subset, identical, disjoint, empty and permutation cases. Synthetic component
  operands only. Canonical source bytes provide the historical string tie key.
- COGNITIVE_PREDECESSOR_PRESERVATION_REV1.json: 18 retained assays refreshed into new
  COGNITIVE_PRESERVATION_* receipts. Their public/component scopes remain unchanged.
  Temporary runner anchors were adapted for the new codec name, original-input
  exclusion and conditional save projection. Old receipts were not overwritten.
- COGNITIVE_QUALIFICATION_EVIDENCE_REV1.json verifies 24 receipts and 446 fingerprint
  references, including frozen shape, allocation and model artifacts, and records
  current production/test hashes. This is consistency evidence, not a behavioral test.

## Frozen vector crosswalk

All dispositions below are PASS within the accepted public/component/exclusion
split. A grouped row does not assert that every branch has a separate public model
or that every proposed fault has its own source mutant.

| Vectors | Owning evidence and scope |
|---|---|
| TW-A, K: authentic input, subject and source | CognitiveModel tests reject changed complete original events and lookalike generated children before reads/allocation; actual IDN projection supplies C. Public input profile excludes authored304, overlap, wrong phase and out-of-bound time. Factory rejects capabilities/accessors. |
| TW-B, C, G: adoption/lifecycle/order | CognitiveTransforms actual-adoption, active half-open window and canonical-tie control; CognitiveModel instruction/adoption and terminal-status controls; public deadline-overlap recipe. No implicit adoption or immortal task pressure. |
| TW-D, E, F, L: capacity and availability | Capacities0..3 and independent task/forecast gates execute; Unknown is distinct from known0. One shared forecast supplies the selected tasks. Reserved-slot, Unknown-as-zero and ignored-forecast source mutants are detected. |
| TW-H, I, J: time, allocation, no writes | Authentic generated seven-stage chain checks allocations and binding even for empty children. Workspace reads committed state at40, while predecessor applications occur at140 and are visible only later. Pure transformers cannot write; actual traces have no patches. Full public prefix and whole-instant rollback protect outputs/allocators. This does not resolve ORD-001. |
| TP-A..D: immutable adopted instructions | Model/state tests reject orphan adoption, wrong definition kind and prospective-owner instruction writes; terminal updates preserve the instruction bytes. Missing/disabled plan access creates no option and does no plan read. |
| TP-E..H, L: availability versus motivation | Two concrete actions, exact candidate-key deduplication with origin union, base-off retaining options, known/unknown appraisal and interval endpoints execute in transform tests and public recipes. Extra-ticket and base-off-deletes-options mutants are detected. No source is widened for a duplicate public instruction model. |
| TP-I..K: admission/capabilities/rollback | Generated children authenticate complete live source facts before reads; selected plan definitions are resolved in the trusted owner. Character transformers have no world/REG/efficacy lookup. Public prefix and final-trace rollback cover the composed path. |
| RI-A..D: raw causes and consolidation | Raw source test uses direct retained I once per origin; task/role source keys and same-C grouping are enforced. Unknown/zero, exact aggregate coverage, sign/role partition and canonical ordering run in production components. Richer multi-source and Avoid branches remain component controls with public profile exclusion. |
| RI-E..I: base, modifiers, no choice | Zero base cannot be resurrected, weak active base gets d4, Avoid negates the whole expression, all die bands and exact activation/modifier arithmetic pass. Workspace/plan/base ablations yield explicit NoOptions/NoActiveReasons. Corresponding floor/sign/zero-base mutants are detected. |
| RI-J..M: meaning, standing and empty identity | Equal alternatives have zero alignment regardless of selected token. Frozen expression/evidence is identical under blocked execution. Direct-I and standing-in-meaning substitutions are detected. Public baseline starts empty and later uses earned identity; standing-off performs no identity read. |
| RI-N..P: qualified source and atomic application | Only live expression/qualification children enter I. Codec disjoint Eligible/Rejected grammar, duplicate/time/limit failures, sealed-preflight control, common-stage cardinality and final-trace rollback execute. Rejected I has no target/prior/patch by the actual application branch; equal-criteria public witness inhabits it. |
| IH-A..D, J: exact history fold | Empty S0; positive/opposing histories; successive half-even ties; raw-sum-once counterexample and independent-rounding mutant. Rounded-zero contributions remain entries. Structural validation does not perform an extra semantic fold or store competing counters. |
| IH-E..I: history is authoritative | Public HistoryProvenance control changes only qualification provenance, preserving e/order/numerical strength, and restore rejects it. Resolution-root mutation also rejects. Component duplicate/time/65th priority checks and genuine public64/65 receipt protect ordered history; no clipping. |
| IH-K, L | Rejected branch is inhabited without history; source-derived target absence means no prior read/write. Full-prefix continuation and post-patch failure cover exact history rollback. |
| TRC4-A..F, L | Actual numerical components and historical differential control establish empty, identical, disjoint, weighted, collective, subset2/3 and canonical permutation behavior. Last-basis-only substitution is detected; the collective fixture independently discriminates retired pairwise-max. The substitution is not claimed to be that exact retired algorithm. |
| TRC4-G..K | Positive unique basis weights, closed atom grammar, canonical source uniqueness and nucleus/role/sign partition belong to their respective codec/kernel/source owners. Forecast basis uses original admitted observation support, not appraisal IDs; standing uses retained expression evidence. Duplicate empty-basis causes reject. Generic numerical operands do not confer source or provenance authority. |
| CS-A..D, G | Actual common-stage missingI/duplicateI/missingA/unexpectedP controls reject before every prior read. Prepared identity cannot execute before sealing. Authentic generated participants are checked against their registered graph, then disjoint targets are resolved against B0. |
| CS-E, F, H | Blocked execution still produces307/A; rejected qualification still produces I. Public recipes inhabit both. Instruction bytes survive all status transitions; I, ADAPT and task write authorities remain separate. |
| CS-I, J | Failure at final identity trace occurs after genuine draws and candidate patches: state, clock, queue, allocations, outputs, committed trace and private addresses remain unchanged. Failed instance is terminal; reconstructed retry matches complete control save. This is a composed downstream witness, supplemented by local post-draw discard and retained rollback assays, not a new exhaustive failure-stage matrix. |
| Work bound | Actual deadline overlap executes27 events and26 runtime allocations at one instant. Lowering the real scheduler limit to26 rejects atomically. All cohort counts fit the frozen27 bound. |

Trace qualification combines actual-source construction and public prefix checks.
CognitiveHistoryProvenance compares nested reason/tie draw records with160 field14
and observes identity quantization/diffs. Independent omission/fabrication of reads,
draws, quantization, mutation evidence or subject association in saved trace rejects
at whole-prefix equality. These substitutions qualify the receiving save boundary,
not an invented generic runtime trace oracle. Instrumented reads and sole patch
authority remain the owning generation boundaries. Final trace failure rolls back.
Observer0/1/2 controls reject both truth references and falsely sharpening2 to a point.

## Adversarial findings and corrections

Integration found the predecessor decoder rejecting the successor's committed307
version nested in an ADAPT result. The trusted evaluator now receives its compiler's
codec; old callers retain their decoder. It does not admit arbitrary callbacks at
the public factory. Registration comparison allows only the frozen producer change
and requires every retained rule and other registration field to remain byte-equal.

The first common-stage integration rejected missingI only at final completion,
after other participants could read priors. Participant cardinality now runs first;
four actual stage mutations verify zero prior reads. Full route closure is derived
from the actual composed registrations (269,270,342,429), not from the smaller
inherited ingress view. Structural identity checks were separated from semantic
folding so trace does not invent repeated quantization. All fixes implement accepted
requirements; none constitutes a new research calibration.

## Validation and limits

Final full active run:118 files/1,063 tests PASS. Preserved reference:43 files/328
tests PASS. COGNITIVE_FINAL_SUITE_RECEIPT_REV1.json records both runs and verifies
the complete source/test fingerprints before and after execution. It supersedes
the earlier117/1,059 checkpoint and separate added-test counts. TypeScript and
reference import boundary PASS after the final test additions. The required npm reference
command hit the environment's config-loader access restriction; the same include
and node environment were then executed through Vitest's API with config:false.

This is finite reference qualification, not psychological sufficiency, seed-space
exhaustion, reduction acceptance or new corpus membership. Protocol efficacy, cost,
coercion, social sources and broader memory are not inferred from task success.
See CAMPAIGN2_COMPLETION_REVIEW.md and CAMPAIGN2_COGNITIVE_PERSISTENCE_QUALIFICATION.md.
