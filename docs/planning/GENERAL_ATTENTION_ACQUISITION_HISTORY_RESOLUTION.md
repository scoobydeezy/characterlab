# Exact bounded-run governance: A accepted

2026-09-12. **ACCEPTED by supplied planning ruling.** Resolves
[history decision](GENERAL_ATTENTION_ACQUISITION_HISTORY_DECISION.md).

Bound distinct qualified sources admitted to ordinary formation, not only successful
rows: successes ≤ admitted domain ≤ N. Current content, acquisition identity and source
governance are separate. Every committed formation keeps exact payload-free metadata
even after total cognitive loss. Absence means no committed successful formation;
it says nothing about attempted failures. No failed-attempt record is introduced.

Save/load, offline continuation, pruning and loss do not free source-domain capacity.
No oldest-row deletion or rollover is allowed. Source N+1 rejects at the declared
admission/lifetime boundary without cognitive loss, graph pruning or character-visible
events. Opaque IDs and digests do not supply finalization or membership semantics.

Exact history remains necessary while this authoritative run can accept requests.
Only actual terminal retirement of the run allows external retention policy to retire
its protocol state. Extending the same world beyond the horizon requires a reviewed
successor; clearing history does not create one. Source finalization/compaction remains
the future direction for arbitrary continuation, with any lost query guarantees explicit.

The first component consumes a supplied fixed universe and tests eight metadata cases.
It does not yet bind a public run domain, allocate records or qualify canonical restore.
The component maximum64 is a safety bound, not an accepted profile parameter.
General Attention remains OPEN; bounded governance is not lifelong memory architecture.
