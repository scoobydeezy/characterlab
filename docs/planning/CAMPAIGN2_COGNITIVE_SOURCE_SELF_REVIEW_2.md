# Cognitive source self-review2 — corrected reference selection

Disposition: internal adversarial review, not external approval. Task lifecycle remains
qualified. All downstream cognitive/action/identity documents remain drafts. Campaign2
is OPEN; no user decision is currently needed.

## Corrections found before acceptance

1. **Conditional intent:** actual277 supports exactly one output, and276 exactly one
   ingress. Keep them unchanged. Always emit a DecisionResolution; only its Chosen
   branch schedules the intent producer, which then emits exactly one intent. The new
   branch-specific ingress needs an explicit symbolic registration. This is thinner
   than changing generic output multiplicity or emitting dummy intents.
2. **Actual-fact provenance:**321 already has registered-transition producer tag2.
   The current ADAPT profile restricts it to authored tag3, so the successor must
   admit the real registered execution bridge without inventing a fourth variant.
   It excludes original authored facts; historical profiles stay unchanged.
3. **Blocked physical effect:** using requested count as bounded-effect potential
   while applied=0 violates the actual clamp identity at bounds[0,2]. Potential for
   this decomposition is completed count after interference; attempt separately
   retains requested count. Overflow stays0 and never means interference.
4. **Zero adaptation count:** the accepted D surface explicitly admits zero and emits
   one307 even then. A blocked execution can preserve this topology with actual count0;
   it cannot suppress rule resolution or copy requested count.
5. **Wrong historical feedback selected:** the first reason/identity draft proposed
   I*Alignment as standing strength after inspecting identityFeedbackRawInfluences.
   That is Phase2.95. The actual Phase2.97 cycle invokes allCognitiveSignalsForOption,
   whose commitmentStandingIdentitySignals supplies I directly to each matching
   commitment. Revision2 corrects the proposal to this retained initial mechanism.
   This is a source-version error, not a defect in the preserved historical code.

The initial source inspection and research script are preserved. In particular,
TASK_EARNED_FEEDBACK_CALIBRATION_REV1 is a valid executed mixed-component comparison,
not evidence that Phase2.97 requires alignment-scaled standing. Its initial report
wording must be read with this correction. The earlier raw standing1/3 intervention
was a legitimate direct-strength control, although it did not itself earn identity.

## Actual comparison evidence

TASK_EARNED_FEEDBACK_CALIBRATION_REV1 samples32 three-choice histories using the actual
historical compiler, resolver, alignment, identity update and older projected-feedback
function.23 samples change the hypothetical later resolution mode.

REV2 repeats32 samples with the actual private commitmentStandingIdentitySignals
function exposed by an in-memory Vite transform. No reference file was modified.
All32 samples change the hypothetical later mode, from Auto to PlayerFacingRoll.
Initial identity is empty; updates use actual historical quantization. The three
choices yield3..6 hypothetical fixture contacts, within the existing tolerance/load
count limits. The precise source fingerprints and operands are in the reports.

Neither report executes canonical task cognition, protocol actions, the later probe
history or the new qualifier. Later means are explicit hypothetical sums, and task
concern→semantic meaning remains a proposal. Thus these are feasibility/comparison
results only. They do not pass DEC-001, PERSIST-I, RNG integration, a phenomenon or
Campaign2. No seed search is a substitute for freezing and executing a real witness.

## Architecture checks

The proposed intent/protocol route keeps chosen intent, expression, plan, attempt and
actual world outcome distinct. World permission is only an execution operand. Existing
SEM/EVID performs safe consequence carriage, and regulatory measurement stays on its
separate probe path. The source cannot masquerade as an InputOnly original fact.

The reason source keeps availability, base motives, current concern, standing history,
meaning and arbitration separate. Base ablation leaves options possible but cannot
leave active nuclei. Meaning excludes standing; equal meanings cannot earn fidelity
merely because one option won. The proposed channel is only relative prioritization
of represented obligations, not a learned success rate or a moral verdict.

The bounded qualifier still requires exact source/schema and generated negative
witnesses. Fear and accepted cost are NotRepresented, not zero. A later profile with
those contexts cannot use this qualifier without an explicit successor. This prevents
a tiny free-choice fixture from silently claiming general authorship qualification.

## Next closure work

Continue task-reason/identity revision2 and CAMPAIGN2_INTENT_PROTOCOL_SOURCE_DRAFT.
Close exact canonical distribution/arbitration math and the accepted RNG address
surface, chronological identity support representation and the complete conditional
ingress/output registration. Then perform whole symbolic review, separate allocation
and profile/work accounting before implementation. Do not reopen accepted lifecycle,
prediction, ADAPT or PRJ/IDN mathematics to simplify a later handler.
