# General Attention pause and resume checkpoint

2026-09-13, approximately21:06 America/Chicago. **PAUSED AT USER REQUEST.** The user
asked to close the current write-scope gap, save a checkpoint and pause because
weekly usage is nearly exhausted. No architectural decision is pending. Do not
resume execution until asked. General Attention remains OPEN.

## Completed gap

The inherited322 write capability names Campaign2 family IDs; it is insufficient
as the direct representation of the already accepted non-learning path scopes.
The correction preserves frozen704/1 and appends GeneralPathWriteCapability705/1
and GeneralStageRegistrationV02 706/1. The latter changes only the write field to a
closed322/705 record union. It does not redefine a route, owner or state family.

Read-only stages require322 ReadOnly. Character-learning owners retain322 StateWrites
with exact owner/family matching. The two perception stages, two goal lifecycle
owners and physical replenishment use705 explicit paths. The component rejects
cross-owner roots, protocol roots, wrong fields/selectors, wrong authorities and
capability substitutions. Declaration map-key wildcards remain legal patterns;
actual holder/observer qualification and exact model path-set equality are still
mandatory at the future public compiler. This is a closed representational gap,
not whole-model qualification.

## Frozen artifacts to preserve

- GENERAL_ATTENTION_CARRIER_SHAPE_MANIFEST.json and CARRIER_ALLOCATION_MANIFEST.json
  under formal:163 records542..704;523 fields;38 finite tag sets;11 unions;
  namespaces1044/1045/1145..1148. Twenty previous allocation tables preserved.
- GENERAL_ATTENTION_REGISTRY_MEMBER_SHAPE_MANIFEST.json and
  REGISTRY_MEMBER_ALLOCATION_MANIFEST.json:218 exact existing-namespace members;
  67 stage templates, eight accessors, six authority scopes. No new namespace.
- GENERAL_ATTENTION_WRITE_SCOPE_SHAPE_MANIFEST.json and
  WRITE_SCOPE_ALLOCATION_MANIFEST.json:705/706 append-only correction;13 fields;
  three scalar roles;22 previous tables preserved. No namespace/member changes.
- GENERAL_ATTENTION_WRITE_SCOPE_POLICY.json is the fixed stage-mode policy derived
  from the accepted correction, not an exact model authorization manifest.

Draft proposals and failed development receipts remain historical. Do not rewrite
frozen tables, reuse numbers, alias704 to706 or rerun write-once allocation scripts.
Frozen corpus0.27.0 remains unchanged at digest
`3cb09115d90485d5975968ffca419a4960a398326b6b6339a5b1e8e8fdcec276`.

## Implemented and verified

- generalAttentionCodecs.ts composes both inherited attention and embodied-receiving
  branches, checks shared schema agreement, exact new carrier grammar and primitive
  context, and preserves inherited domain validation. Initial attention-only composition
  failed to include EMB; that is fixed. Construction canonical-round-trips to reject
  caller-forged schemas. Decoding grants no public authority.
- canonicalRecallProjection.ts preserves acquisition/selection provenance and surviving
  positive evidence while erasing retention protection metadata. Both event and body
  projections pass; full owner structure/prior time/observer/duplicate checks precede
  filtering. Actual source authentication and accessor authorization remain upstream.
- canonicalBodyProduction.ts wraps the actual contextual selector and one-use formation
  producer. Real sensing supplies samples/context; serialization allocates no second
  occurrence. Zero capacity/absence still yields a selector but no acquisition.
- generalWriteScope.ts implements the bounded mode/owner/root check described above.

Latest focused receipt GA_WRITE_SCOPE_TESTS_REV2.json:23 tests across four files PASS.
GA_WRITE_SCOPE_COMPONENT_REVIEW_REV1.json:four injected source faults detected.
Earlier GA_CARRIER_CODEC_REVIEW_REV1:8 faults; GA_CANONICAL_PRODUCTION_REVIEW_REV1:8.
Symbolic/numeric write-scope reviews:8/7 faults. Registry member reviews:12/8 faults.
GA_PRODUCTION_BOUNDARIES_REGRESSION_REV8:314 tests/46 files, before the last write-scope
addition; latest focused run covers the changed codec and all new canonical adapters.
Typecheck and reference-boundary checks PASS after implementation. No reference edits.
The final codec comment change after the mutation review is documentation-only; the
receipt's exact source fingerprint remains historical, not a current-file hash claim.

## Resume here

> **SUPERSEDED AS ENTRY POINT 2026-09-14.** Start at
> [`GENERAL_ATTENTION_RESUME_BRIEF.md`](GENERAL_ATTENTION_RESUME_BRIEF.md). Substantial
> repository change occurred on 2026-09-14 while GA was paused — corpus 0.28.0, five new
> verdicts, two owner rulings on the concern feedback edge, a replaced `CURRENT.md` and a
> new checkpoint discipline. The frozen-artifact list and technical steps 2–5 below remain
> correct and are unchanged; the brief carries what is new plus the work-ceiling input,
> and records that registration/compiler steps 2–3 are owned end-to-end by the primary
> implementation agent from 2026-09-19 and must not be split across agents.
>
> **REDIRECTED 2026-09-14.** Before step 1 below, execute work-order items 1-5 in
> [`CAMPAIGN3_WORK_ORDER_2026_09_14.md`](CAMPAIGN3_WORK_ORDER_2026_09_14.md): run the
> salience-law arithmetic comparison and record its verdict, publish corpus `0.28.0`,
> write the owed EMB and ATTN verdict entries, adopt `CHECKPOINT_TEMPLATE.md`, and
> obtain the ruling in `CAMPAIGN3_PENDING_OWNER_DECISIONS.md`. The frozen artifacts
> above and steps 2-5 below remain correct and unchanged; only their ordering moved.
> Step 1's reading list should now begin with `CURRENT.md` (replaced) and the work order.

1. Read CURRENT, this checkpoint, the carrier checkpoint and escalation policy. Preserve
   the large mixed worktree; no commits, resets, cleanup or delegation were performed.
2. Continue **exact registration/model compilation using706**, not704. Bind each of67
   reviewed templates to its permanent members, actual component version, definitions,
   input/output schemas, full ReadDomain, PRJ requirement and exact write authorization.
   The fixed scope helper is necessary but does not prove whole registration admission.
3. Close remaining definition/content instances, canonical role declarations and domain
   validators, model-specific accessors, state roots and actual output ownership/slots.
   Goal predicate must additionally prove goal/spec/content/referent/holder coupling.
4. Materialize exact cohort models and compute a genuine whole-model work ceiling.
   67 templates is not an event-count ceiling. Inherited source/prediction stages and
   repeated rank/owner invocations must be included. No ModelIdentity is frozen yet.
5. Execute public source/subject/owner joins, rollback/replay and corpus promotion before
   closing GA. Do not promote component/codec success to public qualification.

Important source findings: GA_COHORT_SOURCE_RECONCILIATION_REV1 is still a proposal.
Probe at1, task activeFrom2, terminal concern at2, receiving work later prevents the
warmup probe from fulfilling the future task. R0=50 with D=-50/absent/+50 gives the
intended0/5/10 source controls; canonical D0 is an absent leaf. The frozen full C2
choice continuation can abort for the upper endpoint, so it cannot export transient
concern. Use the already planned separate terminal workspace/appraisal/concern path.
Missing glyph/position fixtures remain component/profile exclusions, not silently
materialized public models. Preserve actual sensed cue admission independently of
durable acquisition, immediate formation followed by same-instant loss, and common-B0
retention/graph/presentation/protocol settlement boundaries.

Continue locally under the escalation policy once resumed. Escalate only a genuine
ontology, epistemic authority or unsupported causal mechanism decision, not routine
representation or first-profile bounds. No further work is scheduled while paused.
