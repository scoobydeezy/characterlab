# Completed absence without invented occurrences

2026-09-13. LOCAL DISPOSITION — no owner ruling required. Symbolic correction to
the unfinished goal/attribution stage packet; public execution remains OPEN.

Choose explicit completed absence over fabricating an assessment or treating a
successful empty observation as a delivery failure. This follows SEM-H's conditional
experience reservation and the existing separation of unavailable evidence from a
missing expected producer. No new cognitive mechanism or identity family is needed.

When completed consequence sensing produces no observer-safe experience, the130
assessment dispatcher performs no goal read or assessment calculation and allocates
no assessment ID. It still schedules its two declared future envelopes, with absent
consequence/qualification values and the actual source instant. GoalQualificationDelivery
therefore has an optional Carry and explicit SourceAt; FocalConsequenceDelivery has
an optional Consequence and SourceAt. A present carry must match that source instant.
The old working revision remains historical; new fields are not numerically allocated.

Attribution consumes an absent focal envelope without inventing a consequence or
attribution result. Independently requested recall can still execute, but this
attribution branch does not calculate or earn use. Its later result delivery carries
an absent result. The significance receiver must consume both actual completed
absence deliveries. It produces no joined result or memory-owner operation.

For this first profile, absent qualification and absent attribution travel together;
inconsistent mixtures reject. A real unavailable assessment remains a real assessment
with its existing identity and unavailable qualification. It is not the absent branch.
Likewise, unavailable attribution for a real focal consequence remains an actual
result and may report lawful partial consumption. Missing/late/forged delivery still
fails authentication even when its expected semantic value would have been absent.

The encoding mirrors existing optional carrier fields. No NoExperienceId,
NoAssessmentId, synthetic outcome, hidden truth query or new absence occurrence is
introduced. Exact public ingress, zero-output declarations, target joins and replay
must execute this branch before whole qualification. Revision2 of the stage packet
and revision3 of the goal-result carrier own this correction.
