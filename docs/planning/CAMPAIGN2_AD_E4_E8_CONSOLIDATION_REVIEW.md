# AD-E4 and AD-E8 bounded consolidation

2026-09-09. Proposed vector dispositions for research review. Neither whole B nor
FCT is claimed. No production implementation, allocation, model grammar or frozen
packet changed in this pass.

## Accepted foundation

The write-boundary correction and the subsequent split-scope ruling are accepted.
The scope interpreter and alternate-qualified-subject guard have accepted generic
component positives plus explicit frozen-profile exclusions. Their future public
witnesses are conditional, not current blockers. Qualification-only topology
widening is prohibited. Accepted Remove/equal-value and post-evaluation association
controls retain their precise scopes. The formal register, seam ledger and verdict
ledger now record the ruling and its future triggers.

## AD-E4 mapping

| Frozen clause | Evidence and bounded interpretation |
|---|---|
| Construct all five exact keys | Mutation-evidence fixture executes regulatory and procedural consumers through real authored-source admission. Five-leaf WRT evidence and the new independent RuleId/StatePath oracle check the exact roots, fields and complete typed keys. |
| Wrong subject | Accepted generic qualified C2 positive and bounded one-character exclusion; distinct from the malformed-role control. |
| Wrong exposure | Fresh/stale wrong-exposure Set pair; wrong-exposure Remove and equal-value Set. All fail target authorization after the accepted prefix. |
| Wrong V/L/procedure | Existing exact-key operand controls change tolerance V, sensitization V, displacement V, load domain and procedure with valid identity families; each fails ADAPTATION_TARGET_PATH_VIOLATION. |
| Prefix precedence | Key grammar, canonical role and authority controls retain their earlier failures; accepted scope-interpreter split checks stage3 without manufacturing production reachability. |
| Input cannot select family/Step/V | Frozen304/305/306/307 schema fields remain the input boundary. Resolved311 declarations own routing/Step/domain operands. Existing closed-schema and consumer construction evidence remains applicable; no additional input field or callback is introduced by this consolidation. |

Propose AD-E4 PASS for these named controls and the accepted split scopes. This is
not universal malformed-byte or arbitrary interpreter equivalence.

## AD-E8 mapping

| Frozen clause | Evidence |
|---|---|
| p=6, n=3, Step=-2 | `campaign2IndependentAdaptation.test.ts` uses the committed decrement specimen; exactly one tolerance Remove with expected old value6, at root302/field1. Other rules have their separately reported operations. |
| Absent, n=0 | Added same-prepared-model control: all five dispatch/evaluation outputs remain, all evaluations are NoStateChange, aggregate patch empty, state unchanged. |
| Save/load equals never-present target | Exact Remove test restores its save byte-for-byte and compares canonical state with an independently constructed state lacking that target, with the other leaves held equal. |
| Explicit zero Set | Additional value0 variant injects a Set at a resolved no-change target; actual diff mismatch rejects before publication. Existing five-family static controls separately reject persistent explicit-zero entries. |
| Absent Remove | Exact target reaches STALE_PRECONDITION; wrong target reaches ADAPTATION_TARGET_PATH_VIOLATION earlier. Accepted by the scope ruling. |
| Equal-value StateChange | Ordinary WRT first proves equal-value Set is legal and state-preserving; ADAPT rejects disagreement with its governed increment via ADAPTATION_MUTATION_DIFF_VIOLATION. Wrong target still rejects earlier. Accepted by the scope ruling. |

Propose AD-E8 PASS for this finite matrix, without changing WRT effective-operation
semantics or broadening persistence qualification.

## Stronger rule-association challenge

The accepted staged-swap control only intervened after evaluation. The new oracle
authors the five expected RuleId-to-complete-StatePath associations directly in the
test. It never asks the resolver under test for those expected associations.

A new in-memory substitution swaps tolerance/sensitization rule labels while the
resolver builds its results, before expected outputs/diffs are captured. Physical
paths and arithmetic remain unchanged. The independently authored oracle detects
the wrong label/path relation, even with equal counts and Steps. The proof runner
specifically requires this oracle test to fail for that mutation.

This addresses one meaningful pre-snapshot faulty-resolver challenge. It does not
prove all possible pre-snapshot implementations equivalent. Review should decide
whether this finite additional challenge suffices for these vector dispositions;
no universal claim is inferred from it.

## Verification and historical evidence

- `CAMPAIGN2_ADAPT_WRITE_BOUNDARY_PROOF_REV5.json`:25-test baseline PASS; five
  substitutions DETECTED (pre-snapshot label swap, scope check, target check, actual
  diff check, staged-output check). Scope/subject splits retain the accepted ruling.
- TypeScript PASS on the final tree, including the same-model zero-count extension.
- Independent adaptation and domain files:9 tests PASS before that extension;
  the extended AD-E8 test then passed alone (the other3 tests in its file were
  explicitly skipped in that focused rerun).
- Predecessor rev3 is the accepted scope checkpoint; rev4 records the intermediate
  24-test assay, and rev5 adds explicit-zero Set. None is aliased to another source
  fingerprint. Production source did not change during these additions.

Other source-sensitive historical mutation reports still need deliberate refresh
before a new whole-B/FCT claim. This packet makes neither claim. AD-E3/7/9/10/11/12/13
and the remaining composed factory work are not closed by accepting these two vectors.
VAL remains QUALIFIED; bounded PERSIST-A..H, deferred PERSIST-I and PHEN-ADAPT PASS
are unchanged.

Requested disposition: accept AD-E4 and AD-E8 in the specified bounded/split scopes,
including the finite pre-snapshot association challenge, or identify a remaining
clause needed for either vector. Keep whole B and FCT OPEN.

## 2026-09-09 accepted disposition

AD-E4 PASS in bounded/accepted split scopes and AD-E8 PASS in the finite operation/
persistence matrix. The pre-snapshot oracle and rev5's five substitutions are accepted
at their stated finite scopes. Whole B and FCT remain OPEN. Evidence accounting remains
one dispatch plus four regulatory evaluations in the zero-count exposure case (five
outputs), not five evaluated rules. The next vector proposal is
[AD-E13 consolidation](CAMPAIGN2_AD_E13_CONSOLIDATION_REVIEW.md).
