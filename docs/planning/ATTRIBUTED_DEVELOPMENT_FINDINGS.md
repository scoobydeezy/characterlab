# Attributed knowledge development findings — 2026-09-24

LOCAL DISPOSITION; no psychological candidate rejection. Initial scaffolding script
failed before writing files (duplicate JavaScript local name). The subsequent source
copy missed bigint record references: first model compilation rejected requires1083/1.
The pre-correction source is preserved in attributed-development-rev1. No model or
public run was frozen. Corrected exact new IDs before retrying; prior profiles untouched.
RO-C3-020 preserves record/admission integrity;019 owns broader coverage.

The first partial freeze then exposed the inherited stage ReadDomain list max1:
Oracle explicitly reads own and target leaves, requiring max2. Partial model files
and original allocation are preserved here; no FREEZE receipt or public plan existed.
Corrected the structural bound to the already-declared two-read Oracle contract.

REV1 conformance passed34 tests; production build rejected generic CanonicalValue
reads lacking explicit record narrowing. REV2 source/test preserves that construction
cohort; narrowed math and test helper before repeating validation. A source review
also strengthened the report-only intervention with failed delivery in both arms,
keeping B actual state identical throughout, not just at current probes. No runtime
law/model identity changed. The original fixtures remain archived.

ATTRIBUTED_DEVELOPMENT_PRESERVATION_REV1.json binds both archived source cohorts
and partial model files by hash. ATTRIBUTED_TESTS_REV1.json records34 passing
pre-strengthening checks; ATTRIBUTED_BUILD_REV1.json records the typing failure.
ATTRIBUTED_TESTS_REV2.json and ATTRIBUTED_BUILD_REV2.json are the corrected receipts.
