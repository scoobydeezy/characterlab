# General attention source integration — revision 3

2026-09-13. Continues revision2 under the user's autonomous-work/escalation policy.
No architectural ruling is pending. GA and public integration remain OPEN.

The [panel perception component](../formal/TRIAL_PANEL_PERCEPTION_COMPONENT.md) now
owns actual SEM event-file start/continuation/end in the controlled fixture. The host
loop no longer allocates a trial file and reattaches it by schedule. Missing cues end
the window; matching glyph after a gap receives a new file, so grouping cannot recover
the missing stage. [25 component/composition checks](TRIAL_PANEL_PERCEPTION_COMPOSITION_TESTS_REV1.json)
pass, with actual SEM operations and unchanged source/formation controls.

Consequence-time goal assessment now obtains its earlier sample from actual body
recall, filtered by signal and admitted perceived context. It runs before the current
After sample's formation. SC-H removes baseline recall capacity while preserving later
attribution: qualification becomes unavailable and no significance is added. The
[21-check receipt](CONSEQUENCE_BASELINE_RECALL_COMPOSITION_TESTS_REV1.json) covers this
composition, body recall and panel perception. [Nine source/owner faults](GA_SOURCE_OWNER_INTEGRATION_REVIEW_REV1.json)
are detected, including a source-cache fallback, missing-cue bridging, replay and
erasure of prior significance during later formation.

The [surviving-child carrier](GA_SURVIVING_CHILD_CARRIER_REV3.md) now has a machine
draft and [structural review](GA_SURVIVING_CHILD_CARRIER_REVIEW_REV3.md):14 records,
40 fields,12 rejected corruptions. It stores only surviving children and independent
use/direction metadata, with one acquisition header. It does not retain a complete
initial formation envelope as an archive. Its source document fingerprints were
verified against the generated packet. No permanent numeric allocation is proposed.

The [body branch draft](GA_BODY_SOURCE_BRANCH_CLOSURE_REV2.md) explicitly separates
experience reservation, event detection, selection and cueing. A present body cue does
not depend on winning acquisition selection. It also requires a lawful visual fallback
when panel context is absent, so losing context cannot erase perception or encoding.
These are public source obligations, not already passed by component sensing.

The [goal/result identity review](GA_GOAL_AND_RESULT_IDENTITY_REVIEW_REV1.md) reuses
the existing qualified abstract-referent key precedent for a distinct maintenance-goal
record, keeps goal instance separate from definition, and avoids silently reusing the
task-only appraisal occurrence family. New assessment/attribution occurrence families
remain symbolic. Exact-expiry goal reads are excluded from the first bounded lifecycle
profile; broader concurrent expiry/appraisal remains unqualified, with no new phase.

TypeScript and reference boundaries pass. Earlier whole-suite and targeted receipts
retain their exact source scopes. The controlled composition still uses fixture
position/selection/subject admission and a local historical qualification value; the
separate handoff test does not make it a single public committed chain. Public codecs,
source/role admission, memory/protocol/goal owners, delivery authentication, full-prefix
persistence and corpus promotion remain required. Continue closing that inventory
locally before allocation and public implementation.
