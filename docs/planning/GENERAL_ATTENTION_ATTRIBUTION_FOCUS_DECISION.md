# What episode does the four-trial attribution assess?

2026-09-13. Historical packet; [A subsequently accepted](GENERAL_ATTENTION_ATTRIBUTION_FOCUS_RESOLUTION.md).
Child-local credit,
significance state and retention ordering are accepted; this is an unresolved source
scope, not a request to reconsider those decisions.

## Inspected gap

contrastive-attribution-component/0.1-candidate compares four trials: two stroke,
two stationary, matched before intervals and separated after-change intervals. It
requires the future source to authenticate a common perceived candidate, distinct
ordered acquisitions and same subject/observer. The output says Supported/Unavailable.
retained-attribution-use-component adds exact consumed operands, but no attributed
target. The historical ATTENTION_OUTCOME_ATTRIBUTION_DRAFT_REV1 proposes a retained
episode target and an experienced outcome; it does not settle this repeated-trial focus.

Two stroke trials may contain the same perceived candidate in different acquisitions.
Matching its referent across both does not identify which episode the current outcome
is being attributed to. Nor can consumed-operand membership choose it. The new target
normalizer deliberately performs no such inference.

## A — one focal consequence and episode-specific candidate (recommended)

An admitted attribution opportunity concerns one actual observer-safe consequence
and an explicitly selected, previously committed episode-specific candidate child.
The focus must be supplied through a governed recall/connection-evidence path before
the attribution verdict; it is a hypothesis being assessed, not a caller-authored
causal truth. Four-trial contrast provides permitted supporting connection evidence.
Supported yields the exact focal child as attributed target; Unavailable yields none.

Prior corroborating stroke episodes and stationary trials are supporting evidence,
not automatically additional targets of this focal consequence. They may earn use
protection if consumed. Replaying the same candidate in several views still yields
one target address. No truth-origin binding or same-semantic lookup creates focus.

The source contract must still freeze how the actual focal opportunity/retained
candidate is selected and how its observed connection is bound. This proposal does
not give arbitrary callers permission to declare focus. It also does not add a fifth
trial, assume current recall can reveal a forgotten child, or decide whether the
focal trial is a member of the four; exact temporal/source construction remains in
the subsequent shape review.

## B — retrospective target set across supported stroke episodes

The assessment treats each corroborating stroke episode's corresponding candidate
child as a target and may credit multiple episode-specific children together. This
is a stronger cross-episode retrospective consolidation hypothesis. It still excludes
incidental siblings and stationary controls, so it is not the rejected episode-context
spread rule. It needs explicit binding of each target to the credited consequence(s),
not a referent-equality shortcut.

## Discriminator

Acquisitions812 and813 both retain candidate alpha from distinct stroke episodes;
900 supplies stationary gamma. A lawful opportunity is focused on812's consequence.
A attributes only812/alpha; B can attribute812/alpha and813/alpha under its broader
retrospective rule. Holding use, concern and retention parameters equal lets later
survival expose the scope. Neither credits gamma or siblings merely through use.

Requested ruling: use A's focal episode/consequence scope first, retaining B as a
separate cross-episode consolidation candidate. After that, complete the actual
observer-safe source, identity/role and result-binding packet before public writes.
