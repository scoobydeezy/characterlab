# EMB receiving permanent allocation

**embodied-receiving-allocation/0.1-candidate — PERMANENT AND FROZEN.**
2026-09-10. Separate primary-agent numeric review under autonomous-work authorization.
This is a one-to-one realization of the accepted bounded receiving shape, not a new
semantic design or an external verdict.

The exact [machine allocation](EMBODIED_RECEIVING_ALLOCATION_TABLE.json) assigns:

* Records485..515, schema1,31 records and110 fields in accepted order.
* 49 fixed text members in existing namespaces, with their exact NFC payloads.
* Five fixed UnionVariantId1024 pairs: [494,1], [494,2], [507,1], [507,2], [507,3].
* 19 scalar roles, one independently required StateMapKey role, four collection-identity
  checks, ten output occurrence mappings and four finite scalar domains.

There are54 typed fixed members when the49 text members and five union-pair members
are counted together. The JSON `members`/`counts.members` fields count the49 text rows;
`unionDefinitions` separately specifies the five pairs and their exact payload grammar.
This is a counting distinction, not an additional allocation or an alias.

No identity namespace is added. Existing1132..1137 and1139..1141 serve the corresponding
new output kinds under the successor279; each actual output gets exactly one ordinal.
Two candidate-producing stages do not share an output occurrence. Inline ground,
instruction origin, coverage, score distribution and random draw gain no occurrence.

The body instruction root is487/field1, map CharacterId ->486. It is read-only262;
its StateMapKey independently requires validator/character-qualification. No1031/1032
logical family or leaf member and no mutation authority is allocated for that root.
Definition references use1027 plus exact kind/version/adoption checks, not a new domain
validator. Existing body/diagnostic/contact unit distinctions remain unchanged.

The [numeric review](../planning/EMBODIED_RECEIVING_ALLOCATION_REVIEW_REV1.json) records
485 checks and six rejected altered tables, with prior allocation fingerprints intact.
The displayed proposal remains historical review evidence. Neither these checks nor
allocation acceptance qualifies runtime behavior.

No renumbering, reuse or insertion by shifting; future additions append. Adjacency of
record numbers has no semantic meaning. Next materialize and review the13 exact model
recipes, then implement and execute the frozen ER/EMB obligations. EMB-M..O remain
unpassed; broader BODY/Need/learning claims and corpus promotion are not implied.
