# Focal attribution component

2026-09-13. `focal-attribution-component/0.1-candidate`.
Primary-agent component shape acceptance implements the user's one-focus ruling.

A trusted coordinator supplies an already-admitted opportunity containing observer,
character, focal consequence reference and one full acquisition/child address, plus
the retained-use component's memory/read/operand inputs. Bind and detach that focus
before evaluation. Require matching owner subject/observer, extant focal child and
acquiredAt strictly before the evaluation instant. The consequence reference is a
bounded NFC stand-in for an existing qualified public occurrence; no new occurrence
or payload resolver is created.

Execute actual retained-attribution-use-component/0.1-candidate. Its completed result
determines the target projection: Supported produces the one bound focal address;
Unavailable produces no target. Preserve its independent consumed-operand projection
and candidate use-protected memory. Additional views, supporting stroke episodes,
same semantic keys and stationary controls cannot enlarge the target set. Use
protection remains governed by actual semantic consumption, not focal status alone.

Session-local tokens and one-shot finish are inherited from the retained-use component.
finish returns candidates only; the actual public coordinator must atomically commit
the result and derived writes. No significance application is performed here.

This is NOT the governed focus producer. The supplied opportunity remains trusted;
matching symbols and an extant child cannot prove observer-side connection evidence,
actual consequence origin or prior hypothesis selection. The source shape must still
decide whether the focal episode is one of the four trials or a later application.
No membership check or new fifth-trial inference is silently invented in this component.
Public PRJ/IDN, producer binding, result identity, timing and persistence remain open.
