# Selected evidence encoding component

`selected-encoding/0.1-candidate`. SHAPE ACCEPTED by primary-agent review on
2026-09-11 at component scope. Implements the first actual selected-capability to
strength path beneath the accepted attention and encoding-access math components.
It creates no canonical EncodingEvidence record, character state, public ingress,
learning-evidence producer or allocation.

The only content input is a live SelectedView from attention-selection-component.
Consume exactly its complete selectedReferences list once. The consumer receives no
pool, audit, experience, trace/archive resolver, truth input or persistent state.
Unknown/expired capabilities reject. An attempt consumes a valid view even when the
requested calibration is invalid; it cannot be retried against a different law.
Empty selected content produces empty evidence and rows, without invented observer,
experience or owner. A public successor must attach its actual selected envelope.

The bytes come from actual validated SEM224 bindings and SEM240 causal claims.
Group by the existing observer-relative PerceptualReferentId212 canonical value.
The producing attention component allows only one event and one role per eligible
unit, so grouping introduces no multievent or multirole aggregation rule. Every row
has exactly one selected causal claim. No ConceptId alias or new occurrence is made.
Keep the exact consumed bytes and their existing CharacterEvidenceRefs once; each
row identifies its claim by its existing ref. Existing SEM support is the provenance.

The fixed homogeneous-prior calibration is B=3/10. It is not category evidence.
Causal role R is Actor=1, Target=9/10, Participant=3/5, read only from the selected
claim. Continuous attention A has a separately traced calibration with those same
three numbers; a named disabled-attention alternative sets A=1 for every admitted
role. Equality of R/A in the reference calibration does not merge their causes.
No unsupported-role fallback is defined.

Need and surprise channels are Disabled, not observed zero. Their multiplicative
terms are exactly1 by calibration; no need, belief, forecast, outcome comparison,
appraisal or value is read. Trace the disabled disposition instead of fabricating
factor evidence. This leaves the full MEC-007/EXP-007 source obligations open.

Raw=B*R*A. Apply exactly encoding-access-math/0.1-candidate using the selected
continuant keys, one of independent/historical-shared/historical-hybrid/retired-flat,
budget1 and important threshold1/4. These are separate named comparisons; retired-flat
remains RETIRED+CONTROL. No association, accessibility or state write occurs here.
The returned rows retain base, role, attention, raw and strength distinctly. A future
writer must obtain CharacterId through actual accepted PRJ/IDN and attach this exact
producer output; it cannot turn this component result into an admitted state patch.

Frozen SEC-A..H: exact role-factor arithmetic and selected bytes; unselected footprint
invariance; selected footprint contrasts across all four laws; attention-disabled
contrast with role held fixed; forged/expired/invalid-law single-consumption rejection;
empty selection without invented identities; detached output and immutable source;
actual binding/claim ref closure with no truth, causal reinterpretation or state input.

This component does not admit spatial-context operands. The spatial candidate needs
its own future source/view shape; appending arbitrary coordinates to SelectedView
would defeat its closed evidence boundary. Neither component qualifies the other's
source. Public encoding, episodes, association mutation, retrieval, later affect and
corpus promotion remain later gates.
