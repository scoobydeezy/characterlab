# Strength-Priority Graph Retention Baseline

2026-09-12. `strength-graph-retention-component/0.1-candidate`.
Component shape accepted by primary-agent self-review under autonomous authorization.
Implements the supplied weakest-weight-first ruling; not a public owner contract.

Consume the complete resolved graph in graph-edge-loss-component/0.1-candidate's
domain and its separate finite capacities. Normalize supplied exact empty structure;
enumerate only positive directed entries. Sort ascending by exact rational weight,
then lexicographic canonical bytes of cenc list(text(source),text(target)). The tie
rule has no learned meaning. Source/target mean the existing matrix row/column address.

Compute incident degrees and remove the shortest prefix of this fixed ordering whose
remaining edge count and incident-node count both fit. Decrement both endpoint degrees
per lost directed entry; a zero degree frees a node slot. No minimum-deletion search,
bridge protection, node-release preference or newly learned grace period occurs.
Apply the completed plan through the accepted edge-loss component. No intermediate
candidate becomes owner state. Final zero/orphan normalization preserves survivor
weights. Zero capacities yield actual complete loss; a finite prefix always suffices.

Return immutable detached graph/counts plus ordered structural loss addresses, without
lost weights, edge occurrence identities, timestamps or history. All safety/domain
limits are inherited; they are not frozen public capacity values. Input permutation
preserves the semantic loss plan, while returned graph arrays retain input ordering.

Current resolved strength determines survival order, not cost, importance or optimal
retained influence. Learning/weakening → current weight → survival is an explicit
causal hypothesis. Keep edge-specific learning-recency as a serious later comparator;
its timestamp/refresh semantics are not accepted here. Public admission, complete
batch ownership, rollback, persistence and rich retention remain unqualified.
