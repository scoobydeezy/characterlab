# Exact saturation evidence through learning and encoding

**SHAPE ACCEPTED by agent self-review,2026-09-21.** Version
**epi-public/0.1-candidate**. Construction authority, not qualification.
Implements accepted SEM staging, LEARN gated lower-bound equations, and
encoding-access-math/0.1-candidate EAM-1/2 under this bounded composition.

## Frozen domain and controls

One observer/character, two named scalar propositions. Original frames contain
potential[0,1], before[0,1], permission and target; no authored cognitive result.
At most64 increasing instants<=128, two distinct targets per instant, empty S0.
Actual applied=min(potential,1-before), overflow=potential-applied, after=before+applied.
Record912 is truth-only and excluded from the declared character projection.
Safe projection is applied, LowerBound iff after=1 else Point, precision2.
Safe sample900 contains no before/capacity/potential/overflow/provenance/truth handle.

Profile field2 selects Safe, PotentialLeak or OverflowLeak. Leak profiles deliberately
replace the safe measurement with the named hidden value while retaining the actual
point/bound classification. They are violating researcher controls, never lawful
sensor or character access. Denied frames emit no sample under every profile.
Profile field3 selects exact rational versus millionth ties-to-even commit for belief.
Encoding strength remains exact under both. No general scalar inference law is chosen.

## Causal ordering and complete bounded consumer roster

probe50 reads retained belief and last encoding independently. world110 computes
and exposes912 only in research outputs/trace. observe120 projects the physical
frame to900. freeze124 creates actual SEM support iff samples exist. evidence130
reads prior belief and freezes904 plus910 from that same safe sample. apply140
updates belief903; encode140 independently writes latest encoding911. Both children
are emitted by130; encoding consumes its already-frozen prior and never the newly
written belief. Encoding owner cannot mutate belief. No event follows140 in the
same instant; no current phase30 learning is introduced.

The complete character output roster is900 observation,907 SEM envelope,904 learning
evidence,910 cognitive encoding,905 belief application and906 subsequent probe.
SEM is semantic encoding support,910 is the actual admitted encoded measurement,
surprise and salience readout,911 retains the encoded unit. No trace/source resolver
or memory reconstruction exists. No empty echo output is counted as salience.
This roster covers the finite experiment's same-event learning/encoding consumers,
not all unrelated character domains, arbitrary graph learning or general appraisal.

## Surprise and salience

For known prior mu, Point surprise=abs(x-mu); LowerBound surprise=max(0,x-mu).
For unknown prior the source-domain reference mu=0 is used only for this numeric
candidate; absence remains explicit in910. No stored zero belief is manufactured.
Inputs in[0,1] keep surprise in[0,1]. Fixed base=role=attention=1, Need=0,
alphaN=0,alphaS=1. Existing EAM raw=1+surprise and independent strength=raw/(1+raw).
Those constants are explicit controlled conditions, not learned Need or inferred
causal role. Opaque proposition identity is used only for grouping.
910 records occurrence, sample, optional prior, surprise, raw and strength. It
feeds its own retained encoding owner unchanged. Learning does not read salience or
count it as extra evidence; no precision-from-urgency law is added.

## Learning, retention, trace and closure

Gated LEARN equations alone: rho2, strict bound informativeness before commit,
missing leaf unknown, mean/precision/support as in learn-public/0.1-candidate.
The predecessor's laws and bytes are unchanged. Root903 uses the existing
belief-expectation authority. Root911 maps character/proposition key901 to the
latest910 under authority/episodic-encoding. Overwriting that bounded last-sample
slot is explicit profile retention, not general forgetting or GA replacement.
Probe906 has optional prior and separate optional encoding; present encoding must
never imply known belief. Private computation has no source archive read.

Trace records exact registered reads, owner patches, physical decomposition and
safe inputs/outputs. Canonical public raw outputs include research-only912; the
explicit character projection excludes only912 and validates all remaining kinds.
Do not describe raw research outputs as character knowledge. Generic quantization
trace list is empty;905 contains raw/final belief arithmetic as in LEARN.

Records897..912/schema1 and occurrence namespace1159 are separately allocated.
Exactly six frozen models (three projections x two numeric profiles), data-only
original admission, original/generated payload authentication, whole-instant
rollback and exact complete-prefix restoration. Model preparation admits only frozen
identities. A new consumer, permitted sensor, hedonic signal, source, evidence
linkability or ordering claim reopens the bounded qualification.
