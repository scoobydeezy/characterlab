# Intention forgetting by retained-content and access dissociation

intention-forgetting-component/0.1-candidate, ACCEPTED before implementation,
2026-09-26. LOCAL DISPOSITION — no owner ruling required. Architecture6/10/11,
MEC005/011/020 and EXP004/013 preserve memory, access, feasibility and lifecycle.
Composed component experiment, as in BIO; not a new public scheduler/factory.
No records, namespaces, state roots or general forgetting formula allocated.

One initially unadopted deliver-item task; one admitted instruction at1 chooses
routeA/B. Its action operand is encoded only into a retained acquisition, not an
immutable task instruction lookup. Goal adoption/status is separate from the route.
A neutral acquisition at2 competes for retention. Existing canonical-child-key and
significance-first-retention-component/0.1-candidate select surviving units; all
protection metadata empty. Capacity1 removes the older instruction, capacity2 retains
both. These are explicit controlled resource interventions, not endogenous memory
pressure or a new psychological loss law. No archive may reload removed bytes.

Eight consecutive original frames,1..8. Each step reads only prior safe cue/opportunity,
retained memory and goal. Existing canonical-event-recall-component/0.1-candidate
uses beta1/2,scale1000,lambda1,exponent1,omegaB0,omegaA1,K1 and empty association graph.
Present keys1intention/2neutral preserve exact existing top-K semantics, including
zero-score selection. Absent cue does not read memory through the recall entry.
Only a returned instruction unit can supply the action operand. A reminder never
contains a new instruction. Retrieval alone adds neither acquisition nor presentation,
protection nor adoption. PersistentAccess substitutes the intention cue as an explicit
control; NoLoss uses capacity2. The baseline remains RetainedCue. No loss necessity
or general access-law selection follows from these controls.

If adopted Open, before deadline7, visibly feasible and recalled instruction exists,
its option-specific task ground402 has value1. Use inherited compileReasonNuclei and
exact analyzeOptions. One positive candidate resolves Auto; empty option domain means
no intent. No roll is needed or fabricated. Intent and frozen expression precede
attempt/physical execution. A current hidden execution gate determines success but
never contributes to recall/reasons. Visible outcome140 can fulfill the goal; hidden
success cannot. Cancellation admitted140 retires Open; deadline7 expires it separately.
Terminal status stays terminal, regardless of reminder or retained content.

At120 the controlled source projects visible instruction1, neutral content2 and
cue/opportunity/cancel signals. Retention/consolidation140 uses only these admitted
signals, then next step can recall. Receipt may be authentic/false-success/false-failure;
this convention is fixed for the run. Physical truth and private source bit remain
outside the safe character projection. Same safe source/receipt implies whole later
safe-view equality. Input gating and ordering are exercised here; generic public
source authentication, scheduler ownership and canonical save APIs are not qualified.

ModelIdentity commits version, law, calibration/content and source-artifact manifest;
RunIdentity commits empty state, all originals and deterministic seed; ExperimentIdentity
and ComparisonCase freeze before execution. Component prefixes reexecute and compare
complete snapshots and next transitions, not public save restoration. No character
reader consumes diagnostic histories. Validate dense exact data-only input schemas.

Require retained-inaccessible goal, reminder recovery followed by actual execution,
actual content loss defeating the same reminder, both action instructions (prevent
hidden fixed-plan fallback), false/no cue, opportunity and execution failures,
NoLoss/PersistentAccess competitors, cancel/expiry, denied source and whole-view privacy.
Preserve future generalization in RO005/006/007/012/014/019/020;021 history gate remains.
MEC012..017/019 reuse exact reasons/Auto/expression/attempt boundaries; MEC018 acquired
identity controls remain separate. General natural cue recognition, time-driven decay,
prospective-memory storage reduction and lost-intention reconstruction are not claimed.
