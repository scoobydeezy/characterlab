# Selected description and aggregate evidence contract

2026-09-20. LOCAL DISPOSITION, accepted component successor
`multisource-selected-descriptions/0.1-candidate`. Old single-view selected-source
and source-join versions remain unchanged. This version does not admit public models.

Consume one actual local-reserve selected capability, with at most three signal groups
and two views per group. Validate the local observation version. One to three immutable
description declarations each name a distinct opaque ID, one observer-side signal,
one or two distinct observation channels, a positive criterion threshold and declared
FavorBelow/DisfavorBelow polarity. Each description belongs to one motive/role in the
eventual receiver; these operands do not create grounds or grant instruction adoption.
Expected observer must match. Source samples must be current selected observations;
all declared channels must belong to the named selected signal. Missing support makes
the whole description Unavailable; never shrink its basis to the available subset.

A singleton uses its actual interval. A two-view description uses the conservative
interval hull [min(L1,L2),max(U1,U2)] and response max(0,(T-U)/T), with declared polarity.
The hull is an explicit bounded summary candidate, not optimal fusion or a new Need
law. Both selected readings refer to the same declared signal/unit. No averaging or
addition of reserve quantities, hidden source key or unselected view is permitted.
Evidence is the exact set of observation237 references, each weight1, under400/399;
retain original samples and interval endpoints. Known zero retains the full basis.
Two descriptions citing the same view preserve the same fact identity, not new facts.

The intended collective control uses real same-signal views a,b and hull(a,b), sorted
by the receiving law's existing magnitude/canonical source order. With level20,
widths10/20 and threshold60, strengths are1/2,1/3,1/3. Declare source IDs so singleton b
precedes the equal-magnitude hull in this fixture; report the tie and do not generalize
to arbitrary ordering. Aggregate coverage yields third0 and bounded total5/11;
pairwise-max yields third1/6 and total1/2. This is provenance redundancy, not proof
that the two readings are statistically independent. The separate independently
intervened A/B reserve control remains mandatory.

Missing/denied second view, capacity exclusion, same-observer bounds, foreign signal,
duplicate channel, forged/reused capability and hidden-within-bin invariance are
required negatives. Declaration/source ordering is nonpsychological except for the
already-declared deterministic equal-magnitude tie. No arithmetic summary authenticates
adoption, parent identity, public read domains or an output occurrence.

Preserve MEC-012/013/014/016 and EXP-009/014, and RO-C3-001. Public successor shape must
carry description identity separately from ground identity: frozen task495 has no
description slot and cannot express multiple descriptions of one task/role. Do not
manufacture new task IDs to obtain that slot. Exact inventory and public admission
must resolve that representation before allocation, then receiver/model/replay proof.
