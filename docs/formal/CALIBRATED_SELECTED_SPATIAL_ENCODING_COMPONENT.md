# Calibrated selected spatial encoding

`calibrated-selected-spatial-encoding/0.1-candidate`, 2026-09-13. LOCAL DISPOSITION
— no owner ruling required. Extends the component's explicit comparison entry points.

The existing default encoder keeps budget1 and threshold1/4. The separately named
calibrated entry consumes the same one-use selected-only operands and an exact model
calibration: one of the four accepted laws, budget in(0,1], threshold in[0,1]. It
calls the existing exact encodingBudget kernel. No rescaling, conserved-resource
interpretation, learned calibration or state read is introduced. Independent and
retired-flat remain invariant to budget; historical shared/hybrid retain their exact
denominator semantics. Public recipes further close the allowed parameter values.

Separate canonical EqualPriority and Unlimited selector exports use the already
frozen AttentionSelectionPolicy519 algorithm values2/3. Capacity remains0..2.
Unlimited deliberately ignores it and can retain all three eligible units, as
SelectedEvidenceView534 already permits. Eligibility and one-binding/one-claim533
constraints remain unchanged. Each real selector still allocates one1143 audit.
No namespace, policy record or generic observation contract is widened.

The formation adapter exposes separately named preparation controls and a calibrated
encoding entry. Positive projection still owns no identity; its later producer owns
one acquisition for all positive children. Role-priority default behavior is unchanged.
Calibration and comparator source admission remain obligations of the public model.
