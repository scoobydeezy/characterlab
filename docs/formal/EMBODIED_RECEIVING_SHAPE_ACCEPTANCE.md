# Bounded embodied/task receiving — symbolic shape acceptance

2026-09-10. Accepted by primary-agent adversarial review under explicit autonomous-work
authorization. This is not an external planning-agent verdict. Numeric allocation,
exact model freeze, runtime qualification and parent EMB completion are not implied.

The exact packet is fingerprinted by EMBODIED_RECEIVING_SHAPE_MANIFEST.json. The current
receiving draft is revision2; closure revision3; model proposal revision2; symbolic
inventory revision3; vocabulary revision2. Earlier draft statuses/open-gate language
are superseded **for symbolic shape only** by this document and the whole-shape review.
Earlier inventory revisions are historical and ineligible as allocation inputs.

## Accepted executing versions

| Component | Version |
|---|---|
| Pressure with one receiving child, unchanged p law |embodied-pressure/0.2-candidate |
| Genuine task source branch in this composed graph |embodied-task-source/0.1-candidate |
| Immutable adopted response instruction and body options |embodied-response-instruction/0.1-candidate |
| Shared complete option and explicit origins |embodied-task-options/0.1-candidate |
| Reference ground/role/sign receiving |embodied-task-reasons/0.1-candidate |
| Declared competing no-coverage receiver |embodied-task-reasons-no-coverage-control/0.1-candidate |
| Exact shared arbitration and scoped RNG |embodied-task-arbitration/0.1-candidate |
| Separate intent and frozen per-ground expression |embodied-choice-expression/0.1-candidate |
| Separate plan, attempt and terminal truth outcome |embodied-protocol-attempt/0.1-candidate |
| Exact original/child/sibling graph admission |embodied-receiving-admission/0.1-candidate |

Candidate labels replace the corresponding draft labels in the accepted packet; no
previously frozen candidate is renamed or edited. Model/registry/parameter/numeric
candidate versions follow the exact proposed stems in the model document. Proposed
ordered-input, trace and persistence profiles are embodied-receiving-ordered-input,
embodied-receiving-trace and embodied-receiving-persistence, each /0.1-candidate, with
the exact rules in closure/model. Their byte commitments remain a separate gate.

## Binding dispositions

There are31 new symbolic records/110 fields,49 proposed fixed members and no new identity
namespace. Existing output occurrence families are explicitly extended in the successor
model's279 to the new representations of the same semantic output kinds. Distinct actual
body/mixed candidate outputs get distinct ordinals; no duplicate identity for one output
or inline ground is created. Old mappings/decoders/profile admission remain unchanged.

The field and member inventories are binding together with every prose refinement.
Scalar namespace admission does not replace state-key role, exact definition kind,
source/subject equality, collection completeness, one-shot ingress or admitted evidence
producer checks. Stage alternatives are the exact closed sixteen-row table, not an
interpreter for arbitrary model-authored transitions. NoStateWrites applies to all new
rows. Original377 has IDN projection; generated rows inherit subject from actual inputs.

Only the original deadline owner and body delivery owner may write. Proposed sibling
joins require both completed sources before allocation, preserve actual parent and
source identities, and roll back with the entire instant. Pressure0.1's terminal graph
and task-cognitive-path/0.1's intact graph remain unchanged. The new versions name their
actual outgoing edge differences without replacing the accepted formulas beneath them.

The one new read-only prospective instruction root has no1031/1032 family/leaf members
or writer. Existing exact body unit is not the diagnostic prediction unit or contact
unit. The actual dormant359 declaration retains its original diagnostic semantics.
Missing forecasts, acquired standing, mixed identity qualification and mixed consequence
learning are explicit absent paths; no fake task, efficacy estimate, successful relief,
TaskQualificationContext or generated InputOnly478 is introduced to fill them.

The finite public cohort has thirteen recipes including a separately versioned, runnable
no-coverage comparison. It can fail the duplicate-description phenomenon without being
rejected as an invalid reference-model run. This does not qualify it as the reference
law or decide universal cross-family normalization. The old task identity/dice controls,
body kernel, CTL-001 and all historical intake obligations retain their scoped status.

## Implementation gates

EMB-M..O and ER-A..R are FROZEN, NOT PASSED. Arithmetic and abstract scheduling receipts
establish feasibility only. The exact body+task public runtime, thirteen canonical
models, real competing receiver, field/role/source mutations, occurrence/RNG closure,
all-stage rollback, full-prefix restore and predecessor preservation must still run.
No numeric allocation is authorized except through the next separate reviewed table.
No general BODY, Need ontology, learned relief, mixed identity loop, corpus promotion
or parent EMB completion follows from this shape acceptance.
