# Bounded public attention — whole symbolic shape acceptance

2026-09-11. **SHAPE ACCEPTED by primary-agent adversarial review under the user's
autonomous-work authorization.** This is not an external review or runtime verdict.
The exact accepted files are committed by ATTENTION_PUBLIC_SHAPE_MANIFEST.json.

Accept `attention-public-integration/0.1-candidate` and
`attention-public-declarations/0.1-candidate` for the finite three-port observer source,
post-perception selection and selected-only diagnostic consumer. Candidate labels
replace the corresponding draft labels within this accepted packet only. Earlier
draft artifacts and their review receipts remain historical and unchanged. The
declaration closure takes precedence over the public integration inventory's abstract
union layouts and its then-open declaration questions. Allocation input is solely
ATTENTION_SYMBOLIC_DECLARATIONS_REV1.json with the binding prose refinements.

The shape contains26 new symbolic records/89 fields, ten fixed stages, two proposed
occurrence families,52 fixed member payloads,62 scalar RecordField roles plus two
ObserverId StateMapKey roles, and three explicit identity-collection checks. The
model proposal contains32 finite recipes and seven originals selectable per model.
No new record, field, union tag, namespace or member has a permanent numeric allocation.
Exact model/version bytes remain a later gate.

The bounded causal path is actual observer-safe role detection → actual SEM tracking,
binding, freeze and role derivation → phase40 role-priority selection → phase40
selected-only processing receipt. A fully denied observation uses the explicit empty
branch without reserving or fabricating an experience. K0 preserves actual perceived
history. Actor/Target/Participant retain exact priorities1,9/10,3/5; equal-priority and
unlimited controls remain separate committed models. Missing, unsupported and multiple
roles do not receive fallback importance. This is not residual-pool salience.

Explicitly accept the observer-only subject refinement: no CharacterId, PRJ requirement
or IDN access is needed by this diagnostic. This supersedes only the component
contract's prospective statement that a public adapter must attach PRJ/IDN. The
component's accepted input validation, mathematics and access semantics remain
unchanged. Any future character-state consumer must use accepted PRJ/IDN; no inferred
subject or duplicate roster is authorized by this refinement.

The dedicated finite registration compiler is required because existing V04 admission
does not admit these identity-free transports and tracking writes. Existing V04 and
receiving515 remain unchanged. The separate finite content compiler admits the three
governed scene objects through generic CONTENT, their registered329 scene-object kind
and exact scalar VAL semantics. It does not relabel objects as characters, alter the
character-only VAL compiler, accept public callbacks or substitute empty content.
Both compilers must use the same copied, committed model source.

Only Track writes the four existing241/242 leaves under authority/perception.
Selection and processing have empty persistent read/write domains. Input-evidence
reads appear in the processing receipt, not the persistent-state read ledger. The
consumer receives no full audit, source experience, observation envelope, archive,
truth handle or generic resolver. No new CharacterEvidenceRef alternatives exist.

Whole positive execution is bounded by9 events/15 outputs/18 shared runtime slots;
empty execution by4/4/7. Observer-file counters remain separate. Nested identities and
conditional experience reservation are explicitly accounted for; wrappers and selected
views allocate no identities. Trace160 and save132 remain the sole standard trace/save
surfaces. Whole-instant rollback and exact complete-prefix replay are required.

The declaration review checked symbolic closure and detected12 specified corruptions.
Existing-content probes established non-character content expressibility and preserved
the old VAL rejection boundary. All453 prior EMB fingerprints remain unchanged.
These are shape/expressibility checks, not new runtime qualification or behavioral proof.

AT2-A..N and the declaration/admission controls in the closure remain **FROZEN, NOT
PASSED**. Next: separate permanent allocation/parity review; exact32-model commitment
construction and review; canonical factory implementation; public/component execution,
trace corruption, work8/9, all-stage rollback, complete-prefix restore and preservation.

No encoding, memory, retrieval, residual salience, affect feedback, learning, general
ATTN closure or corpus promotion is accepted. MEC-005, MEC-007 and CTL-004 retain their
recorded controls. BODY/MULTISOURCE remain PARTIAL; ATTENTION corpus intake remains
BLOCKED. Campaign3 remains open. Corpus0.27.0 and all prior frozen artifacts are unchanged.
