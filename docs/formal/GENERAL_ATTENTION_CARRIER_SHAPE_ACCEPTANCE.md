# General attention carrier shape acceptance

2026-09-13. **PRIMARY-AGENT SHAPE ACCEPTANCE — bounded carrier scope.**

Accept `general-attention-carrier/0.1-candidate`: the163 new record layouts, their
field order and optionality, finite enum alternatives, distinct-record unions,
recursive identity-role boundaries and the six already proposed identity families.
The accompanying manifest commits the exact allocation input and review receipts.

This is a representational acceptance beneath the already documented component
contracts. It does not accept a new psychological mechanism, grant a new state
authority, or change any accepted mathematical law. It follows the user's autonomous
review authorization and Campaign3 escalation policy. No owner ruling is required.

## Basis

The revision17 role crosswalk covers163 records,165 scalar identity positions and208
recursive boundaries. Its33 structural controls pass. The carrier expressibility
review checks every field/reference, rejects recursive record graphs and nested
optional collection elements, and verifies11 distinct-record unions. Nine injected
layout faults reject. Every primitive leaf has executable grammar coverage.

The67-template registration carrier reuses subject projection266 and write
authorization322. Its nine structural controls pass, including physical phase110
and direct ObserverId input-field coverage. Output declaration revision3 separately
distinguishes fresh, reserved, borrowed and observer-file ownership. The graph and
output reviews pass12 and14 structural controls. No new record name collides with
the existing permanent allocation tables.

The accepted layouts preserve acquisition identity independently of selection,
safe evidence independently of hidden source truth, graph independently of episode
survival, and retention metadata independently of recollection content. An actual
formation can commit with immediate complete loss. Protocol history has its own
noncognitive root. Those distinctions remain separately testable.

## Allocation gate and limits

The separate numeric gate may now propose RecordTypeIds, field ordinals, field-local
enum tags, and namespaces for LocalReserveId, InteroceptiveSignalId,
AcquisitionOccurrenceId, GoalOutcomeAssessmentId, RetainedAttributionResultId and
ProposedRecollectionOccurrenceId. It must preserve existing assignments, reject
collisions and supply an independent allocation audit before permanent freezing.
No numbers are assigned by this acceptance.

Fixed registry members, exact model instances, source calendars, contextual record
relations, executable accessor/registration compilation and the work ceiling still
require their model-packaging closure. They need not be falsely called complete to
allocate these reviewed layouts. This restores the intended sequence: symbolic
shape → separate allocation → exact model packaging → public execution/proof.

Public source/subject authentication, exact output slots, whole-B0 owner/trace
composition, rollback, replay, corpus promotion and General Attention remain OPEN.
The inherited concern-source controls do not qualify a same-run GA feedback join.
Missing-facet controls retain their explicit component/profile-exclusion scope.
No public factory activation or change to frozen corpus0.27.0 follows from this receipt.
