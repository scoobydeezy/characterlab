# Panel window local transaction

`panel-window-transaction/0.1-candidate`, 2026-09-13. LOCAL DISPOSITION — no owner
ruling required. Uses panel-window-perception/0.1-candidate, preserving its boundaries.

A trusted local manager holds only observer, existing SEM event-file owner and the
last panel window. Construction clones supplied data; it does not grant canonical
restore authority. Preparing a source operation validates and derives a detached
candidate, with one pending token. Closing aborts, committing publishes once, and
stale tokens reject. Returned transition/context views cannot alter the candidate.
The requested multimodal source uses this owner alongside the bounded marker owner;
both publish only after successful freeze. It preserves the previous panel-only,
shared-event and ephemeral visual-only result shapes for differential controls.

GSO-J exercises complete outputs across seven request masks, eight presence cases
and both lanes, and every one of fifteen source allocation-failure slots. Source
occurrence ordering and allocator rollback remain separate runtime obligations.
This local transaction is not an admitted state-path writer or public persistence
implementation. No memory, association, physical or protocol ownership is added.
