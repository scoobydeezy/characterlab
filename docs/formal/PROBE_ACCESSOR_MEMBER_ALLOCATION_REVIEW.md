# Probe accessor — append-only member review

**Status: PERMANENT AND FROZEN.**
Version: `probe-accessor-member-allocation/0.1-candidate`.

Authority: [accepted symbolic ruling](../planning/CAMPAIGN2_PROBE_ACCESSOR_REVIEW.md).

| Existing namespace | Exact canonical NFC text payload |
|---|---|
| 1028 | accessor/regulatory-diagnostic-displacement-prior |

One new member of existing ProjectionAccessorId. No numeric namespace, record, role, registry
kind, union, state root or save field. It names only the diagnostic consumer's direct read of
root 302 / field 3 / RegulatoryAdaptationKey(C,V). It grants no capability or observer access.
The shared runtime ordinal allocator is unrelated to this text vocabulary member.

Prior ResolvedCharacterSubject, accessor/adaptation-target-prior and accessor/adaptation-gate-prior
remain unchanged and are not aliases. The machine table snapshots prior frozen allocation files;
the mechanical audit verifies exact one-to-one membership, absence from prior authority and
preservation. Numeric/member acceptance is separate from runtime qualification.

Accepted 2026-09-07: this member is permanent with no reuse, renaming or insertion-by-shifting.
The frozen probe .1 model cannot acquire it retroactively. Successor trace/RulesVersion and
ModelIdentity review follow; this allocation does not modify a registry manifest or model packet.
