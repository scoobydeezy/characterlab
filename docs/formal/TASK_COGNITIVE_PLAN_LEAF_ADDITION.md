# Cognitive prospective instruction leaf addition

Version: task-cognitive-plan-leaf/0.1-candidate.
Status: INTERNALLY SHAPE ACCEPTED; separate additive allocation required.

Model declaration construction exposed one inventory omission after
task-cognitive-allocation/0.1-candidate froze. The accepted plan shape requires both
materialized fields of373/schema2 to appear in the prospective topology, but the
fixed-member inventory named only the new identity leaf, omitting an identity for
the already specified immutable instruction field. No cognitive model was frozen
or activated with the incomplete topology.

Accept the exact additional existing LeafFamilyId1032 member `leaf/task-instruction`.
Campaign2Storage286 for the existing prospective-commitments family maps existing
`leaf/task-commitment` to field1 and this member to field2 under root373. Field2 is
the already allocated AdoptedInstructions map, with262 read-only admission and371
record-key grammar. It has no mutation authority. Lifecycle authority still owns
field1 only; identity authority still owns415/1 only.

This is a one-to-one physical leaf discriminator, not a second logical prospective
family, task identity, plan registry, acquisition transition or new state field.
No namespace, record, field or enum is added or renumbered. The frozen103-member
base allocation remains exactly as reviewed; the composed profile additionally
consumes this separate one-member allocation after its numeric gate.

The accepted model must contain exactly these two prospective leaf mappings and
the disjoint writable/read-only declarations. Missing/extra/swapped mappings or a
write permission for field2 must reject. The old task profile still has only field1
and rejects the successor declarations. Model construction must test this joined
closure rather than assume the earlier scalar-role/output audit covered topology.

This correct-forward addition supersedes only the omission from the base fixed-member
inventory. It does not reopen source mathematics, selected task/plan semantics,
previous allocations or runtime qualification gates. It is internally reviewed under
the user's independent-work authorization; no external approval is claimed.
