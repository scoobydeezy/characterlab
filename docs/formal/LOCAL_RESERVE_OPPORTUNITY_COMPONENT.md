# Body-only local reserve opportunity component

`local-reserve-opportunity-component/0.1-candidate`, 2026-09-13.
LOCAL DISPOSITION — no owner ruling required. Composes the actual sample bridge with
SEM-001H lane admission and freeze at component scope; public registration stays OPEN.

For the body-only branch, derive presence from the actual produced461/463 batch.
Reserve exactly one ExperienceId through admitObservationLane iff any sample is
present. Freeze one support-only experience at the selected lane's exact freeze phase,
containing exactly the present observation identities. Multiple views share it; all
unavailable samples reserve and freeze nothing. No event detection/file is invented.

The returned opportunityId is explicitly null when there is no experience. The new
local-reserve selector and cue entry points accept that absence; an actual present
sample with a null opportunity rejects. No placeholder ordinal or duplicate occurrence
is used. A completed empty selector may still be a protocol source, with its separately
allocated SelectionOccurrenceId. Cue absence is not a successful durable formation.

Existing singleton component entry points still require their original nonnegative
bigint opportunity operand. New positive inputs keep that same shape. The private
shared algorithms retain identical selection/ranking/cue laws; only the new input
branch can represent no experience. A combined panel/visual/body source must use its
one combined reservation and must not invoke this body-only adapter as a second
reservation. Its branch matrix remains a separate integration obligation.

The result exposes actual staged SEM data for component validation, not proof of
public event completion. No recognition, EVID output, PRJ subject or memory mutation
is produced by this adapter. The first current/consequence sample versions remain
the explicit local-reserve producer version, with the lane supplied by the trusted
coordinator. Exact public lane/source/time admission and successful whole-settlement
matching remain separate gates.
