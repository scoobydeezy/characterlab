# Common-evidence source join

2026-09-20. LOCAL DISPOSITION. `multisource-source-join/0.1-candidate` is accepted
for the trusted transaction implementation and symbolic successor shape below.
No new numeric allocation, registered public original, model identity or replay
qualification follows from this acceptance. Frozen receiving499 cannot carry this
join: its embedded491/498 ancestors describe a different source graph.

## Transaction authority

A compiler-issued private profile binds one observer, holder, task371, selected
instruction/action, body instruction, task active interval, actual inherited
workspace378/concern385/motive392 definitions, and both body-evidence criteria.
The profile admits exactly three unique channel/signal pairs, one view per signal,
and capacity0..3. Task criterion is attached to the selected task instruction;
body criterion is attached to the adopted body instruction. The model compiler
must eventually bind these declarations to its immutable registry/content.

Execution receives the compiled profile, authoritative state, an actual opaque
LocalReserveSource, one instant and an occurrence allocator. It accepts no parent
workspace/raw output, selected capability or observation from its caller. Read only:
268/1[observer] ->267 qualified holder;373/1[task] ->372 active status;
373/2[task] ->390 selected instruction;487/1[holder] ->486 adopted body instructions.
Verify each binding and activeFrom <= instant < deadline before sensing/allocation.
Reject this paired profile when either instruction is absent. Body-only adoption
semantics need a separate profile; Base-off is the genuine inherited motive392
control with the adopted task/plan still present, not deletion of task adoption.

The narrow workspace permits exactly one selected task, capacity1 and disabled
forecast access. Standing is disabled. Appraisal/concern are inherited and unavailable
without forecast. Task commitment is generated by the actual motive definition;
no caller Boolean overrides it at execution. Generate workspace/appraisal/concern/
motive/candidates/raw through the existing transforms. Observe actual local reserves
at the same instant in the Current lane and stage the inherited experience. Verify
channel->signal correspondence against the compiled profile, select through the
existing gate, then invoke multisource-selected-source/0.1-candidate. One allocator
wrapper rejects reused/negative ordinals across both paths. No state writes occur.
Failure consumes no authoritative state; allocator rollback remains the enclosing
scheduler transaction's responsibility. This API alone is not a scheduler rollback.

## Output shape and provenance

Return a detached internal bundle containing version, observer/holder/instant,
exact state-read witnesses, actual task raw403 with its complete ancestor chain,
actual observation batch and staged experience, selection audit, and the separately
interpreted source result. Unselected samples belong to the source-side audit only;
they must not enter reason support. No physical reserve keys or truth values enter
the character-side assessments. Source declaration mapping is checked but the
opaque source's parameter/state authenticity remains the later model boundary.

The successor public shape needs a fresh join/output envelope referencing those
actual parents plus the compiled criterion definitions. Use existing canonical
observer/task/option/evidence identities as borrowed references; reserve a fresh
output occurrence only after the symbolic record inventory is accepted and allocated.
Do not wrap this bundle in frozen499, append fields to old schemas, use unregistered
accessor identities, or call a component object a public canonical output.

## Comparison/preservation obligations

Preserve MEC-012/013/014/015/016/020, actual adoption, ground/role/sign distinctions,
and RO-C3-001. Tests must reject foreign holder, retired task, absent/rebound plans,
absent body adoption, forged profile/source, channel mapping mismatch, duplicate
allocation, and out-of-window execution. Preserve Base-off, hidden-within-bin and
independent-source interventions and no state mutation. Repeat runs with the same
state/source/allocator start must return equal bundles; that is component determinism,
not a public RunIdentity or complete-prefix replay proof.

Next gate is exact public record/registry inventory and receiver shape, including
serious normalization and actual duplicate/third-aggregate source controls. Accept
that inventory before allocation. Public input/model admission and registered
ownership, output validation, arbitration, execution, rollback and replay remain open.
