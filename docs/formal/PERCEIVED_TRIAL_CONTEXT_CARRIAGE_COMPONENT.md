# One observed context companion check

`perceived-trial-context-carriage/0.1-candidate`, 2026-09-13. LOCAL DISPOSITION — no
owner ruling required. Factors the already implemented body companion checks for
reuse by visual acquisition, without defining another provenance system.

The exact companion has Experience, Context and Panel. Panel has Observation and
a Present sample with time, observed glyph and stage. It must match the SEM-H
experience's identity, observer, instant, event membership and supporting panel
observation. Unknown/executable fields reject; returned data is detached. Numeric
glyph and stage domains stay those of the existing panel source. No IDs allocate.

These checks establish structural linkability, not source authenticity. Each caller
still binds the actual producer, admitted source and its own observer/time envelope.
The companion alone neither creates a selected child nor proves causal attribution.
It contains no truth handle or raw sample resolver. Public record allocation and
actual producer admission remain OPEN. CB-A..E retain the body instantiation proofs;
the visual instantiation must execute its own selected-capability checks.
