# person-goal-public/0.1-candidate

ACCEPTED LOCAL DISPOSITION,2026-09-24, before implementation. Architecture9/11,
Brief12.10 clause4; no owner ruling. Ordinary plan selection follows GOAL precedent,
not a new Decision law. Goal, plan, attempted motion, achieved location, observation,
inferred desired state and observer appraisal remain distinct.

One target and two observers share a controlled delivery setting. The target adopts
one standing desired state: repeated delivery to destination1 or2. Explicit adoption
at140 of the first request creates an immutable goal record; later requests neither
readopt nor retire it. This is a standing delivery control, not goal fulfillment or
general persistence. All originals repeat the same adoption value, validated constant.
Empty S0;1..8 requests at strictly increasing instants1..16.

Route catalogue is fixed common prior knowledge, not learned recognition: route1 is
the shared hub, compatible with destinations1/2; routes2/4 reach1, routes3/5 reach2.
Routes2/3 are direct branches,4/5 are alternate paths. Route identity denotes physical
motion/location, never the target's purpose. This known affordance mapping is bounded
source authority and must not be generalized to unguided goal discovery.

At10 target receives routing mode0 unavailable/1 shared/2 direct/3 alternate. At90
ordinary plan reads only its adopted goal plus this admitted mode. Absent goal or
mode0 selects0; mode1 selects hub1 for either goal; mode2 selects desired+1;
mode3 selects desired+3. No contested choice, intent or DecisionExpression claim.
At100 produce actual attempted route (0 means no attempt). Execution110 succeeds
only for a nonzero route and enabled physical success. A successful hub traversal
does not reach either destination; other routes produce their catalogue destination.
Attempt and outcome remain separately traceable. Failure does not erase the goal.

At120 observers independently admit attempted route and achieved destination through
separate access. The action classifier can independently mirror left/right branches
(2<->3,4<->5;hub unchanged); it can therefore mislead without injecting a goal label.
Outcome sensors report actual achieved destination through access. No execution
means no action reading; failed execution can still have a visible attempted route.
Every observation names holder,target,setting. No source/run/hidden-goal provenance
is observer evidence. At140 append only observations with at least one admitted field.

At40 infer from prior retained evidence. Candidate1 RouteAndOutcome keeps the latest
admitted route and destination. Route factor in odds goal1:goal2 is3 for a route whose
catalogue destinations are only1,1/3 for only2,1 for shared hub. Outcome factor is3
for achieved1,1/3 for achieved2. Multiply factors once, prior odds1; p=O/(1+O).
Unknown both fields gives unknown, while an observed shared hub gives known ambiguity
1/2. This is a finite candidate compatibility law, not calibrated inverse planning.
Candidate2 OutcomeOnly uses only achieved destination; it misses failed attempts.
Candidate3 NoLearning retains no evidence. Candidate4 GoalOracle reads the target's
adopted goal directly (or unknown before adoption), explicit epistemic negative control.
Five frozen models: laws1..4 observer goal1, plus law1 observer goal2.

At50 observer appraisal consumes only the frozen judgment and its own goal: goal1
values delivery to1, adverse=1-p; goal2 values delivery to2, adverse=p. SplitExposure
with s=v=1,c=0 gives E=U=adverse; unknown stays unknown. No observer action claim.
Learning140 changes only later inference40/appraisal50; old judgments are immutable.
Repeated probes recompute from retained fields, never repeatedly multiply evidence.

Stages: source0, private10, infer-a40, infer-b40, appraise-a50, appraise-b50, plan90,
attempt100, execute110, observe-a120, observe-b120, adopt140, learn-a140, learn-b140.
Independent scheduled branches respect these phases; fixed reservations include every
empty/failure branch. Target goal owner alone writes root1192/field1; plan reads that
leaf. Observers read/write only their map leaf in1185; only Oracle adds goal read.
No mutable plan root is claimed; each ordinary plan is a frozen per-request record.
Max settlement32. Records1178..1194/schema1, occurrence namespace1175; allocation
table governs. Data-only recipes, frozen model/run/experiment/comparison identities,
seed32 bytes filled13 (no random draws), whole complete-prefix restore/advance required.

Require stable goal with direct/alternate strategy, shared action under different
goals, ambiguity versus absence, failed attempt versus absent receipt, erroneous
classification and later revision, two observers, opposing observer goals, outcome-only
and Oracle controls. Hidden-goal equal-evidence comparisons must include common later
received observations after denied goal-specific actions. Test actual trace phases,
all14 stages/commit rollback, exact authority and malformed state/model/input/save.

MEC-004 identity/affordance channel is controlled; MEC-019 attempt/outcome and MEC-022
frozen judgments execute. MEC-012..018 actual arbitration/identity are preserved in
prior qualified profiles, not replaced by this uncontroversial planner. P3-007/009
retain trust/epistemic limits. RO-C3-014/019/020 preserve learned affordances, source
quality/correlation, calibrated inverse planning, multi-goal dynamics, recognition,
fulfillment, social response and broad privacy horizons. No state reduction authorized.
