# Selected spatial encoding component

`selected-spatial-encoding/0.1-candidate`, 2026-09-13. LOCAL DISPOSITION — no owner
ruling required. This composes the accepted spatial allocation and selected encoding
laws under an explicit successor; it does not widen old SelectedView or its encoder.

Consume one actual live SelectedView and bind the supplied completed observation,
tracks and SEM experience through spatial-evidence-binding. Every consumed binding
must equal the same experience's canonical binding bytes. Selected claims must name
that experience, observer, event and continuant. Only selected witnesses reach the
encoding result. The complete preselection list is a preparation input, not retained
encoding evidence. Any attempted consumption, including an invalid join, closes the
selected capability. No free-standing caller-selected key list is admitted.

Use the existing homogeneous base3/10 and exact semantic role factor, but substitute
the independently computed spatial Allocation as the continuous attention operand:
Raw = Base * Role * Allocation. This is the named spatial candidate, not a rewrite of
role-calibrated selected-encoding/0.1. The prepared peripheral divisor remains fixed
before selection. Apply the existing independent/historical-shared/historical-hybrid
or retired-flat control to the known raw rows with budget1 and threshold1/4. Unknown
allocation produces UnavailableAllocation with no invented numerical factors or
strength. Known rows are Positive iff the resulting strength is positive, otherwise
KnownZero. Retired-flat may make a known zero input positive by its existing retired
control law; this is not an intact zero-preservation claim.

Source causal-role evidence is unchanged by spatial classification. A peripheral
Actor remains an Actor. Output contains only selected evidence and selected rows;
no unselected position, source scene, perception state or truth handle is retained.
Need and surprise remain disabled. This component neither allocates acquisition nor
writes memory. Public source completeness, actual completion/PRJ, canonical successor
wrapper registration and downstream positive-only owner admission remain OPEN.
