# Selected common-evidence source component

2026-09-20. **LOCAL DISPOSITION: accepted for component implementation only.**
Version `multisource-selected-source/0.1-candidate`. No numeric allocation, public
model/ingress, persistent state or amendment to frozen EMB/GA producers is admitted.

Use the existing local-reserve observation profile, actual observation461 and
selected `SignalSelectedView` capability. Consume the view once, without reading
truth, unselected channels or retained history. The first profile is observation-only,
one view per selected signal and at most three selected signals. Preserve observer,
instant, opportunity, channel, original sample and exact interval. A missing selected
signal is Unavailable, never a known zero. No new sensing bypass is introduced.

Inputs from the trusted component transaction are the expected observer, an existing
task raw output403, one complete option395, one task key371, and declared body/task
criteria. The task must be a selected candidate origin in398; its holder must match
the option. Copy its actual Base unchanged (or preserve its absence), including its
empty observation basis. Reject a pre-existing nonzero Situation for this option/task:
this profile does not combine diagnostic and body criteria. Existing Standing is
outside this component profile and must also be absent. Decoding checks structure,
not public provenance. Actual task adoption, observer-holder qualification, temporal
join and immutable criterion authorization remain runtime/model admission obligations.

Each criterion binds an observer-side signal and a positive threshold in that signal's
declared reserve unit. No cross-domain summation, mean inference or conversion to the
diagnostic0..10 forecast is allowed. For interval[L,U], both independently compute
`d=max(0,(T-U)/T)`. Body emits positive Base d. Task emits Situation +d for an adopted
instruction to favor this option when below its criterion, or -d for an instruction
to disfavor it while below the criterion. This is declared contextual suitability,
not a prediction that the action replenishes a reserve. Task threshold and polarity
are explicit independent criterion operands, not derived from the body response.

Each nonzero interpretation carries singleton evidence400/399/237 pointing to the
actual selected observation1115. Shared signal means the very same selected sample;
independent signals use distinct actual samples. Independence must be demonstrated
by intervention on separately controlled source operands, not by IDs alone. Return
zero assessments with their evidence and a Known disposition; omit zero raw signals.
Both family assessments read the sample independently and preserve separate roles
and grounds. Neither copies the other's strength. No task Base is synthesized.

This component returns a detached symbolic bundle with actual canonical task/option,
evidence and samples; it does not invent a record identity or emit frozen499 with a
false ancestor join. The receiver bridge and public output grammar remain separate.
The baseline is ground/role/sign aggregate coverage. Preserve GroundPairwise,
GroundUncovered, role-blind FamilyNormalized and applicable DescriptionDice controls.
Any repaired normalization requires its own explicit version and active-role scope.

Required component checks: shared versus independently controlled source; actual
adoption rejection; Base-off orphan; opposite task suitability; unequal thresholds;
unavailable versus known zero; selection exclusion; counterfeit/reused capability;
observer/holder mismatch; hidden-within-bin invariance; detached output. Preserve
MEC-012/013/014/016/020 and EXP-009/014. Third aggregate support remains a component
comparison obligation, not an admitted source output in this single-view profile.

RO-C3-001 stays ACTIVE. Public qualification still requires data-only original
admission, observer-holder/time/source authentication, successor output grammar and
allocation if required, registry/model commitments, receiver/arbitration composition,
actual duplicate/aggregate source production, execution, rollback and replay. This
acceptance does not discharge those gates or qualify whole PHEN-MULTI-001.
