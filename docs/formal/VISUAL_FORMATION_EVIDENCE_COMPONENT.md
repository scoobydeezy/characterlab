# Selected visual acquisition evidence

`visual-formation-evidence-component/0.1-candidate`, 2026-09-13. LOCAL DISPOSITION
— no owner ruling required. Local component composition, not public registrations.

Preparation binds the shared observed context, runs the existing canonical selector
and transfers its selected-only spatial operands into a one-use capability. It owns
the actual1143 audit. Encoding consumes that capability through the existing exact
positive-projection law and yields a separate one-use encoded capability. The
evidence producer consumes that result and allocates exactly one symbolic acquisition
only when positive units exist. All prepared observations, claims and full source
experience stop before encoding; the evidence contains only positive selected units
and their previously bound context. No replacement role derivation or selection occurs.

The APIs are separate so public40/130 preparation/encoding and130 acquisition
allocation can be registered at their accepted boundaries. These pure calls do not
themselves prove scheduler phases, source admission or subject projection. Missing
visual detections retain the existing no-detection selector path; this preparation
requires an actual nonempty observed scene, even if selection is empty or all
selected allocation is zero/unavailable.

Selection and acquisition identities are different occurrences. Multiple positive
children share one acquisition. Empty/zero/unavailable candidates produce no evidence
or owner work, while the actual selection still exists for protocol enrollment.
No persistent owner is mutated. The140 owner must reuse the admitted output identity
and complete its declared batch; a provisional evidence output is not committed memory.

VF-A freezes actual observed positive production with one acquisition for multiple
children; VF-B separates selected-but-zero/unavailable/capacity-zero from formation;
VF-C protects forged/reused/closed stage capabilities; VF-D protects selected-only
retained content and bound context; VF-E protects allocation failure and absence of
an early acquisition allocation. New codecs/allocation/public model gates remain OPEN.

The separately named earlier-concern encoder consumes the same selected-only stage
through the existing prior-concern positive projection. It preserves selected units,
source identity, focal allocation and evidence allocation rules. Its outer adapter
must authenticate the earlier completed producer, delivery and projected subject;
this component API does not certify them. VF-F/G exercise actual concern arithmetic,
unavailable delivery and rejection of current feedback. Feedback diagnostics remain
trace-side; durable children contain their historical encoding factors only.
