# Biological multi-ground choice — 2026-09-26

ACCEPTED LOCAL DISPOSITION, biological-choice/0.1-candidate. Finite component
extension of reason-dice/task-arbitration, not a change to frozen public codecs.
Candidate count0..8 and nuclei0..32. Each nucleus is constructed through the unchanged
compileReasonNuclei kernel in its own option/task group, retaining sign, consolidation,
die brackets and situation/standing modifiers. All nuclei for one option convolve.
Independent grounds are never averaged or collapsed into a utility total.

Exact option win probability uses independent score distributions and fair ties.
For option i and score s, multiply each other option's polynomial
P(score<s)+P(score=s)*x; coefficient of x^k contributes1/(k+1). Multiply by P_i(s)
and sum scores. This equals the old exhaustive enumeration for one/two options.
Existing margin, contest, top-two conflict mass, bounded stake and authorship thresholds
are retained. All option probabilities remain visible. Larger-choice significance
is a conservative candidate extension, not a new calibrated psychological law.

Auto chooses the highest analytical probability (canonical tie order). QuietRoll and
PlayerFacingRoll execute one existing addressed random draw per active nucleus and,
if necessary, a uniform addressed draw among the tied top scores. This is the retained
reason-dice grammar, not a weighted probability lottery. No active reasons yields no
choice. Each instant has unique semantic root; addresses bind actor/action/task. No
unrelated diagnostic draw. Rollback discards staged random transcript; prefix replay
rebuilds it. Component safe output includes canonical nuclei, exact distributions,
choice mode, probabilities, draw transcript and immutable intended action.

Compatibility gate: independently compare exact analytical results to inherited
analyzeOptions for the full declared two-option matrix, and compare multi-option
results against brute-force enumeration on small supports. Check fair ties and
permutation invariance. Keep old public reason/arbitration sources byte-unchanged.
Full biological integration remains OPEN; no permanent allocation or owner ruling.
