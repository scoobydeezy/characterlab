# Bounded belief-relative affect

**SHAPE ACCEPTED by local self-review, 2026-09-21, before implementation.**
Version `affect-public/0.1-candidate`. This authorizes a bounded successor, not a
qualification verdict. LOCAL DISPOSITION — no owner ruling required. The canonical
belief/appraisal/affect/reasons distinction and strictly later appraisal edge supply
the authority; no new psychological owner or truth permission is introduced.

## Admitted mitigation source

One observer/character, at most fifteen panel observations and seventeen increasing
original instants in 1..64. Originals contain a physical trial display or an empty
probe. Physical action/outcome and visible displays are independent. Source truth
is never nested in character outputs. The literal panel glyph (0 current cue,
1 training cue) and Before/Motion/After lamps are perceived through the accepted
trial-panel source and SEM event-file transition. Missing panel observation ends
continuity. Equal glyphs after a gap do not recover an old event file.

The separately visible Motion lamp reports unknown action, observed inaction, or
observed mitigation. The After channel reports opportunity, monitoring completeness
and adverse outcome. All three experiences must share the actual perceived event
file with exactly one ordered Before/Motion/After observation. Conditions are
derived from the observed glyph and action, never source array position, hidden
action, event ancestry or a caller-supplied belief target. Glyph0 estimates current
cue risk; glyph1/observed inaction estimates baseline risk; glyph1/observed mitigation
estimates treated risk. Missing action excludes training evidence. Each completed
episode supplies at most one conditional estimate. Support is its actual After
observation; reuse across mutually exclusive targets is forbidden.

Phase120 projects safe observations,121 performs event tracking,124 freezes actual SEM experiences,130 derives
conditional evidence,140 alone applies learned-state changes. EvidenceMean,
LastObservation and NoLearning retain BELIEF's exact arithmetic and censored/no-
opportunity rules. Equal quality and independence across distinct trials are bounded
candidate assumptions. The successor has three keys and its own state root; frozen
two-key BELIEF models are unchanged. The sole mutation owner remains
`authority/belief-expectation`. Initial learned state is empty.

For known baseline b>0 and treated t, relative control is max(0,(b-t)/b).
AbsoluteReduction max(0,b-t) is a retained comparator with distinct units. Unknown
b/t or b=0 gives unknown efficacy. t>=b gives known zero efficacy. An independently
observed complete empty action catalogue gives available control zero; unknown
catalogue gives unknown control; an available mitigation uses its learned efficacy.
These are observational contrasts, not identified causal effects. Misleading displays
may establish wrong beliefs. Hidden truth variation with equal safe inputs must
preserve every character output and learned state.

## Appraisal, derived affect, and later delivery

At50 read the three belief leaves and the latest *completed* safe context, if any.
Likelihood is the current-cue mean. Severity is an immutable, governed adopted safety
goal's known consequence valuation, in [0,1]; varying it is a goal/context
intervention, not an evidence intervention. Vulnerability =1-observed reserve
fraction from the controlled visible reserve channel. Its displayed value is
fallible; physical reserve is never a cognitive operand. Absence is unknown.
The independently observed catalogue and reserve are current-context observations,
not extra belief precision. No confidence multiplier is applied.

Apply `affect-factor-comparison/0.1-candidate` unchanged to p,s,v,c, retaining
HistoricalProduct, SplitExposure and ScalarUncontrolled. Unknown factors yield
Unavailable; this conservative complete-factor scope does not settle affect under
uncertainty. All coordinates are derived outputs, not authoritative emotion state.

Named later receiver: appraisal readiness. Feedback uses only the most recent
completed earlier Known affect's first coordinate a. Each newly derived coordinate
z becomes z+(1-z)*a/2. NoFeedback uses a=0 exactly. The previous output is identified
in the appraisal trace and must have earlier time; no current result can recurse.
This saturating persistence candidate is an explicit bounded implementation of the
already-authorized affect→later-appraisal edge, not an earned general law. It never
changes belief, evidence quality, support, or the likelihood operand. Split's first
coordinate means exposure, the scalars mean their declared summaries; comparison
therefore tests complete models, not equal psychological units. No scalar necessity
claim may follow just from those chosen receiving maps. Relief is a decrease in
derived coordinates and supplies no learning event.

## Motives, action and inherited controls

Two explicitly adopted goals ground two option/task pairs: mitigate danger and
continue the competing obligation. Their genuine base strengths come from the
governed commitment profile, independently of affect. The last affect coordinate
is a situation-role modifier of the safety ground only. It cannot create a reason
nucleus without that base and never supplies an independent Fear ground or die.
Use actual inherited raw402, role coverage, compileReasonNuclei407 and exact
reason-dice distributions. Retain the inherited arbitration analysis and addressed
reason-face/tie RNG. Do not fabricate multisource ancestry. Expose all grounds,
roles, distributions, analytical probabilities, draw records and selected option.

Decision60 → intent70 → expression80 → execution plan90 → attempt100 → physical
outcome110 remain separate records. Execution feasibility is physical protocol
content; failed mitigation is possible without rewriting the decision. No attempt
or relief generates evidence without the separately admitted observation source.
Regulatory impulses and ORD-005 are outside this profile.

## Public admission and proof obligations

Closed canonical grammar, exact recipe and frozen ModelIdentity admission. Data-
only originals; no authored belief, affect, safe evidence or generated events.
Standard scheduler phases, authenticated originals/generated children, exact read
domains and one learning owner. Prior safe outputs are an immutable, explicitly
selected carry projection; omniscient trace is never its source. Panel state is
reconstructed from that bounded safe prefix; it adds no hidden persistent state.
All same-instant outputs, state, queue and allocators roll back together. Standard
canonical saves must restore by complete-prefix reexecution with whole-save byte
equality, including all RNG draw records. No callbacks in the public factory.

Qualification requires independent factor interventions; fixed truth/different
belief; hidden truth/same safe output; missing versus observed inaction; unknown
versus ineffective control; matched affect/different competing motive with at least
two response outcomes; later feedback and exact ablation; relief without evidence;
distinct fear-command, extra-die and relief-learning negative controls; failed
execution; every-stage rollback; and complete-prefix restore/continuation. Broader
social affect, attribution, uncertainty, extinction/generalization and physiological
affect dynamics remain explicit reopen conditions, not silently passed.

## Exact profile details (pre-freeze)

Allocation747..765, occurrence namespace1151. AffectBeliefKey765 uses the actual
semantic character1002, matching inherited task grounds; BELIEF key738 with its
older1000 identity is not repurposed. Root753 contains only learned estimates.
Initial authoritative state contains exactly the two adopted task statuses373,
immutable in this profile, and no learned estimates. Phase51 reads those statuses.
The profile admits severity0,1/2,1; obligation0,1/10,1,10; safety0,1. Situation and
standing modifier units are1/4 with cap3; the inherited five die cutoffs are
1/20,1/10,1/5,3/10,2/5, activation threshold0. Arbitration thresholds are the
frozen predecessor baseline values. This is calibration, not a new dice grammar.
Addressed RNG uses each actual decision occurrence1151, purpose/affect/reason-face
and purpose/affect/tie in1042, and actor/action/ground subject roles in1043.
The scheduler allocator guarantees unique decision roots; replay retains every
draw in the whole saved trace. There is no externally supplied random session.
Frozen models separately commit all settings and inherited definitions.
