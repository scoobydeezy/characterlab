# General Attention trace binding

`general-attention-trace-binding/0.1-candidate`, 2026-09-19.
LOCAL DISPOSITION: accepted bounded representation under the Campaign 3 escalation
policy. This defines the new candidate family only; frozen prior bindings stand.
Public qualification and exact model freeze remain separate gates.

Each actual scheduler event produces one TraceRecord160 under trace/0.2-candidate.
Its event, model/run identity, payload, output list and scheduler-allocated children
are the actual execution values. RecordKind is EventTypeId. For GA stages the exact
706 registration determines SeamId, component version and complete read domain.
Inherited probe, measurement intake, M1, prediction and task deadline retain their
existing seam identifiers and component versions. Their domains are respectively
the probe displacement path, empty, empty, roster plus prediction, and task state.

SourceRecordIds enumerates the unique runtime occurrence atoms recursively present
in the actual input payload, sorted canonically. This omniscient index is never
returned as character evidence. World has no subject; other rows identify the fixed
observer and qualified holder as trace context. This does not imply a roster read.
Input/output projections are exact canonical values, including empty output lists.
The deterministic profile performs no random draws. Exact-rational component
calculations retain their own output witnesses; no synthetic quantization operation
is appended to the substrate trace.

ActualReadRecords come from the actual adapters. All actual paths and derived-source
paths must lie within the registered domain. Direct values and underlying derived
sources must equal the stage snapshot. Projection semantics remain independently
enforced by the compiled accessor, including erasure and subject qualification.

Ordinary cognitive patches and structural diffs use their sole registered owners.
All terminal owner groups prepare from one B0; multiple events contributing to one
owner are folded before preparation. The earliest event of that owner group carries
the actual common-B0 reads, combined patch and diffs; the remaining contribution
events carry empty patches. Their actual inputs and ancestry remain separately
visible. Terminal traces finalize only after successful combined application.

Formation governance is runtime protocol state, not a cognitive read or write.
The selector enrollment or earliest memory-owner trace carries one InvariantResults
entry: `[formation-governance-component/0.1-candidate, StatePatch, [MutationDiff...]]`.
It records the actual separately authorized protocol patch and differences. It never
appears in the cognitive StatePatch, cognitive read domain or recollection content.
No protocol mutation means an empty invariant list. Replay compares this evidence
along with the entire canonical trace, state, outputs, queue and allocator.

The implementation must reject a changed trace field relative to completed facts,
prove stage rollback and complete-prefix replay, and freeze the precise registry,
parameters and runtime version bundle before public activation. Canonical encoding
and successful component execution alone do not discharge those obligations.
