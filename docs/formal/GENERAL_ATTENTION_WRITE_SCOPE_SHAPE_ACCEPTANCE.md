# General attention write-scope successor shape acceptance

2026-09-13. PRIMARY-AGENT SHAPE ACCEPTANCE. Accept the two-record general-attention-registration-write-scope/0.1-candidate representational successor. GeneralStageRegistrationV02 preserves the prior wrapper fields except the explicit write-capability union. GeneralPathWriteCapability uses the existing authority and path semantics. Eight adversarial controls pass. This corrects a wrapper expressibility gap locally under the escalation policy; no ontology, route or authority boundary changes.

Read-only uses322 ReadOnly; character-learning writes retain322 StateWrites. Exactly the two perception stages, two goal lifecycle owners and physical replenishment use explicit path authorization. No transition receives protocol-root authority. Exact patterns and model-bound holder checks remain mandatory.

704/1 and all prior allocation bytes remain frozen and unchanged. A separate append-only allocation may assign these two record types and fields; no namespace or member is required. This shape receipt does not itself allocate numbers or qualify model/public execution.
