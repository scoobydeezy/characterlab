# Actual source role batch

`general-source-role-batch/0.1-candidate`, 2026-09-13.
LOCAL DISPOSITION — no owner ruling required.

Validate the already admitted SEM-H experience and its first-profile maximum of
three bindings. Construct the existing SEM-G same-experience occurrence index from
those actual binding records. Apply INITIAL_CAUSAL_ROLE_RULE once per distinct
event/continuant unit in canonical unit order, preserving SEM-G multi-role behavior.
Do not infer a role from world identities, positions, glyphs or selected winners.

Derive semantic content before requesting actual shared-allocator slots. Allocate
exactly once per resulting CausalRoleEvidence240, using existing1111. Unresolved or
otherwise unmapped bindings allocate no claim occurrence. Nonconsecutive allocator
values are assigned individually; duplicate/negative values reject. Pure local zero
placeholders never become produced records or a parallel identity system. The frozen
attention runtime already follows this pattern; this component extends its use to
the separately bounded source without changing that runtime or SEM-G mathematics.

The eventual phase15/125 registration emits these exact records, and visual
selection consumes their authenticated batch. Component calls do not prove public
origin, PRJ or registered-output association. Failed allocation belongs to the
enclosing whole-instant rollback; the function creates no persistent state.
