# Selected allocation controls in the GA source profile

`selected-role-allocation-control/0.1-candidate` and
`selected-disabled-allocation-control/0.1-candidate`, 2026-09-19.
LOCAL DISPOSITION: bounded composition of accepted selected-encoding/0.1-candidate
and calibrated selected-encoding budget laws. No new mechanism or allocation.

Consume the same actual selected capability, completed experience and spatial
provenance used by the spatial comparator. Base is the existing homogeneous 3/10;
Role is the existing actor1, target9/10, participant3/5 table. The role-calibrated
control uses Attention=Role; disabled allocation uses Attention=1. In both cases
Raw=Base*Role*Attention. Apply the exact committed independent, historical-shared,
historical-hybrid or retired-flat law, budget and threshold. All four laws remain
separate retained controls, including the retired law's known defects.

The spatial witness continues to describe actual source geometry and classification.
It is not relabelled as a role-based allocation. The separate encoding Factors
carry the chosen law's actual Attention and Raw. The first profile still requires
complete sensed position; SpatialUnknown remains unavailable in this composed
profile. Existing standalone selected-encoding controls retain their wider component
scope. Positive-only formation and all source/evidence/occurrence ownership remain
unchanged. No second selection or acquisition occurrence is allocated.

Exactly these versions appear in 706 for current/consequence visual encoding and
the disabled-feedback encoding wrapper in their respective models. Spatial models
retain the existing spatial/prior-concern versions. Nonspatial models cannot enable
the spatial residual-feedback mechanism; that is not an unannounced interaction.
Disabled feedback still records its actual source status and disabled branch.

RoleCalibrated, Disabled and Spatial must have distinct model commitments and live
factor witnesses. Review revision1 failed this gate and is not eligible for freeze.
The corrected model identity also commits its exact required S0 admission profile;
zero/above-target source variants cannot share an identity while admitting different
sole initial states. RunIdentity independently commits the actual initial state.
