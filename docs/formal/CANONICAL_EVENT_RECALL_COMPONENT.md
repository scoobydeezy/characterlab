# Canonical event acquisition recall composition

`canonical-event-recall-component/0.1-candidate`, 2026-09-13. LOCAL DISPOSITION —
no owner ruling required. Composes existing direct/learned access and event ranking.

Input is a separately admitted Present canonical continuant cue or Absent, current
event acquisitions, independent graph keys/weights, actual presentation histories,
instant and the existing exact ranking calibration. Present contributes the first
bounded unit cue seed1. No body scalar, significance or use-protection value is a
ranking operand. No missing presentation history is synthesized from acquisition time.

Validate/detach at most32 acquisitions with1..3 child units each through the existing
fragmentation substrate. Key and acquisition adapters preserve canonical ordering
without returning temporary labels. The working graph/episodic union remains bounded
by the accepted32-key direct-access limit. Apply the exact DirectMatch+LearnedSpread
decomposition and existing rankAccessibleEpisodes law, then return only actual winning
surviving acquisitions. Graph-only membership never creates an acquisition. Body recall
remains a separate partition with its existing law and slots.

Absent returns no candidates/diagnostics and accesses neither supplied memory, graph
nor history. Static scalar calibration is still checked. Present with zero scores
retains the existing ranking law's top-K behavior; no new positive-score threshold
is invented. Evaluated and UnavailableCue remain distinct dispositions.

Return score diagnostics with actual acquisition ordinals, and access diagnostics
with original canonical keys. Recalled content is detached and contains no retention
metadata or source archive. No occurrence allocation, presentation, use credit, graph
write or memory write occurs. The caller still owes actual cue, source/subject and
state-read authentication, public schemas and persistence; this is not public admission.
