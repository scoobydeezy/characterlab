# Canonical visual selection completion

`canonical-visual-selection-component/0.1-candidate`, 2026-09-13.
LOCAL DISPOSITION — no owner ruling required.

Reuse frozen AttentionSelectionAudit532, SelectedEvidenceView534 and the existing
SelectionOccurrenceId1143. Actual priority selection produces both records and a
matching one-use selected capability. The audit alone owns the fresh occurrence;
the view references it. Empty and ineligible completed selections still allocate
one selection, with no synthetic experience when the source has no detections.

This first adapter admits capacities0/1/2, at most three units, one binding per unit
and one claim per selected unit, matching frozen533 exactly. It cannot widen that
record to the broader proposed child binding bound. Failed projection closes the
capability. Domain checks precede allocation; enclosing scheduler rollback owns
any reserved identity on later failure. Actual parent completion, qualified PRJ,
producer registration and formation hook authentication remain upstream obligations.

The original public attention runtime and its frozen cohort remain unchanged.
These new component outputs name their own transformation version, not the old
public runtime version. Structural canonical decoding is not producer admission.
No new record, identity family, memory write or public qualification is inferred.
