# BODY ownership comparison — 2026-09-21

**ACCEPTED / LOCAL DISPOSITION — no owner ruling required.**
Version: `body-ownership-comparison/0.1-candidate`. Stage C before execution.
Counters970/0. No new canonical record, namespace, public factory or state owner.

## Authority and scope
Architecture7.2/7.3 and North Star10 prefer interoceptively mediated pressure while
leaving independent Need storage experimental. PHEN-BODY-001 and RO-C3-008 require
the stored-meter/authored-reference comparison on a common observation/probe domain.
This is a **component ownership experiment driven by actual public EMB runs**,
not a new integrated character model. Existing EMB/receiving qualifications retain
their exact scope. No candidate enters a production character state root here.

MEC-003/006 preserve effect decomposition and epistemic mediation; P3-009/010 keep
constitution, time and adaptation distinct. CTL-001 ports the historical bounded
level/passive-rate/deficit/urgency control, with K=1,p=1, normalized capacity1,
setpoint3/5, rate -1/100 or -1/200. The selected integer-time fixtures lie exactly
on the historical 10^-6 lattice, so rounding is identity; no general historical
quantization port is claimed. No imports from reference/. Existing REG0.5 is
executed unchanged: its authored constant operating point is80, not current body
truth or a replenished reserve. Adaptation displacement is fixed0 throughout.

## Common domain and candidates
All candidates face the same scheduled samples, deliveries, capacity100,
threshold60, permitted width10/20, availability and external initial body amount.
They expose current pressure Known(q) or Unavailable at the same sample times.
The motive probe is an option-free unit-importance body ground of magnitude q;
Unavailable produces no ground, Known(0) remains explicitly known neutral.
This probe is component scope; no new choice/execution claim follows.

1. **Mediated**: unchanged public EMB sample461/463, actual SEM settlement, pressure464.
2. **MeterBehindSensor**: separate research-owned normalized stored level, lazily
   advanced by passive rate, clipped at0; delivered amount/100 clipped at1.
   Sole writer is the external body transition. Sensor bins 100*level using the
   same width; pressure uses the bin upper bound. This is a serious alternative
   representation of body truth, not independently authoritative psychological Need.
3. **IndependentNeed**: same stored-level transition, but current urgency uses its
   level directly, bypassing the sensor and availability. This is CTL-001's direct
   meter-as-motivation hypothesis, retained only as a boundary-violating control.
4. **AuthoredReference**: actual REG0.5 supplies constant80; the same permission,
   bin and pressure operators apply. It tests substituting operating point for
   current state. Failure does not invalidate REG as a target/reference owner.
5. **CachedEvidence**: a separate component-owned retained pressure cell is replaced
   by each admitted current pressure, including Unavailable; no drift or hidden
   effects. It reads only the current public pressure, never truth. Query returns
   the stored value. Equality earns bounded derivability, not universal elimination.

Memoryless initial-level and direct-truth controls are covered respectively by the
constant reference and IndependentNeed in this domain. No prediction of independent
psychological dynamics is invented merely to distinguish CachedEvidence.

## Exact execution and evidence boundary
Positive original times; source10 and pressure60 precede delivery110. A simultaneous
delivery cannot repair that instant's pressure. Component meter advances to the
sample, emits its probe, then applies delivery; query count does not alter later
values. Research trace records initial/kinetic parameters, exact body value, time,
meter level, delivery/applied/overflow, sensor interval, actual SEM and pressure
separately. Trace body values never enter Mediated/CachedEvidence calculations.
IndependentNeed is explicitly inadmissible, not silently granted a character read.

Public source model images remain the frozen embodied-model-rev2 cohort. Original
S0/input bytes, source identities, comparator definitions and implementation hashes
are frozen before matrix execution. Preserve every public whole-instant prefix and
verify exact save equality plus next continuation. Component cell/meter snapshots
are separately replayed; do not call them new scheduler-owned canonical saves.

Matrix: existing baseline/slower/coarser/hidden89/overflow/denied/unavailable runs;
equal initial sample80 with rates1 and1/2, delayed delivery30 and later probes;
42/50 initial bodies sampled at1 under width20 (actual41/49); same41 width10;
depletion/refill; partitioned and direct query; omitted-delivery comparison.
Compare kinetics, exposure and resolution one factor at a time. Initial kinetic
pair shares S0=80 at0 and samples1/31/41/61; initial equality is the committed S0,
not an assertion that positive-time samples remain equal after different decay.

Expected: MeterBehindSensor and CachedEvidence match public pressures throughout;
IndependentNeed distinguishes hidden41/49 despite equal safe evidence and ignores
availability; AuthoredReference cannot express delayed exposure or kinetic history.
MeterBehindSensor must preserve saturation/depletion and query partition invariance.
These are predictions, not an executed qualification.

## Reopen and nonclaims
RO-C3-008 retains general physiology, nonlinear kinetics, constitutive adaptation,
learned efficacy, action-conditioned relief, mixed learning, different legitimate
evidence histories, pressure persistence between opportunities and hybrid dynamics.
No state root is deleted; no globally final Need ontology is selected. No new
causal edge, authority transfer or owner ruling is needed for this comparison.
