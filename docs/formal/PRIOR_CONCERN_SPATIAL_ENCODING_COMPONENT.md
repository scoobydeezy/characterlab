# Prior concern with selected spatial encoding

`prior-concern-spatial-encoding/0.1-candidate`, 2026-09-13. Local composition of the
prior-concern feedback and selected-only spatial capability components.

Consume the actual prepared capability once before any feedback checks. Its actual
source experience instant determines the receiving time; the caller cannot relabel
an older acquisition as a later event to admit future concern. Subject and delivery
authentication remain the public join's obligations, not inferred from equal fields.

With known enabled q, multiply only selected SpatialPeripheral allocation by1-q.
Its preselection denominator is already bound in the capability, so this is exactly
P*(1-q)/n without reading unselected content. Focal allocation stays fixed; Unknown
stays unavailable. Recompute raw factors and apply the same explicit encoding budget
law. Trace the feedback branch separately. Disabled/unavailable feedback retains
the baseline numbers with distinct status.

No selection changes, new occurrence, source read, old-memory write, graph mutation,
presentation or outcome significance follows. This is the lawful earlier-concern
variant retained by GA_VISUAL_PIPELINE_SCOPE_CORRECTION_REV1, not retrospective
encoding from the acquisition's own later consequence. Public source, PRJ and whole
prefix qualification remain OPEN.
