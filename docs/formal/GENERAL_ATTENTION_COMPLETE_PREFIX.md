# General Attention complete-prefix persistence

`general-attention-complete-prefix/0.1-candidate`, 2026-09-19.
LOCAL DISPOSITION beneath save/1-candidate, identity/1 and the accepted common-B0
settlement semantics. Exact model freeze and public qualification remain owed.

The first bounded profile admits only its exact S0, committed original calendar and
32-byte run seed. Generated scheduler events never acquire original authority.
There is no mid-transition save, arbitrary state injection, caller callback,
replacement queue, warm learned state or partial-instant continuation.

Canonical SchedulerSave132 contains actual model/run identities, clock, complete
authoritative state, all three allocators, pending events, full trace and outputs.
AnalyticalAnchors is the ordered list of exact state-path/value pairs for root649.
There are no random-relevant authoritative IDs or continuing external inputs in
this deterministic closed calendar, so those fields are empty lists. The complete
ordered original event list, including inherited probe/task originals and the
credit cohort's goal, replenishment, significance and retention originals, is the
RunIdentity input commitment. Observation definitions alone are insufficient.

Restoration first checks canonical grammar, exact ModelIdentity and reconstructs
RunIdentity from the original S0/calendar and saved seed. It then executes the
actual original prefix to the claimed clock and requires byte equality of the
entire reconstructed save. Pending cross-instant capabilities are recreated only
by those actual producers. Clock, state, trace or count equality alone does not
authorize restoration. Resume returns that replayed runtime, preserving its live
pending-delivery authentication without serializing private object capabilities.

Required evidence includes restored continuation across pending feedback and credit,
tampered state/allocator/queue/anchors/trace/output rejection, seed copying, foreign
model rejection, whole-stage rollback and exact source/output identity closure.
No psychological equivalence follows from deterministic replay.
