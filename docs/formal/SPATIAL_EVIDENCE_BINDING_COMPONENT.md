# Spatial evidence binding component

`spatial-evidence-binding-component/0.1-candidate`, 2026-09-13. LOCAL DISPOSITION —
no owner ruling required. Structural join of supplied completed SEM products;
public source completion authentication remains OPEN.

Join one positive visual observation's1..3 detections to exactly one actual track
transition per detection, its actual event transition and frozen SEM experience.
Observer and instant must agree at every join. Event detection identity must equal
the observation's event detection. The experience names exactly that event file.
Every binding uses that file and one resulting continuant file, at the same instant,
with current visual observation support. Track support includes that observation;
prior continuity support may additionally remain. Track and binding unit sets must
match exactly. Detection ordinals never stand in for file identities or list indices.

Run the accepted spatial-context allocator over the complete observed detection
list before selection. Attach each resulting spatial value to the actual
event/continuant unit through the verified track. Missing position remains Unknown;
known zero remains allocated zero. Preserve the complete preselection peripheral
count, which is not recomputed over winners. This join creates no causal-role claim,
character identity, selection, encoding strength, memory or new occurrence.

The output is an intermediate preparation. A later selected-only projection must
prove exact selected-unit coverage and exclude unselected witnesses; it must not
hand this full preparation to a character encoder as selected evidence. Existing
SelectedView stays unchanged. The new public source and selected wrapper are still
required. Input consistency does not authenticate fabricated completed products.
