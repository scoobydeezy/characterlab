# Task cognitive model/profile commitment

Version: task-cognitive-model-profile/0.1-candidate.
Status: INTERNALLY PACKAGING ACCEPTED. Exact bytes freeze through the separate
campaign2-task-cognitive-model/FREEZE.json receipt; runtime vectors remain unpassed.

| Binding | Exact version |
|---|---|
|RulesVersion|rules/campaign2-task-cognitive/0.1-candidate|
|RegistrySchemaVersion|campaign2-task-cognitive-registry/0.1-candidate|
|NumericProfileVersion|numeric/task-cognitive-exact/0.1-candidate|
|Ordered input|campaign2-task-cognitive-ordered-input/0.1-candidate|
|Trace|campaign2-task-cognitive-trace-binding/0.1-candidate|
|Persistence|campaign2-task-cognitive-persistence/0.1-candidate|

The exact78-entry semantic dependency bundle is the reviewed packet's semanticBundle.
Earlier profile versions in that dependency history are not aliases for these current
receiving profiles. ContentSchemaVersion, ParameterSchemaVersion and RNG algorithm
retain the accepted predecessor bindings. Content bytes stay unchanged; model-owned
task specs have the exact new windows/criteria. Ordering parameter133 is exactly27.

The21 recipes are the exact accepted cohort, not all combinations of their switches.
The constructor and every model digest are frozen together. Model admission must
read and validate the complete declarations and select one exact committed member;
a caller-supplied recipe name or digest alone is not model admission. No arbitrary
parameter, source, function, compiled handler or initial trait is accepted.

## Exact receiving changes

The registry uses373/schema2, the separately allocated1032 task-instruction leaf,
and root415 for identity evidence. It preserves existing qualified character/task
roles and all inherited role positions except the explicitly scoped200/9 replacement
from1121 to1141. Only protocol execution supplies200 in this input profile; probe331/332
does not. This role replacement is accepted here as the receiving binding required
by the already accepted observation projection. It grants no authenticity from a
namespace and changes no generic200 or prior model. Its canonical265 row has no
DomainValidator, as for the actual execution occurrence role.

Only the regulatory exposure318 source changes to actual registered producer321/tag2,
ProtocolActualFactBridgeTransition. Its basis, rule set, exact mathematics, ownership
and output grammar remain unchanged. Authored304 originals are excluded; predecessor
practice/source declarations retained as historical dependencies cannot create a new
admitted branch. The profile's normalized graph and original-source admission jointly
govern reachability.

The16 new scheduled registrations have exact input/output schema joins, phases,
registered producers and either no writes or the identity-only capability. Fourteen
new output identity rules use279/278; no private allocator or second truth ID exists.
Seventeen union definitions use259 with payload-only field sets and the accepted1024
unsigned-pair identities. All new role/type/member values are from the base allocation
and its one-member plan-leaf addition.

## Independent packaging evidence

The canonical declaration packet constructs21 distinct commitments with148 registry
rows and297 schema descriptors per model. Its source/definition/role audit verifies
all16 new producer joins and the isolated inherited role replacement. The actual
ownership compiler accepts disjoint status/instruction/identity families for all21
recipes and rejects read-only/write overlap and a missing instruction key grammar.
Separate receiving checks reject wrong definition kind, wrong observation producer
and wrong registry kind. Recipe review checks exact changed-row sets, literal task
windows and access controls, and unchanged content/parameter/other-slot bytes.

These are declaration checks, not live psychological input authentication. The
definition audit's traversal counters include partial rejected-input traversals; they
are not behavioral test counts. The new runtime's complete canonical role/value
admission, truth isolation, mutations, occurrence/trace boundaries, whole140 rollback
and real RNG save/restore still require execution. Historical dice/calibration scripts
are research controls and do not satisfy those runtime obligations.

## Activation and completion gates

After the exact model freeze, semantic implementation may proceed under the named
candidate contracts. The public surface remains data-only prepared model/run handles
with private compilation and complete-prefix restore. Initial prediction/memory/
identity learning state is empty; initial instructions are immutable and may bind only
actually adopted tasks. Original input contributes no psychological intermediate.

No frozen runtime vector is passed here. PERSIST-I and the full generated causal
witness remain OPEN, as do broader DEC-001 and ORD-001 in their documented scopes.
This packaging acceptance does not complete Campaign2 or supersede qualified prior
model/corpus commitments. Internal acceptance follows the user's autonomous-work
authorization and claims no external planning-agent approval.
