# Local reserve sample bridge component

2026-09-13. `local-reserve-sample-bridge-component/0.1-candidate`.
LOCAL DISPOSITION — no owner ruling required. Component acceptance under autonomous
review; public producer registration and whole successor activation remain OPEN.

Consumes the actual opaque three-local-reserve source, a trusted observer identity,
instant, explicit requested channel names and the transaction's shared allocator.
Validate observer and the complete channel request before allocating. Requests are
dense, unique, known, nonempty and at most9. Unrequested channels are not observed.
Exactly one actual461 or463 is produced per requested channel in canonical channel
order. Reuse462's exact positive-width bins unchanged. Each sample owns one existing
ObservationId1115 allocated here; no experience or selection occurrence is allocated.

The exact producing version is `local-reserve-level-observation/0.1-candidate`.
This does not claim production by the old singleton embodied profile. A separate
safe channel-to-signal declaration accompanies the actual sample batch; no physical
key, raw amount, anchor, permission cause or resolver is released. Signal labels
remain component symbols pending their already-required permanent allocation.

Separately named `selectLocalReserveSignals` and `admitLocalReserveSignalCue` consume
only this exact producing version, using the same established selection/cue laws.
Their older singleton counterparts remain restricted to their old version. No generic
caller-supplied version whitelist or compatibility flag enters character processing.
The same batch can feed both paths; cue admission is independent of durable capacity.

These functions do not authenticate an original, PRJ subject, actual public completion,
SEM reservation or state ownership. The allocation callback is an internal transaction
operand, never a public data input; outer transaction failure must discard its advances.
Codec validity and a matching version string alone still do not establish provenance.
Source changes, registration, canonical state and full-prefix proof remain public gates.
