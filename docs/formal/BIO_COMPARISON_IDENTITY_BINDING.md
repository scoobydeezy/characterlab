# BIO component experiment identity binding — 2026-09-21

**ACCEPTED / LOCAL DISPOSITION.** Supplement to bio-coupled-comparison/0.2-candidate.
No behavioral change or allocation. Research Brief6.1 requires canonical model,
run, experiment and comparison identities even for a composed component experiment.
The initial matrix plan committed exact sources and inputs but did not instantiate
those substrate identities. Do not present it alone as a complete verdict package.

Before a separate qualification rerun, freeze BIO_IDENTITY_PLAN_REV1.json using the
existing identity constructors. Every candidate has its own ModelIdentity. Content
is the ordered actor/tasks/options tuple; parameters are candidate label, K1,
learning/probe units1 and1/16, cap3, exact five die bands, arbitration thresholds1/2
and authored control1/2. Registry is the named context/raw/reason/decision/expression/
qualification/identity stage roster and committed contract/source hashes. Primitive
lists use the positional schemas specified here; no new record type is implied.

S0 is empty414 plus empty archive. I is the same19-step schedule for both histories:
each row(time,learning,reverse-context,shared-seed-or-empty), followed by the fixed
non-decision seed254. The four early shared-seed slots are empty: R is the32-byte
early seed, and the rules use it only for those stages. Other stages' seeds are
explicit common comparison inputs. Early seeds remain run fields, never model fields.

The registered research RNG profile name is
`rng/bio-address-routed-sha256/0.1-candidate`: the actual existing SHA256 addressed
algorithm is unchanged; only this declared finite seed routing is added by the
comparison rules. Each resolution has its existing1135 root and purpose/subject
bindings. This is not a ComparisonDrawMap remapping or a change to existing public
factories' natural RNG semantics. All routes remain research-side. A future public
admission of this profile requires its own exact contract/registration and proof.

ExperimentIdentity names corpus0.29.0, the BIO comparison version and harness version.
ComparisonCase holds all ordered model/run identities plus explicit early-only
variation, fixed probes, fixed non-decision and shared contrary routing. Identity
metadata must not perturb any component transition or natural draw address.

After this identity manifest is frozen, reexecute all13 complete component cases and
require each exact final snapshot hash to equal the original matrix. Earlier260
prefix comparisons remain evidence for the identical committed transition sequence,
not fresh public-factory persistence. The new13 reruns are reported separately and
are not hidden by counting only semantic case names. No historical receipt is rewritten.
