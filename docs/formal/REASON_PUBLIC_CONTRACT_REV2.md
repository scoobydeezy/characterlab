# Joined independent-reason receiving contract

Version **reason-public/0.2-candidate**. SHAPE ACCEPTED by agent self-review,
2026-09-21, under the escalation policy. No change to predecessor bytes.

## Domain and source

One observer, LONG's governed actor/two task referents and two action options;
two exact motive identities Commitment and Safety. A closed catalog of adopted
task/panel contexts supplies at most six descriptions, each with a governed ID,
option, motive, referent, role, signed gain and subset of two panel channels.
Mappings are model content. They are not hidden-target inference or natural-language
recognition. Roles1/2/3/4 are MotiveGenerating, SituationalEvidence,
StandingDisposition, ContextModulating. General cross-role overlap remains open.

Original915 is instant, kind(1 training/2 probe), context index, exactly two physical
panel values in[-1,1], permission, and a hidden physical diagnostic in[0,1].
At most16 increasing instants1..64. Empty identity history only. Training admits
context0; probe admits one closed catalog context. Neither accepts cognitive state.

Probe source10 projects permitted readings to917 observations and allocates1115
identities. Actual Current SEM reservation/freeze14 uses these samples. Frozen918
carries context index, samples and optional227; no hidden diagnostic or denied
measurement. Raw51 derives each ordinary description from its admitted samples:
strength=gain*minimum(sample values), basis is the exact unit-weight observation
atom set. All required samples must be present. Zero/absent sources are rejected
with explicit927 reason, not supplied as motivation. Standing descriptions instead
read the sole identity history, fold with K=1/100 and use genuine qualification
atoms; no report or panel becomes identity evidence. Empty/zero history rejects.
919 preserves descriptor, strength, basis and its exact sample/history support.

Training uses LONG's biographyContext and C2 rawSignalOutput with standing disabled,
then actual compileReasonNuclei, arbitrationOutput (thresholds1/2), intent/expression,
qualification and appendIdentityContribution. Training dice threshold0 and modifier
units1/4; no probe-law intervention changes this history. Identity root925 belongs
only to authority/reason-identity; contribution/history types413/414 retain their
exact contracts. Probe does not update identity. Numeric fold and its millionth
commit are inherited, not calibrated confidence. RNG belongs to the actual C2
address/session grammar and must survive rollback and prefix restore.

## Compilation52

Group by exact option/motive/referent before direction is resolved. For each group,
partition by role then positive/negative sign. Sort descending absolute raw
strength, then canonical description ID. Compute weighted Jaccard against the
aggregate max-weight union of all earlier raw bases; effective=abs(raw)*(1-overlap).
Retain each original basis even when effective contribution is zero. Sum positive
minus negative effective strength, then bound once x/(1+abs(x)) per role.
This is the inherited candidate, including subset residual and canonical tie limits.

Base0 rejects the group even with nonzero modifiers. Otherwise relevance is the
sum of four absolute bounded role strengths. Require relevance>=37/100. Base die
is d4 floor, then d4/d6/d8/d10/d12 at1/5,2/5,3/5,4/5,1. Modifier families each truncate
bounded role/(1/4) toward zero and cap at3. Context uses the same numeric calibration
as situation but retains a separate role and trace. Apply resolved base sign to the
whole die+standing+situation+context expression; exact distribution sums to1.
Key922 is option/motive/referent/direction. At most one die per resolved key.
Rejected groups928 preserve key, rejection, relevance, base and coverage witnesses.
No direction-as-independent-identity alternative is selected by this experiment.

Profiles:1 Aggregate;2 PooledChannel collapses motive/referent grouping by option
while preserving raw provenance;3 PairwiseOnly replaces aggregate coverage with
maximum pairwise overlap;4 PerFactDie groups each description separately and
ignores overlap;5 IdentityIndependentDie promotes standing to its own base-die group
and removes it as modifier from the original group. Diagnostic group keys have
optional description IDs; lawful keys never do. These are explicit comparisons,
not flags weakening the predecessor contracts. No free numeric settings admitted.

## Ordering, trace, persistence

Stages source10/freeze14/context40/raw51/reasons52/decision60/expression80/
qualification130/learn140. Training runs context through learn; probe runs source
through reasons and stops. Each raw producer authenticates generated parents and
reads only its registered identity leaf. No probe reads world diagnostics, archive
or training result through a hidden resolver. source10 is the sole world-side cut.
Every trace includes raw/provenance/coverage/roles/keys/rejections/dice/modifiers;
training includes exact RNG and qualification records. Observation and SEM IDs
are observer-relative; task mappings are explicitly governed fixture context.

Records913..929/schema1, namespace1160. At most nine events per instant. Exact
data-only model/original admission, explicit state owner, all-stage+commit rollback,
whole-prefix save identity/byte equality and continuing RNG ledger required.
Frozen five-model images and experiment plan precede qualification. Reopen for new
source/role, nonparticipant referent, different direction identity, correlation,
arbitrary renaming/ties, learned mapping or broader interpretation of this fixture.
