# EMB model packaging — shape acceptance

2026-09-10. **PACKAGING SHAPE ACCEPTED; MODEL IMAGES NOT FROZEN.** Agent review
under the user's autonomous-work instruction, not an external planning-agent verdict.

Accept the receiving composition in
[packaging revision1](../planning/CAMPAIGN3_EMBODIED_MODEL_PACKAGING_REV1.md), with the
explicit clarifications below. The companion review receipt commits the exact draft,
its inspection inventory, this disposition and the executed component evidence.
The accepted EMB seam mathematics and permanent allocation remain unchanged.

## Accepted receiving profiles

Rules `rules/campaign3-embodied-reserve/0.1-candidate` binds exactly the proposal's
registry, content, parameter, numeric, random, input, trace and persistence versions.
In particular the new registry/parameter/input profiles and
`numeric/embodied-reserve-exact/0.1-candidate` are accepted in shape. No older numeric
profile is widened. A RulesVersion string is not a substitute for independently
committed version operands or validated canonical content/registry/parameter bytes.

The six registry slots contain329 schema descriptors,82 union entries and14 other
semantic entries (425 slot0 items), one writable reserve family, one immutable roster
family, two identity-key grammars and45 role positions. The new model does not declare
historical373/schema1 merely because the decoder supports it. Existing unused schemas
and union declarations confer no executable entry or original-input authority.

The14 other entries are11 EMB instances, two VAL definitions and the existing279
admission singleton with empty routes/assignments and four exact occurrence rules.
The new compiler must validate this exact closure. Old V04 registration dispatch is
not an adapter for the new source or pressure registrations; its rejection remains
correct. No change to279 or generic occurrence mathematics is necessary.

The baseline has one complete character170 with14 explicit empty list fields,
parameters list([133(8)]), and the two state maps described in the proposal. Physical
anchor bounds and exact model-declared map membership are additional model checks.
Generic role validation alone does not prove either condition.

## Review findings and clarifications

| Question | Disposition |
|---|---|
| Does the extra physical root require an ADAPT logical-family identity? | No. Existing WRT/PRJ ownership, path and grammar records express455/1 directly. No unnamed logical family or leaf member is added. |
| Does empty character content omit body parameters? | No.170 declares the authored character identity; exact body definitions are independently committed in registry entries. Neither is a duplicate parameter authority. |
| Are pressure/roster map keys expressible through existing roles? | Yes. Fourteen actual inherited component checks confirm the proposed required positions and rejection boundaries, with limits below. |
| Does the old registry declaration equal the decoder inventory? | No.297 declared pairs versus298 supported pairs; the accepted model inherits297 and adds32. This distinction is retained. |
| Are all input/event ordering keys authored? | No. The five-position ordered-input format contains time, phase, type, payload and empty dependencies only. IDs/sequences/parents are owned by original-input compilation. Canonical map/set byte order is not numerical ID order. |
| Does hidden89 need another model? | No. Same parameters/content/registry and versions imply the same ModelIdentity; changed S0 implies another RunIdentity. |
| Does the work7 control belong to the six semantic models? | No. Six is the semantic-comparison cohort count. Work7 is an additional separately committed negative-control parameter image, not a seventh semantic candidate or host override. |
| Does no RNG imply omitting its version or copying empty save metadata? | No. Keep the exact random-version operand; derive empty metadata from validated admitted no-draw closure. |

The exact seed operand and complete initial-state serialization for the seven run
specimens must be explicit in their later byte packet. They are fixture commitments,
not new psychological semantics. Equal seeds are required for paired specimens even
though this admitted closure makes no draws. Do not claim a RunIdentity before those
bytes are present. No canonical digest is approved by this document.

## Evidence and remaining proof

[Component receipt](../planning/EMBODIED_PACKAGING_COMPONENT_REVIEW_REV1.json):14 checks
execute existing VAL character qualification, state ownership/key-grammar admission
and four occurrence definitions using the allocated structural schema context.
Negative controls reject wrong namespace/nonidentity keys, missing grammars, writable/
read-only overlap, wrong occurrence role, optional occurrence fields and absent roles.
All446 prior frozen artifact fingerprints remain unchanged.

This evidence does not implement EMB numeric-domain decoding, complete slot0 dispatch,
set-member target closure, body/channel/parameter consistency, runtime provenance,
state exact coverage, model activation, work-limit execution or save/restore. The
structural test codec is not permitted as the production semantic validator.

Freeze EPACK-A..H as **NOT PASSED**, alongside all76 accepted EMB implementation
obligations. The14 inherited component checks are bounded supporting evidence, not
eight whole packaging passes. No source or persistent learning state is activated.
Whole EMB-001, BODY+MULTISOURCE and Campaign3 remain open.

## Authorized next construction

Construct complete canonical declaration images for the six semantic models and
the separate negative parameter control, with seven semantic run specimens. Implement
the trusted declaration/domain interpreter needed to validate these images, retaining
the fixed data-only source boundary and existing exact compilers. Validate all registry
slots and new graph/role/reference/domain constraints before freezing model digests.
Then review and freeze the concrete model packet separately before canonical runtime
execution. No new permanent allocation is needed for this packaging shape.
