# Bounded public COMMIT successor

**commit-public/0.1-candidate — ACCEPTED local receiving contract,2026-09-21.**
Allocation COMMIT_PUBLIC_ALLOCATION_TABLE.json:952..970/schema1,namespace1162.
Acceptance permits implementation/freezing, not qualification. No owner ruling.
MEC-020,EXP-013,RET-010/011 retain active-only concrete commitment pressure;
MEC-012..018 retain actual reasons/dice/earned identity. SOCIAL's controlled
identified communication and observer-owned LastObservation remain precedents.
No old model, contract, reference source or corpus member changes.

## Domain and causality
One actor, two predeclared distinct concrete task371 keys A/B, one common action,
two observers. Both task identities are governed authored concrete instances,
not one mutable series ID. Initial state is empty. The first instant is training:
actual LONG controlled two-option context, accepted raw403/compiler408/arbitration409,
intent425/expression426/qualification429 append one acquired identity contribution
413 into414 at140. No authored identity bonus or imported learned state is accepted.
Training uses unchanged d4..d12 bands, activation0, modifier unit1/4,cap3,
arbitration thresholds1/2. The registered seed7 supplies positive matching identity.

Subsequent instants never write identity. Identity strength uses accepted fold K1/100;
probe compilation uses modifier unit1/10,cap3 and active base1/10. The actual earned
matching standing must reach+3 in the registered experiment. Identity remains byte
identical across none/active/retired/recurrence states and across models.

Original954: increasing time1..16, training flag, lifecycle command0none/1adoptA/
2cancelA/3adoptB, communicated instance0none/1A/2B, recipient set{1,2}, hidden scalar.
At most8 instants. Exactly one initial training event; no lifecycle/message then.
Command admission checks real predecessor state and rejects duplicate adoption,
cancellation before adoption and recurrence before retirement. Creation sets955
version1/statusActive/causeCreated; cancellation version2/statusRetired/causeCancelled.
New B is independent version1. Terminal A is preserved.

Source30 schedules lifecycle write140. Context40 reads prior committed lifecycle
and earned identity; therefore an adoption/cancellation affects the next instant's
reason, never retroactively this instant's decision. Workspace381 filters effective
active tasks; appraisal/concern are unknown/disabled controls. Motive394 contributes
1/10 per eligible concrete instance; candidate398 uses a common action but preserves
distinct origins. Raw51 and compiler52 retain independent task referents and standing
modifiers. Arbitration60 and frozen expression80 execute for active choices; empty
options produce the accepted no-choice status. Only training continues to identity140.
Trace959 records statuses, fixed identity, raw403 and compiled408 at every probe.

## Four separately frozen models
1 Lifecycle: only Active concrete instances produce pressure.
2 CoreNeed: fixed recurring deficit1/10 produces one source using a separate stable
Need referent, regardless of any commitment instance. This is a bounded erroneous
commitment-as-Need hypothesis, not qualification of a general physiological Need law.
3 Immortal: every created instance continues producing pressure after retirement;
recurrence produces both old A and new B.
4 ReusedId: recurrence reactivates A with version3 and the old referent; B is never
created. This is an explicit alternative transition law, not a change to baseline
terminal irreversibility. It must fail the required new-referent comparison.

## Ownership and social boundary
Root956 owns per-instance955 lifecycle; root958 owns actor414 identity; root965 owns
observer/instance knowledge967 under explicit966 keys. Separate mutation authorities
own the three domains. Only lifecycle140 writes instances; only identity140 writes
earned identity; each observer update140 writes only that observer's knowledge.
Every read/write is registered and traced, with original/child authentication.

Each observer probes its own known statuses at40. Communication110 reads the selected
instance's prior-committed state and produces research truth961. No message means
no evidence. At120 each permitted recipient gets962 identifying the actual communicated
concrete task and status0Absent/1Active/2Retired. Evidence130 wraps that observation;
update140 writes LastObservation status with its own observation occurrence support.
No automatic inference from private existence, another observer or a non-event.
Absence of knowledge differs from a permitted report of absence. Unwitnessed
retirement may leave an observer's old belief stale; no private synchronization.
This is SOCIAL-style controlled identified truthful communication, not general
language, trust, attribution, or a generic SEM observation claim.

Observer views admit only their own968 probes,962 observations,963 evidence and970
updates; private lifecycle, actor identity, decisions and raw RNG stay research-side.
Positive selective witnesses must learn the exact communicated instance/status;
nonrecipients remain unknown. Swapped/both-recipient and retired-status controls
test real sensitivity. Hidden scalar changes preserve behavior; no-message private
lifecycle changes preserve observer views even when actor reasons differ.

## Qualification and reopening
Freeze four complete model recipes and exact run plan before public execution.
Each whole-instant prefix must restore and continue by exact whole-save replay,
including live/serialized addressed RNG. Inject faults at every reached stage and
before commit; reject edited inputs/saves, unrelated writers and foreign schemas.
Compare none/active/retired/new recurrence under identical earned identity. Required
counts0/1/0/1, A then B referents, nonzero standing and actual active decisions;
each named control must fail. Inspect persisted terminal records and observer keys.
Counters970/19 before verdict. RO-C3-018 remains active until execution qualifies;
RO-C3-014/020 preserve partial fulfillment, delegation, beneficiaries, series identity,
consequences, social trust and broader source/calibration limits. No reduction,
general commitment theory, whole BIO or Campaign3 exit is implied.
