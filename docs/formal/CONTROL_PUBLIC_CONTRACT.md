# control-public/0.2-candidate

Accepted LOCAL DISPOSITION, 2026-09-22, before implementation/allocation.
Architecture10.2 and its Workspace/Control -> OptionGeneration edge authorize this
bounded candidate-inhibition experiment. Brief12.6 clauses3/4 are the targets.
No general control resource, inhibition law or physiological fatigue is selected.

Reuse habit-public0.1 exact acquired cue folds, observation eligibility, reward
belief, dice/arbitration, intent/expression and execution as unchanged kernels.
The successor uses inherited817..841 records with separate model identity and seventeen
new records1008..1024/schema1; occurrence namespace remains1155 for inherited
records only. No new occurrence namespace is required: new records have no IDs.
All runtime occurrence reservations remain one per stage in the inherited14 stages.
Two additional goal/workspace state roots1012/1014 have named exclusive authorities.

One actor/action plus idle; actual skill1, identity0, body adaptation0. Empty history,
unknown belief, inactive goal and empty workspace initially. At most16 originals,
increasing instants1..32. Each original1010 contains a complete habitual opportunity818,
goal instruction0 none/1 adopt/2 retire, visible reminder Boolean, maintenance support
Boolean, competing neutral card Boolean and board visibility Boolean. The card is
one actually displayed/maintained load item, not hidden task difficulty or motivation.
Invisible board projects reminder=false/card=false; hidden reward/report remain
excluded until observation120. Goal instruction is an explicit received instruction,
never a requested cached workspace or chosen response. Adopt at most once; retirement
requires prior adoption and cannot repeat. Goal patches at140 affect only later40.

The retained goal is to withhold the practiced action. Workspace capacity is two
unit slots: goal content occupies one if active and either visibly reminded or
previously maintained with support; the visible neutral card occupies one. A
maintained goal and zero neutral cards leave one slot for the inhibitory operation.
With one card the goal stays maintained, but there is no spare operation slot.
This is a finite resource candidate, not a cross-domain effort exchange rate.
Workspace writes at40; goal instruction writes at140 using the authenticated safe
projection transported from40. No owner reads another owner's hidden truth or trace.

Free-probe target availability depends only on the existing cue fold >=1/2, deliberately
separating learned availability from reward belief. Reward belief still supplies the
existing +1 target reason when the candidate is present. A maintained active goal
supplies a distinct +1 idle reason. Training remains instructed target only; information
mode remains idle only. Inhibition removes the target before raw reasons. No new die,
habit strength bonus or identity update is introduced. The main fixed-positive-belief
comparison has balanced target/idle reasons when inhibition fails, and idle alone
when inhibition succeeds. Exact inherited dice and addressed ties remain authoritative.

Candidates:1 SpareSlot;2 NoInhibition;3 LoadBlind;4 RetainedEqualsMaintained;
5 EraseHistory;6 NoHistory. Laws1 Residual and2 Linear remain serious alternatives.
Candidate4 uses retained active goal as accessible content despite missing maintenance.
Candidate5 inhibits normally then clears history at140 when inhibition was effective;
it is a labelled violating learning control. Candidate6 computes h=0 without erasing
actual history. All other candidates leave history intact when no outcome is observed.
Release of the goal must expose retained habit again under1, distinguishing5/6.

appraise40 -> options50 -> raw51 -> reasons52 -> decision60 -> intent70 -> expression80
-> plan90 -> attempt100 -> execute110 -> observe120 -> history/belief/cache140.
The existing cache140 slot instead owns goal updates; history140 alone owns history;
appraise40 owns workspace. Goal instruction and inhibition trace are transported in
authenticated private stage context from the original projection; fixed stage/occurrence
roster prevents denied fields shifting later observer provenance. Safe views exclude
execution835 and any nested execution records; use826/827,1017..1021,836..838 and1015/1016.

Model identity commits exact parameters, content, owner registry and stage reads/writes.
Empty prescribed S0, frozen public model allowlist, complete original/seed identities,
ExperimentIdentity and ComparisonCase precede qualification. Whole-prefix replay
authenticates queue/state/trace/RNG draws; every stage/commit fault rolls back terminally.
No general incremental RNG ledger claim beyond the inherited trace-address replay.

Preserve MEC-015/016/017/019/022 as active downstream controls; MEC-018 identity
feedback remains a separate qualified control, not part of this experiment. MEC-020/
EXP-013 active-only lifecycle pressure is retained. EXP-003 learned context access,
EXP-004 accessibility versus inhibition and P3-009 habit/skill/identity separation
are mandatory comparisons. No reference mechanism is retired. General learned control,
fatigue, rumination, monitoring and reappraisal remain unqualified. RO-C3-012/015/019/020.

Development correction before public qualification: inherited HABIT raw/reason lists
admit only one reason. Successor1017..1024 replaces the typed raw-to-execution chain
with up to two reasons and three draws (two faces plus tie). Old records/codecs remain
closed. Preserve CONTROL_DEVELOPMENT_TESTS_REV1.json and control-development-rev1/.
Seven initial model identities are superseded; freeze successor version0.2 before runs.
