# One requested multimodal observation opportunity

`general-source-opportunity-component/0.1-candidate`, 2026-09-13.
LOCAL DISPOSITION — no owner ruling required. Trusted component composition, not a
public transition, record allocation, physical read grant or model qualification.

Consume an explicit body/panel/visual request, positive instant, observer and lane.
Validate its exact data shape before allocation. Each requested body channel uses the
actual local-reserve safe sample producer. A requested panel uses the actual panel
source. Requested visual items use the actual bounded scene's SEM-B world bindings
and observer-safe role projection. Do not invoke an unrequested source even if its
handle is available. The local single-request check does not replace whole-run
schedule validation or authenticate compiled channel membership.

World210/binding211 outputs remain separately trace-side. Allocate one actual1115
observation per requested body channel, one for a requested panel and one for a
requested visual surface, including unavailable outputs. One shared1113 event
detection exists iff the panel is present or at least one visual item is detected.
Allocate one actual continuant detection and one binding result per visual item.
Canonical sorting of bindings does not reserve an implicit range of shared IDs.

Prepare the actual tracking and event-file owners without publishing them. Sampled
panel absence follows its accepted context rule; an unsampled panel cannot terminate
an active context. Standalone visual evidence uses the accepted uncontextualized
event-file path and rejects such an unsampled active context. Body-only sensing does
not mutate either perception owner or fabricate an event file.

Exactly one conditional SEM-H reservation/freeze serves the completed request. It
exists iff there is at least one present body sample, panel or visual detection.
Supporting observation identities are exactly the present products in required
canonical order. All-unavailable requests have no experience. Current freezes at14;
consequence freezes at124. A panel companion refers to that actual experience and
actual event transition. Publish both prepared owners only after freeze validates.
Failures leave those local owners unchanged; allocator rollback still belongs to the
enclosing scheduler. No rollback claim follows merely from retrying this component.

The body consumer's input references the same shared experience, including when
other modalities also contributed. Body cue/selection and visual cue/selection remain
independent later operations. A body-generated experience cannot stand in for a
missing visual detection. No extra sensory occurrence is allocated just to evaluate
another cue. Zero durable capacity does not suppress the current body cue.

The direct function composes ordered pure stages; it does not authorize collapsing
the public10..15 or120..125 registrations. PRJ/IDN, exact producer attachment,
canonical pending payloads, public owner batches, trace read enforcement, protocol
hooks and persistence remain required at those boundaries. Returned source truth is
never included in the body input or admitted as a cognitive resolver.
