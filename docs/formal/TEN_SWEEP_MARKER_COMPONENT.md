# Tenth visual source for the competing acquisition

`ten-sweep-marker-component/0.1-candidate`, 2026-09-13.
LOCAL DISPOSITION — no owner ruling required.

The prior nine-sweep source supports eight trial positions and one later cue. It
cannot also justify a fresh subsequent competing acquisition. The final competitor
must pass actual visual observation, SEM tracking/role projection, selection and
positive encoding; fixture bytes alone are insufficient source evidence.

New fixed constructors admit at most ten ordered display frames/sweeps with at most
three detections per sweep, using the unchanged observed-glyph SEM continuity law.
The old eight/nine constructors retain their exact bounds. No caller-sized budget,
truth-key matching or new occurrence namespace is introduced. The new source may
allocate at most30 continuant files when every sweep has three unmatched detections.

The first competitor is sensed after recall and significance qualification, with
an observed distinct glyph and an independently observed role. It has no panel
request and cannot inherit trial context. Its acquisition must use the existing
shared allocation and selected positive evidence. The later supplied qualified-use
control remains explicitly component-scoped; this change does not authenticate it.

Prefix reconstruction and transaction abort/retry use the same ten-sweep limit.
Historical nine-sweep bounds and declarations remain unchanged. The future public
profile must explicitly choose this successor and raise its possible graph-key
bound from27 to30, with corresponding work/cohort/restore checks. Numeric allocation,
public source registration and General Attention qualification remain open.
