# Event association settlement

`event-association-settlement/0.1-candidate`,2026-09-13.
LOCAL DISPOSITION — no owner ruling required.

The first bounded owner composes accepted associationCandidate and weakest-edge
retention. One actual positive event acquisition supplies at most three child strengths;
null supplies only an independently requested graph-retention operation. Body formation,
episode survivor lists and use/significance metadata are not graph operands.

Prior state has at most30 nonempty graph nodes and its own LastUpdatedAt. Formation
derives elapsed time exactly from target instant minus that committed timestamp, uses
the accepted rational/round-even row arithmetic, then applies independent graph bounds
and empty-node normalization. Formation may leave an empty graph; its update time still
advances. A retention-only operation preserves LastUpdatedAt and performs no decay or
learning. It cannot erase history time merely because every edge was removed.

Capacity is0..30 nodes and0..N*(N-1) directed edges; scale1..1000. Positive new strengths
are bounded by1. Prior structurally empty nodes reject instead of silently admitting
noncanonical state. A proposed31st node rejects the first profile's carrier bound.

Input is trusted component data, including projected canonical operation labels and
authenticated formation. Exact public record admission, canonical identity projection,
one-event-per-instant model enforcement and state mutation authority remain upstream.
The component returns detached candidate state and allocates no identity. Public owner
and persistence gates remain OPEN. Multiple event formations at one instant are outside
this first profile; no unreviewed sequential update or aggregate rule is inferred.
