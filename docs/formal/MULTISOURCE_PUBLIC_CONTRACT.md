# Public bounded multisource experiment contract

2026-09-20. LOCAL DISPOSITION: accepted complete bounded implementation direction
under the Campaign 3 escalation policy. Version `multisource-public/0.1-candidate`.
The accompanying shape/allocation table governs exact records, identity namespaces
and fields. No frozen predecessor schema, model image or verdict is replaced.

## Scope and actual sources

One governed character/qualified observer, up to two independently adopted tasks and
two distinct protocol actions, three independently controllable local reserves A/B/C,
up to two permitted views per signal, selection capacity0..3. The second task supplies
an actual competing option, never an authored comparison die. Each task uses inherited
workspace/appraisal/concern/motive/candidate/raw production with its own committed
motive392. Workspaces select one task each, forecast disabled, Standing disabled.
Task specs and instructions are governed content/registry references; criteria are
bound to those instruction/action identities in the new committed model profile.

Up to four observation descriptions: at most three positive body Base descriptions
sharing one body criterion/ground, and at most one signed task-A Situation. Duplicate
descriptions have different description IDs but the same ground and actual support.
One/two-view interpretation follows multisource-selected-descriptions/0.1-candidate:
singleton interval or conservative hull, d=max(0,(T-U)/T), signed task suitability.
The integration consumes one actual selection and evaluates all admitted descriptions
without issuing a caller-editable evidence capability. Unavailable support is not zero;
known-zero assessments retain evidence but emit no raw signal. Body support requires
actual adopted body instruction, task Situation requires actual selected/adopted task
instruction; disabled task Base does not disable an otherwise adopted task criterion.

Grounds remain distinct. Body-only/no-task controls omit task adoption; the task-Base-off
control retains adoption. Independently controlled A/B reserves are not conflated with
multiple views of one reserve. Character computations receive safe samples only;
unavailable/denied channels authorize no corresponding physical-anchor read. Source
reads and protocol outcomes are trace-side; state-presence never qualifies a character.

## Receiver and competing models

The complete option is holder/action. Raw source identity separates ground, role and
description identity; no fake task ID stands for a description. Up to six raw signals,
three ordinary reason nuclei (two task grounds plus one body ground). DescriptionDice
may expose up to five per-description dice only in its Base-only diagnostic models.

GroundAggregate, GroundPairwise, GroundUncovered and FamilyNormalized implement the
exact laws in multisource-coverage-comparison/0.1-candidate, extended solely to this
six-signal domain and canonical source keys. Original component limits are unchanged.
FamilyNormalized stays the measured negative candidate, including orphan influence;
no silent role-aware repair is adopted. DescriptionDice bypasses consolidation only
in explicitly separate Base-only models. Negative controls do not become baseline laws.

Within each complete option/ground/role/sign partition, order descending magnitude
(after any normalization), then canonical source key. Preserve every raw basis in
aggregate updates, even fully discounted descriptions. Sum signed effective support
then bound once. Zero Base excludes a nucleus before relevance; modifiers cannot
create motive. Inherit the exact reason-dice bands, integer modifier step/cap,
relevance activation, convolution and analytical arbitration semantics. A separately
implemented new envelope must match the existing kernel in overlap-domain vectors;
do not manufacture old499/504 ancestors for convenience. Exact intermediate values
are compared even when dice/mode/winner equality hides a difference.

## Ordered transaction and outputs

The accepted current-lane stages are sensing10, experience freeze14, selection20,
task sources40, family assessment/join50, raw receiving51, reason receiving52,
arbitration60, intent70, expression80, execution plan90, attempt100, outcome110.
The original opportunity is data-only observer/profile. Generated events cannot be
supplied publicly. All descendants have exact actual-parent identity and same instant;
selection and task/body branches cannot be replaced with caller-authored records.
Experience is conditionally reserved and actually frozen under SEM-001H. Selection
allocates its own1143 occurrence even when capacity excludes every group.

Actual task state reads use268/1 observer mapping,373/1 status and373/2 instruction;
body adoption uses487/1. Local physical anchors remain649/1[644] under the existing
local-reserve owner. Original replenishment651 at110 updates only its exact local
anchor under accepted reserve math; private deadline events at140 retire expired
Open tasks under the existing task owner. Input times are positive and bounded by100,
at most one opportunity and one replenishment per instant, at most six opportunities.

Intent, expression, plan, attempt and terminal outcome remain separately traceable.
Protocol action391 supplies the actual requested contact count; execution434 controls
whether it succeeds. This is the inherited bounded protocol, not learned body relief.
Expression uses per-ground evidence excluding Standing and preserves its evaluated
meaning separately from execution success. All stages use the real deterministic
scheduler and registered exact read/write/output/occurrence domains. Per-instant work
limit is committed. All output and trace schemas are closed and canonical.

2026-09-20 implementation clarification, LOCAL DISPOSITION before model freeze:
expression meaning for each ground/option sums its effective signed Base and Situation
support before one bounded transform. Coverage retains the declared role/sign partitions;
all admitted options are present in every ground's meaning map, with zero for no support.
The inherited choiceAlignment kernel then evaluates the chosen option. DescriptionDice
changes the dice unit only and retains GroundAggregate expression meaning. Without a
chosen option there are no alignment entries. An absent task at its private deadline
emits a DeadlineMissed disposition without creating task state; actual present Open
state alone is retired. This disposition is a trace output, not a new adoption.

## Identity, RNG, persistence and experiment gate

ModelIdentity commits content, registry/profile/stages and parameters; the public
factory accepts only the frozen cohort. RunIdentity commits actual S0, ordered originals
and seed. Use the existing addressed RNG with explicit actor/action/ground bindings,
plus description identity only for DescriptionDice. Retain committed address ledger;
failed instants commit no state/output/trace/queue/allocator/RNG changes. Full-prefix
restore reexecutes from committed originals and requires whole-save equality, not
merely a checksum or locally decodable snapshot. Mutation hooks remain internal.

Required paired contrasts: shared fact/two motives; shared fact/orphan modifier and
modifier removal; independent facts/two motives; duplicate and collective descriptions;
polarity intervention; unavailable/known-zero/capacity exclusion; no-task and no-body
adoption; hidden-within-bin equality; independent reserve replenishment; execution
failure. Declare equal-magnitude ordering scope for the aggregate witness, preserving
the original counterexample even if a different public calibration changes numbers.

Freeze named serious competitors and all originals, prove replay/rollback and boundary
rejections, run preservation suites, and record a scoped verdict. Missing coverage stays
in the denominator. RO-C3-001 closes only to the extent actually discharged; broader
receiving law necessity and general physiology/Need remain unresolved. This contract
does not authorize new Need ownership, a cross-domain motivational exchange rate or
arbitrary public authoring. Zero architectural owner rulings are opened by these choices.
