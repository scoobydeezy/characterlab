# Public goal/strategy persistence

**ACCEPTED LOCAL DISPOSITION — 2026-09-22.** `goal-strategy-public/0.1-candidate`.
Records989..1007/schema1, namespace1164; counters1007/19 before verdict.
This admits implementation/freezing, not qualification. No owner ruling.

## Preserved distinctions
Architecture11 permits bounded ordinary plan adjustment without a Decision.
MEC-020/EXP-013 retain real adoption, active-only desire and terminal lifecycle.
MEC-015..018/022 remain unchanged arbitration/identity controls; this experiment
neither reduces them nor fabricates random rolls for an uncontroversial route selector.
MEC-019/P3-006/009 retain attempt/outcome, evidence and identity separation. The373
adopted-instruction leaf stays immutable. No reference code is imported.

One actor, one observer, one governed goal/delivery identity, desired outcome
item-at-destination and two concrete route definitions. Goal993 is absent initially,
then Active, PerceivedFulfilled or Abandoned. AdoptedAt and goal identity persist.
Only an explicit one-time adoption command may create the goal, at140; an already
adopted goal cannot be recreated. Fulfillment is perceived, never world-authoritative.
Abandoned is reachable only in the named controls. Terminal goals never reactivate.

## Finite source and interpretation
At most8 originals at increasing integer instants1..8. Each991 route display has a
visible receipt1..8, route A/B, polarity, issuedAt, validUntil and visibility. At most
one display per route in an original; at most8 distinct receipts over the run.
Require1<=issuedAt<=currentAt, issuedAt<=validUntil<=8. A repeated receipt preserves
route/polarity/issuedAt/validUntil, but may be exposed later. Two different receipts
for the same route/issuedAt reject. Denied displays never enter character knowledge.

Original992 also supplies route-open truth A/B, blocker, competence, independently
external destination attainment, execution visibility and goal-criterion display mode
0silent/1truthful/2inverted. These are controlled world/sensor inputs, not learned
state. Character planning consumes only retained observations. Route display may be
wrong about route truth. A failed attempt does not itself prove route unavailability.

World execution success means a selected route exists, its actual route is open,
there is no block, and competence permits execution. Destination attainment means
execution succeeds OR the independently controlled external delivery occurs.
Execution success and the desired-state observation are projected separately.
No attempt yields no execution observation; it can still yield a destination reading.
Only a received positive destination reading can fulfill an active goal. Missing or
negative criterion evidence does not fulfill it. False readings can yield mistaken
fulfillment; hidden attainment does not privately synchronize the character.

## Exact phases and owners
Context40 reads prior committed goal, selected plan and knowledge; evaluates the
accepted goal-strategy-component/0.1-candidate using the latest perceived execution
failure (if absent, false). Known route evidence expires inclusively after validUntil;
newer expired evidence does not revive an older reading. The component's Missing,
Expired, KnownUnavailable and KnownAvailable diagnostics are emitted distinctly.

Plan90 writes only selected-plan root996; attempt100 emits an actual route attempt
only for a nonzero selection; execution110 emits immutable world truth1003;
observe120 reserves all four observation IDs every instant, even on denied branches;
evidence130 carries only own observations; lifecycle140 writes goal root994;
knowledge140 writes root1000. Every original runs this same stage roster, including
no-opportunity instants. Actual trace phases must be tested before freezing.
There is no Decision/intent/expression claim in this ordinary-planning profile.

Goal, plan and knowledge have separate authorities, each restricted to its own leaf.
Goal reads only its prior lifecycle and receives the admitted criterion plus explicit
adoption/control proposal; plan reads only its own prior leaf; knowledge reads only
its prior evidence. Context alone can read the three character-owned leaves together.
World-source operands never enter these state ReadDomains. Profile-local observations
do not masquerade as generic SEM experiences. S0 must be empty.

Goal evaluation at40 uses the prior committed state, so adoption/evidence at140 first
affect the next context. Plan90 can feed this instant's attempt100. Fulfillment140
affects next-instant planning; it cannot rewrite the already performed attempt.
If adoption and a positive criterion coincide, create the active goal only; it must
receive a later qualifying criterion while active before fulfillment. This narrow
adoption ordering is explicit, not a general instruction lifecycle theory.

## Four candidates and authority-preserving controls
1 SeparateGoalPlan preserves goal when no current route is available; retains a usable
prior route, otherwise takes the first known-available route A then B.
2 GoalEqualsPlan proposes Abandoned when an active goal has no selected route.
3 FixedRoute selects A whenever the goal is active despite contrary route evidence.
4 FailureAbandonsGoal proposes Abandoned after any perceived previous failure.
Only the goal owner applies abandonment at140; the plan owner cannot delete goals.
Terminal goals have no active desire or plan. No plan is written before goal adoption.
Prior plan selection0 is retained as an explicit no-route plan once a goal exists.

The authored A/B order is a finite comparison control, not psychological preference.
The selector creates no general efficacy belief, persistence trait, effort valuation,
procrastination, delay discount or goal competition. Source and selected-plan history
remain independently traceable. Cross-episode attribution remains outside scope.

## Public gates
Compile exact data-only recipes, stage registry and mutation authorities. Freeze four
ModelIdentities, complete original/seed RunIdentities, ExperimentIdentity and
ComparisonCase before qualification. Seeds are committed but this deterministic
ordinary planner consumes zero draws; no stochastic qualification is implied.
Require goal-preserving route switches and gaps, resumption without readoption,
failure-only controls, external/hidden/false attainment, evidence expiry and absence,
positive/denied route observations and identical admitted views under changed truth.
Each rejected candidate must fail a named contrast. Observer output whitelists only
own planning/attempt/evidence/lifecycle/knowledge records, excluding truth/source data.
Verify complete-prefix restore/continuation, actual phases, later provenance after
denied displays, rollback from nonempty state, foreign writes and edited saves/inputs.
RO-C3-019/012/014/020 remain; Brief12.7's procrastination, gratification, forgetting
and conflicting goals are not qualified by this slice.

At lifecycle140 a positive received criterion takes precedence over a simultaneous
control abandonment proposal. A newly adopted goal is Active even if that same
instant carries a positive criterion; a later observation is required as above.
