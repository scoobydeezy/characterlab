# longitudinal-public/0.1-candidate

ACCEPTED bounded local successor, 2026-09-21, under the Campaign3 escalation policy.
Authority: Architecture8.4,13,15; North Star14; PHEN-LONG-001; LONGITUDINAL_READINESS.
Predecessor contracts and frozen cohorts remain unchanged.

## Public domain and ownership

Data-only model recipe and exact empty S0; at most16 strictly increasing integer
instants1..64. One actor/observer, one identified social target. Originals specify
kind0 delay/probe,1 biography opportunity,2 instructed practice,3 social interaction,
4 person claim; physical success/cooperation, access, displayed report (actual/positive/
negative), own participation, skill impairment, skill difficulty, skill interference.
Inputs are world/apparatus controls, never injected learned state. A display can be
false. Kind does not itself establish observer evidence. No general SEM recognition.

Safe roots: identity414, skill784, relationship summary851, person estimate (optional),
episodic safe journal and, in FullHistory only, relationship detail journal. Exactly
one authority per root; one atomic scheduler instant. DestructiveSharedSlot uses a
separate diagnostic root/owner containing only one learned family at a time. It is
not the safe ownership architecture. Character consumers never read original truth,
archived outputs, trace or previous saves. Closed observer view exposes current learned
projections and retained safe episodes; research snapshots/saves are omniscient.

## Biography

Two authored task motives of strength1 and1/2, with fixed meanings1/2 and1/3. Actual
Campaign2 compileReasonNuclei, arbitrationOutput, intentOutput, expressionOutput and
qualificationOutput execute, followed by appendIdentityContribution(K=1). Standing
adds signed fold strength to each existing task ground, never creates a base motive;
meaning excludes standing. Dice use inherited base bands, activation0 and standing/
situation unit1/4 cap3; arbitration thresholds1/2,1/2. StandingOff is a comparator.
Only kind1 applies accepted qualification at140; other instants are read-only biography
probes. Authorship and signed alignment are not supplied by originals. Existing typed
occurrence families are retained; resolution root1135 is reserved deterministically
as the authored instant ordinal, so unrelated family events cannot recouple draws.
Identity contributions and historical expressions never change retrospectively.

## Skill and relationship

An immutable adopted exercise instruction supplies intent at70 only on kind2, plan90,
attempt100 and execution110. Execution requires this attempt; effective capacity is
K*(1-impairment), success iff effective capacity >= difficulty. Attempted practice
updates K by min(1,K+1/4), matching the SKILL linear practice control, independent of
observer access/success. Skill interference is an explicit truth-side procedural loss:
subtract1/4, floor0, before adding new practice. NoRust leaves K over delays;
HalfAtGap halves K once at a committed instant whose gap from the prior commit >=4,
before interference/practice. This event-sampled law is not continuous decay and does
not act retroactively on execution at that instant. Practice count is cumulative.

Only admitted own kind3 participation updates relationship. Cooperation increments
count; breach sets rupture. StickyRupture retains rupture; LatestInteraction clears it
after cooperation. Kind4 updates current person claim only, including false claims.
Comfort=count>=2 and not rupture; caution=rupture. Claims never rewrite interaction
history. NoLearning disables the selected family update, not evidence admission.

FullHistory derives summary from the complete relationship journal. CompactRelationship
updates only summary incrementally; there is no hidden character history sidecar.
EpisodicOnly derives summary from the age-filtered episodic journal. Equivalent learned
projections between FullHistory and CompactRelationship are a horizon-limited result;
their accessible detail is intentionally not equivalent. FullHistory relationship
journal itself remains accessible and is not described as forgotten.

## Retention and stages

Recent safe episodes contain admitted time, kind and observed outcome, never truth.
At140 retain entries with currentAt-entryAt <4, then append this instant's admitted
observation (if any). KeepAll is a comparison. Expiry actually removes entries from
authoritative character memory. Summary retention and episodic retention are distinct.
Relearning is fresh admitted evidence/practice; no automatic archive resurrection.
Identity's minimal qualification contributions remain retained; no identity compression
or defining-memory theory is claimed. Episode values are historical and immutable
while retained; a later opposite claim changes person estimate only.

Stages: probe40; biography-context40, raw51, reasons52, decision60, expression80,
qualification130, identity140; practice-intent70, plan90, attempt100, source110,
observe120, skill140, relationship140, memory140. Source emits practice truth and safe
observation routing; observer access controls only observation-dependent learning.
Current probes read prior committed state. All consumers have enumerated read domains;
every write has expected prior bytes and registered authority. Output evidence and
before/after patches persist in research trace. Fault at any stage/commit rolls back
state, queue, allocation, draws, outputs and trace. Restore executes the whole committed
prefix and requires exact save equality before continuing.

## Gates and limits

Freeze models, originals, seed and comparison plan before qualification. Require
coexisting publicly acquired biography/skill/relationship, family-isolation projection
equality, destructive control failure, hidden-truth equality under identical displays,
unknown versus false claim, history order, committed gap, skill interference and
relearning contrasts. Require actual episode loss with learned structure retained,
Compact/Full learned projection equality, and EpisodicOnly loss of relational influence.
Check actual later execution and effective arbitration probabilities, not just changed
embedded traces. No whole Brief12.15, lifelong equivalence or Campaign3 closure follows.
