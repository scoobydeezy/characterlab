# Selected encoding denominator control

`selected-encoding/0.2-candidate`. SHAPE ACCEPTED at component scope by primary-agent
review, 2026-09-11. This additive successor retains selected-encoding/0.1-candidate
unchanged and adds two explicit budget calibrations: UnitBudget=1 and SmallBudget=1/5.
The input is the same live selected capability; all content, homogeneous prior,
causal-role, attention, disabled need/surprise and once-only consumption rules remain.
The important threshold remains1/4. No source, persistent-state or public authority
is added. Invalid budget calibration rejects after the valid view is consumed.

The component calls the existing selected encoder for the content/factor extraction,
then applies the exact EAM comparison to those same rows using the declared budget.
It emits one result with its successor version and explicit budget calibration. The
intermediate computational result is not a second evidence occurrence or episode.

This resolves the known arithmetic witness gap without widening the observed source
or changing the old component. For two versus three selected Actors, Raw=3/10 each:

| Law | UnitBudget, two / three | SmallBudget, two / three |
|---|---|---|
| independent | 3/13 / 3/13 | 3/13 / 3/13 |
| historical-shared | 3/10 / 3/10 | 1/2 / 1/3 |
| historical-hybrid | 3/13 / 3/13 | 3/13 / 3/13 |
| retired-flat | 1 / 1 | 1 / 1 |

The shared SmallBudget column genuinely inhabits the aggregate denominator branch.
For one Actor plus one versus two Participants, SmallBudget hybrid has important
Actor strength3/13>1/5, so RemainingBudget=0 and low Participant strengths are1 versus
1/2 each. This historical control can exceed total1; no clipping or conserved-budget
claim is permitted. Actor strength remains3/13.

Frozen SBC-A..E: exact two/three-Actor table; important/low hybrid footprint;
unchanged selected bytes/refs across calibration; invalid calibration consumes the
view; changing unselected content still has no effect under either budget.
These are source-connected component comparisons, not public encoding, association
state or corpus qualification. Neither budget is accepted as a universal attention law.
