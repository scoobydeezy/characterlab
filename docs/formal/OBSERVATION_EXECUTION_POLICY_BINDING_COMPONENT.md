# Observation execution policy binding

`observation-execution-policy-binding/0.1-candidate` implements the purpose relation
in GA_CALIBRATION_DEFINITIONS_REV1 at component scope. Local disposition: no owner
ruling required. Definition references reuse1027 and the existing governed registry.

Each requested use requires exactly its policy slots. Visual selection requires
selection, spatial allocation and encoding definitions. Body cue and goal baseline
have separate recall slots. Goal assessment requires baseline and qualification
definitions. Unrequested slots must be absent. Sensing alone needs none of them.
All data shapes are validated before the trusted immutable model resolver is called.
The resolver checks the expected definition kind; it receives detached identities.

This component does not admit definition values, implement their canonical codecs,
authenticate an original input or authorize a registration. Exact schema/version
admission remains the governed compiler's responsibility. No runtime state, current
instant, source result or future eligibility chooses a policy. Selection capacity zero
does not remove a requested selector or prevent a separately requested recall cue.

The eight lower-camel-case fields are the component projection of the symbolic record;
they are not a new serialized schema. No allocation or public qualification follows.
